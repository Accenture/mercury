#
# Facade flows: the synchronous REST facade and the reply-delivery flow.
#
flows:
  - 'sync-to-async.yml'
  - 'soa-reply.yml'
  # JSON Schema variant of the two facade flows
  - 'sync-to-async-json.yml'
  - 'soa-reply-json.yml'
  # Avro variant of the two facade flows
  - 'sync-to-async-avro.yml'
  - 'soa-reply-avro.yml'
