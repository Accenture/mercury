[package]
name = "mercury-redis-test-double"
description = "In-process RESP2 server double for Mercury's Redis-backed test suites (dev-only, never published)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
name = "redis_test_double"

[dependencies]
tokio = { version = "1", features = ["rt", "net", "io-util", "sync", "macros"] }
