flow:
  id: 'kafka-poison'
  description: 'always fails - the dead-letter path'
  ttl: 10s

first.task: 'kafka.poison.task'

tasks:
  - name: 'kafka.poison.task'
    input:
      - 'input.body -> body'
    process: 'kafka.poison.task'
    output: []
    description: 'poison by design'
    execution: end
