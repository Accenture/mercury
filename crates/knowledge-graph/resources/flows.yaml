# Flows shipped with the knowledge-graph engine (Java module parity).
# flow-11.yml joins at increment K-6 with the graph.extension skill.
flows:
  - 'graph-executor.yml'

location: 'classpath:/flows/'
