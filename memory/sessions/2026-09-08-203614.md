# Session (2026-09-08T20:36:14.000Z)

**Agent:** Claude Code

## What happened

Lock-step webapp sync from the Java repo (Eric's directive: "replace the whole
webapp — the UI is the same because the backend protocol is compatible"). Replaced
`crates/knowledge-graph/webapp/` wholesale with the Java repo's
`system/minigraph-playground-engine/webapp/` at Java main `af9f5600` (PR #331 merged
today, on top of PR #330) on branch `feat/playground-connect-ux`. This carries BOTH
Playground UI polishing arcs the Rust copy was missing:

- Arc 1 (Java PR #330): console hide/restore toggle, overlap-free content-sized node
  layout with measured relayout, thumbnail/expanded node toggle, in-place magnified-node
  editor for edit AND create (NodeDialog deleted), sorted `[]`-signature keys +
  drag-reorder.
- Arc 2 (Java PR #331): Neo4j-style connect UX (halo ring = connect, body = move;
  click-to-connect; anchored ConnectionPopover — ConnectionDialog/GraphAuthoringModals
  deleted, zero modals), selectable edges, relation-aware edge deletion (per-relation
  right-click menu: Delete 'fetch' / Delete 'test' / Delete all (n)), directed-removal
  planner (compounds pair-delete + reconnect-survivors — fixes the latent defect where
  deleting a→b silently destroyed b→a), frontend-only undo via paced compensating
  commands, panel-aware auto-fit, command-input autosize fix, always-blue halo.

Java-only agent artifacts stayed excluded (webapp `memory/`, `AGENTS.md`, `CLAUDE.md`,
`agent-skills/`, `.cursorrules`, `codex.md`). Engine-layout divergences preserved on the
Rust side (see the new continuity fact): deploy/clean targets, help glob, vite fs comment,
graph fixture imports in `graphTransformer.test.ts` (mapped to `resources/graph/` +
`tests/resources/graph/`), the localHelp test comment, and the COMPANION_ENDPOINT_README
graph.js line ("this Rust port never carried `graph.js`"). README.md and
`docs/Technical Documentation.md` ride the Java wording verbatim — established precedent
in this repo's prior copies.

## Validation

- `npm run typecheck` clean; **254 vitest tests green in 26 files** — identical totals to
  the Java repo at the same commit.
- `npm run release` rebuilt the bundle into `crates/knowledge-graph/resources/public`
  (old hashed assets swapped out by clean.js).
- **Live smoke test against the RUST engine** (release build of
  `examples/minigraph-playground`, port 8085): built a 3-node fixture over the console
  WS, exercised the per-relation edge menu (items exactly as designed), Delete 'fetch'
  kept the sibling `test` relation, whole-edge delete of fetcher→end PRESERVED the
  reverse end→fetcher `retry` (the pair-wipe compound + reconnects worked against the
  Rust `delete connection` / `connect` grammar), and Ctrl+Z undo restored both cases.
  The undo executor's pacing (per-command `graph.mutation` confirmation) held — Rust
  console reply wording parses identically, confirming the engine-identical-messages
  contract in practice.

## Memory References

- inv-telemetry-presentation-parity (consulted — the cross-engine lock-step contract and
  Java-as-reference rule governed doing this sync and trusting message parity for the
  undo pacing)
- webapp-lockstep-sync-preserved-paths (created, tier: working)
