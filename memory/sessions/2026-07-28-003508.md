# Session (2026-07-28T00:35:08.000Z)

**Agent:** Claude Code

## What happened

**ManagedCache gate APPROVED and increment 71 DELIVERED** (commit `a2f89856` on branch
`feature/managed-cache`, NOT pushed — awaiting Eric's PR flow).

**Gate outcome (Eric):** "design approved - moka confirmed, all three adopters, test
constructor ok" + a fourth ruling at approval: **"Please ensure deterministic eviction"**
→ the store is built with moka `EvictionPolicy::lru()` (≥0.12.5) instead of the default
TinyLFU. Eric also asked whether the Java original has the same issue and, if so, for a
refactoring note to the mercury-composable team.

**Java-side verification (research with primary-source citations):** YES — Java
`ManagedCache` (Caffeine 3.2.4, `maximumSize`+`expireAfterWrite`) is genuinely
non-deterministic under capacity pressure on three levels: W-TinyLFU frequency admission
(a cold newcomer loses to a hot resident), DELIBERATE HashDoS jitter
(`BoundedLocalCache.admit()`: losing candidates with sketch freq ≥ 6 admitted with
probability 1/128 via `ThreadLocalRandom` — the wiki's own words: jitter "so that the
decision is non-deterministic"), and documented approximation (lossy read buffer, may
evict early/overshoot). Caffeine has NO policy switch (builder audit; issues #100/#1627
unshipped). moka's LRU arm is source-verified admission-free and RNG-free; sequential use
+ `run_pending_tasks()` is exactly reproducible. **Handoff filed:**
`/tmp/managed-cache-eviction-determinism-handoff.md` (options: accept+document; Guava
`concurrencyLevel(1)`; synchronized access-order LinkedHashMap — no maintained concurrent
exact-LRU-with-TTL Java cache exists).

**Implementation (per the approved design, docs/design/managed-cache-port.md now marked
APPROVED):** `platform_core::util::managed_cache` — `ManagedCache` + `CacheValue =
Arc<dyn Any + Send + Sync>`, re-exported from lib.rs; moka sync engine wrapped (LRU
policy per the ruling); named registry (first creation's params win); expiry clamped
[1 s, ~100 years]; the exact Java telemetry-stamp map (put/remove→last_write incl.
remove-on-absent, get/exists→last_read hit-or-miss, clear→last_reset; clear = stamp +
invalidate_all + run_pending_tasks); Java log wordings; 10-min housekeeper wired in
app_starter's essential-services phase. Three adopters: WS dedup (`last.ws.message`/1 s —
bounded + ANCHORED window, Java debug log), actuator info-lookup cache
(`health.info`/5 s, `info/{route}`, map bodies only; dedicated test binary
tests/health_info_cache.rs for process-wide-cache isolation), ext-state-machine fixture
(`state.machine`/10 s, `Arc<Mutex<Map>>` handles). 12 unit tests + 2 adopter regressions.

**Pre-commit adversarial review (18 agents, 3 lenses + per-finding verification; 15 raw →
12 confirmed, ALL fixed):**
1. **CRITICAL:** my fixture migration kept `instances = 4` while removing the global
   Mutex that had masked a lost-update race — Java's fixture is a DELIBERATE singleton
   ("would guarantee orderly execution"); flow_runtime went ~50% red (my earlier full-suite
   green was luck). Fixed: `instances = 1` (Java parity). 5/5 consecutive re-runs green.
2. **MAJOR:** the create log's TTL rendering used the pre-existing Rust `elapsed_time`,
   which deviates from Java `Utility.elapsedTime` at whole-unit values ("2 minutes
   0 seconds" vs Java "2 minutes") — silent divergence on a telemetry-parity surface.
   Fixed: exact port (strict `>` boundaries, zero components omitted, sub-second "N ms")
   — also corrects `/info` uptime, which shared the helper (CHANGELOG Fixed #2).
3. **MAJOR:** duplicate debug log emitted TWICE (I added Java's log inside is_duplicate
   but left the pre-existing caller-side line). Fixed: caller line removed — once, like Java.
4. Test-margin hardening (TTL tests now ≥ 400 ms slow-CI headroom), 3 stale-docs sweeps
   (platform-core-port §5f/§7, INCREMENTS overview rows 68–71 backfilled), MC8 doc wording
   aligned to the stricter `#[cfg(test)]` implementation, /tmp path de-referenced in the
   committed design doc.

Also fixed en route: the LRU unit test needed a write-log flush BEFORE recording reads
(moka applies the read log before the write log in a maintenance pass — a read recorded
ahead of its entry's write is a recency no-op).

**Gate:** workspace 287 (273+14) / clippy 0 / fmt; flow_runtime 5/5.

**Also: MERGED same sitting** — PR
[#185](https://github.com/Accenture/mercury/pull/185), merge commit `6326da2e`, CI green
first try (build 25s / test 1m53s / memory floor 5s). `ot-managedcache-port` closed;
the schema-registry adoption rides the connectors backlog.

Continuity: Stack & Tools gains moka 0.12 (sync); `ot-managedcache-port` updated to
implemented-awaiting-PR.

## Memory References

- ot-managedcache-port (gate approved; increment 71 implemented; substance updated)
- inv-telemetry-presentation-parity (drove the elapsed_time exact port + the double-log fix)
- port-bottom-up-faithful (map-don't-mirror throughout; the fixture singleton restoration)
- conventions-rust-baseline (gate discipline; INCREMENTS row+section convention restored)
- string-plugins-unicode-scalars (carrier-idiomatic precedent cited in the design)
- registration-metadata-contract (same precedent)
- bp-kafka-connectors-backlog (schema-registry = the future heavy consumer shaping MC2/MC4)
- vision-mercury (increment serves the connectors runway + API-surface completeness)

Co-Authored-By: Claude Code <noreply@anthropic.com>
