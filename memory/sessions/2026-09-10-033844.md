# Session (2026-09-10T03:38:44.000Z)

**Agent:** Claude Code

## What happened

**Playground UI live-graph-source mirror** (branch
`feature/playground-live-graph-source`; wholesale webapp lock-step with the
Java repo's same-day arc, Eric's design). The graph view's single source is
now `GET /api/graph/session/{id}` — initial load, SPA-return restore, and
mutation auto-refresh fetch the live session graph directly; no
`describe graph` round-trip, no temp-file link, no expiry special-casing.
`describe graph` / `export graph` stay human-operator (and companion-agent)
console commands; a console link-row click live-refetches + shows the Graph
tab. Deleted UI machinery: `usePinnedGraphPath`, `useSessionGraphRestore`,
the `initialFetchFailed`/quiet-flag surface.

Sync ritual applied per the amended fact: rsync wholesale with the exclusion
set; preserves restored (`scripts/deploy.js`/`clean.js`, graphTransformer
fixture imports, COMPANION_ENDPOINT_README graph.js line); the three PORT
files (helpContent.ts, localHelpCommand.test.ts, vite.config.ts) reduced to
preserves this round — Java did not touch them. The resulting diff was
EXACTLY the Java arc's file set — the trees were in perfect lock-step before
the sync. Increment 113.

Gates: `npm run typecheck` + 321 vitest green; `npm run release` (bundle
index-CYIb2tlQ.js — hash legitimately differs from Java's, help md inlined);
live smoke against THIS engine (4.12.5, `cargo run -p minigraph-playground`)
on the packaged bundle: mount fetch 200 via `get.live.graph`, mutation
refetches on the session endpoint, away-and-back restored with ONE live
fetch, zero browser console errors.

## Memory References

- webapp-lockstep-sync-preserved-paths (consulted — the sync ritual executed
  verbatim; PORT files carried no new Java content this round)
- inv-telemetry-presentation-parity (consulted — presentation surfaces ship
  lock-step; the webapp rides Java wholesale)
