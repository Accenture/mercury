# Session (2026-07-30T18:06:59.000Z)

**Agent:** Claude Code

## What happened

**v4.11.0 pre-publication tag move (Eric's ruling: release tags include the
updated docs).** PR #190 (the docs-parity fix — home footer "Explore the
docs" clusters, the Project block with the cross-engine "Java version"
line, the Release Notes nav link) merged as `167484bd`. Sequence executed
per the 4.10.2-round precedent:

1. **Verified `167484bd` BEFORE the move**: `Cargo.toml` reads 4.11.0 at
   that exact commit; the docs fix is present (Explore-the-docs + Java
   version line + the nav link); both `48b0dabf` (docs parity) and
   `8ee9b431` (release prep) are ancestors of the merge.
2. **Deleted the remote `v4.11.0` tag** (was on `cc529071`; the release was
   NOT yet published, so the tag could move — a tag never moves after
   publication), deleted the local tag, **re-tagged `v4.11.0` on
   `167484bd`**, pushed.
3. **Remote dereference verified**: `refs/tags/v4.11.0^{}` →
   `167484bdb5063af755e85b7e241d6a0e0ed68c0d`.

Publication pending Eric (drafted release notes delivered earlier today).
The thread closes when v4.11.0 is published on both repos.

## Memory References

- thread-release-4-11-0 (extended: pre-publication re-tag on the docs-inclusive merge)
