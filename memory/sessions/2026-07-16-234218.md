# Session (2026-07-16T23:42:18.000Z)

**Agent:** Claude Code

## What happened

Maintainer committed increment 17, directed proceed → **Increment E-7 (repo
increment 18) — sub-flows, shared parent state, external state machine, SHIPPED.**

1. **`flow://` sub-flows**: execute_task forwards to the manager (dataset
   {ttl, body, header?}; headers parent/flow_id + inherited business cid; composite
   `uuid#seq` cid; trace + span carried); the manager resolves the ROOT ancestor
   (Java resolveParent recursion → `flows::resolve_root_ancestor`) and the child's
   end/abort response arrives as the parent task's ordinary callback — output
   mappings, exception routing (nested error unwrap from E-4 now exercised) and fork
   barriers all apply unchanged. Dangling reference → 500 "flow://X not defined".
2. **Shared parent state — the one real Rust divergence** (recorded in instance.rs +
   design §5g): Java aliases model.parent/model.root to the root ancestor's shared
   ConcurrentMap; rmpv can't alias. Design: `Arc<Mutex<Value>>` per flow family;
   parent-referencing tasks (compiler-tracked flags from E-1!) materialize the tree
   at model.parent for the mapping pass UNDER THE SHARED LOCK (Java
   ancestor.modelSafety analog; lock order shared→dataset everywhere — no cycles),
   write back after; model.root.* normalizes to model.parent.* (one Java object →
   one canonical name). Holding the shared lock across the whole pass is what makes
   concurrent sub-flow `[]` appends lossless (copy-in/copy-back without the lock
   would drop updates — considered and rejected).
3. **`ext:`**: calls collected during mapping passes, dispatched after lock release
   (fire-and-forget — observationally equivalent to Java's inline sends, order
   preserved; also required since dispatch is async and the passes hold sync locks).
   Route form (headers type/key + body {data}) and flow:// form both ported.
   `SimpleExceptionHandler` built-in ported (second engine service).
4. **Fixtures activated** (all canonical, E2E): parent-greetings ↔
   daughter-greetings (daughter writes model.parent.name/hello, parent reads via
   model.root.* — the alias round-trip; status/headers/cid inheritance asserted);
   missing-sub-flow (runtime abort); externalize-put/get (trace-scoped
   ExternalStateMachine port; ${app.id} resolved by adding `app.id: A12` to the test
   config — Java test-config parity; put + remove verified across two flows on one
   shared trace); fork-n-join-flows (sub-flow branches coordinating via parent
   state); **the canonical fork-n-join-with-dynamic-model-test** — 5 concurrent
   flow://echo-flow sub-flows, every ITEM/INDEX appended to the shared parent state
   exactly once. All passed FIRST RUN.

**148 workspace tests, 0 failures; clippy 0 (one type-complexity alias added); fmt
clean.** Docs: ES design §5g; INCREMENTS row + section 18.

## Decisions

- Shared-lock-across-the-pass over copy-in/copy-back (lossless concurrent appends);
  root-alias path normalization over dual-subtree sync (Java-identical semantics).
- Parent refs in loop conditions/sequencers out of scope (no fixture; doc'd).

## Memory References

- referenced: `ot-design-event-script` (E-7 shipped), `bp-event-script`,
  `vision-mercury`, `conventions-rust-baseline`
- created: (none)
