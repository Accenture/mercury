# Session (2026-07-19T16:42:00.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-9 (L8, reuse under governance: off-path modules +
`EXECUTE`): PASSED with one self-corrected iteration** (session `ws-783755-2`, verified empty).
Maintainer correction absorbed up front: tut-9 is about the reusable **module** (a node borrowed
via `graph.math` `EXECUTE`), NOT subgraphs — `graph.extension` comes in later tutorials.

- **Result:** 19/19 commands `ok:true`, zero failed commands, zero in-band grammar lookups. First
  `run` produced an empty output; the companion self-diagnosed purely in-band (three `inspect`s:
  model ✓ → module.result empty → caller.result has the sum), identified the **EXECUTE
  result-landing rule** (results belong to the invoking node — "it just borrows the logic"),
  fixed with one `update node`, passed: `{"sum": 30.0}`. Independently re-verified with different
  operands (42.0). Exported `reusable-addition`.
- **Design = canonical shape:** off-path `Module` with neutral `model.*` operands; caller with
  MAPPING×2 → EXECUTE → result-mapping; island anchoring the module
  (`root -[contains]-> library -[module]-> add-formula`) — the #22/#30 convention extended to a
  module node unprompted.
- **Findings #31–#33, all FIXED same day:** #31 EXECUTE result-landing unspecified (cost the
  iteration) → EXECUTE semantics + full reusable-module worked example in
  `command-reference.md#math-statements` + JSON catalog + skills-reference; #32 `#create`
  type-case contradiction (types are Capitalized labels; engine accepts) → corrected; #33 island
  scope now names **module/library** nodes ("off-path nodes: config, data-entity, Module") +
  "never executed" phrasing corrected to "executes only to sink" (matches the run log).
- **Docs updated:** tutorial-9 stage record + L8 ladder row + rollup #31–#33 in
  `docs/AI-companion-test.md`. (JSON catalog edited surgically after reverting an accidental
  whole-file reformat — clean 4-line diff.)

## Decisions

- Tutorial-9 defined as **L8 (reuse under governance)** on the validation ladder; the brief
  stated reuse as an architectural requirement (authored-once, off-path, borrowable) without
  naming the mechanism — the companion derived `EXECUTE` from the grammar.
- The sweep's first **functional** (vs command-level) defect was handled entirely by the in-band
  `/sync` + `inspect` loop — evidence the self-correction design scales beyond parser errors.

## Memory References

- ot-companion-validation-sweep — advanced (tutorial-9 done; next tutorial-10); substance updated
- bp-companion-sync — referenced (in-band self-diagnosis of a functional defect — the strongest
  validation of the sync design yet)
- ot-help-pages-rewrite — referenced (tut-9 walkthrough's Module teaching point was the missing
  grammar rule)
- port-bottom-up-faithful — referenced (judged against the canonical fixture shape)

Co-Authored-By: Claude Code <noreply@anthropic.com>
