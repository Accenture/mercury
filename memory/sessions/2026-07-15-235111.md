# Session (2026-07-15T23:51:11.000Z)

**Agent:** Claude Code

## Summary

**Implemented increment 4 — the application lifecycle** (`bp-platform-core`, Design §5c).
The foundation is now **bootable**: a runnable example proves increments 1–4 end-to-end.

**Harvested first** (Java v4.8.6): `AutoStart.java` (run-once entry; Spring branch — out of
scope), `AppStarter.java` (phase order: version banner → BeforeApplication by sequence →
preload → HTTP-if-rest.automation → MainApplication by sequence; "Missing MainApplication"
error), `EntryPoint.java` (one contract for both hook kinds), `EssentialServiceLoader.java`
(sequence 0, framework-reserved), Platform `getName()`/`getOrigin()`.

**Built:**
- `src/app_starter.rs` — `EntryPoint` async trait + `AppStarter` **builder** (explicit
  registration per D6 — no classpath scanning; `#[preload]` macro later):
  `before_application(seq, hook)` / `preload(route, f, instances)` /
  `main_application(seq, entry)` / `run(args)`. Exact Java phase order; sequences clamped at
  999 (0 framework-reserved); stable sort preserves registration order within a sequence;
  before-hook failure **aborts** startup; missing main = 400 error. Divergences doc-noted:
  no global run-once guard (builder consumed; framework phases idempotent — test-friendly);
  `rest.automation=true` logs a not-yet-ported notice.
- `platform.rs` — **Platform identity**: `get_instance()` (process-wide `OnceLock` registry;
  `new()` stays for isolated tests), `name()` (`application.name` → `spring.application.name`
  → `untitled`, Spring key verbatim per the migration-comparison decision), `origin()`
  (uuid/process).
- `util/elastic_queue.rs` — **housekeeping completed** (increment 3's deferred items):
  `start_housekeeping()` (idempotent; 20 s RUNNING keep-alive task + one `scan_expired_stores()`
  pass — skipped when `running.in.cloud`), `scan_expired_stores()` (stale >1 h marker with
  segments → removed; unknown dir with segments → removed; fresh marker survives),
  `shutdown_cleanup()` (purge + marker removal; explicit call — OS-signal wiring deferred).
  Holding-area naming now uses `Platform::name()`/`origin()` (replaced the local origin stub).
- **Example app** — `examples/hello_world.rs` + `examples/resources/application.yml`: the
  mercury-composable README "greeting.demo" taste in Rust. Config
  (`greeting.user: '${GREETING_USER:world}'`, instances from config) → preflight
  before-application hook → typed `greeting.demo` function (instances=10) → main does
  route-name RPC and prints the reply + worker instance + exec_time. env_logger added as a
  dev-dependency (examples only).

**Verification:** **55 tests pass** (13 unit + 18 config + 6 elastic + 11 event-bus + **7 new
lifecycle**: phase ordering, multi-main sequencing, failing-hook abort [no preload/no main],
missing-main error, shared global platform + identity, unknown-holding-area removal +
fresh-marker survival, shutdown cleanup) · clippy + fmt clean · **example executed live**:
`[before-application] configuration validated` → `[main] Welcome, world (worker #1, 0.06 ms
round trip)`; `GREETING_USER=eric` → `Welcome, eric` (env-substitution proven at runtime).
One test race fixed pre-flight: marker assertions isolated to the shutdown test (shared
process-wide holding area). Honest limit: the 1-hour stale-marker branch is code-reviewed,
not time-simulated (no mtime manipulation without a new dep).

**Doc:** design §5c added; §7 deferred list updated (lifecycle/housekeeping shipped;
OS-signal wiring + `yaml.preload.override` added as future items).

## Memory References

Updated:
- `ot-design-platform-core` — increment 4 done; foundation bootable
- Project State status (increments 1–4, 55 tests, bootable)

Referenced: `bp-platform-core`, `vision-mercury`, `platform-core-stack`,
`config-first-spring-out` (Spring naming verbatim in Platform::name),
`elastic-fifo-no-bdb` (housekeeping completed), `conventions-rust-baseline`,
`inv-never-couple-functions` (example: route string is the only caller↔function link)
