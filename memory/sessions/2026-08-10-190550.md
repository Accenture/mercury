# Session (2026-08-10T19:05:50.000Z)

**Agent:** Claude Code

## Summary

Lock-step mirror of the Java reference engine's field-review follow-ups (Java
[PR #271](https://github.com/Accenture/mercury-composable/pull/271), squash `adfb2a0d`,
merged earlier today). Commit `24eeef89` on `feature/graph-scoped-state-and-error-context`
— awaiting Eric's push/PR gate. Increment 84.

## What changed

- **Graph-scoped workflow state (Rust ADR-0012 proposed, BREAKING):**
  `persistence_envelope` and the resume get body gain `graph`
  (`suspend.rs`); the Redis store keys records `graph:{graph_id}:{cid}` via
  `connection::store_key` and both functions fail fast on a missing `graph`
  (`required_field`); the graph_runtime mock file store scopes files by
  `{graph}:{cid}` and rejects a missing graph.
- **Business-cid inheritance through graph.extension:** `build_forward` gains a
  `business_cid` parameter stamped as the `correlation_id` header (the
  `event_script::manager::BUSINESS_CORRELATION_ID` constant) — one change covers the
  single and `for_each` branches and both target protocols; `get_business_cid` reads
  trimmed `model.cid`. This closes the same hidden blocker as on Java (per-call random
  UUID as the child's cid).
- **Generic exception context (Rust ADR-0013 proposed):** `common.rs` gains the
  `ERROR_SOURCE/CODE/MESSAGE/STACK` constants + `stage_error_context`; both walkers
  (`executor.rs`, `traveler.rs`) stage it at their exception choke points.
  **Port divergence (documented in the ADR, CHANGELOG and INCREMENTS):** this engine has
  no native stack-trace transport, so `error.stack` appears only when a record carries
  one — the "when available" contract holds on both engines.
- **Fixtures byte-identical from Java:** unit-test-orchestrator, unit-test-sub-suspend,
  unit-test-error-context, unit-test-error-alias; manifest + compiler.rs counts (47
  compiled, 14 invalid incl. error-alias).
- **Tests:** three new graph_runtime sub-functions (per-graph isolation, orchestrator
  e2e with cid-propagation/model-restoration/no-re-execution pins, generic-exception
  pair 400-task vs 401-http distinguished by error.source + alias-404) + the companion
  dry-run `inspect error` twin (sid ws-770013-1); the suspend suite's store_file calls
  graph-scoped + the envelope `graph` pin; both Redis suites reworked to the scoped
  contract (isolation scenario + missing-graph negatives; GETDEL and MULTI/EXEC paths).
- **Docs mirrored with port wording preserved:** workflow-suspension guide (at-a-glance
  scoping bullet, orchestrator-pattern section adapted to the graph_runtime pin,
  store contract, Redis key note), command-reference failure-routing (error.* table) +
  inspect, skills-reference, six help pages (three byte-copied: task/suspend/resume;
  three adapted to Rust variants: fetcher/extension/inspect), AI catalog (notes +
  error.* namespace + reserved-name list), reserved-names guide, Redis README + module
  docs, CHANGELOG Unreleased, INCREMENTS 84. Webapp bundle regenerated (212/212).
- Clippy: one needless_borrow fixed; workspace clippy clean.

## Verification

Workspace gate: **58 suites / 305 tests green**; clippy clean; webapp 212/212.
The knowledge-graph + minigraph-state-redis suites passed on the FIRST run after the
mirror — including the new orchestrator and exception-context e2e.

## Remaining

Eric's push/PR gate for this branch. The Java side also has a post-merge polish pending
his gate (his IDE cosmetics + the S3776 `handleSkillSuccess` extraction in
GraphExecutor — the traveler's split adopted by the executor; engine 112/112).

## Also (same working session)

Pushed as [PR #200](https://github.com/Accenture/mercury/pull/200); the first CI run
FAILED on the **Format check** — the scripted test-file edits were not rustfmt-clean
(this repo's gate is tests + clippy + **fmt**; I ran the first two and skipped fmt).
Fixed with `cargo fmt` (whitespace-only, 4 files) as `7dadd1ff`. Lesson: `cargo fmt
--check` belongs in the local gate run, same command line as clippy.

**MERGED as merge `283d41e2`, CI green on the re-run (test job 2m25s).** The Java polish
PR #272 (squash `0612ec6d`) merged the same day — the whole field-review arc is COMPLETE
ON BOTH ENGINES; both halves ride the next release via CHANGELOG Unreleased.

## Memory References

- referenced: (Java-repo threads) thread-field-graph-scoped-state-and-error-context;
  (this repo) the redis store-module fact updated below
- updated: the minigraph-state-redis continuity fact — store key now
  `graph:{graph_id}:{cid}` with the `graph` contract field (substance update, not a
  supersession: same fact, new key layout)
