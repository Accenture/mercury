# Session (2026-08-14T16:43:31.000Z)

**Agent:** Claude Code

## Summary

*(lightweight)* agent-memory **Mode B upgrade v4.34.0 → v4.34.1** — secret-guard output
readability (output-format only, no detection change): `[secret-material]` finding lines now
end at the hit count; the redact/rotate/history guidance prints once per run — in the
pre-commit hook's `-> fix it` footer (blank separator line added) and as a single trailer in
scan/lint runs. Re-copied the memory-lint built-in + `.githooks/pre-commit`; stamped
4.34.1. Verified here: 49/49 both runtimes, byte parity, and a staged-scratch print showing
the new format with the block exit intact.

## Memory References

(none)
