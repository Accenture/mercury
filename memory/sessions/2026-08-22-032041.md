# Session (2026-08-22T03:20:41.000Z)

**Agent:** Claude Code

## Summary

**The system/ move + the v4.11.10 release cut — both engines SHIPPED in lock-step at
v4.11.10 (2026-08-21 local); GitHub release publishing pending Eric.**

1. **Move (Eric's ruling at the release review):** `ai-contract-provider` relocated
   `examples/` → `system/` — the SAME path as the Java module, completing the cross-repo
   convention that `system/` is the consumer-facing discovery surface on both engines.
   Pure relocation (26 renames at 100% similarity; the crate's relative paths keep depth —
   no code changes); references updated: workspace member, system/AGENTS.md link + module
   table, llms.txt, CHANGELOG entry; INCREMENTS 89. MERGED as mercury PR #210, merge
   `9d1e4c28` carrying `666e5303` (tree verified identical), CI green (test 2m24s +
   recheck). Sequenced BEFORE the release cut (content merges first; release PR stays
   mechanical). Also: the stale archive INDEX.md placeholder ("*nothing archived yet*")
   Eric spotted was removed — a past review had appended entries below it.
2. **Release v4.11.10:** branch recreated on top of the move — workspace version
   4.11.9→4.11.10 (Cargo.toml + Cargo.lock, 12 members, zero stale), CHANGELOG Unreleased →
   Version 4.11.10, 8/21/2026 (contents: f:setConfig + system/ai-contract-provider). Gate:
   63 suites / 317 tests, clippy 0, fmt clean. MERGED as mercury PR #211, merge `b77f17e8`
   carrying `1beff96d` (tree verified identical), CI green (test 2m20s + recheck).
   **Tag `v4.11.10` on the merge, dereference-verified.** Branches deleted both ends
   throughout.

Java side of the same cut: release PR #291 squash `5cb65f04` == gated `689adf5e` (34-pom
sweep, full reactor green), tag dereference-verified on the squash — see the Java repo's
log 2026-08-22-032106.

## Memory References

- referenced: thread-ai-contract-provider-port (the moved crate; thread record updated with
  the relocation), thread-set-config-plugin (release content)
- created: thread-release-4-11-10 (tier: working)
