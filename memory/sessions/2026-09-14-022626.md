# Session (2026-09-14T02:26:26.000Z)

**Agent:** Claude Code

## What happened

**Experiment K1 conducted — `system/minimalist-kafka` exists** (Eric's go:
"conduct K1", after merging the approved design in PR #272). Branch
`feat/minimalist-kafka-k1`. The outbound half of the Kafka port, built on the
ruled decisions (rdkafka client, MockCluster unit double, Q1 crate home):

1. **`client_config`** — the template pipeline: librdkafka-named templates
   read via ConfigReader with `${ENV_VAR:default}` substitution; compiled-in
   defaults (`include_str!` — the library-jar classpath analog) shadowed by
   app-resources files and overridable with location chains; known JVM-only
   keys (serializers, partitioner.class, sasl.jaas.config, callback handlers)
   dropped with a log line each; the veto-not-trigger opt-out flags; the
   health-probe rule (consumer template normally, the producer template's
   connection/security surface on a produce-only leg).
2. **`notification`** (`simple.kafka.notification`) — the full Java contract:
   topic/partition routing headers; bytes verbatim / Map+List→JSON / Nil→
   tombstone / anything else rejected with the Java-parity message; header
   propagation with the reserved-header exclusions; the correlation-id
   auto-stamp fallback from `my_correlation_id` (explicit value wins); a
   FRESH W3C traceparent from this hop's own span (custom names stamped
   alongside); `subject` refused 501 naming the Q2 deferral; the
   disabled-producer 500 naming the flag. Publishing awaits the broker ack —
   the Mono contract carried by an async fn.
3. **`health`** (`kafka.health`) — the platform health contract: Metadata
   probe (no group join, ACL-tolerant, topics count), placeholder + background
   warm-up under `kafka.health.startup.grace`, lazy per-rebuild template
   resolution, waiting-vs-outage boundary (unbuildable/unresolvable config =
   passing waiting), `{text, code}` 503; the blocking metadata call offloaded
   to the tokio blocking pool — the async-correct mapping of Java's
   `@KernelThreadRunner`.
4. **`bootstrap`** — `#[main_application(sequence = 20)]` auto-start
   collected from the library by inventory (Java classpath-scan parity):
   builds the shared producer, honors the flags, logs the inert case, and
   names the K2 deferral when `yaml.kafka.flow.adapter` is set.
5. **Platform-core additions** (Java-parity accessors the module needed):
   `PostOffice::my_span_id()` (zero-traced hops own no span) and the public
   `w3c_trace` export.

**Suites: 16 green first run** — 7 outbound-contract scenarios against
MockCluster (incl. a traced `po.request` through a registered worker proving
the ack path and the fresh traceparent end to end, and a keyless-spread pin on
the partitioner), 5 health scenarios, the disabled-path in its own process
(the runtime holder is process-wide), 3 unit pins.

**Deltas recorded** (spec §7 item 4 new): the Java `SimpleRandomPartitioner`
class maps to librdkafka's built-in `murmur2_random` — same semantics
including the Java-compatible key hash (a key maps to the SAME partition from
either engine), configured not coded; and nothing needs serializer pinning on
a byte-native client.

**Layout correction (same session, Eric):** Q1 was clarified after the K1
build — in this repository `crates/` is the Java repo's `system/` analog (all
system modules live there), and the Rust `system/` folder exists for the
scope-specific AGENTS.md consumer surface. Relocated accordingly:
`system/minimalist-kafka` → `crates/minimalist-kafka`, and (same convention)
`crates/redis-test-double` → `extensions/redis-test-double`. Path deps and the
workspace member list updated; all gates re-run green after the move.

## Memory References

- ot-minimalist-kafka-port (updated - K1 done, next gate K2)
- port-bottom-up-faithful (consulted - SimpleKafkaNotification /
  KafkaClientConfig / KafkaHealthCheck / KafkaFlowAutoStart read line by line
  before any Rust; every divergence is a documented delta)
- conventions-rust-baseline (consulted - crate layout, license headers,
  inline unit tests, integration suites under tests/, publish-set metadata)
- inv-telemetry-presentation-parity (consulted - error messages, health
  status strings and the {text, code} shape kept byte-compatible; the
  traceparent stamp is what keeps one trace across the polyglot hop)
- eric-release-rhythm (consulted - branch prepared and pushed; PR creation
  and merge are Eric's gates)
