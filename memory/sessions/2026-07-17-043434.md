# Session (2026-07-17T04:34:34.000Z)

**Agent:** Claude Code

## What happened

**Increment K-7a shipped (repo increment 27) — the platform-core WebSocket server.**
K-6 (incl. the graph.js fixture activation) was committed and pushed at the
maintainer's direction (c5f25c9).

- **K-7 split deliberately**: K-7a = the lockstep layer-1 substrate (this); K-7b = the
  Playground (GraphUserInterface/GraphSession, the 1,494-line GraphCommandService
  grammar, GraphTraveler, JsonPathHandler, companion API, K-6-deferred REST endpoints,
  dev-gating K9). Rationale: each increment stays independently verifiable; the
  Playground alone is ~2.5K lines of Java + a 664-line session test.
- **`automation/ws_server.rs`** (design K6a): rides the REST server's HTTP upgrade
  path (hyper upgrade + tokio-tungstenite 0.24). Java WsRequestHandler protocol:
  `/ws/{name}/{token}`; per-connection private route pair `{session}.in`/`.out`
  (`ws.{random}.{seq}`); open/string/bytes/close lifecycle events; transmitter
  (string→text, bytes→binary, map/list→JSON segmented >62KB, type:close with
  status/message); idle sweep (websocket.idle.timeout ≥10s, default 60); housekeeper
  releases the route pair after the service consumes the close signal (reply-to
  pattern, Java parity). Explicit `register_ws_service(name, factory)` replaces the
  `@WebSocketService` classpath scan (same substitution as fetcher features).
- E2E with a REAL tungstenite client: 101 handshake, open greeting via tx path,
  text/binary echoes, JSON framing, client- and server-initiated closes, negative
  (unregistered path doesn't upgrade).
- Fix along the way: the WS housekeeper takes the session's platform (not the global
  instance) so per-test platforms clean up correctly.
- Verification: workspace 179 green, clippy 0, fmt clean. INCREMENTS row 27 + section;
  design doc K-7 split note.

Next: **K-7b — the Playground** on this substrate.

## Memory References

- referenced: `ot-design-knowledge-graph` (K-7a shipped — thread updated),
  `bp-active-knowledge-graph`, `bp-platform-core` (WebSocket server joins layer 1 —
  the second K6 lockstep extension), `vision-mercury`, `conventions-rust-baseline`,
  `port-bottom-up-faithful`
- created: (none)

**Also (maintainer follow-up, same session): declarative `#[websocket_service]`.**
Full Java `@WebSocketService(value, namespace)` parity via the established macro
pattern: a `WsServiceEntry` inventory (platform-core registry), the macro in
platform-macros (positional `#[websocket_service("graph")]` and named
`name/value + namespace` forms), and lifecycle collection in AppStarter phase 4 —
URL paths register (with Java's `validServiceName` check, invalid → error + skip)
before the HTTP server starts, and the server now starts when rest.automation OR any
websocket service exists (Java `startHttpServerIfAny` parity; the app also stays
alive for WS-only services). The programmatic `register_ws_service` API remains for
tests/dynamic use. Proven E2E in `tests/ws_macro.rs`: an AutoStart-booted app with
rest.automation=false serves a declared endpoint. Workspace 180 green / clippy 0 /
fmt clean.

**Also (maintainer follow-up #2, same session): declarative `#[fetch_feature]`.**
Field installations use declarative FetchFeature for load-bearing cases (OAuth 2.0
access-token fetch/refresh + bearer insertion into the provider request), so the
macro path matters beyond ergonomics. New `knowledge-graph-macros` crate (the
`#[simple_plugin]` pattern): `#[fetch_feature("name")]` (positional + name/value
forms) → link-time `FetchFeatureEntry` inventory → loaded by the engine's
GraphResources hook at startup (the Java PlaygroundLoader scan analog); explicit
`features::register` remains. E2E: DemoAuth (the Java test feature) declared via the
macro; a rust-auth supplement graph's provider lists `demo-auth`; the auth-echo mock
returns the wire Authorization header; asserted `Bearer fetcher` end-to-end.
Workspace 180 green / clippy 0 / fmt clean.
