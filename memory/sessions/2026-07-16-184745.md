# Session (2026-07-16T18:47:45.000Z)

**Agent:** Claude Code

## What happened

Maintainer committed E-1 and directed **E-2 with a refinement**: (1) MultiLevelMap
(direct map access) is the PRIMARY data-mapping tool — much lighter weight; (2) JSONPath
serves the user's **complex JSON queries**. Verified against the Java source that this
is exactly how the original is layered (`MultiLevelMap.getElement` delegates only
`$`-prefixed paths to Jayway JsonPath) — the refinement and Java parity coincide.

**Increment E-2 (repo increment 12) — data-mapping engine, SHIPPED.**

1. **Value-tree decision (E3 refined, recorded in the design doc):** the flow state
   machine runs on **`rmpv::Value`** — the bus currency — instead of the originally
   drafted `serde_json::Value`. Rationale: zero conversion between envelope bodies and
   flow state (the "lighter weight" the maintainer asked for) and **byte fidelity**
   (`Value::Binary` = Java's `byte[]`; serde_json cannot hold real bytes, which would
   have broken `:binary`/`:b64`/`file(binary:)` parity). `$.…` queries convert an
   on-demand JSON view for `serde_json_path` (RFC 9535) — the query path pays the
   conversion, the primary path pays nothing.
2. **`mlm.rs`** — runtime MultiLevelMap: Java get/set/remove/exists/key_exists
   semantics (null≠missing, list padding, stable indices, `key[]` append via
   `appendIndex`); Jayway-style query results (0→none, 1→scalar, many→array).
3. **`mapping.rs`** — the `DataMappingHelper` resolution half: constants (incl.
   `map(config.key)` from base config and `file()`/`classpath()` with
   text/json/binary/append modes via `SimpleFileDescriptor`), `f:plugin(...)`
   invocation (top-level-comma split so `text(,)` args survive; nested plugins → null,
   the Java execution-loop guard), legacy `:type` commands (error → ERROR log +
   pass-through, Java parity), `{model.key}` interpolation
   (`substituteRuntimeVarsIfAny`: strings/numbers only, else "null"; non-model braces
   untouched).
4. **`conversions.rs`** — `TypeConversionUtils` port with Java `String.valueOf`
   display parity (Nil→"null"; floats keep the decimal point via `{:?}`) and the Java
   numeric fallbacks — **str2int/str2long return −1 (not 0) and drop decimals**
   (verified in Java `Utility`; fixed my E-1 util which had used 0 — compiler behavior
   unaffected, checked).
5. **`plugins.rs`** — 19 core plugin bodies implemented (text/int/long/float/double/
   boolean/binary/b64/uuid/length/not/and/or/concat/substring/isNull/notNull/eq/ne —
   the set the legacy-syntax converter emits, verified body-by-body against the Java
   plugin classes incl. SubstringPlugin's exact bounds rules and the one-or-list
   multi-arg pattern); remaining built-ins are name-only and fail loudly until E-8.
6. **Parity capstone**: `tests/mapping_engine.rs` evaluates the **compiled greetings
   fixture** (E-1 output) against a simulated HTTP dataset and asserts the exact
   function-input body Java produces — conversions, negation chains, uuid identity
   across the 3-part decomposition, interpolation, `f:concat(…, text(,), …)`, header
   targets. Passed first run.

**Verification**: 34 event-script tests (25 unit + 9 integration); **139 workspace
tests, 0 failures**; clippy 0; fmt clean. Docs in lockstep: design doc E3 row amended
(dated refinement) + §5b; INCREMENTS.md row + section 12.

## Decisions

- E3 value-tree refinement (serde_json → rmpv) recorded IN the design doc's decision
  table with the date and rationale — the trace survives (VBDI: design amended, not
  silently diverged).
- New deps in event-script only: rmpv, serde_json, serde_json_path 0.7, base64 0.22,
  uuid (platform-core untouched — JSONPath stays out of layer 1).

## Memory References

- referenced: `ot-design-event-script` (E-2 shipped), `bp-event-script`,
  `vision-mercury`, `conventions-rust-baseline`
- created: (none — progress on the layer-2 thread)
