# Session (2026-09-02T18:27:19.000Z)

**Agent:** Claude Code

## What happened — v4.12.2 publish completion

Pre-publish verification caught one stale pin the release sweep missed:
`extensions/minigraph-state-redis/Cargo.toml` still declared
`mercury-platform-core = "4.12.1"` (the sweep grep globbed root + `crates/*` +
`examples/*` — never `extensions/`; the crate's own version was correct via workspace
inheritance). Fixed in commit `e6e4f499`, PR #227 (CI green, merged) — main-only; the
v4.12.2 tag keeps the stale pin (harmless: `^4.12.1` resolves to 4.12.2), recorded here
instead of re-cutting a published tag. Both GitHub releases published from the UI
(v4.12.2, Java + Rust; `gh release create` blocked on the token's missing `workflow`
scope — tags had been pushed over SSH). **All seven `mercury-*` crates then published
to crates.io at 4.12.2 in one uninterrupted chain** (existing crates — no new-crate
burst limit, as predicted); index verified (`newest_version: 4.12.2`).

**Durable lesson:** a release version sweep is a repo-wide grep for the old version
string, never a hand-globbed manifest list — the Java side already carries the same
lesson as its non-reactor placeholder poms; this is the Rust-side rhyme.

## Memory References

- ot-json-plugin-twin (its feature reached crates.io in this publish)
