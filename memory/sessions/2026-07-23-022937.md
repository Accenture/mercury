# Session (2026-07-23T02:29:37.000Z)

**Agent:** Claude Code

## What happened

**v4.10.0 release prepared** (Eric's direction; lock-step with the Java engine, whose
v4.10.0 shipped/published 2026-07-22 — tag on merge `af21e6f6`, PR #216). Branch
`chore/release-4.10.0`, cut from main `1d18883e` (deliberately carries the unpushed
memory-review commit — bookkeeping riding a release PR is the established pattern).
NOT pushed — Eric pushes and opens the PR.

- **Version bump 4.9.0 → 4.10.0:** the version surface is exactly ONE field — the root
  `Cargo.toml` `[workspace.package] version` (all ten member crates inherit via
  `version.workspace = true`; no inter-crate version pins — path deps only). Cargo.lock
  regenerated by a workspace build (10 member entries at 4.10.0). Swept with a
  digit-guard: no other 4.9.0 references exist outside HISTORY (CHANGELOG's 4.9.0
  section, INCREMENTS, session logs — all left untouched; the webapp package-lock's
  `>=4.9.5`/`^2.4.9` hits are unrelated dependency semvers). Runtime version strings
  (e.g. `/info`) come from `CARGO_PKG_VERSION` — auto-correct.
- **CHANGELOG:** `## Unreleased` → `## Version 4.10.0, 7/22/2026`, led by a
  release-summary paragraph mirroring the Java CHANGELOG's framing (language-neutral
  wire format, demo pair both patterns, default-on log context, span-lineage telemetry,
  validated by the live bidirectional Java ⇄ Rust drives + link to the Java site's
  Interop Test Report). Added the missing PR #166 entries (wire-format conformance,
  /api/event service + client with private-by-default, declarative
  `yaml.event.over.http`; Fixed: D2 ttl ceiling/grace, D3 echo binary-body fix) and
  stamped PR #166/#167 references on every entry.
- **Docs:** `event-over-http.md` "Same demo, different language" now links the Java
  docs' permanent Interop Test Report
  (accenture.github.io/mercury-composable/test-reports/event-over-http-interop/) rather
  than duplicating it.
- **Gate at the new version:** workspace 244 tests green, clippy 0 warnings,
  `fmt --check` clean.
- Continuity: status line version → 4.10.0; new release-in-flight thread
  [[thread-release-4-10-0]] (close when tagged + published).

Also (same session, closure): **v4.10.0 SHIPPED** — tag `v4.10.0` on merge commit
`4dc70337` (PR #168, CI green), release notes delivered, release published 2026-07-22;
[[thread-release-4-10-0]] marked `- [x]` in continuity (left in place for the review to
sweep).

## Memory References

- created: thread-release-4-10-0 (born tier: working)
- referenced: port-bottom-up-faithful, conventions-rust-baseline (CHANGELOG/INCREMENTS
  conventions, fmt/clippy gates)

Co-Authored-By: Claude Code <noreply@anthropic.com>
