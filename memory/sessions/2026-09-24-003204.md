# Session (2026-09-24T00:32:04.000Z)

**Agent:** Claude Code

## Summary

**`serde_yaml` retired for `yaml_serde` (Eric's ask after seeing `serde_yaml v0.9.34+deprecated` in the 4.12.16 publish log;
Increment 137).** Branch `chore/yaml-serde`, commit `5e9689b8`. Two manifest lines — `crates/platform-core/Cargo.toml` and
`system/ai-contract-provider/Cargo.toml` now declare `serde_yaml = { package = "yaml_serde", version = "0.10" }` — so Cargo's
package renaming keeps every `use serde_yaml::` import and no source file moved; the lock swaps exactly four packages
(`serde_yaml`, `unsafe-libyaml` out; `yaml_serde` 0.10.7, `libyaml-rs` 0.3.0 in) and `cargo tree -i` no longer matches either
deprecated crate. **Why this fork (evaluated against the field on crates.io):** published under github.com/yaml by the YAML
specification's co-author, "the actively maintained fork … with full compatibility", 7 releases since 2026-01 (latest
2026-08-18), ~1.9M downloads / 90 days; `serde_yaml_ng` and `serde_norway` have had no release since 2024 and still sit on
`unsafe-libyaml`, `serde_yml` is itself deprecated, `serde-saphyr` is pure Rust but a different API with a heavy dependency
tree. **Caveat recorded:** `libyaml-rs` is libyaml transliterated by c2rust — the same technique as the retired
`unsafe-libyaml`, now maintained; a pure-Rust parser would be a separate decision. RustSec: `serde_yaml` carries only the 2018
recursion advisory (fixed long ago), `unsafe-libyaml` the 2023 one fixed in the 0.2.11 we shipped — so this is hygiene, not
a fix. Gates: fmt clean, clippy clean, `cargo test --workspace` 603 / 0 / 9, check-doc-claims 28, check-llms-links 37. The
Stack section of continuity (footer-less prose) now names `yaml_serde` in place of the old "archived upstream" warning.
Follow-up worth taking (not done): `cargo audit` in the `rust` workflow so a deprecation surfaces in CI, not in a publish log.
No Java lock-step: a Rust-only dependency. PR to follow (Eric opens; this repo merges without squash).

## Memory References

- conventions-rust-baseline (consulted: docs and manifests stay aligned with the reference; a dependency swap needs no Java twin)
