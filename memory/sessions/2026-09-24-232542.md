# Session (2026-09-24T23:25:42.000Z)

**Agent:** Claude Code

## Summary

**A separate Schema Registry identity for the consumer side — Increment 139, the Rust twin of the Java engine's #458/#459
and its twin-kafka sync #460 (Eric: "before releasing v4.12.17, please sync the feature to twin-kafka and Rust"); branch
`feat/kafka-consumer-registry-identity` `9bd13fee`.** A Java field installation found that one codec per process, shared by
the producer and every schema-enabled flow-adapter binding, pins both directions to one registry identity, while its
Confluent registry grants access per direction (a produce identity pool and a consume identity pool). This port had the same
one-codec bootstrap and the same prefix seam (`SchemaCodec::for_registry`), so it gains the same opt-in:
`SchemaCodec::for_consumer(config, url, key_prefix, producer)` returns the producer's codec unless
`<key_prefix>.consumer.properties` names a template, then builds a second codec under `<key_prefix>.consumer` (same URL,
that template, its own caches); presence is the opt-in, blank = unset, the URL stays the feature switch; the bootstrap passes
the resolved codec to `start_flow_adapter` as a parameter (no second runtime slot). **The one delta from Java, stated in the
guide:** no serde layer and no CSFLE here, so the Java `schema.registry.consumer.serde.*` override route has no analog
(reported as ignored like any serde key) — the consume identity lives in the consumer template (typically the producer's
OAuth client with a different `bearer.auth.identity.pool.id`) and the codec sends it on every registry call; the simpler of
the two shapes. Docs: guide subsection, configuration row + reference entry, the template's note, llms.txt, a commented
sample in the sync-over-async demo (`application.yml` + `schema-registry-consumer.yml`, as on Java), Increment 139. Extends
[[schema-registry-native-codec]] (addendum); the port ships it at 4.12.17 with Java — twin-kafka itself stays deferred here
([[ot-minimalist-kafka-port]], Q3), so the Rust surface is the primary prefix only.

## Verification

`cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` clean — after one fix of my own: an
`assert!` message containing `${VAR:}` was read as an inline format argument (E0425), reworded. `cargo test --workspace`
**603 passed / 0 failed / 9 ignored** across 117 suites, started only after the Java suites had finished (the two share
ports); `schema_codec` scenario 14 pins unset/blank → the producer's `Arc` (`ptr_eq`), the opt-in alone → schema features
stay off, a configured template → a distinct codec under `schema.registry.consumer` (its own `ManagedCache`) that decodes the
producer's frame against the same embedded registry. `cargo audit` clean on the unchanged lock (320 crate dependencies);
`check-doc-claims` 28/28, `check-llms-links` 37/37, `mkdocs build --strict` clean.

## Memory References

- schema-registry-native-codec (addendum: the consumer-side identity on the same seam)
- ot-minimalist-kafka-port (consulted — twin-kafka deferred by ruling, so the Rust surface is the primary prefix)
