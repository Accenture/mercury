# Session (2026-07-19T15:57:15.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-7 (L6, mapping-language depth): PASSED — the cleanest
run yet: 8/8 commands, zero failures, ZERO in-band lookups, first-attempt dry-run pass.**

- **Pre-flight (maintainer prompts):** (1) tutorial help pages carry known errors (backlog #16) —
  used carefully; (2) the tutorial needs the `f:now()` simple plugin and not all Java plugins were
  assumed converted — **verified present + correct** (registry, `plugins_e8.rs`, and a live
  scratch-session probe of `iso`/`local`/`ms` in a real graph mapping) before briefing; no engine
  work needed.
- **Test:** fresh companion on primary `ws-783755-2` (verified EMPTY), problem-only brief: pure
  transformation graph — `{profile:{name,address1,address2}}` →
  `{hello:"world", name, address:[a1,a2] ordered, time:<execution-time local timestamp>}`.
  Output verbatim
  `{"address":["100 World Blvd","New York"],"hello":"world","name":"Peter","time":"2026-07-19 08:54:28.815"}`;
  independently re-run (fresh timestamp proved runtime generation). Exported `profile-reshaper`.
- **The updated grammar visibly drove the design:** indexed `address[0]/[1]` chosen with the
  documented determinism rationale (#23/#25); `f:now(text(local))` found via the #21 pointer into
  the event-script plugin catalog (cross-layer llms.txt map worked); nested instantiate seed
  lines inferred from the composite-key rule; **no cargo-cult island** (correctly reasoned #22
  doesn't apply without config nodes). Design more economical than canonical (2 nodes,
  end-as-mapper, the walkthrough's own "better solution" variant chosen independently).
- **Frictions #26/#27 (inference-only) FIXED same day:** example `f:` generator plugins now named
  inline in the KG grammar's non-constant-sources row; nested-seed `instantiate` example added
  (both + `minigraph-commands.json`).
- **Docs updated:** tutorial-7 stage record + L6 ladder row + rollup #26/#27 (DONE) in
  `docs/AI-companion-test.md`.

## Decisions

- Tutorial-7 defined as **L6 (mapping-language depth)** on the validation ladder.
- Findings #26/#27 fixed immediately under the standing maintainer direction (every finding round
  to date has been ordered folded into the grammar same-day).

## Memory References

- ot-companion-validation-sweep — advanced (tutorial-7 done; next tutorial-8); substance updated
- ot-help-pages-rewrite — referenced (maintainer re-confirmed help-page errors; tut-7 walkthrough
  self-corrects its own append+clear advice)
- bp-companion-sync — referenced (test driven wholly over `/sync`)
- port-bottom-up-faithful — referenced (plugin parity verified against the Java contract before
  the test)

Co-Authored-By: Claude Code <noreply@anthropic.com>
