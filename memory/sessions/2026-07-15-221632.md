# Session (2026-07-15T22:16:32.000Z)

**Agent:** Claude Code

## Summary

Harvested canonical context from **mercury-composable** (the Java source) and produced a
**Design proposal for the platform-core Rust port** — the first VBDI increment. No Rust code
written yet; implementation waits on the human gate (deliberate — Design altitude).

**Harvest gate satisfied.** The maintainer added `~/sandbox/mercury-composable` and authorized
reading it. Treated as a **read-only reference** (never modified — the target repo is
`~/sandbox/mercury`; scope discipline held). It is itself agent-memory-enabled, so its
`memory/` layer distilled much of the work.

**What I learned (folded into memory):**
- **North star (inherited):** AI-assisted Semantic Application Development — the **Active
  Knowledge Graph *is* the application**; Event Script + Platform Core are the foundation
  beneath it. Corrected my enable-time hedged layer descriptions in `vision.md`.
- **Three layers, accurately:** platform-core (event-driven foundation — route-named function
  actors exchanging immutable `EventEnvelope`s over an in-memory bus; `PostOffice` RPC;
  `Platform` registry; Vert.x + Java 21 virtual threads), event-script (YAML flow DSL +
  state-machine `model` + data mapping), active knowledge graph (graph model executes behavior
  via node skills).
- **Core invariant** added to continuity: **never couple functions directly** (route-name +
  `EventEnvelope` only) — the defining decoupling the whole design rests on.
- **Module map** (instructions.md): in scope `system/platform-core`,
  `system/event-script-engine`, `system/minigraph-playground-engine`(+`core/graph`); out of
  scope the Kafka mesh (`minimalist-kafka`, `twin-kafka`, all `connectors/`); deferred
  spring-rest adapters / scheduler / extensions / examples.
- **Canonical version 4.8.6**; platform-core alone ≈ 24.5K LOC / 121 files → a multi-increment
  port. Read: README, its memory/{vision,instructions}, root pom (module list),
  docs/guides/architecture.md, EventEnvelope/LambdaFunction/TypedLambdaFunction contracts.

**Design proposal (platform-core → Rust), pending gate** (`ot-design-platform-core`):
- Cargo **workspace** + `crates/platform-core`; **tokio** async runtime (virtual-thread analog;
  N `instances` = N worker tasks/route); **serde + rmp-serde** (MsgPack bus) / **serde_json**
  (HTTP boundary); explicit registration now, compile-time macro later (no runtime annotation
  scanning in Rust).
- **First increment (smallest provable foundation):** `EventEnvelope` (+ MsgPack round-trip),
  `ComposableFunction` async trait, `Platform` registry (register/has_route/release + per-route
  worker pool), `PostOffice` `send` + `request` (RPC via temp reply inbox + correlation id) —
  with tests (round-trip, RPC, timeout, N-instance concurrency).
- **Forks put to the maintainer:** (a) confirm tokio; (b) MsgPack **wire-format fidelity**
  (idiomatic serde vs. byte-compatible with Java's compact flag-keyed encoding); (c) workspace
  layout. Implementation begins once these are settled.

## Memory References

Created:
- `inv-never-couple-functions` (core, Architectural Invariant)
- `ot-design-platform-core` (design thread for the first increment)

Updated:
- `vision-mercury` — accurate three-layer descriptions + inherited north star & non-goals
- `ot-harvest-mercury-composable` — gate satisfied; high-level harvest done, per-layer to come
- `instructions.md` — Core Abstractions + Port Scope & Source Mapping sections
- Stack & Tools — canonical source (v4.8.6) + Rust-stack-pending-gate note

Referenced (blueprint context): `bp-platform-core`, `bp-event-script`,
`bp-active-knowledge-graph`
