# Session (2026-07-20T00:31:31.000Z)

**Agent:** Claude Code

## What happened

**Backlog item done: discovery commands `list graphs` / `list flows` (increment 42, BOTH
ports)** — closes sweep finding #38 (tut-11): agents and humans no longer need an out-of-band
brief for `extension={graph-id}` / `extension=flow://{flow-id}` delegation targets.

- **Design (one design, both engines):** the `list` command grows two **read-only** forms.
  `list graphs` = compiled registry ∪ deployed-location `*.json` files (Rust enumerates every
  resource root; Java enumerates exploded classpath dirs — inside a packaged jar the compiled
  registry still covers the models), each with its **root node's `purpose`** so the listing
  reads as living documentation of enterprise knowledge. `list flows` = the Event Script flow
  registry. Both work on the WS console AND both companion endpoints (the agent-facing path).
- **Rust:** `list_graphs`/`list_flows`/`deployed_dirs`/`graph_purpose` in
  `crates/knowledge-graph/src/commands.rs`; assertions in `tests/playground.rs`. Workspace
  202 tests / clippy 0 / fmt clean.
- **Java:** mirrored in `GraphCommandService` (imports `CompiledGraphs` + `Flows`); tested via
  `/sync` in `CompanionSyncTest#discoveryCommandsListDeployedGraphsAndFlows`; module suite
  **70 tests** green; branch **`feat/discovery-commands`** (`be85fe85`) pushed — maintainer
  opens the PR (based on post-#198 main, no stacking).
- **Docs:** grammar `#describe` section + discovery paragraph; JSON catalog `list graphs`/
  `list flows` entries; AI agent guide recipe step 1 ("discover before delegating");
  `skills-reference.md#extension` pointer; `help list.md` rewritten (webapp bundle
  re-released, 124 tests green). Rollup **#38 → DONE** in `docs/AI-companion-test.md`;
  ledger increment 42.
- Also this session (maintainer-directed): deleted the 7 merged local branches in the Java
  repo + pruned remote-tracking refs.

Also (maintainer browser-test refinements, same session): **`list flows` now shows the
mandatory `flow.description`** (the `Flow` model retains it in both engines — it was validated
at compile but discarded), and **the graph compiler enforces the discovery contract**: a
manifest graph whose root lacks a non-empty `purpose` is rejected at compile (Rust
`rust-no-purpose` fixture asserts the rejection; `unit-test-1.json` gained a purpose in BOTH
repos — the only fixture missing one). `help.md` overview + `help list.md` updated in both
engines (bundle re-released). Java branch second commit `6a93322a` pushed (suites 142+70
green); Rust gates all green.

## Decisions

- **Discovery is command-surface, not new REST endpoints** — `/sync` already gives agents
  REST access to every command, so `list graphs`/`list flows` ride the existing contract
  (read-only, no session-topology concerns).

## Memory References

- ot-discovery-commands-backlog — COMPLETED this session (increment 42, both ports)
- ot-companion-validation-sweep — referenced (finding #38 closed)
- port-bottom-up-faithful — applied (designed once, landed identically in both engines)
- conventions-rust-baseline — applied (gates, ledger row, doc surfaces)

Co-Authored-By: Claude Code <noreply@anthropic.com>
