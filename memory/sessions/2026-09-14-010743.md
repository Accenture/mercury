# Session (2026-09-14T01:07:43.000Z)

**Agent:** Claude Code

## What happened

**Experiment R4 conducted — the polyglot dry-run. THE WIRE-PARITY ACCEPTANCE
GATE IS MET, and the R-series is complete.** Eric's go: "conduct R4". One
`redis-standalone`, four pods across two engines (Java demo jar 4.12.8 built
via the reactor with `-am`; the Rust demo from R3), Node SSE witnesses.
Permanent record: `docs/test-reports/streaming-return-route-polyglot.md`
(mkdocs nav + AI-contract `files.list` updated). No code changes — the branch
is evidence, spec, and memory.

The round, all ✅ on the normative wire alone (no shims anywhere):

- **P1/P2** — Rust producer → Java UI pod and Java producer → Rust UI pod:
  exact token order + terminal metadata both ways (and the §5 item 1 vehicle
  delta proving interop-neutral: Lua-appended segments drained by the
  MULTI/EXEC engine and vice versa).
- **P3/P3b** — two engines posting into ONE rendezvous on each UI pod, either
  engine closing. Settles the R3 report's "presentation nuance": BOTH edges
  render a metadata-less terminal as `data: {}` — parity, not divergence.
- **P4** — the Java watchdog's final drain recovered RUST-stored lost
  segments (idle + 5 ms): the recovery machinery crosses engines.
- **P5** — the real-LLM leg cross-engine: the Java pod pulled Gemini's SSE
  (gemini-3.8-flash, 37 tokens — quota-polite) and the Rust edge rendered it
  progressively with the provider's usage metadata on `done`.

**Findings:**

1. **Bounce-path delta (maintainer ruling requested — report note 3):** after
   a Redis restart, redis-rs's `ConnectionManager` fails the FIRST command
   (`broken pipe`; its retries govern connection attempts, not command
   replay) where Lettuce requeues not-yet-written commands. Clean failure
   surface (500s), heals on the next call. Options laid out: accept and
   document, or retry-once on the store's idempotent ops only
   (SETEX/GET/DEL/LLEN) — the append stays fail-fast either way (an ambiguous
   RPUSH replay risks an undetectable duplicate under D7's no-sequence-number
   design).
2. **Both engines healed subscriptions live through a full server restart** —
   the Rust explicit resubscribe loop and Lettuce's auto-reconnect, on camera
   (§5 item 2 equivalent in practice).
3. **Operator note:** `kill -9` on the helper's wrapper JVM orphans
   `redis-server` (keeps the port; a later helper start wipes its data dir →
   `MISCONF`, writes disabled). SIGTERM reaps the child cleanly — verified.
   This mistake (mine, mid-round) is what produced findings 1 and 2.

Thread `ot-sync-over-async-port` CLOSED [x] with the terse record. Deferred
on their own gates: crates.io publication at the next release (path-only
dev-dep check for both Redis-backed crates), facade tasks (Q1), the note-3
ruling.

## Memory References

- ot-sync-over-async-port (closed - R-series complete, gate met)
- port-bottom-up-faithful (consulted - the delta found was pinned to library
  semantics read from redis-rs source, reported with options, not patched
  unilaterally)
- inv-telemetry-presentation-parity (consulted - the gate itself is this
  invariant made load-bearing at run time; the {} rendering was verified
  IDENTICAL rather than assumed divergent)
- conventions-rust-baseline (consulted - report frontmatter/nav/files.list
  conventions)
- eric-release-rhythm (consulted - branch prepared and pushed; PR creation and
  merge are Eric's gates; publication rides the release gate)
