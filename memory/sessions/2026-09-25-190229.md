# Session (2026-09-25T19:02:29.000Z)

**Agent:** Claude Code

## Summary

**graph.math on the Rust engine: CONDITION, booleans are never numbers, unknown functions and overflow fail by name
(Increment 141) — the lock-step twin of mercury-composable's `feat/graph-math-condition-and-typed-arithmetic`.** Eric ruled
on a field page of nine "behaviours that return a wrong answer" (measured on 4.12.7); four were engine changes and the
same evaluator shape lives here — `as_number` coerced `Value::Bool` to 1/0 for arithmetic, `<`/`>` and function
arguments while equality type-checked, `eval_call` failed generically, no finite check — so the same JSON `true` in a
numeric slot computed three different ways and an overflow travelled on as `Infinity`. Changes: `as_number` rejects a
boolean uniformly (`Boolean operand in '<op>': Boolean(true)`) and `ExpressionEngine::eval_number` rejects a boolean
RESULT; `eval_call` names the callee (`Unknown function: mn`, `'PI' is not a function`) and passes each argument
through the same typed check (a string argument now reads `Expected number in argument of sqrt(), got String(4)` — the
Java text, `StringValue.toString()` renders `String(4)` too); `finite()` on every unary, binary and call result
(`Arithmetic overflow in '*' (result Infinity)`, `Division by zero or arithmetic overflow in '/'`, NaN by name);
`common::name_offending_selectors` generalizes `name_null_identifier` — a boolean operand is mapped back to the selector
with the remedy (`Boolean operand: model.flag (true) in '…' - a boolean is not a number; store a boolean with CONDITION or
assert the type with f:validate`); `CONDITION: var -> expr` (`CONDITION_TAG`, counted by the compile gate; both gate
messages list it) substitutes in a logical context whatever operators it carries (`substitute_var_if_any_logical`),
evaluates with `eval_boolean`, stores a boolean at `{node}.result.{var}`. Minimalist boundary kept (Eric): exact-decimal
money is a `graph.task` function, never a math-package feature. Documentation rulings landed as the Java pages' twins
(`run` on the same instance keeps `model.*`, `instantiate graph`/`start` resets; a taken `IF` inside `for_each`
ends the walk; the end node is the terminus; `model.x[]` append/reseed) plus the in-Playground help, the command JSON,
claim `math-boolean-operand-rejected` (pinned to `graph_runtime::graph_task_matches_java_semantics`) and Increment 141.
Fact [[graph-math-typed-arithmetic-rust]] created. Branch `feat/graph-math-condition-and-typed-arithmetic`; PR text handed
to Eric (he opens PRs; a code change reaches main through a PR).

## Verification

`cargo test --workspace --no-fail-fast`: 117 suites, **602 passed / 1 failed / 9 ignored** — the one failure was
`expression_engine::random_and_argument_checks` asserting the pre-change string-argument message; updated to the typed
check's text and its binary re-run **16 / 16**. `unit-test-math-2` (the Java fixture byte-for-byte, registered in
`graphs.yaml`; the compiler manifest count 54 → 55) drives every case from `graph_runtime.rs`: CONDITION on a comparison,
a bare boolean and a boolean operation over a prior result; boolean rejection in arithmetic, a relational comparison and a
bare result, each naming `model.flag`; `Unknown function: mn`; overflow on `1e308 * 10`; division by zero as an error and
the same division computing with a non-zero divisor. `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets
-- -D warnings`, `cargo audit` (320 crates, no advisory), `check-doc-claims` (29 claims), `check-llms-links` (37),
`mkdocs build --strict` all clean.

## Memory Review (incidental sweep)

This log entering the window put three facts at sslu 21 > `archive_window` 20 and `memory-lint` raised `[overdue]`;
archived as faded with `archive-fact` (`memory/archive/2026-Q3.md`, INDEX updated, retrievable via `origin`):
playground-session-broker, for-each-suspend-index-key-rust, typed-function-envelope-reply. `refresh-metadata` also decayed
conv-template-version-sweep-rust active → archive-candidate. Not a review — no other fact moved.

## Memory References

- graph-math-typed-arithmetic-rust (created)
