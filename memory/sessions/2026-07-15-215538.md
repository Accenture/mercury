# Session (2026-07-15T21:55:38.000Z)

**Agent:** Claude Code

## Summary

AI-enabled this repo (`~/sandbox/mercury`) with the **agent-memory** protocol,
**v4.29.1**, **Mode A (Fresh Enable)** — a **greenfield** case (only a placeholder
`README.md` with the title `# mercury`, one commit, no source, no AI footprint).

**Consent + discovery depth (Step 3).** Presented the informed-consent advisory; the
maintainer chose **Proceed**. Because the repo is greenfield (no code), *standard scan* and
*deep analysis* are equivalent — there is nothing to scan or harvest, so no code/markdown
discovery was run (recorded here per Step 8.8). No `docs/` tree or knowledge markdown exists
to harvest (Step 4b was a no-op by absence).

**Vision (greenfield inversion — asked first, never inferred).** The maintainer set the
Vision directly: **mercury is a Rust port of `mercury-composable`** (Accenture's event-driven,
composable application platform; canonical implementation in Java), carrying the *same*
product vision. Scope — three layers ported bottom-up (**platform-core → event-script →
active knowledge graph**, foundation → UI); the **Kafka service mesh is out of scope** for
simplicity. It's a **private prototyping repo** (keeps rapid iteration quiet for public
readers) that **graduates to the official Accenture repo** once the foundation is sufficient.
Written as a **confirmed** Vision (no DRAFT banner). The authoritative detailed vision/specs
live in the Java project — captured *map, don't mirror* (not copied), since that repo is
outside this target's scope and was not read.

**Blueprint derived** (the target is confirmed): five `(blueprint)` gaps
(platform-core → event-script → active-knowledge-graph → foundation-to-UI → graduate-to-Accenture)
plus a `(knowledge-harvest)` thread to fold the Java project's specs into per-layer Designs
when a local checkout is available and authorized.

**Installed:** `memory/` (instructions, continuity, vision, decay-policy, smoke-test,
archive/INDEX, this session log); steering (`AGENTS.md`, `CLAUDE.md`, `GEMINI.md` with native
`@`-imports, `.cursorrules`, `.windsurfrules`, `.github/copilot-instructions.md`,
`.github/pull_request_template.md`); protocol docs (`DECAY.md`, `REVIEW.md`, `SKILLS.md`,
`MERGE.md`); `.agent/schema.md` + `.agent/version.md` (v4.29.1, Mode A); 7 built-in skills →
**42 adapters** (6 vendors × 7); ritual triggers (`.githooks/` **activated** via
`core.hooksPath`, CI floor `agent-memory.yml`); `.gitignore` + `.gitattributes`. Nothing
committed — left to the maintainer.

## Memory References

Created (seeded at enable):
- `vision-mercury` (core) — the confirmed Vision
- `ai-enabled-greenfield`, `port-bottom-up-faithful`, `kafka-mesh-out-of-scope`,
  `private-repo-then-accenture` — Key Decisions
- `ot-vision-confirm-blueprint` (done), `ot-greenfield-no-code`,
  `ot-harvest-mercury-composable` — Open Threads
- Blueprint: `bp-platform-core`, `bp-event-script`, `bp-active-knowledge-graph`,
  `bp-foundation-to-ui`, `bp-graduate-to-accenture`
