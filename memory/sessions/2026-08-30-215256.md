# Session (2026-08-30T21:52:56.000Z)

**Agent:** Claude Code

## Summary

**Route pool platform API — the Java engine's registerRoutePool twin, MERGED same day
as [PR #220](https://github.com/Accenture/mercury/pull/220) merge `9dea9182` carrying
gated `d753d004` (tree verified, CI green — build/test 2m34s/memory, authoritative
recheck — branches deleted both ends). ADR-0017 Proposed→Accepted via the merge (flip
commit `e2914058` straight to main). Increment 94; rides the next release via
CHANGELOG Unreleased.** Java half: PR #303 squash `b3741664` (design record + full arc
in the Java repo's log 2026-08-30-211721; design spec
draft-design-specs/register-route-pool.md there, D1–D10 ratified by Eric).

- **API** (`platform.rs`): `register_route_pool(prefix, function, count)` registers
  private singleton routes `{prefix}.0..{count-1}` (strict FIFO lanes, one shared
  stateless function) and returns the ordered member list; `release_route_pool`
  symmetric. Pool registry = `Arc<RwLock<HashMap<String, usize>>>` (lifecycle metadata
  only — `routes()` stays truthful); mutations serialize on a dedicated
  `Arc<Mutex<()>>` (the Java twin's ReentrantLock); RELOAD semantics on
  re-registration; `warn_if_pool_member` guards in `register_with_options` and
  `release` — range-checked (canonical digits, `< 10` length guard for exact Java
  behavior, `n < count`), so `{prefix}.10` beside a count-3 pool is never
  misclassified; entry-removed-FIRST on release so internal member releases never
  self-warn (also deadlock-free: the mutation mutex is separate from the registry
  RwLock).
- **Adoption** (`server.rs`): the streaming reply-lane pool registers through the API
  on every server start — the pool reload rebinds lane workers to the current runtime
  (the per-test-runtime idiom preserved), the once-per-process checkout fill
  (`POOL_FILLED: OnceLock`) unchanged; ONE "Reloading route pool" warn replaces 500
  per-lane reload warns per restart. `ASYNC_HTTP_RESPONSE_STREAM_PREFIX` (dotted)
  became `ASYNC_HTTP_RESPONSE_STREAM_POOL` (un-dotted base; Java's same-day touch-up)
  — lane names on the wire/telemetry unchanged; `automation/mod.rs` re-export updated.
- **Tests**: `tests/route_pool.rs`, the Java `RoutePoolTest` twin, 6/6 on isolated
  `Platform::new()` registries — ordered private singleton members + live RPC through
  a lane, symmetric release, reload-no-orphans, invalid args (count 0, non-canonical
  prefix, trailing-dot prefix; Java's null-lambda case has no Rust analog — noted as
  a parity comment), tolerated individual member updates, out-of-range neighbor
  untouched. Actuator display pin (`async.http.response.stream.0 - 499`) unchanged.
- **Docs**: ADR-0017 (Accepted; formalizes route-pool-registration-design — the
  design fact lives in the Java repo's continuity, the established cross-repo
  convention); Increment 94; CHANGELOG Unreleased (Added + Changed); api-overview
  guide gains both method sections. **INCREMENTS.md style cleanup (Eric's
  direction):** the Overview summary table REMOVED (it had drifted — rows stopped at
  increment 86 while sections run to 94; "a duplicate index drifts out of sync with
  the sections it summarizes" — convention note updated to one-section-per-increment),
  and all 56 `---` horizontal rules removed for consistent heading-only separation.
- **Gates**: cargo test 68 suites / 367 passed / 0 failed; clippy --all-targets 0
  warnings (verified UNPIPED — a `grep -c` pipeline had made the clean run look like
  exit 1: the piped-exit-code trap cuts both ways); fmt --check clean (after one
  rustfmt chain-wrap); mkdocs --strict green.

## Memory References

- referenced: port-bottom-up-faithful, conventions-rust-baseline,
  inv-telemetry-presentation-parity, thread-http-streaming-twin,
  ot-progressive-rendering-v4120
- created: ot-route-pool-twin (tier: working, closed at creation — merged same day)
