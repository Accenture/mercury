# Session (2026-07-19T17:51:17.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-13 (L12, the custom-logic seam: `graph.task`): PASSED
— 16/16 commands, zero failures, ZERO in-band lookups, first attempt. THE SWEEP IS COMPLETE:
all 13 tutorials (plus the tut-5 retest) passed.**

- **Pre-flight (maintainer prompt):** `v1.hello.task` verified **ported** — `mock.rs`
  `#[preload(route = "v1.hello.task", instances = 50)]` with the first-class
  `#[optional_service("app.env=dev")]` stacked above (increment 34 in action), Java-contract
  parity, fixture-tested.
- **Test:** problem-only brief with a twist — the `x-app` header value (`portfolio-app`)
  deliberately differs from the docs' example (`minigraph`), so the header idiom must be
  understood, not copied. The companion nailed it: `input.body -> *` whole-body merge +
  `text(portfolio-app) -> header.x-app` + `result -> output.body`; output verbatim
  `{"app":"portfolio-app","doubled":42.0,"greeting":"Hello, world"}`; independently re-run
  (`Eric`/`50` → `Hello, Eric`/`100.0`). Exported `hello-task-app`. Island documents the
  capability + response entity.
- **Frictions #47/#48 FIXED same day:** bare-`result` rule for `graph.task` now stated (was
  shown-only; aligned with `graph.extension`); the "(Tutorial 13)" example label (a pointer
  outside the self-sufficient set) relabeled.
- **Sweep scorecard added to `docs/AI-companion-test.md`:** Java-docs era (1–4, incl. the tut-4
  fail→fix→re-verify cycle) → this repo's docs (5–6, gaps #9–#22) → **eight consecutive
  zero-lookup first-attempt passes (5-retest, 7–13)**. 48 rollup findings, all resolved or
  tracked; engine deliverables along the way: `/sync` (both ports), the read-only companion rule
  (#194), first-class `#[optional_service]` (increment 34), layer-1/2 AI-doc port, the island
  knowledge-layer mandate. Open follow-ups: help-pages rewrite, discovery commands, `/sync`
  ok-heuristic (#40).

## Decisions

- Tutorial-13 defined as **L12 (the custom-logic seam)**; the sweep thread is **complete** and
  marked `- [x]` in continuity (follow-ups live in their own threads).

## Memory References

- ot-companion-validation-sweep — COMPLETED this session (all 13 tutorials passed; thread marked
  done); substance updated with the final scorecard
- bp-companion-sync — referenced (the entire sweep ran over `/sync`; its thesis — in-band
  self-correction — proven at every level)
- ot-help-pages-rewrite — referenced (remains the main documentation follow-up)
- ot-discovery-commands-backlog — referenced (remains the main engine follow-up)
- port-bottom-up-faithful — referenced (v1.hello.task parity verified before the finale)

Co-Authored-By: Claude Code <noreply@anthropic.com>
