# Session (2026-07-20T01:44:42.000Z)

**Agent:** Claude Code

## What happened

**Human docs phase 2 shipped (increment 44)** — seven source-verified pages by two parallel
writer agents, reviewed file-by-file (mechanical sweeps + spot-reads), `mkdocs build
--strict` green.

- **IA refinement (maintainer screenshot):** the nav now mirrors the Java site's
  layer-organized sidebar — Foundations / Layer 1 — Event-Driven / Layer 2 — Event Script /
  Layer 3 — Knowledge Graph, with **each layer's AI agent guide inside its section** ("the
  repo is AI-enabled" as a navigation statement). The earlier Reference→AI-docs bucket is
  dissolved into the layers.
- **Pages:** architecture (mermaid pipeline), methodology, observability (telemetry record +
  context log line **verbatim from a live hello-world run**, matching trace/span ids);
  event-driven index + write-your-first-function (typed/untyped, `#[preload]` def-list
  reference, lifecycle) + function-execution (pools, elastic queue, back-pressure,
  send/request/send_later, interceptors, tracing); event-script index (hello-flow
  walkthrough).
- **Divergence honesty — 17 port-note admonitions**, several engine truths the writer agents
  dug out of source: no broadcast/multicast (my brief had wrongly claimed it — the
  verification rule caught it), no fork-n-join RPC, no execution strategies, no `round_trip`
  telemetry metric, task-local trace propagation (no per-request PostOffice rule), OTLP
  forwarder not ported, MsgPack only on the spill path, `send_later` doesn't stamp the
  ambient trace, an interceptor failure still auto-routes to `reply_to`,
  `my_correlation_id()` resolves only inside traced execution.
- Remaining: phase 3 (knowledge-graph human pages + REST automation + the D-H2 reference
  conversions), phase 4 (background/port-scope + polish).

## Memory Review

Mini-sweep on lint advisories (not a full cadence review): `refresh-metadata` re-tiered
3 facts (working→active); **archived** `ot-subscribe-via-sync-bug` → `2026-Q3.md` (faded —
completed 2026-07-18, fixed in both engines via PR #194, sslu 21 > archive_window 20;
lint-verified, no over-archival flag).

## Memory References

- ot-human-guides-backlog — referenced + substance updated (phase 2 done)
- port-bottom-up-faithful — applied (map-don't-mirror; divergences boxed, never silent)
- conventions-rust-baseline — applied (verification bar; ledger row)

Co-Authored-By: Claude Code <noreply@anthropic.com>
