# Session (2026-09-14T01:50:14.000Z)

**Agent:** Claude Code

## What happened

**The minimalist-kafka port design drafted, with the mockforge-kafka
investigation conducted live** (Eric's three directives, 2026-09-14: hold the
sync-over-async publication + Q1 facade tasks until minimalist-kafka lands;
investigate `mockforge-kafka 0.3.221` for unit tests with the Java standalone
helpers owning integration/interop; design first). Branch
`feat/minimalist-kafka-design`. Deliverable:
`draft-design-specs/minimalist-kafka-port.md` (DRAFT — §3 canon checklist from
the Java guide, §4 client decision, §5 the investigation with a head-to-head
spike, §6 component map, §8 K1–K5 plan, §9 open questions Q1–Q5).

**The investigation, evidence-first** (a scratch spike: identical
produce-5/group-consume-5 scenario against both candidates, real rdkafka
client): `mockforge-kafka 0.3.221`'s README quickstart FAILS on the first
produce — its topic auto-create lives in the Produce handler, unreachable by
librdkafka's metadata-first flow (Metadata answered "0 topics") — and passes
only after pre-creating the topic through its private Rust test API, with a
malformed LeaveGroup v1 logged on the way out; its tree costs 338 crates
(mockforge-core alone: 63 direct deps incl. axum/sqlx/QuickJS/protobuf — and
rdkafka itself). The control, `rdkafka::mocking::MockCluster`, passed
unchanged with ZERO added dependencies and a config-only activation mode
(`test.mock.num.brokers`) that matches the module's config-not-code
philosophy. **Verdict (spec §5.5): MockCluster for unit suites; mockforge-kafka
not adopted** — recorded with dated evidence, revisitable. Toolchain datapoint
for the VDI constraint: vendored librdkafka built with cc+make on a machine
WITHOUT CMake.

**Client decision (spec §4): `rdkafka`** — the only maintained Rust client
with the full consumer-group protocol the adapter needs (rskafka excludes
groups by design; kafka-rust unmaintained). Threading maps to librdkafka's own
native threads + async awaits (the soa.redis.health stance), not a literal
kernel-thread port.

**Bookkeeping:** new open thread [[ot-minimalist-kafka-port]]; the HOLD
recorded in the sync-over-async spec header and its closed thread record
(publication + Q1 facade tasks now gate on K5, which also delivers the
sync-over-async facade over this transport).

**Rulings follow-up (same session, on the open PR):** Eric ratified Q5
(MockCluster — "the simplicity / minimalist principle") during CI, then ruled
Q1 agree (`system/minimalist-kafka` / `mercury-minimalist-kafka`), Q2 defer
(schema surface to its own post-K5 spec), Q3 defer (twin-kafka), Q4 port
`kafka-demo` at K4. Spec status moved DRAFT → APPROVED; §9 now records the
rulings. Next gate: K1.

## Memory References

- ot-minimalist-kafka-port (created - the port thread, spec DRAFT awaiting
  Q1-Q5 rulings)
- ot-sync-over-async-port (closed thread, record updated - publication and Q1
  facade tasks re-gated on minimalist-kafka per Eric)
- port-bottom-up-faithful (consulted - the canon checklist was read from the
  Java guide and module before any Rust design; the client and test-double
  decisions are argued from verified evidence, not vibes)
- conventions-rust-baseline (consulted - spec format, workspace layout
  question Q1, publish-set conventions)
- eric-release-rhythm (consulted - design gated for review before any
  implementation; branch prepared and pushed; PR creation and merge are
  Eric's gates)
