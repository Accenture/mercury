# Session (2026-07-20T17:47:41.000Z)

**Agent:** Claude Code

## What happened

**GRADUATION DAY.** Java PR
[#203](https://github.com/Accenture/mercury-composable/pull/203) (the AI-grammar
back-port) MERGED — both engines now carry the battle-tested grammar — and the Rust repo
graduated to its official home.

- **The home:** the maintainer repurposed **github.com/Accenture/mercury** — the ORIGINAL
  Mercury (Java v1–v3) repository — as the Rust version's official home (legacy releases
  v1.13.0 and v2.1.0–v2.7.0 remain as release branches; all field installations are on 4.x
  via mercury-composable).
- **The ceremony (branch `graduation` in the new repo):**
  1. Full R&D history populated: `rnd/main` (github.com/acn-ericlaw/mercury, **122
     commits** — every increment since enable, 2026-07-15 → today) merged with
     `--allow-unrelated-histories` onto the repurposed main.
  2. Conflicts resolved: `.gitignore` → the R&D stack-aware version; `README.md` → the R&D
     body **de-privatized** ("private prototyping repo pending graduation" removed, docs
     URL now live at accenture.github.io/mercury) + a **Legacy versions** section
     preserving the repurposed README's history note.
  3. **MkDocs publishing automated:** `docs.yml` flipped from build-only to build + deploy —
     strict build on every push/PR; `mkdocs gh-deploy` to the `gh-pages` branch on pushes
     to main only (write permission scoped to the deploy job). One-time repo setting:
     Settings → Pages → serve from `gh-pages`.
  4. This memory ritual committed ON the graduation branch — the memory layer moves with
     the repo; this log is part of the imported history.
- **CRITICAL for the PR: merge with a MERGE COMMIT, never squash** — a squash would
  flatten the 122 imported commits into one, destroying the populated history that is the
  point of the ceremony.
- After the merge: the R&D repo (acn-ericlaw/mercury) freezes as the prototyping archive;
  all future work flows through the regular PR process on Accenture/mercury (maintainer
  decision 2026-07-20). The Java side still owes a new mercury-composable release (post
  #203) — maintainer's next item.

Also: graduation **PR [#147](https://github.com/Accenture/mercury/pull/147) MERGED** —
a true merge commit (`a4f1dac4`); the 122-commit history is live on the official main.
First post-graduation PR follows immediately: **Rust CI** (`.github/workflows/rust.yml`,
branch `ci/rust-workspace`) — fmt check + clippy `-D warnings` + full workspace tests,
deliberately path-unfiltered so it can serve as a REQUIRED check under branch protection
(a path-filtered required check strands docs-only PRs); gates verified green in the fresh
official clone (206 tests / clippy 0 / fmt clean).

## Memory References

- bp-graduate-to-accenture — executed (ceremony complete; PR pending merge)
- ot-java-ai-docs-backport — updated (PR #203 MERGED; grammar live in both engines)
- vision-mercury — the graduation criterion of the north star is fulfilled
- port-bottom-up-faithful — referenced (foundation → UI → graduation, as set at enable)

Co-Authored-By: Claude Code <noreply@anthropic.com>
