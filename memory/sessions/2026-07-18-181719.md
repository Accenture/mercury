# Session (2026-07-18T18:17:19.000Z)

**Agent:** Claude Code

## What happened

**Opened the Java upstream PR for the synchronous companion endpoint (bp-companion-sync item a).**
Following the agreed plan ("PR the design"), contributed it as an **ADR** — the Java repo's own
convention for a design decision (`docs/arch-decisions/ADR.md`; the repo is agent-memory-enabled with
an ADR ledger).

- **ADR-0008 (status: Proposed)** — proposes an additive `POST /api/companion/{session-id}/sync`
  returning the command outcome in-band `{ok, output, error, result}` + teeing to the WS console for
  real-time human+AI collaboration; captures the gap (fire-and-forget `PostCompanionCommand` leaves an
  AI caller blind), the mechanism (RPC + private capture route + tee, mirrorable in Java), alternatives
  (MCP / handler-returns-transcript, both deferred), consequences (additive; envelope shape is the
  cross-vendor contract), and points to the **proven Rust reference implementation**.
- PR: **Accenture/mercury-composable#188** (branch `docs/adr-0008-synchronous-companion-endpoint`).
  CHANGELOG intentionally untouched (a Proposed ADR is not a shipped change; the entry belongs with the
  Java implementation). `gh` switched to `acn-ericlaw` for the PR API op, then back (EMU can't create
  PRs — see the gh-emu-pr-workflow note); Java checkout tidied back to main.

**bp-companion-sync is now essentially DONE in Rust** (feature shipped + verified + docs + live
multi-party demo); the remaining Java implementation is owned upstream, tracked by ADR-0008.

Also this session: the endpoint URI was renamed `/command` → `/sync` (`b89a997`) so it communicates
intent (chosen over `/interactive`).

## Memory References

- referenced: `bp-companion-sync`, `vision-mercury`
- created: (none)
