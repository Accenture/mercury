# Session (2026-07-26T01:49:08.000Z)

**Agent:** Claude Code

## Summary

Invariant re-verification ceremony, authorized by Eric ("please proceed with
the invariant re-verification for the Rust repo when D5 is done"). Every
never-decay fact verified against LIVE code — actual grep/read of the
implementing surfaces, not a re-read of the facts — with special attention to
registration/plugin/extension mechanics after the P1/P2 macro arcs.

## Per-fact verdicts

- **`inv-never-couple-functions` — CONFIRMED.** User functions address peers
  only by route string over the envelope (`set_to("hello.world")` etc. in the
  examples; PostOffice request/send by route everywhere); no direct
  cross-function references. The macro arcs changed HOW functions/plugins/
  features register, never how they couple — plugins and features are
  name-keyed registries resolved by string, and yaml.preload.override remaps
  route NAMES, reinforcing the route-string as the only coupling. One
  transparent substance refresh (not a supersession — the claim never
  flipped): the fact's ADR-ledger parenthetical said "ADR-0001…0007, adapted
  from the Java repo"; the ledger now also holds the native ADR-0008 —
  parenthetical updated.
- **`inv-telemetry-presentation-parity` — CONFIRMED (and strengthened since
  the last check).** Live mechanics verified: the rpc-tag
  one-record-per-span suppression gate (platform.rs served_rpc), the entry
  scrub + exit sanitize over ENGINE_METADATA_KEYS, app-log-context default-on
  gating (logging.rs embedded default-log-context.yaml + app.log.context).
  The ce_traceparent + hygiene rounds closed with ALL EIGHT echoes identical
  (v4.10.4/v4.10.5) — the invariant's acceptance instrument passed at its
  strictest to date.
- **`port-bottom-up-faithful` — CONFIRMED.** Three layers + macro crates +
  standalone examples in place; the entire annotation-macro arc practiced the
  fact (Java reference implemented first, Rust mirrors with documented
  map-don't-mirror divergences).
- **`conventions-rust-baseline` — CONFIRMED, two code drifts remedied, one
  carve-out flagged.** fmt/clippy-as-done, Java-class-naming module docs
  (24 top-level files spot-verified), tests/ + tests/resources layout,
  INCREMENTS ledger through §70, standalone examples — all hold. Drift found
  and fixed IN THIS COMMIT: `crates/platform-core/src/util/mod.rs` and
  `crates/platform-core/src/automation/mod.rs` lacked the Apache-2.0 header
  (added). Flagged for Eric rather than decided: the `tests/ui` compile-fail
  FIXTURES carry no license header — deliberate, since `.stderr` line numbers
  depend on fixture content and a header shift would force TRYBUILD
  regeneration; proposed disposition = treat fixtures as test resources
  (Java's `src/test/resources` files carry no headers either).
- **Vision (`vision-mercury`) — CONFIRMED.** North star unchanged (Rust port
  of mercury-composable, same product vision, foundation → UI); the
  2026-07-21 current-state context remains accurate (graduated repo, three
  layers closed, runway = bp-foundation-to-ui + connectors backlog); the
  annotation-macro arc directly serves the template-for-future-ports
  trajectory the Vision names.

**No supersessions required.** `thread-reverify-invariants-2026q3` closed
with the verdicts; `last_invariant_check` stamped 2026-07-26 |
2026-07-26-014908.md.

## Memory References

- referenced (verified): `inv-never-couple-functions` (substance refresh:
  ADR-range parenthetical), `inv-telemetry-presentation-parity`,
  `port-bottom-up-faithful`, `conventions-rust-baseline`, `vision-mercury`
- closed: `thread-reverify-invariants-2026q3`
- Superseded: (none)

## Also (same working session, separate housekeeping commit)

docs: moved `docs/AI-companion-test.md` → `docs/test-reports/AI-companion-test.md`
(Eric; consistency with the interop report) — left `exclude_docs`, joined the site
nav under Reference; 4 referencing pages + llms.txt + 4 in-file relative links updated.
