# Session (2026-07-19T03:18:09.000Z)

**Agent:** Claude Code

## What happened

**Ported the layer-1 (platform-core) and layer-2 (event-script) AI grammar + reference docs from
the Java upstream into this repo** (maintainer-directed) — needed before tutorials 6–13, whose
extensions use flows (`graph.extension` → `flow://`) and tasks (`graph.task` → composable
functions). Principle applied per maintainer: **the Event Script YAML syntax is identical in this
port** (flow files port unchanged), while **the composable-function coding surface is the Rust
API** — every code example adjusted and verified against the actual source.

- **New `docs/guides/event-script/`** (from `~/sandbox/mercury-composable/docs/guides/event-script/`):
  - `ai-agent-guide.md` — flow authoring for agents (deploy = `resources/flows/` + `flows.yaml`
    manifest + `rest.yaml` `flow:` binding; checklist; recipe; points at `examples/hello-flow`).
  - `flow-grammar.md` — the deterministic flow DSL rules (structure, task fields, the eight
    execution types from `crates/event-script/src/compiler.rs` `EXECUTION_TYPES`, mapping
    mini-language). Legacy `:type` suffixes documented as **deprecated — do not generate**
    (maintainer ruling; still parse for backward compatibility).
  - `event-script-flow.json` — machine-readable flow schema (+ `deployment` object,
    `conversions_deprecated`, Rust `source_of_truth`).
  - `syntax.md` — the full 2,000-line reference, ported by copy-then-surgical-edit: all Java task
    code → Rust (`#[preload]` + `ComposableFunction`, serde typing, `#[simple_plugin]` free-fn
    plugins, Rust external-state-machine example based on the real test port); Java-only features
    marked **"Not in this Rust port"** (comma route lists, `yaml.preload.override`,
    `inputPojoClass`/serialization strategies — serde replaces them); component scan → link-time
    inventory; virtual threads/Vert.x → tokio; fixture refs → `crates/event-script/tests/`;
    fixed a duplicated `not` plugin-table row from the upstream doc.
- **New `docs/guides/event-driven/ai-agent-guide.md`** — the layer-1 function-authoring contract
  rewritten for Rust and verified against source: full `#[preload]` parameter table (route,
  instances, env_instances, optional_service, typed, zero_tracing, interceptor — from
  `platform-macros`), `ComposableFunction`/`TypedFunction` traits (1-based `instance`),
  pre-write checklist, PostOffice patterns (**trace context propagates automatically** — no Java
  `new PostOffice(headers, instance)` rule; RPC timeout ⇒ 408; no batch API — `tokio::join!`;
  `send_later`/`cancel_future_event`; **no broadcast/multicast in this port**), serde-truth
  serialization table (incl. null-omission), worked example + `auto_start_main!()` + rest.yaml.
- **`docs/llms.txt`** — new "Layer 2" and "Layer 1" sections mapping the six files, with the
  syntax-identical / functions-differ note. Knowledge-graph docs' cross-links to
  `../event-script/syntax.md` **restored** (removed as dangling during tut-5; targets now exist);
  `graph.task`/`graph.extension` sections link the new guides. All cross-directory links verified
  to resolve; JSON validated.

## Decisions

- **Map, don't mirror, applied to docs:** flow DSL content near-verbatim (it IS identical);
  function-coding content rewritten against `crates/platform-core` + `examples/hello-flow` —
  no invented APIs; Java-only features explicitly flagged rather than silently dropped.
- Scope: the per-layer AI set (ai-agent-guide + grammar + JSON + syntax reference). The Java
  repo's broader references (`annotations-reference`, `event-envelope-reference`,
  `flow-schema-reference`, `function-execution`, rest-automation set) NOT ported — add later if a
  tutorial test surfaces the need (the sweep exists to find that out).

## Memory References

- ot-companion-validation-sweep — substance updated (layer-1/2 AI docs now available to
  companion briefs for tutorials 6+ via llms.txt)
- port-bottom-up-faithful — referenced (docs ported layer-faithful; Rust API truth verified
  against source)
- bp-companion-sync — referenced (the doc set serves the same fresh-agent path `/sync` enables)
- conventions-rust-baseline — referenced (examples/<name>/ app-crate convention baked into the
  deployment sections)

Co-Authored-By: Claude Code <noreply@anthropic.com>
