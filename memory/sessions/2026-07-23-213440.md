# Session (2026-07-23T21:34:40.000Z)

**Agent:** Claude Code

## What happened

**Increment 65 — metadata injection hardening** (Eric's latest interop round; Java
reference implemented FIRST on mercury-composable branch
`feature/metadata-injection-hardening`, single commit `65719672`, read for exact
semantics). Rust branch of the same name, NOT pushed — Eric gates. Tracing correctness
untouched — pure header/metadata hygiene.

**Eric's design ruling (the contract, both engines):** a composable function has exactly
3 inputs — headers, body, instance. The headers are a COPY of the envelope headers with
read-only metadata INJECTED by the worker at entry and SANITIZED at exit. **Metadata is
never transported in the event itself.**

1. **Business cid → engine-managed envelope tag** `my_cid`
   (`post_office::BUSINESS_CID_TAG`) riding a new wire-compatible `tags` envelope field
   (same key as the Java standard format; skip-if-empty keeps the golden vectors
   byte-identical; no spec change). Converted at all stamping sites:
   `apply_current_trace` (Java touch — the cid-slot fill is retired; the slot stays free
   for internal correlation), the flow engine's task dispatch (executor.rs), REST
   automation's service events (build_event). Worker resolution: tag > legacy header >
   the port's direct-bus cid-slot convention (documented port note).
2. **Entry injection (worker):** the input header copy now carries ALL FOUR my_* keys —
   `my_route` (worker context), `my_trace_id`/`my_trace_path` (envelope fields),
   `my_correlation_id` (from the tag; a legacy pre-4.10.2 peer's envelope header is
   honored then removed). `x-event-api` is stripped from the function's view (envelope +
   copy — safe: the recursion guard runs on OUTBOUND sends, before delivery), and tags
   are scrubbed (engine-visible only). This port previously injected NONE of the my_*
   keys (po reads the task-local bracket) — the visible copy now matches Java's, so the
   echo demos are replicas (my_route even names the serving ALIAS, obsoleting the old
   docs port-note).
3. **Exit sanitization (worker):** `sanitize_response_headers` filters the four my_* +
   `x-event-api` from a returned envelope on the auto-reply path (Java
   `copyResponseHeaders`) — Eric's accidental-copy scenario. The REST boundary strip
   gains x-event-api too.
4. **HTTP response correlation echo:** the edge stamps the resolved cid onto the request
   dataset headers under the configured name (NEW — previously it injected a
   my_correlation_id dataset key; the flow adapter now finds the SAME edge-resolved
   value), and the response writer echoes it (default `X-Correlation-Id`; function-set
   header of the same name wins, via or_insert after transforms).

**Regression twins (Java names):** `metadata_is_never_transported_in_the_event`
(EventHttpTest twin — remote loopback hop: injected cid intact via tag transport,
envelope carries no my_correlation_id header, no x-event-api in any view, tags scrubbed),
`accidental_metadata_echo_is_sanitized_at_exit` (PostOfficeTest twin + the legacy
honor-then-strip path), `response_echoes_inbound_correlation_id` +
`response_carries_generated_correlation_id_when_absent` (RestEndpointTest twins; the
second also asserts end-to-end identity: response header == injected my_correlation_id).
Fixtures updated: HttpEcho reads the injected copy (the dataset my_correlation_id key is
gone); hello-flow e2e flips x-event-api to ABSENT and asserts the my_* injection +
injected cid == dataset x-correlation-id. NOTE: this port never had Java's legacy
"cid is NOT echoed back" assertion, so nothing to invert — the two new echo tests are
the coverage.

**Live acceptance PASSED** (hello-flow 8100 → hello-world 8085, both patterns):
X-Correlation-Id echoed (explicit `order-9001` and generated — the generated value
identical end-to-end across two apps and the wire: response header == injected
my_correlation_id); callee echo shows the same four injected my_* keys as Java
(my_route = the serving alias); x-event-api absent from every function view; session
info (`user: demo`) intact; direct-function endpoint echoes too.

**Gate:** workspace 249 tests green (245 + 4 twins), clippy 0, fmt clean. Docs:
reserved-names-and-headers (metadata contract rewrite + compatibility note + response
echo + fixed the stale "X-Event-Api unported" note), event-over-http guide (port note
now both-engines; sample shows my_* keys), CHANGELOG Unreleased (mirrors Java wording +
upgrade-together compat note), INCREMENTS §65.

## Memory References

- created: thread-metadata-injection-hardening (born tier: working)
- referenced: inv-telemetry-presentation-parity (this batch serves it — identical
  injected key sets on both engines' callees), port-bottom-up-faithful,
  conventions-rust-baseline

Co-Authored-By: Claude Code <noreply@anthropic.com>
