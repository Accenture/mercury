# Session (2026-07-30T00:44:51.000Z)

**Agent:** Claude Code

## What happened

**P5-2 of the graph suspend/resume lock-step arc: the mandatory CompileGraph
gate ("compiled or 404"), on branch `feature/graph-suspend-resume` (NOT
pushed — Eric gates).** Java reference: PRs #238–#241 merged, ADR-0011
accepted; the Rust port implements the FINAL surface only.

- **compiler.rs rewritten as the deployment gate:** obsolete
  `location.graph.deployed` startup warning; no-manifest WARN ("no deployed
  graph models will be executable"); the manifest carries its own `location`
  (default `classpath:/graph`, file:/-or-classpath:/ validation with fallback
  WARN — the flows.yaml convention), stored in the graphs registry; per-graph
  pipeline convert → import → root purpose → mandatory `end` node →
  `validate_suspend_resume` → register; rejection =
  `Rejected graph {id} - {reason}` error log, never registered.
- **Property-aware mapping rejection (Eric's Java ruling honored):** entries
  without `->` reject the graph for `mapping`/`for_each`/`output`; a bare
  `input` entry is skill vocabulary (fetcher dictionary parameter names) and
  passes silently — the old blanket error log is gone.
- **NEW `model_validator.rs`** — the GraphModelValidator twin (exact Java
  error strings; reuses the shared overflow-guarded ttl parser), called by
  BOTH the gate and the playground `run` command as a pre-run check on the
  instantiated graph: "Unable to run - <reason>" followed by the uniform
  "Graph traversal aborted" terminal (drain-safe for the sync companion).
- **Executor streamlined:** `deployed_graph_location()` and lazy per-request
  loading DELETED — deployed execution is registry-or-404 (`{id} not found`,
  identical presentation for failed and unlisted); the empty-model re-check
  and the per-request end-node check dropped (the gate guarantees); the
  executor no longer touches the filesystem. GraphCommandService reads the
  resolved deploy location from the registry.
- **`model.run` reserved:** joined event-script's RESERVED_MODEL_KEYS — the
  compile guard and the runtime dynamic-target guard share one function;
  `parser-test-32.yml` fixture copied verbatim from Java, registered in
  more-flows.yaml, compile-drop asserted.
- **Tests:** knowledge-graph `tests/compiler.rs` flipped to the full
  CompileGraphTest twin (7 tests — 39 compiled, err1-7 + no-end rejected,
  manifest-location default, per-err static-validator asserts);
  `rejected_deployed_graph_is_not_executable` (8 rejected ids all answer 404
  "not found"); `companion_sync_pre_run_check_rejects_broken_suspend_contract`
  (suspend node without ttl rejected pre-run, terminal still emitted);
  bare-input vocabulary unit test in the gate module.
- **Porting note worth keeping:** compiled-or-404 exposed five rust-*
  runtime fixtures that had been riding the deleted lazy path — the runtime
  test manifest must list EVERY graph a test executes (graphs.yaml is
  deployment intent). The Rust playground example app gained its own
  graphs.yaml (tutorials 1-12; 13 excluded like Java — it invokes an
  engine-test fixture function; tutorial-14 arrives with P5-4) plus
  `graph.model.automation` in application.yml.

Gate: workspace 293 tests / 0 failed, clippy 0, fmt clean.

**Remaining:** P5-3 (Redis store crate, example-app-only), P5-4 (tutorial-14
verbatim + docs/help/catalog surfaces + Rust ADR twins + the live tutorial-14
four-run drive).

## Memory References

- thread-graph-suspend-resume-p5 (extended: P5-2 increment recorded)
- conventions-rust-baseline (referenced: gate/commit conventions)
