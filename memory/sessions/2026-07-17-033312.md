# Session (2026-07-17T03:33:12.000Z)

**Agent:** Claude Code

## What happened

**Increment K-5 shipped (repo increment 25) — platform-core HTTP client +
graph.api.fetcher.** K-4 was committed and pushed at the maintainer's direction
(f45f455).

- **platform-core `async.http.request`** (Java AsyncHttpClient; closes the §7
  deferral): interceptor registered by the app starter (EssentialServiceLoader parity,
  500 instances; untraced via the existing skip.rpc.tracing default). Per-request hyper
  http1 (Java also builds a client per request), header ignore-list, cookies/session,
  x-ttl timeout, content-type response decoding. **Trace propagation insight**: the
  route is untraced, so Java reads trace from PostOffice.trackable(headers) — the Rust
  port reads the ENVELOPE trace + injected invocation headers (first attempt via
  ambient trace state failed; root-caused to skip.rpc.tracing). Header values trimmed
  at wire time (netty strips OWS, hyper rejects — the token-file trailing newline).
  Deferrals documented: streams/multipart, XML-as-text, https rejected explicitly.
- **AsyncHttpRequest** builder/parser added to platform-core automation.
- **E-9 http-client fixtures ACTIVATED** (event-script): echo.endpoint + rest.yaml +
  server.port config; http-client-by-config E2E (classpath bearer token, query/path
  params on the wire); the trace test runs Java's full shape — outer request through
  the REAL HTTP edge with W3C traceparent, adopted by the adapter, carried by the
  flow's declarative client task, observed downstream.
- **graph.api.fetcher** (512 Java lines): dictionary/provider with key:default
  fallbacks, provider input mapping namespaces, per-instance provider cache,
  response→result output mapping ([] per fork-join response), for_each fan-out with
  clamped concurrency. **features.rs**: FeatureRunner registry (explicit registration
  replaces Java's @FetchFeature scan — no runtime annotation scanning in Rust);
  built-ins log-request-headers/log-response-headers; throttled advisory for
  unimplemented features.
- E2E: mocks (mock.mdm.profile, mock.account.details) + 7 mock JSON fixtures verbatim;
  the test boots the real REST server (port 8090). Tutorials 3(+negative)/5/6/12/114 +
  unit-test-1 ALL PASSED FIRST RUN over real HTTP. hello* graphs → K-6
  (graph.extension); tutorial-113 permanently skipped (retired graph.js).
- One integration fix: the client registration tolerates concurrent AppStarter runs
  (parallel lifecycle tests) like the actuator registrations do.
- Verification: workspace 177 green, clippy 0, fmt clean. INCREMENTS row 25 + section;
  design doc K-5 note.

Next: **K-6 — graph.extension** (sub-graph + flow:// delegation) + remaining REST
endpoints; activates hello/helloworld/helloworld2 (minus retired js paths) and
tutorials 10/11 (flow-11.yml joins the engine flows).

## Memory References

- referenced: `ot-design-knowledge-graph` (K-5 shipped — thread updated),
  `bp-active-knowledge-graph`, `bp-platform-core` (§7 http-client deferral CLOSED),
  `bp-event-script` (E-9 http fixtures activated), `vision-mercury`,
  `conventions-rust-baseline`, `port-bottom-up-faithful`
- created: (none)
