# Session (2026-07-17T02:13:50.000Z)

**Agent:** Claude Code

## What happened

**Increment K-3 shipped (repo increment 23) — graph compiler + registry.** K-2 was
committed and pushed at the maintainer's direction at session start (36c1d2f).

- `knowledge_graph::compiler::compile_graphs` (Java `CompileGraph`,
  `@BeforeApplication(6)`): opt-in `graph.model.automation` manifest → per-graph
  load via `ConfigReader` (`${...}` resolves against app config — verified with
  `rest.server.port`) → deprecated simple-type-matching → plugin-syntax conversion
  using the SHARED event-script converter (layer 3 riding layer 2, no duplication) →
  `MiniGraph::import_graph` structural validation → `graphs` registry (Java
  `CompiledGraphs`). Java failure semantics: invalid graph = error+skip, unreadable
  manifest = warn.
- **K8 resource-root hook**: `#[before_application(sequence = 1)]` appends
  `CARGO_MANIFEST_DIR/resources` (jar-classpath analog; append never prepend — the
  app's own resources win). Runs before flow compiler (5) and graph compiler (6).
- **Fixtures verbatim**: 13 tutorials → `resources/graph/`; 13 Java test-only graphs
  + `graphs.yaml` → `tests/resources/`. All 26 manifest graphs compile.
- **Design correction recorded**: Java `PlaygroundLoader` is the FetchFeature
  scanner (API-fetcher features), NOT a graph loader — moved to K-5 in the plan.
- Parity suite: `CompileGraphTest` ported + a reference-resolution check (3 tests).
- Verification: workspace 176 tests green, clippy 0, fmt clean. INCREMENTS.md row 23 +
  section; design doc K-3 shipped note with the correction.

Next: **K-4 — the graph runtime** (GraphExecutor/GraphTraveler/GraphInstance state
machine, {id}@{node} correlation, decision routing, loop detection, core skills
data.mapper/math/task/join/island; graph-executor.yml exposes POST /api/graph/{id};
the retired graph.js gets its explicit failure message here).

## Memory References

- referenced: `ot-design-knowledge-graph` (K-3 shipped — thread updated),
  `bp-active-knowledge-graph`, `vision-mercury`, `conventions-rust-baseline`
- created: (none)
