# Session (2026-07-17T23:53:50.000Z)

**Agent:** Claude Code

## What happened

**Increment K-7b shipped (repo increment 28) — the MiniGraph Playground** on the K-7a
WebSocket substrate. Resumed after a mid-increment crash; the Playground source was already
written (`commands.rs`, `session.rs`, `traveler.rs`, `ws_ui.rs`, `rest.rs`, the dev-gated
`PlaygroundLoader` in `lib.rs`, help/template resources), and this session drove it to a
green, verified, shipped state.

- **The Playground** (Rust port of the 1,494-line Java `GraphCommandService` +
  `GraphUserInterface`/`JsonPathHandler` + the `rest` package): per-session draft-graph
  workbench text grammar (open/close/command, create/connect/list/describe/instantiate/run/
  execute/inspect/export/import/seen/session…), the `graph.traveler` dev walker, and the
  **AI-companion REST hop** `POST /api/companion/{id}` (dispatches to the
  `graph.command.singleton` handler; output streams to the session console) + the
  K-6-deferred dev REST endpoints. Help served from the 39 ported `help/*.md` (verbatim Java
  filenames, incl. the space-in-name convention). Dev-gating K9: `PlaygroundLoader`
  (`#[before_application(sequence = 8)]`) registers the workbench only when `app.env=dev`
  (default `dev`, matching Java `application.properties`); the home page registers regardless.

- **platform-core contract fix — the crash-adjacent bug.** Booting the engine now registers
  websocket services, which flipped `serve_http` true, so `AutoStart::main` hit its
  serve-until-`ctrl_c` wait and **blocked** — hanging every test that boots the engine and
  awaits `main` (this is what made the workspace suite "never return"). Root cause: the
  serve-forever wait lived in the async `main`, wrong for an embedder. **Fix:** moved the wait
  (+ telemetry-beat + `shutdown_cleanup`) into `AutoStart::run` (the standalone `fn main()`
  path). `AutoStart::main` now returns once booted — the HTTP/WS accept loop runs as a
  background task — so `run()` still serves-until-Ctrl-C but embedders/tests get control back.

- **Idempotency misstep, caught by the actuator suite.** My first attempt made
  `start_http_server` globally idempotent (one server per process). That broke
  `crates/platform-core/tests/actuator.rs`: each `#[tokio::test]` has its own runtime and the
  accept loop is spawned on the *calling* runtime, so a later test that reused the first
  test's address connected to a server that died when the first test's runtime dropped.
  **Correction:** `start_http_server` binds a fresh listener per call again; kept only a
  `server_address()` accessor (first-bind-wins) so an ephemeral (`rest.server.port=0`) boot
  can recover its port. `graph_runtime` now reads `server_address()` (the lifecycle already
  serves) instead of starting a second server on 8090.

- **Fixture/format fixes for the E2E** (`tests/playground.rs`): `list connections` rendered
  `root -["first"]-> mapper` (Rust `{:?}` quotes) → matched Java `ArrayList.toString()`
  (`root -[first]-> mapper`); `boot()` runs the dev lifecycle on an ephemeral port and reads
  `server_address()`.

- **Follow-up correction (maintainer caught it):** the inspect assertion first mapped
  `input.body.id -> end.message` and read `inspect end`. That was self-consistent but
  non-idiomatic — `end` is an arbitrary top-level key nothing reads. The graph's result
  namespace is **`output`**: `execution_complete` returns `output.body`/`output.header`
  (`common.rs:50-51`), and the canonical fixture pattern is `-> output.body`. Corrected the
  mapper to `input.body.id -> output.body` and the assertion to `inspect output`, so the E2E
  now exercises the real result path. (Shipped as a follow-up commit after increment 28.)

- **Verification:** `cargo test --workspace` **181 green** (was 180 after K-7a's macro
  follow-ups; +1 = the playground E2E), `cargo clippy --workspace --all-targets` 0 warnings,
  `cargo fmt --all --check` clean. INCREMENTS row 28 + section; design-doc K-7 section marked
  K-7b shipped. (Noted: the INCREMENTS K-7a row shows 179 — the pre-K-7b baseline was
  actually 180 after the ws_macro/fetch_feature follow-ups; not back-patched.)

Next: **K-8 — the React webapp + layer-3 milestone close.**

## Memory References

- referenced: `ot-design-knowledge-graph` (K-7b shipped — thread updated),
  `bp-active-knowledge-graph`, `bp-platform-core` (the `AutoStart::main`/`run` serve-contract
  fix is a layer-1 refinement), `vision-mercury`, `conventions-rust-baseline`,
  `port-bottom-up-faithful`
- created: (none)
