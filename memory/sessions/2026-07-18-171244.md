# Session (2026-07-18T17:12:44.000Z)

**Agent:** Claude Code

## What happened

**End-to-end verification of the synchronous AI-companion endpoint (`bp-companion-sync`) — PASSED.**
The maintainer restarted the Playground on the new binary and opened session `ws-776722-2`; probed
`POST /api/companion/ws-776722-2/command` → the sync endpoint is live and returns the structured
envelope (`{ok, output, error, result}`).

- **Full autonomous re-run of Tutorial-4 via the sync endpoint.** A *fresh* companion (canonical docs
  only + told about the new `/command` tool) rebuilt the compare-a/b decision graph, ran it, and
  **self-validated all three cases entirely through `/command`** — no WS console, no `GET` fallback.
  Its transcript: every command `ok:true` first try (the fixed graph.math docs held), and the run's
  `result` carried `output.body` plus the traversal trace, letting it confirm the **correct branch
  fired** (not just that output looked right):
  - `a=5,b=3` → `{less_than:false, sum:8.0, message:"…greater than or equal…"}` ✅
  - `a=3,b=5` → `{less_than:true, sum:8.0, message:"…less than…"}` ✅
  - `a=4,b=4` → `{less_than:false, sum:8.0}` ✅ (tie → `>=` path, per spec)
  The companion explicitly reported it **never needed information the `/command` response didn't give**
  — the exact Tut-4 blind spot, now closed.
- **Independent corroboration** (orchestrator, via `/command`): `a=7,b=2` → `ok:true`,
  `result.output.body = {less_than:false, sum:9.0, …}`; a garbage command → `ok:false`,
  `error:"Please try 'help' for details"` (error in-band). Final graph: 5 nodes / 5 connections.
- **Observation (design follow-up, logged on `bp-companion-sync`):** because sync mode captures output
  to the caller (not the WS `.out`), a *watching human* on the same session sees a quiet console even
  as the model populates — confirms the design note's "tee to both" as a real future enhancement.

**Net:** the companion tooling gap is closed and proven — both the error path (integration test +
live) and the full autonomous success path (live companion). Remaining on `bp-companion-sync`: PR the
design to the Java upstream; document the sync endpoint in the ai-agent-guide; optional WS tee.

## Memory References

- referenced: `bp-companion-sync`, `ot-companion-validation-sweep`, `vision-mercury`
- created: (none)
