# Session (2026-07-17T03:54:03.000Z)

**Agent:** Claude Code

## What happened

**Increment K-6 shipped (repo increment 26) — graph.extension.** K-5 was committed and
pushed at the maintainer's direction (852deea).

- **graph.extension** (266 Java lines): sub-graph delegation (launched as its own flow
  instance via the graph-executor flow + path_parameter.graph_id — recursion through
  the manager works naturally since each sub-graph gets its own flow/graph instance
  pair) and flow:// delegation (validated against the flow registry). for_each
  fork-join with clamped concurrency; results []-appended; fetcher-style output
  mapping; break-on-exception parity. flow-11.yml joins the engine flows.
- **Two parity gaps found and fixed at the correct layer** (both surfaced by fixtures):
  (a) `no.op` is a PLATFORM-CORE built-in in Java (NoOpFunction, 500 instances,
  worker.instances.no.op override) — my port had it only in the event-script test
  binary; now registered by the app starter (echo headers+body), test duplicate removed.
  (b) JSONPath hyphen tolerance: Jayway accepts `$.fetcher-ext.result` dot notation;
  serde_json_path is RFC 9535-strict → event-script MultiLevelMap rewrites offending
  segments to bracket notation when strict parse fails (unit-tested; only engages on
  parse failure so existing paths untouched).
- **Scope adjustment recorded in the design doc**: the K-6-sketched REST endpoints
  (describe/upload/inspect/live-graph) all depend on GraphCommandService + Playground
  draft/temp dirs → moved to K-7 with the Playground.
- E2E: tutorial-10 (extension → tutorial-3 sub-graph), tutorial-11 (flow://flow-11
  echo), helloworld2 (fetcher → for-each extension over helloext → math → end, incl.
  $.result[*] JSONPath output + x-hello response header). hello/helloworld stay
  non-runnable (retired graph.js).
- Verification: workspace 178 green, clippy 0, fmt clean. INCREMENTS row 26 + section;
  design doc K-6 note.

Next: **K-7 — platform-core WebSocket server (lockstep) + the Playground**
(GraphUserInterface sessions, GraphCommandService grammar, GraphTraveler, companion
API, the REST endpoints deferred from K-6, dev-gating K9).

## Memory References

- referenced: `ot-design-knowledge-graph` (K-6 shipped — thread updated),
  `bp-active-knowledge-graph`, `bp-platform-core` (no.op built-in added),
  `bp-event-script` (JSONPath leniency), `vision-mercury`,
  `conventions-rust-baseline`, `port-bottom-up-faithful`
- created: (none)

**Also (maintainer follow-up, same session):** the `graph.js`-carrying fixtures are
activated by swapping the skill to `graph.math` — the statement grammar is identical
(that is exactly why graph.math is the sanctioned replacement). `hello`, `hellojs`,
`tutorial-113` swapped mechanically; `helloworld`'s js-3 node carried genuine
JavaScript (`.filter()`, object literals, `.toFixed()`) and was adapted to
math-grammar computes. The former JS variant now renders numbers as doubles (558.0,
math semantics). `rust-js-retired` remains the SINGLE `graph.js` case validating the
retirement error. E2E added: hello, helloworld, hellojs, tutorial-113 (the retry
pattern: error-handler + clear-exception + f:add attempts + DELAY + RESET/NEXT jumps)
— all green first run. Docs amended (INCREMENTS 25/26 sections, design K-6 note).
