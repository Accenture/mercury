# Session (2026-07-18T19:15:55.000Z)

**Agent:** Claude Code

## What happened

**Regression-tested the Java sync AI-companion endpoint after #189 merged — automated green, and
live-verified on the example app. The feature now works in BOTH ports.**

- **Automated regression (merged Java main, `3dc0f39f`):** full multi-module `mvn` suite green — 601
  tests, 0 failures (platform-core 397 [3 pre-existing skips], event-script 136, minigraph 64 incl.
  `CompanionSyncTest`).
- **Found + fixed a gap in #189:** the standalone `examples/minigraph-playground` Spring Boot app loads
  its **own** `src/main/resources/rest.yaml`, which #189 didn't touch (I'd updated the engine module's
  main + test rest.yaml only) → `/api/companion/{id}/sync` **404'd** on the example app while the
  fire-and-forget endpoint worked. Opened follow-up PR **#190** adding the route (the
  `PostCompanionCommandSync` service is already on the example app's classpath via the engine
  dependency — no code change). Swept all three rest.yaml files; no others missing.
- **Java LIVE regression PASSED** (example app :8085, rebuilt from the #190 branch, session
  ws-185205-2): `/sync` bad command → `ok:false` + `error:"Please try 'help' for details"` (in-band);
  create/connect/instantiate/run → `ok:true` + `output`; `inspect output.body` → `result` with
  `outcome:"hello world"`. **Tee confirmed** — the maintainer's Playground console streamed every
  command and the graph rendered `root → end` live.
- **Cross-port observation (not a defect):** Java `run` emits only the traversal trace to the console
  (`Walk to root/end`); output.body is fetched via `inspect` (which `/sync` folds into `result`). Rust
  `run` additionally echoes output.body to the console, so its run response carries it directly. `/sync`
  faithfully captures whatever each engine emits. Align the two `run` consoles only if desired.

`bp-companion-sync` is live-verified in both Rust and Java; only #190's merge remains (upstream). The
Java repo working tree is on `fix/companion-sync-example-restyaml` (so the example rebuild had the
route) — return to `main` after #190 merges.

## Memory References

- referenced: `bp-companion-sync`, `vision-mercury`, `port-bottom-up-faithful`
- created: (none)
