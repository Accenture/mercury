# Session (2026-08-14T00:20:42.000Z)

**Agent:** Claude Code

## Summary

agent-memory **Mode B in-place upgrade v4.33.2 → v4.33.3** (tool-operator session; also the
release's pre-tag test drive on a live target):

- Re-copied the `memory-lint` built-in (scripts, tests, SKILL.md body). v4.33.3 hardens the
  `[secret-material]` check after a security review: quoted JSON/YAML credential keys and
  `Authorization` headers now flag (no value echo); the ALL-CAPS enum exemption is key-scoped
  (uppercase literals on secret-bearing keys no longer bypass); placeholder recognition is
  full-value anchored, with `${VAR:}` / `${VAR:-x}` / `${a.b.c}` template forms recognized as
  safe.
- Re-copied `.github/workflows/agent-memory.yml` (verified stock before replacing): the wrapper
  now invokes `memory-lint --strict`, so advisory findings surface as CI warning annotations,
  and `AGENT_MEMORY_STRICT=1` genuinely blocks. **Verified both branches here by simulating the
  wrapper**: advisory exit 0 with the annotation emitted; strict exit 1. Expect warning
  annotations on pushes until this repo's overdue memory review runs — advisory by design.
- Verified: 46/46 mirror tests both runtimes; runtime output byte-identical; `[secret-material]`
  scan across the full memory layer: **zero findings**.
- Stamped `.agent/version.md` → 4.33.3 (enabled_with 4.29.1, mode A preserved).

## Memory References

(none — tool upgrade only; no project facts referenced or created)
