# Session (2026-07-19T17:36:08.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-12 (L11, resilience engineering): PASSED — 27/27
commands, zero failures, ZERO in-band lookups, BOTH dry-runs first attempt** (session
`ws-783755-2`; the most demanding exercise of the sweep).

- **Prep:** #38 discovery-commands backlog saved as [[ot-discovery-commands-backlog]] (maintainer
  direction). Canonical `tutorial-12` scratch-probed end-to-end before briefing (retry machinery
  verified live — not covered by fixture tests); the probe surfaced **#40**: `/sync` `ok`
  heuristic false-negative on `import`'s benign "not found → Found deployed" fallback line
  (caveat documented; engine fix = maintainer call, both ports).
- **Result:** the companion composed `exception=` failure routing + a **three-node retry
  machine** (count/bound → DELAY-500 pause + mutual RESETs + NEXT → recovery clearing the flag)
  — equivalent to canonical, arguably cleaner (mutual resets instead of comma-list self-reset;
  no defensive status check — the handler only runs on failure). Failing path: 3 real failures,
  two visible pauses, `attempts: 3.0`, recovery, success; happy path untouched. Independently
  re-run with person 200. Exported `resilient-profile`.
- **Findings #41–#46 — seven inferred load-bearing behaviors, all engine-verified and FIXED same
  day:** new **Failure routing** section (`exception=` semantics + canonical bounded-retry
  pattern); RESET truths (comma lists, no-op on unexecuted, self-reset, loop guard >10/s);
  taken-`IF`-jump ends the statement list / `next` falls through; `NEXT:`/`DELAY:` written forms
  + timing (NEXT applies after the list, last wins; DELAY pauses after the node); unresolvable
  mapping source **skips** (default idioms documented); dedup = per graph instance,
  success-only cache (retries always re-call). All mirrored in `minigraph-commands.json` +
  `skills-reference.md`.
- **Docs updated:** tutorial-12 stage record + L11 ladder row + rollup #40–#46.

## Decisions

- Tutorial-12 defined as **L11 (resilience engineering)**; failure-simulation switches are
  legitimate environment facts in briefs.
- #40 recorded as an engine-fix candidate (heuristic `ok` flag) — maintainer call, both ports.

## Memory References

- ot-companion-validation-sweep — advanced (tutorial-12 done; next tutorial-13); substance updated
- Created: ot-discovery-commands-backlog (born tier: working)
- bp-companion-sync — referenced (the `ok` heuristic finding #40 is a refinement candidate for
  its envelope)
- port-bottom-up-faithful — referenced (six engine-truth verifications before documenting)

Also (same working session, maintainer follow-up on #42): **self-RESET verified in code — it is
EFFECTIVE, not a no-op.** The traveler/executor mark `node_seen` BEFORE executing the skill, so a
self-reset during statement processing survives and the node can run again (canonical tut-12
relies on it; empirically confirmed by the earlier live probe). Nuance documented: a self-RESET
also wipes the node's own in-flight state — place it before `DELAY:` (whose pending pause is
stored in node state); canonical order RESET → NEXT → DELAY is the safe pattern.

Co-Authored-By: Claude Code <noreply@anthropic.com>
