# Session (2026-07-17T00:25:47.000Z)

**Agent:** Claude Code

## What happened

Maintainer committed increment 19, directed proceed → **Increment E-9 (repo
increment 20) — HTTP adapter, resilience, mock, hello-flow. THE EVENT-SCRIPT
(LAYER 2) MILESTONE IS CLOSED.**

1. **`HttpToFlow`** (`http.flow.adapter` ×200, interceptor): AsyncHttpRequest → flow
   dataset (ttl from the route timeout — added "timeout" to the platform-core
   request map, additive; header/body/path_parameter/query/method/uri/ip);
   configurable `http.correlation.id.header` (default X-Correlation-Id) → business
   cid; launches with the HTTP edge's reply routing preserved so the flow's end
   response completes the request. Missing x-flow-id → 400 error envelope.
   Cookies/streams/filename/session keys absent (platform-core §7; null-tolerant).
2. **`Resilience4Flow`** (`resilience.handler` ×500, interceptor): full port —
   gatekeeper (200 → decision 1), retry with attempt counting + `send_later` delay,
   abort (decision 2, original error), alternative path (decision 3; codes + ranges
   "401, 403-404"), cumulative-failure backoff (503 with the Java message shape).
3. **`EventScriptMock`**: reassign/restore task routes via a dispatch-time override
   registry (Java mutates Task; our immutable shared template made a registry the
   safer equivalent — recorded divergence; before/after monitors NOT ported, no
   fixture uses them).
4. **`examples/hello-flow`** — the milestone showcase: one rest.yaml endpoint with
   `flow: 'hello-flow'`, a 3-task decision flow in YAML, two annotated functions,
   `auto_start_main!()`. **Gotcha caught live**: an app that never references the
   event-script crate gets its inventory dropped by the linker — the engine didn't
   register on first run. Fix doubles as a feature: the `#[main_application]` calls
   `event_script::flows::get_all_flows()` to announce deployed flows (real usage =
   guaranteed linkage). Live-verified: lang=fr → "Bonjour, eric!", lang=en →
   "Hello, eric!", edge X-Correlation-Id (order-777) flowing through as model.cid.
5. **E2E scenarios added**: resilience gatekeeper/retry-exhaust-abort/401-alternative
   (with the fixture's temp-file cumulative/backoff store), simple-circuit-breaker
   (recovers on accepted attempt 2 — `exception.simulator` upgraded to the faithful
   Java port with status-coded exceptions + accept/attempt), adapter round-trip +
   missing-flow-id 400, mock reassign→400 through the mock→restore→normal.

**148 workspace tests, 0 failures; clippy 0; fmt clean.** Docs: ES design §5i +
milestone summary; INCREMENTS row 20 + milestone banner.

**Layer-2 milestone recap (E-1…E-9, repo increments 11–20, one day)**: compiler →
mapping engine → platform-core extensions → core runtime → direct-execution
optimization → parallel/fork-join → pipelines+loops → sub-flows/shared-state/ext →
42 plugins + #[simple_plugin] → adapter/resilience/mock/hello-flow. Validated
against the canonical Java fixtures throughout (66 flows compiled, working set E2E).
Not ported, traced: HTTP-client fixtures (needs platform-core §7 http client),
stream payloads, mock monitors.

## Memory References

- referenced: `ot-design-event-script` (E-9 — milestone closed),
  `bp-event-script` (CLOSED), `bp-active-knowledge-graph` (next),
  `vision-mercury`, `conventions-rust-baseline`
- created: (none)

## Archival (window-triggered)

`kafka-mesh-out-of-scope` crossed the 20-session window → archived via `archive-fact`
(the scope decision lives durably in vision.md). Lint 0/0 (17 active, 4 archived).

_also: added test-drive README.md to both example apps (examples/hello-world — run
variants, greeting API, actuators, etag/304 + filter walkthrough; examples/hello-flow —
both language branches, cid propagation, tracing, and a "change the transaction
without touching code" section). Docs-only follow-on to this session._
