# Session (2026-09-13T16:32:16.000Z)

**Agent:** Claude Code

## What happened

**Health-check route renamed in the port spec: `redis.health` → `soa.redis.health`
(Eric's ruling, 2026-09-13, applied to BOTH engines).** The plain `redis.health`
route name is reserved for the health check of a planned generic Redis
distributed-cache module, so sync-over-async and that module can coexist against
the same Redis server. Route name only — the `redis.health.timeout` /
`redis.health.startup.grace` config keys are unchanged on the Java side (config
keys do not collide; routes do). The spec's §1 scope bullet, §6 component-map row
(now carrying the reservation rationale as normative), and Q3 updated. The Java
twin renames the live implementation (mercury-composable branch
`chore/soa-redis-health-rename`).

## Memory References

- ot-sync-over-async-port (updated in substance via its spec - the §6 health row
  is now normative on the route name)
