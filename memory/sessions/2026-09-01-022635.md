# Session (2026-09-01T02:41:40.000Z)

**Agent:** Claude Code
**Round:** Streaming reply-lane rotation (FIFO round-robin checkout) — **MERGED
2026-09-01 as [PR #221](https://github.com/Accenture/mercury/pull/221) merge `0372313a`
carrying gated `29a986d1`; tree verified; branches deleted both ends. Increment 95;
rides the next release via CHANGELOG Unreleased.**

## What happened

Lock-step twin of the Java engine's same-day change (mercury-composable PR #304),
surfaced by the Java side's live agent-orchestration regression run: every sequential
streaming request re-picked `async.http.response.stream.499` because the checkout pool
was a LIFO stack (boot fill 0..499 left 499 on top; release pushed back on top).

- `lane_pool()` became `Mutex<VecDeque<String>>`: `checkout_lane` pops the FRONT,
  `release_lane` pushes to the BACK — sequential streams walk `.0`, `.1`, `.2` and a
  just-released lane rests the full pool length before reuse (maximal separation from any
  straggler cleanup on it); lane selection is predictable in telemetry.
- Test pin flipped: `lane_checkout_is_lifo` → `lane_checkout_rotates_through_the_pool`
  (a released lane must go to the tail, not be reused immediately).
- Increment 95 appended to docs/INCREMENTS.md; CHANGELOG Unreleased entry.
- Gates: platform-core cargo test green; clippy clean; fmt clean (one wrap: rustfmt wants
  the `lane_pool().lock().expect(...).push_back(...)` chain broken across lines — the
  known chain-width rule).

## Memory References

- Referenced: ot-route-pool-twin (the reply-lane pool this refines),
  inv-telemetry-presentation-parity (lock-step rule)
- Created: (none — the change is recorded as Increment 95 + this log; the Java repo's
  ot-agent-orchestration-e0 thread carries the round)
- Reactivated: (none)
- Superseded: (none)
