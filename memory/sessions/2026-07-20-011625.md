# Session (2026-07-20T01:16:25.000Z)

**Agent:** Claude Code

## What happened

**Design drafted for the human documentation site** (`docs/design/human-docs.md` v1 —
Design-altitude artifact for [[ot-human-guides-backlog]], **gate pending maintainer review**).
Maintainer direction: use the Java repo's human guides as the content reference, this repo's
source + AI docs as truth, and the `agent-memory` MkDocs site (`~/sandbox/agent-memory`) as
the presentation model — the Java site's wide reference tables (e.g. the 4-column
`configuration-reference`) overflow and force horizontal scrolling.

Key design decisions (proposed, not yet approved):
- **D-H1 toolchain:** MkDocs + Material (pinned 9.7.6), reusing the agent-memory recipe
  (tabs/sections/indexes, def_list, admonitions, superfences+mermaid, snippets, strict build);
  `docs/` is already the docs tree; internal material (`design/`, `INCREMENTS.md`,
  `AI-companion-test.md`) excluded; Pages deploy deferred until graduation (CI = build-only).
- **D-H2 presentation rule (the core fix):** no reference table wider than 3 columns —
  reference entries are **entry-per-heading** (deep-linkable anchors, full-width prose, code
  blocks under the entry), definition lists for short sets; mermaid diagrams; `!!! note "Rust
  port"` admonitions for divergences.
- **IA:** Home / Getting Started / Concepts / Guides / Reference / Background tabs; new human
  pages under `docs/guides-human/` so the AI set's paths stay untouched; the engine-verified
  AI docs surface under Reference as-is (link, never restate — one source of truth per fact).
- **Source map:** 18 Java pages → new pages with port-truth notes (tokio, `#[preload]`,
  cargo, port 8100, `APP_PROFILES_ACTIVE`/`application.name`, content-type dispatch,
  discovery commands, `composing-the-layers` finally created — closes the sweep's dangling
  link); 8 Java pages explicitly skipped (Kafka/Spring/mesh/etc. → `background/port-scope.md`).
- **Verification protocol:** same bar as the help-page rewrite — code verified against
  source/examples, config keys against `AppConfigReader` reads, `mkdocs build --strict`.
- **4 increments** (scaffold+getting-started → concepts+layer guides → references with D-H2
  conversions → background+integration), 4 open questions (tree name, site identity, palette,
  in-app help pages unchanged).

Also (same working session): **GATE PASSED implicitly + phase 1 SHIPPED (increment 43)** —
the maintainer provided the `uv` toolchain ("You can create python virtual environment in
/tmp"), taken as approval on the design defaults. Landed: `mkdocs.yml` (Material, agent-memory
recipe) + `docs-requirements.txt` (mkdocs-material 9.7.6 pinned) + `docs/index.md` (three
layers, mermaid, port-note convention) + `docs/getting-started.md` (all three examples,
source-verified endpoints/code) + `.github/workflows/docs.yml` (build-only `--strict` CI;
Pages deferred to graduation) + `site/` gitignored. AI docs surfaced under Reference as-is;
ADR under Background; internal material excluded. `mkdocs build --strict` green locally.
Also: **PR [#199](https://github.com/Accenture/mercury-composable/pull/199) MERGED** —
discovery commands live in BOTH engines; Java main synced (`1767334d`).

## Decisions

- (pending gate) The design itself decides nothing until the maintainer approves — recorded
  here as the Blueprint→Design transition for the backlog thread.

## Memory References

- ot-human-guides-backlog — referenced + substance updated (Design drafted, gate pending)
- ot-companion-validation-sweep — referenced (dangling-link finding #9 addressed by the plan)
- port-bottom-up-faithful — referenced (map-don't-mirror applied to the docs plan)

Co-Authored-By: Claude Code <noreply@anthropic.com>
