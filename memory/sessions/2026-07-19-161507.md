# Session (2026-07-19T16:15:07.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-8 (L7, reshaping toolbox: JSONPath + transformation
plugins): PASSED — 17/17 commands, zero failures, ZERO in-band lookups, first-attempt dry-run
pass** (session `ws-783755-2`, verified actually empty before briefing).

- **Task (problem-only):** strip the internal `description` field from every account in
  `{profile:{name, account:[{id,amount,description,type},…]}}` → `{name, account:[…]}`, order
  preserved, list length data-driven.
- **Technique chosen:** the one-mapping `f:removeKey(input.body.profile.account,
  text(description))` route — the canonical walkthrough's own "indeed easier" recommendation;
  invocation form inferred from the generic plugin pattern (→ #28, fixed). JSONPath/`listOfMap`
  not needed by the winning design; unused fallback was a `graph.math` `for_each` loop (→ #29,
  candidate). Independently re-run by the orchestrator — reproduced.
- **Emergent knowledge-layer behavior:** with NO config nodes, the companion still built an
  island with two **data-entity nodes** (`person-profile`, `account`) documenting the domain —
  incl. "description is internal-only". The #22 living-documentation idea now shapes designs
  unprompted; scope for pure-transformation graphs raised as maintainer question (→ #30).
- **Fixes (same day):** #28 — `removeKey` syntax + worked example in
  `guides/event-script/syntax.md` (code-verified contract: map or list-of-maps, N key args,
  returns a copy); `removeKey`/`listOfMap` added to the KG grammar's `f:` examples; JSONPath
  wildcard-yields-a-list note in `minigraph-commands.json`.
- **Docs updated:** tutorial-8 stage record + L7 ladder row + rollup #28 (DONE), #29 (candidate),
  #30 (maintainer question) in `docs/AI-companion-test.md`.

## Decisions

- Tutorial-8 defined as **L7 (reshaping toolbox)** on the validation ladder.
- #29 deferred until a tutorial exercises `graph.math` iteration (needs engine verification).
- #30 (island scope for graphs without config nodes) put to the maintainer with proposed wording:
  *required* when config/data-entity nodes exist; *encouraged* otherwise.

## Memory References

- ot-companion-validation-sweep — advanced (tutorial-8 done; next tutorial-9); substance updated
- ot-help-pages-rewrite — referenced (tutorial help pages remain the flagged error source;
  contract derived carefully per the standing note)
- bp-companion-sync — referenced (driven wholly over `/sync`)
- port-bottom-up-faithful — referenced (removeKey contract verified against the port before
  documenting)

Also (same working session): **#30 RESOLVED — maintainer agreed the proposed wording**: island
**required** whenever config/data-entity nodes exist, **encouraged** otherwise as ER
documentation — landed in `command-reference.md#island`, `ai-agent-guide.md`,
`minigraph-commands.json`. Maintainer color captured in `syntax.md`: `listOfMap`/JSONPath exist
for **"impedance matching"** between external (3rd-party) and internal API contracts — the
tutorial had hoped to test-drive `f:listOfMap`, and the companion's more direct `removeKey` route
was a pleasant surprise, not a miss.

Co-Authored-By: Claude Code <noreply@anthropic.com>
