# Session (2026-07-15T22:28:16.000Z)

**Agent:** Claude Code

## Summary

Maintainer hint reshaped the platform-core port order → Design doc revised to **v2
(config-first)**. Still no Rust code; implementation awaits review of the v2 doc.

**The hint (maintainer, 2026-07-15):** Spring is also out of scope — it is Java-only. But the
foundation **configuration management** — `AppConfigReader`, `ConfigReader`, and the
`resources/` folder convention — is *essential* and should be ported **first**: everything
relies on it (main app, unit tests, integration tests).

**Harvested this session** (from `system/platform-core` at v4.8.6): `AppConfigReader.java`
(230 LOC — singleton; bootstrapped by an `app-config-reader.yml` manifest listing
`bootstrap.properties` → `bootstrap.yml` → `application.properties` → `application.yml`;
active-profile overlays via `SPRING_PROFILES_ACTIVE` env / `spring.profiles.active`; flat-merge
with sorted-key normalization into a MultiLevelMap; references resolved once after merge),
`ConfigReader.java` (464 LOC — classpath:/file: prefixes, `.yml`↔`.yaml` interchangeable,
yaml/json/properties, `${VAR:default}` substitution with env-var → base-config-reference
precedence + loop detection, `System.getProperty(key)` checked first, `../` traversal rejected,
cached composite flat map), `MultiLevelMap` public API (dot-bracket composite keys `a.b[0].c`),
and the shipped `app-config-reader.yml`.

**Design doc v2** (`docs/design/platform-core-port.md`):
- **Increment 1 = configuration management:** `MultiLevelMap` + `ConfigValue` enum,
  `ConfigReader` (formats, substitution, loop detection), `AppConfigReader` (`OnceLock`
  singleton, embedded default manifest via `include_str!`, app override, profile merge),
  `ResourceResolver` (ordered resource roots as the classpath analog; `./resources` default;
  tests prepend `tests/resources` — shadowing like Java), process **override registry** (the
  `System.getProperty` analog, needed by tests). Deps for inc-1: serde, serde_yaml, serde_json,
  thiserror only.
- **Increment 2 = event-bus foundation** (previous v1 content preserved: EventEnvelope, trait,
  Platform + MPMC pool, PostOffice send/RPC).
- New decisions **D7** (Spring fully out of scope; platform-core's own Vert.x-based REST
  automation stays in scope later), **D8** (config first), **D9** (config-file syntax kept
  verbatim — config files are data and should port unchanged Java↔Rust).
- §8 open questions: keep `SPRING_PROFILES_ACTIVE` naming verbatim?, resource-root defaults,
  route naming, edition, async-channel, increment scoping.

**Memory updates:** instructions.md (Spring → Out of scope with reason; port-order note),
continuity (new Key Decision `config-first-spring-out`; `ot-design-platform-core` → v2 status;
Stack & Tools inc-1 dep subset).

## Memory References

Created:
- `config-first-spring-out` (Key Decision — maintainer hint)

Updated:
- `ot-design-platform-core` — Design doc v2 (config-first), awaiting review
- Stack & Tools — increment-1 dependency subset
- instructions.md — Port Scope & Source Mapping (Spring out; port-order note)

Referenced: `bp-platform-core`, `vision-mercury`, `platform-core-stack`,
`kafka-mesh-out-of-scope`
