# Session (2026-08-22T02:31:08.000Z)

**Agent:** Claude Code

## Summary

**Lock-step port of the Java engine's ai-contract-provider (AI discovery app) + the
associated onboarding doc surfaces — branch `feature/ai-contract-provider` commit
`30497961` (34 files, +2068) — MERGED 2026-08-22 as mercury PR #209, merge `c0f5245e`
(tree verified identical to the gated commit), CI green (test 2m18s + authoritative
recheck), branches deleted both ends.** Eric's directive after the Java
app shipped and the onboarding assessment closed: "do a lockstep update to ~/sandbox/mercury
with the new ai-contract-provider and the associated doc update." Placement ratified via
checkpoint: crate at `examples/ai-contract-provider` (playground precedent); consumer guide
at `system/AGENTS.md` — SAME path convention as the Java repo, so one tool convention finds
both engines.

**The headline proof: the seven flow YAML files are BYTE-IDENTICAL to the Java app's and ran
on this engine unchanged** (rest.yaml too) — the portability convention proven on a whole
application's orchestration layer, first try.

**Structural adaptations (each a deliberate divergence, documented in module docs):**
- Behavior anchors = fully-qualified Rust paths in contracts.yaml, verified at COMPILE TIME
  by the anchor test (`Class.forName` analog): the test references every anchored item AND
  asserts the yaml set == the verified list; knowledge-graph is a dev-only dependency (the
  runtime dependency arrow stays inverted — a runtime dep would preload the playground).
  Anchors: AutoStart/PostOffice/EventEnvelope, automation::routing::RoutingTable,
  event_script::compiler::compile_flows, knowledge_graph::{commands::handle,
  compiler::compile_graphs, model_validator::validate}. Contract ids IDENTICAL to Java
  (platform-core/rest-automation/event-script/minigraph) — the consumer vocabulary is
  engine-portable.
- Snapshot embedded at BUILD TIME by build.rs from resources/skill/files.list (the Maven
  resource-include analog): missing doc → build failure; single self-contained binary (the
  nested-jar-safety analog). 41 inventory entries + generated installed-contracts.md.
- mercury_version = workspace-pinned crate version; the Java mixed-assembly startup refusal
  is structurally unnecessary (one Cargo lockfile) — documented divergence.
- The packaged `references/llms.txt` replaces Java's llms-link rewrite: this port's
  llms.txt links only into guides/, so it is self-contained INSIDE the snapshot (verified
  by the link validator) — a structural improvement over the Java exclusion+note approach.

**Gotcha worth keeping:** a bin crate included into test binaries via `#[path]` must use
`super::`-relative imports between its own modules — `crate::` paths resolve to the TEST
crate root under inclusion and break compilation (hello-flow never hit this: no inner
modules).

**Tests (4 binaries, 11 tests, all green first run after the import fix):** anchors
compile-verified == catalog + negative validation fixtures; inventory == the docs closure
walk (a new guide file fails until files.list carries it); manifest recompute; reference
reader 404 contract; byte-identical double export + 409 never-overwrite (refusal proven
mutation-free) + invalid roots; e2e over every endpoint on the real REST server (ephemeral
port). **CLI export proven live**: `cargo run -p ai-contract-provider -- --export` → 43
files, exit 0, every per-file hash independently re-verified in python (snapshot
`52e0ce72…`). Gate: **63 suites / 317 tests** (was 58/306), clippy 0 (one `--fix` round for
needless-borrow on `AppError::new(&format!…)`), fmt clean.

**Docs:** system/AGENTS.md (consumer starting point); root AGENTS.md → the sanctioned
contributor/consumer fork with the ratified role-resolution ladder (same text as the Java
repo's, adapted); llms.txt "Version-matched discovery" section + getting-started bullet;
crate README; CHANGELOG Unreleased item 2; INCREMENTS 88.

## Memory References

- referenced: thread-set-config-plugin (same-day predecessor arc; the byte-identical-fixture
  pattern), the Java repo's conv-telemetry-presentation-parity + thread-ai-contract-provider
  + thread-onboarding-efficiency (this port completes their cross-engine story)
- created: thread-ai-contract-provider-port (tier: working)
