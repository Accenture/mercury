# Session (2026-07-16T17:46:44.000Z)

**Agent:** Claude Code

## What happened

Maintainer accepted increment 10 (praised `auto_start_main!()` — "exactly the intent of
the original Java version"; committed + pushed) and directed: **proceed with layer 2 —
event-script.** Following the platform-core precedent (VBDI design gate before
implementation), this session is the **survey + design**, not implementation.

**Survey of the canonical `system/event-script-engine`** (Java v4.8.6, ~8.0K LOC main /
69 files; 90 @Test methods over 4 test classes; 51 flow fixtures + 20 negative
`parser-flows` fixtures):

- **Authoritative DSL spec**: `docs/guides/event-script/flow-grammar.md` explicitly
  mirrors `CompileFlows` validation (8 execution types, task-field rules, the
  data-mapping mini-language, compile-time invariants). `syntax.md` (2,121 lines) is the
  tutorial; `flow-schema-reference.md` the field reference.
- **Engine anatomy**: `CompileFlows` (@BeforeApplication seq 5; reads
  `yaml.flow.automation` → flows.yaml `location` + `flows:` list) → `Flows` static
  registries → `Flow`/`Task` compiled model → `FlowInstance` (per-transaction state
  machine: dataset `{input, model:{instance,cid,ttl,flow,trace,parent,root}}`, TTL
  watcher via `sendLater`) → `EventScriptManager` (launch interceptor) → `TaskExecutor`
  (the 1,351-LOC heart: composite correlation `uuid#seq`, input mapping → invoke →
  output mapping → route by execution type) → adapters `HttpToFlow` (x-flow-id;
  X-Correlation-Id → business cid) + `FlowExecutor` (programmatic launch/request).
  Plus `SimplePluginLoader` (@BeforeApplication seq 3, `@SimplePlugin` classpath scan
  with a bytecode-safety allowlist; ~40 built-in `f:` plugins) and `Resilience4Flow`
  (max_attempts/alternative/backoff/cumulative).
- **Key integration findings** (verified in source):
  - rest.yaml has a **`flow:` key** — Java REST automation injects the `x-flow-id`
    header for `http.flow.adapter`. Our Rust routing doesn't parse `flow:` yet.
  - Java `MultiLevelMap.getElement` delegates `$.…` to the **Jayway JsonPath library**
    (a full JSONPath engine, not a subset).
  - Engine core classes are **@EventInterceptor** (manual reply, no auto-reply) — our
    Rust worker always auto-replies; needs a registration mode.
  - TTL watcher needs **send_later/cancel** — not yet in Rust platform-core.

**Wrote `docs/design/event-script-port.md` (DRAFT v1, gated)** — decisions E1–E9:
new `crates/event-script` crate (E1); flow YAML verbatim + **reuse Java fixtures
unchanged** (E2); flow state on `serde_json::Value` + `serde_json_path` (RFC 9535) for
`$.…` (E3); engine self-registers via the increment-10 annotations, cross-crate
inventory (E4 — the annotation layer built one session earlier is load-bearing here);
four minimal platform-core extensions in lockstep — interceptor mode, send_later/cancel,
`flow:` routing key, deep-copy (E5); `#[simple_plugin]` compiled-code registry, Java's
bytecode allowlist documented as a non-ported divergence (E6); abortable-timer TTL (E7);
EventScriptMock ported later (E8); stream fields deferred with streams (E9). Increment
plan E-1…E-9: compiler → mapping engine → platform-core extensions → core runtime
(sequential/response/end/decision + exceptions + TTL) → parallel/fork-join → pipelines &
loops → sub-flows/ext state → plugins → HTTP adapter + resilience + `examples/hello-flow`
(milestone close). Four open questions for the maintainer (fixture reuse, JSONPath dep,
E5 packaging, plugin-porting order). INCREMENTS.md gained the design-gate row.

## Decisions

- Design-first honored for layer 2 (same gate as platform-core); implementation waits on
  maintainer approval of E1–E9 (esp. the `serde_json_path` dependency and verbatim
  fixture reuse).

## Memory References

- referenced: `bp-event-script` (design drafted — gate pending), `bp-platform-core`
  (foundation it rides on), `vision-mercury`, `ot-design-platform-core` (precedent),
  `conventions-rust-baseline`, `inv-never-couple-functions` (carried up an altitude in
  the design), `platform-core-stack`
- created: `ot-design-event-script` (the layer-2 design/implementation thread)
