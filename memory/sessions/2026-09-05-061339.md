# Session (2026-09-05T06:13:39.000Z)

**Agent:** Claude Code

The white paper succession, Rust edition — synced from the Java engine's PR #321 on Eric's
direction: `docs/ai-grammar-methodology.md` now carries **"Intent-Driven Development and the
Architecture of Human-AI Collaboration"** (the synthesis of Eric's IDD article with "The
Mercury Story"), replacing the story paper at the same URL. Deck and pointers follow.

Commit `bf885afb` on `docs/idd-white-paper` (5 files: paper, deck, index.md, INCREMENTS.md,
mkdocs.yml). Increment 103.

## What happened

- Ported the Java repo's new IDD white paper (its PR #321, squash `629428f0`) with this
  repository's established edition adjustments carried forward: no frontmatter; the byline
  names this repo the methodology's **founding proof point**; §7 Step 1 keeps the
  AI-enabled-before-first-line-of-code proof (Vision + memory landed 2026-07-15); tokio
  async/await named alongside Java virtual threads in §1's ergonomics passage; §7 Step 2
  keeps "both engines carry the same grammar — choose the engine, keep the methodology";
  the twin-kafka recursion tale attributed to the Java engine; §11 benchmark phrased as
  "this repository's documentation map"; sources footnote names the canonical Java guides
  as the behavior specification.
- Content parity with the Java edition otherwise: three-eras table, complementary
  responsibility (+ reviewer personas), four forms of drift, memory-vs-grammar, corrected
  release attribution (v4.12.2 fixes; v4.12.3 discovery gates), URL'd references [1]–[19]
  with in-text anchors, the closing charge, and the succession/positioning end matter
  (Mercury = official Accenture OSS; agent-memory described neutrally, Apache-2.0 — the old
  disclaimer retired per Eric).
- Deck: the same 7-edit set as the Java deck, 14 → 19 slides in the unchanged design system
  (`mercury·rust` wordmark untouched; final byline points at accenture.github.io/mercury).
- Pointers: mkdocs nav → "Intent-Driven Development (white paper)"; hero caption updated,
  keeping the founding-proof-point sentence; hero links stay absolute site URLs for the
  packaged-home-page gate (Increment 102's rule).
- Gates: `scripts/check-llms-links.py` OK (30 references); `cargo test -p
  ai-contract-provider` 11/11 green (the packaged index.md re-validated); 19 slides
  structurally verified (full visual verification was done on the Java twin, byte-similar
  design system).

## Memory References

- referenced: this port enacts the Java↔Rust lock-step docs convention (the presentation-
  parity rule extended to the shared product story in the Increment 102 round) and the
  Increment-102 absolute-URL rule for the packaged home page; release-rhythm gating
  observed (branch prepared and pushed; PR-open gated on Eric)
- created: (none — the succession is repo-visible; Increment 103 records it)
- Superseded: (none)
