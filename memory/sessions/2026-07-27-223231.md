# Session (2026-07-27T22:32:31.000Z)

**Agent:** Claude Code

## What happened

Two maintainer-directed actions since the memory review log.

**1. Harvest thread closed (Eric's ruling).** Eric marked the human gate raised by the
review: `ot-harvest-mercury-composable` → `[x]` — the enumerated per-layer harvest is
complete (all three in-scope layers ported, specs folded into
vision/instructions/invariants/ADRs); the remaining connectors/sync-over-async harvest
rides `bp-kafka-connectors-backlog`.

**2. ManagedCache design study → design doc for the gate (Eric: "please design the
counterpart of 'ManagedCache' for the Rust port", refined mid-study: "no need to port
SimpleCache. Just adopt a proper self-expiring in-memory cache").** Delivered
`docs/design/managed-cache-port.md` (DRAFT v2) — design only, implementation awaits
Eric's approval (VBDI human gate). Method: 3-perspective parallel study (Java API
fidelity / engine strategy / consumers+tests) → synthesis under the two rulings → a
3-lens adversarial critique workflow (Java-source fidelity, moka technical accuracy,
Rust consumer/test accuracy) → v2 with all 22 precision findings folded in.

Key design decisions proposed (MC1–MC8): engine = `moka` (`sync` feature; Caffeine
lineage, no threads of its own since v0.12, wrapped so it stays swappable; honest
bus-factor-1 caveat); value carrier = `Arc<dyn Any + Send + Sync>` (`CacheValue`) +
`get_as::<T>()` — the faithful Rust carrier of Java `Object`, load-bearing for the
future schema-registry consumer (compile-verified against moka's bounds; double-wrap
trap documented + test-pinned); ONE type per Eric's ruling with the SimpleCache→
ManagedCache mapping rule stated once; monomorphic registry returning `Arc` handles
(schema-registry constructor-injection pattern preserved); housekeeper lifecycle-wired
via `start_housekeeping()` (elastic_queue precedent — never spawned from `create_cache`,
which runs outside the runtime; correctness never depends on the sweep); normative
telemetry-stamp map (put/remove→last_write incl. remove-on-absent, get/exists→last_read
hit-or-miss, clear→last_reset + Java's 3-op clear) — the critique graded the v1
omissions here MISLEADING, now normative in §2.

Adopters proposed: (1) WS dedup `commands.rs::is_duplicate` → `"last.ws.message"`/1000ms
— fixes the sliding-vs-anchored window divergence (Rust re-puts on duplicate; Java
doesn't) and the unbounded map (session-close never removes entries); Java debug-log
wording pinned (`Duplicated message - {} for {}`, telemetry-parity invariant); existing
WS double-submit regression verified to pass unchanged under anchored semantics.
(2) Actuator per-dependency **info-lookup** cache `"health.info"`/5000ms — corrected
fact verified in ActuatorServices.java: Java never caches the /health result, only the
`info/{route}` lookup and only for Map bodies; type=health re-runs every call;
/livenessprobe reads an AtomicBoolean. (I had earlier over-stated this to Eric as
"/health results for 5s" — corrected in the doc, the thread text, and to Eric directly.)
(3, optional) event-script `ExternalStateMachine` test fixture (critique found it: Java
uses ManagedCache `"state.machine"`/10s; the Rust twin uses an unbounded map).

Gate questions for Eric: MC1 engine confirm; adopter scope (§6.2 + optional §6.3);
MC8 crate-private fast-TTL test constructor (a new pattern for the repo).

Continuity: `ot-harvest-mercury-composable` marked `[x]`; `ot-managedcache-port`
substance updated (ruling recorded, design-delivered status, health.info wording
corrected to "info-lookup cache").

## Memory References

- ot-harvest-mercury-composable (closed by Eric's ruling)
- ot-managedcache-port (design study delivered; substance updated)
- bp-kafka-connectors-backlog (harvest re-scope target; schema-registry future consumer)
- port-bottom-up-faithful (map-don't-mirror method throughout the design)
- inv-telemetry-presentation-parity (log-wording pinning in the WS-dedup adopter)
- conventions-rust-baseline (module/header/test conventions applied in the design)
- string-plugins-unicode-scalars (carrier-idiomatic/semantics-canonical precedent cited)
- registration-metadata-contract (same precedent, cited in MC2)
- vision-mercury (design serves the connectors runway)

Co-Authored-By: Claude Code <noreply@anthropic.com>
