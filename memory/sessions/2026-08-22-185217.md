# Session (2026-08-22T18:52:17.000Z)

**Agent:** Claude Code

## Summary

**The Rust half of the polyglot initiative's only engine change (ratified D5): `graph.task`
reaches declarative Event-over-HTTP targets.** Lock-step with the Java engine (same-day,
commit `10c53ca3` there). A task route is now reachable when registered locally OR declared
in the `yaml.event.over.http` map — the way python/node.js polyglot function hosts (the
rebooted mercury-python / mercury-nodejs wrappers) and remote engine instances are
addressed as if local. Flows already behaved this way (no pre-check); the graph lane's
guard in `skills.rs` additionally consults `event_api::get_event_http_target`; the
teaching error for genuinely unknown routes is unchanged (unit-test-task-5 pin holds).

Pin: `unit-test-task-7` — fixture byte-identical to the Java engine's — a graph.task node
reaches a route existing only as an event-over-http target, served by a stub `/api/event`
peer (raw tokio TCP) speaking the envelope wire format; **proven failing against the
unfixed guard first** (same "does not exist" signature), green after. The compiled-set
parity pin caught the new fixture immediately (49 → 50) — the guard working as designed.
Rust divergence note: no PostOffice passthrough needed (Java added
`PostOffice.getEventHttpTarget`); the Rust skill calls the public
`platform_core::automation::event_api` accessor directly.

Gates: workspace 63 suites green, clippy 0, fmt clean (the recurring lesson: hand-written
test asserts need a `cargo fmt --all` pass). CHANGELOG Unreleased + INCREMENTS 90.
**MERGED same day as [PR #212](https://github.com/Accenture/mercury/pull/212), merge
`c49d6cd7` carrying `83b12c36` (tree verified identical), CI green (test 2m26s + build +
the new agent-memory check's first feature-PR outing); branches deleted both ends; rides
the next release.** Java companion: PR #292 squash `fad24f11`.

## Memory References

- referenced: (Java-repo ids) polyglot-event-over-http-design (D5),
  thread-polyglot-initiative, conv-telemetry-presentation-parity (lock-step + identical
  fixture/messages)
- created: (none — the initiative's records live in the Java repo's memory; this log is
  the Rust-side lock-step record)
