# Session (2026-07-20T03:33:00.000Z)

**Agent:** Claude Code

## What happened

**The discovery-driven delegation test (the maintainer's queued capstone) — PASSED: 21/21
commands ok, ZERO in-band lookups, first-attempt dry-runs for both persons.** The brief named
NO graph or flow id; the fresh agent had to find its `extension=` target itself. This
validates increment 42's discovery commands + the completed docs end-to-end.

- **The discovery loop worked exactly as designed:** `list graphs` → two purpose-matched
  candidates (tutorial-3/5) → empirical contract probes via `POST /api/graph/{id}` →
  `extension=tutorial-3` delegation + `f:concat` greeting; **unprompted island documenting
  the delegation contract itself** (DataEntity + ExternalModel with graph_id/contract).
  Judged first-hand: greeting exact for both persons; exported model verified pure delegation
  (no own fetcher). **Streak: TEN consecutive zero-lookup first-attempt passes.**
- **Findings #53–#57** (`docs/AI-companion-test.md`): #55 `text()` whitespace, #56 `f:` args
  resolve any state-machine path incl. `{node}.result.*` (**empirically verified** with a live
  probe — the agent's defensive `model.*` staging was unnecessary), #57 re-`instantiate`
  replaces the instance — all three **FIXED same day** (grammar + catalog; strict build
  green). **#53 (candidate, engine work both ports):** no contract visibility for deployed
  models — a read-only `describe graph {graph-id}` or contract lines in `list graphs` would
  complete self-service discovery. **#54 (candidate, touches canonical fixtures):**
  tutorial-3/5 share identical purpose text — differentiate in both repos. Both are
  maintainer calls.
- Infra: headless session `ws-575720-1` (same raw-WS client method); server + client stopped
  after judging.

## Decisions

- (methodology confirmed) The headless orchestrator-owned session is now the standard rig for
  post-sweep exercises; investigation via the public execution endpoint is a legitimate
  discovery tool for agents (documented in the exercise brief pattern).

## Memory References

- ot-companion-validation-sweep — referenced + substance extended (drive #2, streak 10)
- ot-discovery-commands-backlog — referenced (increment 42 validated end-to-end by this test)
- ot-human-guides-backlog — referenced (the docs the agent ran on are its deliverable)
- conventions-rust-baseline — applied (doc fixes + strict build)

Co-Authored-By: Claude Code <noreply@anthropic.com>
