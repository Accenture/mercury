# Session (2026-07-16T19:28:30.000Z)

**Agent:** Claude Code

## What happened

Maintainer committed E-3, directed E-4 → **Increment E-4 (repo increment 14) — the
core flow runtime, SHIPPED. Flows execute end-to-end on the Rust engine.**

1. **Read the full Java `TaskExecutor` (1,351 lines) before writing** — callback
   correlation (`uuid#seq`), output-mapping application, execution-type dispatch,
   exception routing, abort/end-flow, response sending, decision resolution
   (bool → 1/2; numeric `max(1, str2int)`; `@retry|fallback` via the task
   reference's error task), executeTask (input mapping → deferred send → event
   dispatch with the `my_correlation_id` header stamped LAST), and
   `substituteDynamicIndex`'s runtime reserved-key re-check.
2. **Key architectural translation** (recorded in ES design §5d): Java builds the
   consolidated mapping view over a map that REFERENCES the instance's model
   (mutations persist by aliasing). rmpv has no aliasing, so the Rust executor builds
   the consolidated view **inside the instance's own dataset tree** — per-callback
   scratch keys (status/header/result/datatype/output/decision/error) inserted then
   stripped — giving identical persistence semantics with zero model copies.
   `model.parent`/`model.root` aliases deferred wholesale to E-7 (they need a real
   shared-subtree design; no E-4 fixture uses them).
3. **Shipped**: `instance.rs` (FlowInstance + TaskMetrics + TTL watcher on E-3
   send_later, cancelled on close), instance registry in `flows.rs`, `manager.rs`
   (launch → instance → first task; caller `ttl` override honored), `executor.rs`
   (~900 lines: the E-4 execution types + exceptions + metrics + traced flow-summary
   span + `FlowExecutor::launch/request`), engine services registered in lib.rs via
   `#[preload]` + `#[event_interceptor]` (one instance each — Java parity, callbacks
   serialize through the service queue like Java's single-instance executor).
   parallel/fork (E-5), pipeline (E-6), flow:// + ext: (E-7) abort with explicit
   "later increment" messages — honest fail-fast, never silent misbehavior.
4. **E2E test** (`tests/flow_runtime.rs`): `AutoStart::main` collects engine +
   Java-parity task functions (Greetings/HelloExceptionHandler/SimpleDecision/
   NumericDecision/DecisionCase/SequentialOne/NoOp — ported from the Java test tasks)
   from the annotation inventory exactly as a user app would. Scenarios: greetings
   (201 status mapping, business-cid exposure via header AND `model.cid` output,
   map(), double negation, result-header copy), decisions bool/numeric/out-of-range
   (500 abort), sequential + `*` wildcard body, response-before-end (early reply wins;
   end task's output never reaches the caller), exception → handler (409 through
   `error.*`), TTL abort (408, caller ttl=500ms vs a 2.5 s task), dynamic
   reserved-key runtime rejection (FlowTests parity), fire-and-forget launch, and an
   inbox-health sanity ping.
5. **One test-expectation fix** (mirror of the Java behavior, not a bug): greetings'
   `'header -> output.header'` REPLACES the earlier content-type mapping with the
   whole result-header map — asserted the replaced form.

**Verification**: **146 workspace tests, 0 failures; clippy 0; fmt clean.** Docs in
lockstep: ES design §5d, INCREMENTS.md row + section 14.

## Decisions

- Engine services are NOT zero-traced (Java parity verified: `tracing = no
  @ZeroTracing annotation` in WorkerDispatcher — Java traces its own engine).
- Consolidated-view-in-dataset over model cloning (lightweight, matches the
  maintainer's E-2 direction; scratch-strip keeps the tree clean).
- Later-increment constructs fail fast with explicit messages.

## Memory References

- referenced: `ot-design-event-script` (E-4 shipped), `bp-event-script`,
  `vision-mercury`, `conventions-rust-baseline`, `inv-never-couple-functions`
- created: (none)
