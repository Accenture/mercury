# Session (2026-09-19T18:26:17.000Z)

**Agent:** Claude Code

## Summary

**Increment 119 — the distributed-cache lock-step of the Java engine's v4.12.9.** Eric: "proceed with
Rust lockstep for distributed cache." Branch `feat/distributed-cache-lockstep`, commit `b9590012`,
pushed; PR-open and merge are Eric's gates (`eric-release-rhythm-rust`). Spec-first, as the
sync-over-async and minimalist-kafka ports were: `draft-design-specs/distributed-cache-port.md` records
the Rust realization against the Java canon (Q1–Q8 inherited), the deliberate deltas (§4) and five open
questions (§9), each with the recommendation the work proceeded under — cluster support now as the seam
+ `cluster-async` branch, `Platform::on_shutdown` now, publication joining the K5 hold, port 8305, the
`RedisSettings` alias. Eric had said proceed, so I did not block on them; they are his to confirm.

**D1 — `mercury-redis-connection`** (`extensions/redis-connection`, new): the shared client foundation
extracted from sync-over-async, in the Java dependency direction (Java Q2). `RedisConfig::from_prefix`
reads `<prefix>*` with every key falling back to plain `redis.*` (`from_config()` = the `soa.redis.*`
default, Java parity), adds `username` (RBAC → `AUTH user pass`, bare password → legacy form) and the
two-key cluster selection. `RedisBackend` is an enum over the `redis` crate's `ConnectionManager`
(long-lived, reconnecting), `MultiplexedConnection` (the probe's `connect_once` shape — a failed probe is
reported, not healed underneath) and `cluster_async::ClusterConnection`, all `aio::ConnectionLike`, so one
`query`/`query_pipeline` serves every topology; `INFO cluster` auto-detect with the inconclusive-probe
fallback; `ConnectError` keeps unbuildable / server / timeout apart so the probe classifies waiting vs
outage. `RedisHealthProbe` = sync-over-async's check generalised (route + prefix supplied by the binding);
`resolve_duration` + `duration_seconds` moved here. sync-over-async now depends on the crate:
`RedisSettings` is `pub use RedisConfig as RedisSettings` (demo and tests compile unchanged),
`soa.redis.health` a thin binding reading `soa.redis.health.*` with the legacy `redis.health.*` fallback —
its 16 + 3 + 13 + 4 + 7 tests unchanged in intent and green. The RESP double gained `MGET` and `SET`
options (`NX`/`XX`/`EX`/`PX`/`KEEPTTL`). `Platform::on_shutdown(hook)` + `run_shutdown_hooks()` (Java
`Platform.onShutdown`, v4.12.9): a process-wide list, newest first, each isolated by `catch_unwind`, run
from `AutoStart::run` before the elastic-queue cleanup; an embedder that awaits `main` runs them itself.

**D2 — `mercury-distributed-cache`** (`extensions/distributed-cache`, new): `CacheAction` (nine names,
Java's messages verbatim), `CacheConfig` (`redis.cache.*` over `RedisConfig::from_prefix(BASE_PREFIX)`; a
garbage default ttl degrades to `1h`, not to the zero Java would pass to Redis), `RedisCacheStore`
(`SETEX`, `GET`, `MGET` → ordered misses-omitted unprefixed pairs, pipelined `MPUT`, `DEL`, atomic
`SET NX EX`, `RPUSH`+`EXPIRE` as one `MULTI`/`EXEC` — the same delta the sync-over-async port ruled for
Lua, and the double cannot run Lua — `LPOP`, `LLEN`; blank key = `Missing 'key'`), `runtime` (lazy
double-checked build over ONE connection, config re-read per failed attempt, `on_shutdown` registered
from the build, `set()` test seam), `RedisCache` = `#[preload(route = "v1.cache.redis", instances = 20,
env_instances = "redis.cache.instances")]` + `#[optional_service("redis.cache.enabled")]` with a public
`handle(headers, body, store)` seam (the Java `Supplier` injection), `CacheRedisHealthCheck` at
`redis.health` behind a `OnceLock` probe. Bodies: `Value::Binary` (a `set_body(Vec<u8>)` would serde a
list of integers — documented); string body = UTF-8 convenience; results `true`/bytes/`Nil`/integer/
boolean/map, in Java's shapes.

**D3 — `examples/distributed-cache-example`** (new): the Java example's `l2-profile.yml` and
`profile-cache.json` copied byte-for-byte (`cmp` proven); five functions (`v1.profile.l1`, encode/decode,
`v1.http.method.action`, `v1.profile.exception`); the value is `rmpv::encode::write_value` of the profile
map = Java's `MsgPack.packMapOrList` bytes — the interop harness Java asked for; dev mode pre-wired with the
broker script. **The app's `flows.yaml` lists the engine's `graph-executor.yml` without shipping a copy**:
the knowledge-graph crate appends its resource root, so `classpath:/flows/graph-executor.yml` falls
through to the engine's file (one source of truth) — proven by the Layer 3 route working.

**Found and fixed — a REST parity gap.** The Layer 1 miss (`Err(AppError::new(404, "Profile not
found"))`) reached the client as `text/plain`, where Java renders the standard error body
`{status, message, type: error}` (`AsyncHttpResponse.handleException`: status ≥ 400, no headers, a string
body not starting with `{`/`[`/`<`). The Rust server had that shape only for its own routing errors.
Mirrored in `automation/server.rs` with the same guard, and — the second half, found by the new test — the
body defaults to `application/json` when nothing negotiated a type (Java `sendError`). Pinned by
`function_failure_is_java_shaped_error_body` (plus the JSON-looking-text guard). The starter templates'
tests assert only the 400 status, so nothing else pinned the old text.

**D4 — docs and contract.** `guides/distributed-cache.md` (adapted: Rust snippets, the `Value::Binary`
rule, the `MULTI`/`EXEC` delta, cluster via `cluster-async`); the configuration reference's `redis.*` block
rewritten as the shared foundation namespace (+ `username`, `cluster.*`) with new `soa.redis.*` and
`redis.cache.*` blocks; reserved names (`v1.cache.redis`, `redis.health`, `soa.redis.health`); `llms.txt`;
mkdocs nav; the AI-contract `files.list`; README + getting-started pointers; crate READMEs;
`docs/INCREMENTS.md` Increment 119; the port spec marked IMPLEMENTED with a §11 findings section.

**Method notes.** (1) My anchor-verified edit helper aborted TWICE because `cargo fmt` had rewrapped a
file between my write and my edit — all-or-nothing, so nothing half-applied, but the background chain hid
the abort in the task output; lesson: anchor on stable single lines (or regex spans between markers) and
never run an anchored edit after an unreviewed fmt. (2) `Platform::new()` spawns worker tasks, so a unit
test of a Platform method needs `#[tokio::test]`. (3) redis 1.5 keeps `ConnectionInfo`'s fields private
(`addr()`, `redis_settings()`, `username()`, `password()`, `db()` accessors). (4) The `#[optional_service]`
attribute is consumed by the `#[preload]` macro — importing it is an unused import. (5) The Java
`crudCycle` asserts nothing about the DELETE body; the Rust twin asserts per layer (`true` from L1's code,
`1` from L2's straight mapping). (6) The two `[stale-metadata]` sweeps of earlier logs aside, this repo's
memory grew three facts today — none archivable yet.

**Open item (Eric's question, answered, fix NOT yet applied):** a Java `TypedLambdaFunction<I, EventEnvelope>`
may return an `EventEnvelope` to set status, headers and body (`WorkerHandler.updateResponse` applies it);
the Rust `TypedAdapter` always wraps `O` as the body, and because `EventEnvelope` derives `Serialize` a
`TypedFunction<I, EventEnvelope>` compiles and silently nests the envelope inside the reply body. The
untyped `ComposableFunction` has the full ability and the docs steer to it, so the divergence is documented
— but the nesting is a trap. Fix: a Java-`instanceof`-style `Any` downcast in the adapter (pass an
`EventEnvelope` through as the reply, else wrap) + a test. Awaiting Eric's call on where it lands.

**Verification.** `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --no-fail-fast` (102 suites, 503 tests); `check-doc-claims.py` (20/20);
`check-llms-links.py` (35); `mkdocs build --strict`; the AI-contract snapshot suite (files.list ↔
`docs/guides/**` closure, link-checked).

## Memory References

- port-bottom-up-faithful — consulted: map-don't-mirror decided the `MULTI`/`EXEC` Lua delta, the
  codec-free backend, the `RedisSettings` alias, and copying the Java flow/graph files byte-for-byte
- conventions-rust-baseline — consulted: fmt/clippy definition of done, `//!` docs naming the Java class
  ported, in-module unit tests + `tests/` suites, the `docs/INCREMENTS.md` entry as part of done
- playground-session-broker — consulted: the example ships the broker script and dev-mode wiring exactly
  as `templates/starter-graph` does
- for-each-suspend-index-key-rust — consulted: the same cross-engine-contract reasoning (a mixed fleet
  shares one Redis) decided that key layout, action names and config keys stay byte-identical
- eric-release-rhythm-rust — consulted: branch + push prepared; PR-open and merge stay Eric's gates
- conv-declare-consulted-references-rust — applied
- vision-mercury — serves (lean, decoupled, composable modules; the last Rust lock-step item of Java v4.12.9)
- redis-connection-foundation-rust — created (working)
- distributed-cache-rust — created (working)
- rest-error-body-standard-shape — created (working)
