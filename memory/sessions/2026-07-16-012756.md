# Session (2026-07-16T01:27:56.000Z)

**Agent:** Claude Code

## Summary

**Implemented increment 7 — actuator endpoints + static HTML content**
(`bp-platform-core`, Design §5f; maintainer-directed scope: `/`, `/info`, `/env`,
`/livenessprobe`, `/health`; `/info/lib` deferred by mutual agreement — Java reads the JAR
manifest at runtime, a Rust binary has no runtime dependency manifest; a build.rs-embedded
cargo metadata could provide it in a future iteration).

**Harvested first**: `ActuatorServices.java` (404 LOC — one function registered at multiple
routes, switching on the `my_route` header; health protocol `type=info` → `type=health`
against `mandatory.health.dependencies`/`optional.health.dependencies`; DOWN = HTTP 400;
liveness follows a shared `healthStatus` flag; `/env` shows only the opt-in
`show.env.variables` + `show.application.properties` lists) + `default-rest.yaml`
(defaults added only when rest.yaml doesn't provide them).

**Built:**
- `src/actuator.rs` — `ActuatorKind` (Info/Env/Health/Liveness) + shared `ActuatorContext`
  (identity resolved once; `AtomicBool` liveness; start-time for uptime). Rust adaptation
  notes doc'd: kind-parameterized registration instead of the `my_route` header switch;
  `/info`'s JVM/memory/streams/personality blocks omitted rather than faked; app version =
  `info.app.version` config (a Rust lib can't see the app's build metadata), fallback
  platform-core version; humanized `elapsed_time`.
- `automation/server.rs` — **default-endpoint merge** (built-in default-rest YAML parsed
  through the same grammar-checked parser; `has_url` = user entries win) + **static
  content**: GET/HEAD falling through the routing table serve `resources/public` via the
  resource-roots convention (`/` → index.html, `<dir>/` → index.html, `..` rejected,
  minimal mime resolver). rest.yaml `/` entry always wins.
- `app_starter.rs` — essential phase registers the four actuator routes (race-tolerant,
  same pattern as Telemetry).
- **Example**: `resources/public/index.html` (landing page linking the endpoints),
  `demo.health` mandatory health-check function, `info.app.description` +
  `show.*` opt-in lists in application.yml.

**Verified live** (hello_world + curl): `/` serves the static page (text/html);
`/info` app{name,version,description}/runtime/origin/time/up_time; `/env` shows exactly
the opted-in env vars + properties; `/health` UP with the demo.health dependency (info
fields merged into the entry, message "demo store is running"); `/livenessprobe` `OK`
text/plain.

**Verification:** **94 tests** (+10 actuator/static end-to-end: info, env opt-in-only,
liveness default, health no-deps hint, health UP + info-merge, health DOWN→400 + liveness
flip, static index, nested asset content-type, traversal/miss/POST=404, rest.yaml
endpoints win; +1 elapsed-time unit) · clippy + fmt clean. One test race preempted: the
three health tests share the `mandatory.health.dependencies` override → serialized with an
async mutex.

> **also:** created **`docs/INCREMENTS.md`** (maintainer-requested) — the historical
> ledger of all migration increments: overview table (enable → increments 1–7, dates,
> design refs, cumulative test counts), the maintainer scope decisions, one section per
> increment, and the deferred backlog. Convention recorded: one row + one section added
> per increment as part of its definition of done (ledger = what/when; the design doc
> keeps the how/why).

## Memory References

Updated:
- `ot-design-platform-core` — increment 7 done (operable foundation)
- Project State status

Referenced: `bp-platform-core`, `vision-mercury`, `config-first-spring-out` (all actuator
config keys verbatim: show.env.variables, mandatory.health.dependencies, …),
`conventions-rust-baseline` (honest-omission discipline for the JVM-only info blocks),
`inv-never-couple-functions` (health checks reach dependencies only via route names)
