# Session (2026-09-20T00:46:27.000Z)

**Agent:** Claude Code

## Summary

**Increment 120 — the distributed cache certified Java ⇄ Rust, Layer 1 surfaces a cache failure, the
port spec closed.** Eric: "confirm section 9 recommendation in draft design specs" and "conduct an
interop for distributed cache between Java and Rust implementation and record outcome in the
test-reports folder. The interop would use the Java helper's redis-standalone server." Branch
`fix/cache-interop-l1-and-spec-confirmation` (`e9fedb55` spec, `9b10b459` fix + report + trim +
Increment 120), pushed; **PR #286 opened and MERGED by Eric 2026-09-20 01:03Z** (merge `01710589`; `eric-release-rhythm-rust`).
**One CI follow-up on the way (`74e5df21`):** the first push failed `inventory_equals_the_documentation_closure`
— the AI-contract snapshot test walks the whole `docs/guides` and `docs/test-reports` trees, so a new report
is a `files.list` change too (the Java repo packages only enumerated reports; here every report ships).

**The drive.** Both `distributed-cache-example` apps — this engine at main `5ac55eaf` (PR #285) on
`-Drest.server.port=8306`, the Java one (mercury-composable main `c74ee6be`, jar 4.12.12) on 8305 —
against one Java `helpers/redis-standalone` on 6379, driven by a scratch Python script (urllib +
redis-py + msgpack): `/info` + `/health` identity; a 6 × 6 writer/reader matrix over both engines'
three layers (18 cross-engine reads); raw key inspection (`cache-demo:{id}`, plain MsgPack maps, TTL
3600 from birth, exactly the 8 keys); 6 cross-engine deletes with the miss confirmed on a third layer;
field-by-field error-shape parity (miss bodies on L1/L2/L3 and the L3 rejection identical); a 54 s
Redis outage; recovery without an app restart. Three runs — discovery (found the defect), fix
verification, certification: **112/112 hard checks**, 1 informational (the same payload is not
byte-identical: Java packs insertion order, serde_json sorts keys; both decode equal — map order is not
the contract). Report `docs/test-reports/distributed-cache-interop.md` (+ mkdocs nav; twin in the Java
repo), nav-only like the suspend/resume report.

**Finding 1 — Layer 1 turned a cache failure into a miss, both engines, fixed.** With Redis down, this
engine's L1 GET answered 404 *Profile not found* while `v1.cache.redis` failed (`broken pipe`); the
Java L1 answered 500 only because its 5 s RPC timeout raced Lettuce's 5 s command timeout. Root cause
identical on both: `ProfileCacheL1` tested the reply BODY (`Value::Binary`/`byte[]` or null), never its
STATUS — an `Err` from a function comes back as `Ok(reply)` with the error status and the message as a
string body (platform.rs `set_status(e.status())` + `set_raw_body(String)`), so `po.request(..).await?`
propagates only transport failures. A fast failure therefore read as "nothing cached", and a POST would
have acked `stored`. L2/L3 never had it: the flow and graph engines check task status for the author.
Fix: `checked()` guard (GET/POST/DELETE); `tests/l1_cache_failure.rs` — real cache off
(`redis.cache.enabled=false`), a `#[preload]` stub on `v1.cache.redis` failing fast with 503; L1 GET/POST/
DELETE → 503 with the message, L2/L3 → 503 through the exception handler. Java fixed the same way on
its branch `fix/cache-example-l1-error-propagation` (`7ccd4fa4`, `Platform.release` + `register` stub
test).

**Recorded, not changed** (report *Findings*): (2) failure latency by client library — Lettuce buffers
to its command timeout (~5 s), redis-rs fails fast then the `ConnectionManager` retries ~4.5 s;
(3) an in-function RPC timeout is **408** here (`Result`, "Request timeout for 5000 ms") and **500** on
Java (`FutureInbox` completes exceptionally, the worker renders 500) — both errors, neither a miss;
(4) after a >30 s outage the Java pod's shared Lettuce connection recovers on its 30 s backoff cap
(attempts at +0/+9/+17/+34 s, `Reconnected` at +64 s = 10.5 s after Redis returned) while `/health` is
already green (its probe uses a fresh connection) — a follow-up ruling on the Java side; this engine's
`ConnectionManager` reconnected on the first command; (5) the health contract is identical (400 `DOWN`
+ dependency 503 `{code, text}`).

**Eric's Cargo question — are `mercury-platform-core` and `mercury-event-script` redundant next to
`mercury-knowledge-graph`?** Cargo has no Maven-style transitive classpath: a crate can only name what
it declares (knowledge-graph re-exports only `inventory` and `fetch_feature`), while transitive crates
still link, `#[preload]` registrations included. `mercury-platform-core` stays (the example names
`platform_core::` macros and types); `mercury-event-script` was redundant (nothing named) — removed on
the branch, and the L2 flow suites still pass. Same trim applies to `templates/starter-graph` and
`examples/minigraph-playground` (offered, not done).

**Spec closed.** `draft-design-specs/distributed-cache-port.md`: status MERGED (#285); the five §9
recommendations CONFIRMED by Eric 2026-09-19, the question form kept as the decision history.

**Method notes.** My first outage expectation (503) was wrong — both engines answer 400 `DOWN` by
design (actuator.rs doc). The fix-verification run's 10 s recovery window was too short for Lettuce's
backoff — measure time-to-recover with retries, never a single probe. Discovery ≠ certification: keep
the discovery evidence and certify on a clean rerun after the fix. Gates: fmt, clippy -D warnings, the
example's suites, check-llms-links, check-doc-claims, mkdocs strict — all clean.

**Finding 3 closed on the Java side (later the same day).** Eric ruled the Java 500 for an in-function RPC
timeout a platform-core gap — status read from the OUTERMOST exception, message from the root cause — and
had it fixed there with a cause-chain rule (first `AppException` / `TimeoutException` 408 /
`IllegalArgumentException` 400 wins). Live re-probe after the fix: Java Layer 1 outage replies **408
`Timeout for 5000 ms`**, this engine's outcome all along (`po.request` yields an `AppError` 408 as a
`Result`; `?` propagation has no wrapper class). Report twin amended on branch
`docs/cache-interop-finding-3-amendment` (`1b21f86e`, docs-only; **PR #287 opened and MERGED by Eric 2026-09-20**, merge `8424bc65`; the memory
push that records this waited for the merge commit's CI, so the `rust` workflow's cancel-in-progress rule
did not supersede that run) with an INCREMENTS
addendum under 120; nothing changes in this engine.

**Validation run (Eric's ask, after mercury-composable #427).** The full two-engine drive re-run against both
mains: **116/116 hard checks**, RPC-timeout status set {408} on both engines, no pre-fix shape; this engine's
rows unchanged. Report twin regenerated with a *Validation run* section + INCREMENTS addendum line (branch
`docs/cache-interop-validation-run`, `1091b036`, docs-only → **PR #288 opened and MERGED by Eric 2026-09-20**, merge `a8f610e1`; the Increment 121
branch, which regenerates the same report file as a superset, was rebased on top as `b7bd6f6a` — report resolved to
the branch's version, INCREMENTS keeping both the validation line and Increment 121; gates re-run green; **PR #289 opened and MERGED by Eric
2026-09-20**, merge `017bf8ed` — Increment 121 is on main).
**Flaky test on main, twice today (`464adb75` 01:43Z, `a8f610e1` 02:10Z — a memory-only and a docs-only commit):**
`crates/knowledge-graph/tests/playground.rs::playground_command_grammar_and_companion` — `command()` sleeps a fixed
60 ms before the console assertions, so a slow runner loses the race ("help connect expected" at line 174; locally
1 failure in 3 runs, at a later assertion). `gh run rerun` needs admin rights. Fix prepared as a test-only branch
`test/playground-console-wait` (`ae79eb79` + `2a72c9a2`): `console_gets()` polls the console for the expected line up to
5 s — all 30 positive assertions converted, the 2 absence checks kept synchronous; 6 green local runs, clippy clean;
**PR #290 opened and MERGED by Eric 2026-09-20**, merge `40119aa3`.

**Increment 121 — Redis failures classified on the command path (Eric: "update v1.cache.redis to pass the
correct status code for both engines").** The Java question was why L2/L3 answered 500 for a timeout: the
engines pass a task's status through faithfully; the 500 was the default for a client-library error that
carries none, on both engines (this backend mapped every redis-crate error and its deadline to 500). Fix
in the foundation: `classify_command_error` (timeout → 408 `Redis request timed out - …`; refused/dropped/
I-O/cluster → 503 `Redis unavailable - …`; server answer → 500 `Redis error - …`), `command_timeout` (the
deadline → 408), `From<ConnectError> for AppError` (503 for a refused/timed-out connect on a caller's path,
500 for an unbuildable configuration), applied by `RedisBackend::query`/`query_pipeline` — sync-over-async
does not use that path (45 tests unchanged). Test `tests/backend.rs::command_and_connect_failures_are_classified`;
cache guide bullet; INCREMENTS 121. Java twin: `RedisFailure.classify` applied by `RedisCache`. **Fifth full
drive on both classified builds: 122/122 hard checks; no outage probe on any layer of either engine answers
500** — this engine: L1 POST + L2 408, L1 GET/DELETE + L3 503. Report twin regenerated with Finding 6 + a
second validation section. Branch `fix/cache-failure-status-classification` (`90d503ac`); PR-open is Eric's.

**Cargo trim applied to the template and the Playground (Eric: "for consistency, please apply the same Cargo
dependency to the starter template and the playground example").** `templates/starter-graph` and
`examples/minigraph-playground` drop their direct `mercury-event-script` dependency — neither names
`event_script::`; `mercury-knowledge-graph` brings it transitively — with the same manifest comment as the cache
example; the root README, the template README and the getting-started guide now state the rule (declare what
your code names plus your top layer's engine crate). Both build, their suites and clippy pass, doc checks and
mkdocs strict clean. Branch `chore/cargo-declare-what-you-name` (`8868613c`) → **PR #291 opened and MERGED by Eric 2026-09-20**, merge `548ce651`.

**Increment 122 — two REST parity gaps in production mode (Eric: `cargo run -p minigraph-playground -- -Dapp.env=prod`
"correctly skips loading the dev mode modules" but (1) still renders the dev-mode REST endpoints and (2) serves the
React UI instead of the plain index.html).** Java reference pinned from source and live: `RoutingEntry.resolveServices`
skips an entry whose service `po.exists()` is false with `Skip [GET] /api/x - Service x not available`;
`HttpRequestHandler` retries a `/` miss as `/index.html`, so `get.index.html` (dev → `/public`, else `/template`)
serves the root too — Java prod: `/` and `/index.html` plain, dev-only URL a plain 404; Java dev: both the Playground.
Rust: `/index.html` already followed `app.env`; `/` came from static `public/index.html` (the React bundle) with no
`app.env` logic, and every dev entry registered ("Route get.live.graph not found"). Fix: `RoutingTable::retain_available`
+ the warn in `start_http_server` (the REST server starts after preload and before the main application on both
engines, so the check sees what Java sees); the request handler's root fallback to the `/index.html` entry before
static content. Tests at REST-suite and routing-unit level; workspace 482/0; docs: rest-automation + playground
guides. Live after the fix — prod: 8 routes (was 18), 10 `Skip […]` warnings in Java's words, `/` and `/index.html`
plain, dev-only URL `Resource not found`; dev: 18 routes, both paths the Playground. Branch
`fix/rest-skip-unregistered-services` (`07f71dbb` after the rebase onto the #291 merge) → **PR #292 opened and MERGED by Eric
2026-09-20**, merge `567bf57e` — Increment 122 is on main.

## Memory References

- distributed-cache-rust — edited: CERTIFIED (report, 112/112), the L1 fix, the dependency trim
- redis-connection-foundation-rust — consulted: the health probe's fresh `connect_once` connection vs
  the cache's `ConnectionManager` explains health-green-first and the first-command recovery
- rest-error-body-standard-shape — consulted: the error-shape parity the drive asserted (identical)
- l1-caller-checks-reply-status-rust — created (working)
- conv-cargo-declare-what-you-name — created (working); later edited: the trim applied to the template and the
  Playground at Eric's direction
- rest-skip-unregistered-and-root-fallback-rust — created (working)
- redis-failure-classification-rust — created (working)
- redis-connection-foundation-rust — edited: the command path classifies failures (408/503/500) for every consumer
- port-bottom-up-faithful — consulted: map-don't-mirror decided to RECORD the 408-vs-500 timeout
  asymmetry rather than change either engine
- conv-proposals-not-in-adr-ledger-rust — consulted: §9 confirmation recorded in place, the questions
  kept as the history of how they were decided
- conventions-rust-baseline — edited: the gates and the INCREMENTS entry as part of done; enriched with the
  packaging rule (new guide or test report ⇒ `files.list` line, or the snapshot test fails)
- eric-release-rhythm-rust — applied: branch pushed, PR-open and merge stay Eric's
- conv-declare-consulted-references-rust — applied
