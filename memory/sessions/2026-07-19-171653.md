# Session (2026-07-19T17:16:53.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-11 (L10, cross-layer composition: flow extension):
PASSED — 15/15 commands, zero failures, ZERO in-band lookups, first attempt** (session
`ws-783755-2`, verified empty; `flow-11` pre-flight-probed live via a scratch session).

- **Task (problem-only; flow id + contract as environment facts):** delegate the graph's input
  to the deployed Event Script flow `flow-11` (echo service) via `extension=flow://flow-11`;
  implementing the echo in the graph forbidden. Output = the flow's echo; delegation proven
  in-band and independently re-run with a different payload. Exported `delegate-to-flow`.
- **Compounding feedback loop, now explicit:** the companion credited the prior rounds' fixes —
  the island convention, `flow://` prefix, bare-key `input[]`, and result-is-flow-output rule
  being "stated as rules (not just examples)" as "exactly what made a zero-error first run
  possible". The island documents the flow dependency (`echo-flow` entity: flow_id, contract,
  ownership).
- **Findings:** #37 whole-body `*` on `graph.extension` — engine-verified NOT supported (named
  keys only; `*` is graph.task-only) → stated in the delegation contract + JSON catalog (FIXED);
  #38 no deployed flow/graph id discovery command (candidate — engine work, maintainer call);
  #39 `Described in /api/graph/model/{name}/{token}` endpoint undocumented → noted in `#export`
  (FIXED).
- **Docs updated:** tutorial-11 stage record + L10 ladder row + rollup #37–#39.

## Decisions

- Tutorial-11 defined as **L10 (cross-layer composition)**; flow ids + contracts are legitimate
  environment facts in briefs (same class as mock URLs and deployed graph ids).

## Memory References

- ot-companion-validation-sweep — advanced (tutorial-11 done; next tutorial-12); substance updated
- bp-companion-sync — referenced (driven wholly over `/sync`)
- port-bottom-up-faithful — referenced (extension `*`-support verified against the engine before
  documenting)

Co-Authored-By: Claude Code <noreply@anthropic.com>
