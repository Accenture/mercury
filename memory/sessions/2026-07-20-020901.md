# Session (2026-07-20T02:09:01.000Z)

**Agent:** Claude Code

## What happened

**Human docs phase 3 shipped (increment 45)** — twelve pages by three parallel writer agents,
reviewed (mechanical sweeps + spot-reads + one correction pass), nav wired, `mkdocs build
--strict` green across 19 pages. The site now covers Foundations + all three layers + a
Reference tab.

- **Layer 3:** KG overview (property-graph folded in), build-your-first-graph,
  playground-and-companion (`/sync` envelope, read-only session rule, live tee, discovery),
  and **composing-the-layers — sweep finding #9's dangling link finally closed** (delegation
  to sub-graphs / `flow://` flows / `graph.task`, mermaid composition diagram).
- **Layer 1/2 additions:** rest-automation.md (entry-per-heading rest.yaml grammar + the
  exact increment-36 content-type dispatch; deferral boxes derived from source, not the
  design doc) and flow-schema-reference.md (the 13 task fields the compiler parses).
- **The D-H2 showcase:** configuration-reference (31 keys **enumerated from source greps** —
  no phantom Java keys; `APP_PROFILES_ACTIVE`/`application.name` correct), macros (8+2),
  event-envelope (27 methods), api-overview (9+10+AppError), reserved-names (dev-gating per
  route verified).
- **Java-doc bugs surfaced (upstream doc-fix candidates for the maintainer):**
  `error.status` documented in Java but the engine key is **`error.code`** in BOTH
  implementations; `error.stack` null here; timeout-unit understatement; the
  content-length/streaming claim. Also one review correction of my agents' work: the
  "discovery Java-pending" notes → cite merged #199.

## Decisions

- (docs) Deferral/divergence lists in reference pages are derived from **source**, never from
  the internal design docs (which the site excludes) — keeps the public pages self-consistent.

## Memory References

- ot-human-guides-backlog — referenced + substance updated (phase 3 done; phase 4 remains)
- ot-companion-validation-sweep — referenced (finding #9's composing-the-layers page now exists)
- port-bottom-up-faithful — applied (map-don't-mirror; source-enumerated references)
- conventions-rust-baseline — applied (review bar, ledger row)

Co-Authored-By: Claude Code <noreply@anthropic.com>
