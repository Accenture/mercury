# Session (2026-07-16T00:55:05.000Z)

**Agent:** Claude Code

## Summary

**Implemented increment 6 — REST automation (core)**: the HTTP protocol boundary
(`bp-platform-core`, Design §5e). The foundation now serves HTTP with edge-started
distributed traces.

**Harvested first**: the Java project ships an **agent-ready grammar**
(`docs/guides/rest-automation/rest-grammar.md` — the authoritative rest.yaml spec, built
exactly so an agent can author/port deterministically) + `default-rest.yaml`,
`AsyncHttpRequest` map keys, `HttpRouter` trace-start/cid logic (~4.1K LOC total —
scoped to the core; relay/A/B/upload/static/flow-binding deferred per §5e/§7).

**Built** (`src/automation/{routing,server}.rs`; new deps per **D10**: hyper 1
[http1/server], hyper-util, http-body-util — deliberately no web framework, rest.yaml IS
the router; tokio += net/signal/io-util):
- `routing.rs` — `RoutingTable`/`RouteInfo`/`CorsInfo`/`HeaderInfo` (RoutingEntry port):
  grammar invariants enforced (methods whitelist, cors/headers refs must exist, relay
  rejected as not-yet-ported, trailing-only wildcard, Access-Control-* validation), timeout
  parse+clamp (1s–5m), match precedence exact > `{param}` > wildcard (case-insensitive
  literals, params preserve case), dotted-key lookup (config normalization nests
  `trace.id.header` — Java ConfigReader behaves identically), header add/drop/keep engine.
- `server.rs` — hyper accept loop + `HttpRouter` dispatch: request → lowercased header map
  → transforms → **cid always ensured** (per-entry/global header name; generated if absent;
  legacy conflation yields ONE id; exposed as reserved `my_correlation_id` header) →
  **trace start** on `tracing: true` (valid W3C `traceparent` wins, parent-id adopted as
  parent span; else trace-id header; else generated; path = `METHOD /path`) → optional
  authentication (route form; non-true → 401, error envelope → its status) →
  AsyncHttpRequest-shaped event (Java keys: method/url/ip/https/host/headers/
  parameters.path+query [url-decoded]/body [JSON or text]) → `po.request(timeout)` →
  envelope→HTTP (status; body Nil→empty, String→text/plain, Binary→octet-stream,
  else JSON; response transforms + CORS headers). Java error shape
  `{status,message,type:"error"}`; OPTIONS preflight → 204 + cors options headers.
- `app_starter.rs` phase 4: `rest.automation=true` → `start_http_server` (port from
  `rest.server.port`, default 8085; 0 = ephemeral for tests).
- **Example**: `rest.yaml` (`GET /api/greeting/{user}` → `greeting.api`, tracing+cors+
  header-transform), new untyped `greeting.api` composing into typed `greeting.demo`
  (automatic propagation → two-span tree), main parks on Ctrl-C while serving.

**Verified live** (app booted, curl):
- plain GET → 200 JSON, generated cid returned;
- upstream `traceparent 00-4bf92f…-00f067aa…-01` + `X-Correlation-Id: order-9000` →
  **complete causal chain**: greeting.api span parented to the UPSTREAM caller's span
  (`00f067aa…`), greeting.demo parented to greeting.api, one trace id end-to-end,
  `order-9000` on every hop's telemetry + log context;
- 404 Java shape; CORS preflight headers; `x-served-by` response transform.

**Two test-infra bugs found & fixed:** (1) a shared test server died with the first
`#[tokio::test]`'s runtime (each test = own runtime; spawned accept-loop/workers belonged
to the first) → server-per-test on port 0; (2) timeout-clamp test expected sub-minimum
500ms to survive — the clamp (1s floor) is grammar-correct.

**Verification:** **83 tests** (28 unit incl. rest.yaml invariants/matcher/transforms/
url-decode/body-parse + 18 config + 6 elastic + 11 event-bus + 7 lifecycle + 5 telemetry +
**8 REST end-to-end**: path/query/cid, POST JSON + supplied cid, 404, preflight, 408
timeout, auth 401/pass, edge trace + traceparent parent adoption, text/plain mapping) ·
clippy + fmt clean · live curl demo.

> **also: first review ritual run** (`[review-overdue]` fired at 10 sessions =
> `review_every`). Steps 2–3: `refresh-metadata` reports all footers current. Step 4–5:
> **nothing archivable** — the repo is 10 sessions old vs `archive_window: 20`; the two
> completed threads (`ot-vision-confirm-blueprint`, `ot-greenfield-no-code`) stay in place
> until they age past the window. Stamped `last_review: 2026-07-16 | through
> 2026-07-16-005505`. memory-lint clean after.

## Memory References

Updated:
- `ot-design-platform-core` — increment 6 done (HTTP-serving foundation)
- Project State status; Stack & Tools (hyper family, D10)

Referenced: `bp-platform-core`, `vision-mercury`, `platform-core-stack`,
`config-first-spring-out` (http.trace.id.header/http.correlation.id.header/rest.server.port
keys verbatim), `elastic-fifo-no-bdb` (dispatch under the HTTP edge),
`conventions-rust-baseline`, `inv-never-couple-functions` (the endpoint↔function link is
the route string in rest.yaml — configuration, not code)
