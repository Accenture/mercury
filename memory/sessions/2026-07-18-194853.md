# Session (2026-07-18T19:48:53.000Z)

**Agent:** Claude Code

## What happened

**Fixed the companion `/sync` drain so its `run` response is complete and byte-identical across the
Rust and Java engines — the AI-companion REST surface is language-neutral.**

- **Root cause (not what I'd noted before).** My prior session's "cross-port note" — that Java `run`
  emits only the traversal trace while Rust also echoes output.body — was **wrong**. The maintainer's
  screenshot proved the Java WS console already echoes `Executed … / {output:{…}} / Graph traversal
  completed in N ms`, identical to Rust. The real defect was the **`/sync` endpoint's drain**: `run` is
  the only *asynchronous* command — the command handler launches the traveler and replies immediately,
  then the traveler streams `Walk to…` / `Executed…` / the `output` map / a terminal line to `out`
  **after** the reply. The FIFO sentinel enqueued right after the reply raced (and usually beat) that
  async tail, so `/sync run` was truncated to just `Walk to root` / `Walk to end`.
- **Fix (both engines, symmetric).** Drain a traversal on the traveler's **terminal line** —
  `Graph traversal completed in N ms` (success) or `Graph traversal aborted` (failure), always emitted
  last, so once seen every prior line is buffered. Synchronous commands keep the FIFO sentinel. The
  bounded poll/await is only a safety net; the signal drives correctness (the maintainer's ask: "a
  clean end-of-transmission signal from the engine, not a timeout").
- **Made the terminal a contract.** Several failure paths (no instance yet, missing root/end, unknown
  skill, dangling next-node) ended with only a *reason* line. They now emit the reason **then** the
  canonical `Graph traversal aborted` (`emit_aborted` / `emitAborted`), so a companion mistake such as
  `run` before `instantiate` returns promptly (`ok:false`) instead of hanging to the timeout.
- **Rust** (`b37b131`, committed to main — R&D): `traveler.rs` (emit_aborted + failure paths),
  `rest.rs` (traversal-aware drain), `playground.rs` (drives `POST /api/companion/{id}/sync` `run`
  end-to-end: asserts `Walk to root` + `Executed mapper …` + `Graph traversal completed` + output.body
  `result` + the live tee, plus a no-instance `run` → `ok:false` + terminal, promptly), test rest.yaml
  gets the `/sync` route, design note updated. fmt + clippy clean.
- **Java** (PR **[#191](https://github.com/Accenture/mercury-composable/pull/191)**, stacked on #190):
  `GraphTraveler.java` + `PostCompanionCommandSync.java` mirror the Rust logic (one CountDownLatch
  fires on the terminal line for a traversal or the sentinel otherwise); `CompanionSyncTest` gets the
  same assertions; ADR-0008 + CHANGELOG updated. Full minigraph module **64 tests green**.
- **Pre-existing note:** `cargo test -p knowledge-graph --test graph_runtime` is flaky when its two
  `#[tokio::test]`s share one binary (each spins its own runtime; the shared server dies with the
  first) — reproduces on HEAD, unrelated to this change. Run either test in isolation to verify.

**Upstream state:** #190 (example rest.yaml route) and #191 (this fix) both OPEN. Merge #190 then
#191 — #191 auto-retargets to `main` when #190's branch merges. Java clone is on branch
`fix/companion-sync-drain-terminal`; return to `main` after both merge.

**Update (same session):** SonarQube flagged `Thread.sleep()` (java:S2925). Swept it out of Java
`src/main` — `Utility.getInstance().sleep(ms)` in BenchmarkApp + kafka TopicManager (dropping the now-
superfluous `throws InterruptedException`, java:S1130) + a doc note in the event-driven guide — as PR
**#192**. Then **merged all upstream PRs** at the maintainer's direction: #190 (example rest.yaml) →
drain fix → #192. The drain-fix PR **#191 was auto-closed** by GitHub when #190's base branch was
deleted on squash-merge (it closed the stacked PR instead of retargeting); recovered as **#193**
(cherry-pick onto updated main) and merged. `Accenture/mercury-composable` `main` now has the complete,
Sonar-clean companion `/sync` feature. Lesson recorded in personal memory
(`github-squash-merge-closes-stacked-pr`).

## Memory References

- referenced: `bp-companion-sync`, `vision-mercury`, `port-bottom-up-faithful`
- updated: `bp-companion-sync` (drain fix; corrected the earlier cross-port note; marked all upstream
  PRs merged / blueprint gap closed)

## Memory Review (2026-07-18, through 2026-07-18-194853)

Triggered by `memory-lint [review-overdue]` (10 sessions since 2026-07-18-061457); ran on command.
- Refreshed metadata: 2 usage corrections via `refresh-metadata` (`port-bottom-up-faithful` uses 18→19,
  `bp-companion-sync` uses 9→8). **No tier changes** — metadata was current.
- Archived: **0**. No non-pinned fact faded past `archive_window` (20). The `archive-candidate` facts
  (`conventions-rust-baseline`, `bp-platform-core`, `bp-event-script`, `ot-design-knowledge-graph`) are
  all still referenced within the window (`last_used` 2026-07-17/18).
- Swept threads: **0**. The two oldest completed `[x]` blueprint threads (`bp-platform-core`,
  `bp-event-script`) are old by completion date but **still referenced within `archive_window`**
  (`last_used` 07-18 / 07-17) — sweeping would trip the over-archival guard, so kept. The other `[x]`
  threads (knowledge-graph blueprint + design, invariant-reverify) completed <20 sessions ago → kept.
- Archive-verify: pass (`memory-lint` 0 errors; no id in both `continuity.md` and the archive).
- Tier changes: 0. Invariants: **not due** (sessions_since_last_invariant_check ≈ 10 < 40).
- Contradiction/drift scan: no contradictions. Observation (not flagged as drift): the platform-core
  and event-script **design** threads (`ot-design-platform-core`, `ot-design-event-script`) remain
  `- [ ]` open though their layers shipped — pinned living docs; they'll sweep once dereferenced.
- Continuity health: 17 facts / well under `continuity_max_facts` (30); no `[continuity-bloat]`.
- Stamped `last_review` → 2026-07-18 through 2026-07-18-194853.
