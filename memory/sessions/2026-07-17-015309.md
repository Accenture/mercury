# Session (2026-07-17T01:53:09.000Z)

**Agent:** Claude Code

## What happened

**Increment K-2 shipped (repo increment 22) — the math expression engine.** K-1 was
committed and pushed by the maintainer's direction at the start of the session
(f86e200).

- **New crate `crates/knowledge-graph`** — created one increment ahead of the K-3 plan
  to host the engine's first module (compiler/registry + resource-root hook still land
  at K-3); its crate doc records the graph.js retirement rationale.
- **`knowledge_graph::math`** — faithful port of `com.accenture.minigraph.math`
  (979 Java lines: TokenType/Token/Lexer, the non-recursive shunting-yard Parser with
  postfix call/member chains, Evaluator, Value union, EvalContext, ExpressionEngine
  facade). Java-exact semantics: strict JS rule (`-2 ** 2` = parse error),
  short-circuit `&&`/`||`, ternaries, **dual number rendering** (display
  `as_string()` keeps Java's `Double.toString` `3.0`; string concatenation uses the
  JS-like plain form `3`), string/number relational comparison, same-type-only
  equality with `NaN != NaN`, constants/functions mirrored into `Math.*`.
- Rust choices: `MathError::Parse`/`Eval` mirrors ParseException vs
  IllegalArgumentException; functions are `Arc<dyn Fn(&[f64]) -> Result<f64>>` (user
  functions can fail — the short-circuit tests rely on it); `random()` via
  `getrandom` (SecureRandom analog); `round` = Java `floor(x+0.5)`, NaN→0.
- Parity suite: all 14 `ExpressionEngineFullTest` methods + an added
  random/arity/coercion test = 15 tests, green first run.
- Verification: workspace 173 tests green, clippy 0, fmt clean. INCREMENTS.md row 22 +
  section; design doc K-2 shipped note.

Next: **K-3 — graph compiler + registry** (graph JSON ⇄ MiniGraph, CompiledGraphs,
PlaygroundLoader, the 13 tutorial fixtures, resource-root hook K8).

## Memory References

- referenced: `ot-design-knowledge-graph` (K-2 shipped — thread updated),
  `bp-active-knowledge-graph`, `vision-mercury`, `conventions-rust-baseline`
- created: (none)
