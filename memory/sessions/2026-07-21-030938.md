# Session (2026-07-21T03:09:38.000Z)

**Agent:** Claude Code

## What happened

**Reviewed and verified a third-party correctness assessment** (GitHub Copilot,
2026-07-20, 24 findings against the Java canon) — every finding independently checked
against both codebases (direct verification of the Critical + 4 parallel agents, file:line
evidence on both sides). **Verdict: 20 CONFIRMED, 2 INTENTIONAL (documented), 2 PARTIAL.**
The report is credible; its layer ranking (knowledge-graph strongest, platform-core
weakest) holds.

Key verdicts (full detail in the `ot-parity-remediation` thread):
- CRITICAL confirmed: REST boundary drops function response-envelope headers
  (`automation/server.rs:342-353` vs Java `AsyncHttpResponse.updateHeaders`).
- Highs confirmed: zero-traced-route trace loss; Event Script dynamic-index array cap
  missing (own docs contradict on `max.model.array.size`); date/time pattern subset +
  dropped zone arg; fetcher cache key = whole staged fetch map (Java: dictionary-declared
  inputs only) → re-fires POSTs Java reuses.
- INTENTIONAL (not defects): null-strip-on-spill (documented design; consequence
  undocumented — needs a maintainer decision: document or normalize); XML console
  advertising (byte-identical Java mirror; XML deferral documented).
- PARTIAL (overstated): literal dotted config keys (config normalization makes both ports
  identical for config; raw MultiLevelMap only); wildcard grammar (Rust mirrors the
  documented grammar; Java engine accepts extra forms; Rust-only empty-remainder match
  found that the report missed).
- Meta-finding: several Rust doc comments claim "Java parity" where behavior diverges
  (F8/F19/F21/fetcher/HostUri) — no-silent-divergence violations to fix alongside code.

**Maintainer decision (human gate): remediation APPROVED** — "serious side effects...
affect functional integrity such as telemetry and response headers." Recorded as the
`(blueprint)` Open Thread `ot-parity-remediation` with the amended fix order (REST headers
first, designed together with the header-model items).

**ManagedCache question answered:** Java's `org.platformlambda.core.util.ManagedCache`
(named TTL+size-bounded Caffeine cache, static registry) has NO Rust equivalent — the port
uses ad-hoc stand-ins (unbounded WS-dedup HashMap; fetcher cache is per-instance state in
both engines). Recorded as backlog `ot-managedcache-port` (needed for the connectors port —
minimalist-kafka's schema-registry client depends on it — and Java-API-surface
completeness; candidate: `moka` or hand-rolled TTL+LRU).

## Memory References

- referenced: port-bottom-up-faithful, conventions-rust-baseline, bp-kafka-connectors-backlog
- created: ot-parity-remediation (born tier: working), ot-managedcache-port (born tier: working)

Co-Authored-By: Claude Code <noreply@anthropic.com>
