# Session (2026-09-21T23:31:14.512Z)

**Agent:** Claude Code (Fable 5.1)
**Title:** minimalist-kafka K5 — the Schema Registry ported (K5a), the sync-over-async facade tasks and the demo mirror driven live against the Java engine (K5b), the guide twin (K5c docs), the lock-step round; v4.12.14 prepared on both engines

## Rulings (Eric, at the K5 checkpoint)

1. "port schema-registry feature if it is viable in Rust. Then sync up the doc for completeness." — Q2 of the port spec re-scoped in.
2. "Mirror Java for sync-over-async-demo" — the facade/backend roles with the raw, JSON Schema and Avro legs, not the raw path alone.
3. "wait for completion of locksteps and then v4.12.14 for both Java and Rust repos" — one release number for both engines once the lock-step round is complete.

## What happened

Four branches pushed, PRs pending (Eric opens; texts in the handoff) — **PR #303 opened from the DOCS branch** (`docs/minimalist-kafka-guide` `424e1515` → main), which is stacked on K5a — so #303 carries K5a `0da85068` + the guide twin together (two commits; the auto-title named only the docs commit — a combined title/body handed to Eric); `feat/minimalist-kafka-schema-registry` then needs no PR of its own; **K5b opened as PR #304** (`feat/sync-over-async-facade` `cca0c1bf` → main, two commits — K5a's `0da85068` rides along until #303 merges; a rebase onto main afterwards leaves its own commit only); **PR #305 was opened from the K5a branch itself** (`feat/minimalist-kafka-schema-registry`, one commit — a duplicate of #303's first commit), not from `feat/lockstep-4-12-13` as intended — flagged to Eric with the recommendation to close #305 and open the lock-step PR from the right branch; the lock-step branch has no PR yet; the Java release PR is mercury-composable #437. Merge order: K5a → docs → K5b (the last two are stacked on K5a); the lock-step branch is independent (from main).

**K5a — Schema Registry** (`feat/minimalist-kafka-schema-registry` `0da85068`). Viability against the Java surface: JSON Schema and Avro yes — the Apache reference crate `apache-avro` 0.22 and `jsonschema` 0.56 with default features off (no aws-lc; the workspace stays on ring), the Confluent frame, subject/version resolution and the two-tier positive-only cache implemented here; registry authentication yes — the `schema-registry.yml` template interpreted by name (OAuth 2.0 client credentials with a cached bearer token, static token, SASL inheritance, basic auth USER_INFO / URL / SASL_INHERIT, the Confluent Cloud headers; unknown keys logged); CSFLE no — Java delegates rule execution to Confluent's serdes and there is nothing to delegate to, so a schema carrying a `ruleSet` or `references` is refused with a 501, never served in plaintext; Protobuf unwired as on Java. New `schema` module (`mod`, `registry` over the platform's own `async.http.request`, `auth`, `avro`, `json`); `schema.enabled` through adapter and consumer (decode BEFORE routing; a decode failure is poison — dead-lettered at once with the raw record); the `subject`/`version` path in the notification (a bytes JSON document, the directives never forwarded); the codec built once in bootstrap, shared through `runtime` (thread-safe — no owner-confined serdes, no classloader pinning). 59 unit tests; a codec suite against an in-process hyper registry double (OAuth end to end, one token fetch); the notification subject suite; the e2e at 19 scenarios. Spec §7 items 12–16, §8 K5a ✅, §9 Q2 re-ruled.

**K5c docs** (`docs/minimalist-kafka-guide` `424e1515`, stacked on K5a): `docs/guides/minimalist-kafka.md` twin with a "Differences from the Java engine" table; nav, `llms.txt`, `files.list`, the configuration reference's Kafka section (the Rust-port note no longer excludes the Kafka keys), two claims pinned to the schema tests, README non-goals and port-scope (minimalist-kafka and sync-over-async ported; twin-kafka deferred). Docs gates green.

**K5b — facade tasks + demo + drive** (`feat/sync-over-async-facade` `cca0c1bf`, stacked on K5a): `tasks.rs` (`sync.prepare` / `sync.await` / `soa.reply`, the Java contracts, transport-neutral), `bootstrap.rs` (the Java `SyncOverAsyncAutoStart`, sequence 15 before the Kafka adapter's 20; the demo's hand-rolled bootstrap removed), `tests/rest_flow_mvp.rs` (the Java `RestFlowMvpTest` twin — green on the first run), the demo mirrored (facade/backend profiles, nine flows, both adapter YAMLs, registry seeds, Node helpers, `soa.rs` with the `SoaTaskTest` twins, README). Live drive on the Java helpers 4.12.13 with the Java demo unchanged (report addendum Scenarios 8–13): every leg on Rust alone with continuous trace ids (the Avro record's defaults applied, `order` dropped — closed shape); 408 in 3 s; Java backend + Rust facade and Java facade + Rust backend — the Confluent frames decoded both ways by both engines; a mixed backend group 9 Rust / 11 Java of 20; **the Java facade delivered 8 of 20 replies to the waiting Rust facade through the Redis return route — cross-pod AND cross-engine**; two Rust facades. Increment 127 records the whole port (K1–K5).

**Finding 5 (fixed in the demo):** the binary linked neither the flow engine nor the Kafka crate — configuration-only activation — so every endpoint answered 404 with `Service http.flow.adapter not available` and no consumer started; two `use … as _;` lines. The in-process e2e had them and was green; only the real binary could show it. Spec §7 item 6 applies to every crate a binary activates by configuration.

**Lock-step round** (`feat/lockstep-4-12-13` `48506b99`, from main; Increment 128): P8 the CompileGraph task↔skill gate (`model_validator::validate_working_node_has_skill`, the Java messages, the byte-identical `unit-test-task-skill-err1..3` fixtures; the rule stated in the skills reference — neither engine's guide had it); P9 the case-insensitive `input.header.*` fallback on a miss (`executor::header_ignoring_case`); #414 snake_case log-context keys (`trace_id`, `trace_path`, `span_id`, `parent_span_id`), the automatic `timestamp: $utc` (fallback `utc`, warning if both taken), `update_context` refusing the reserved names in both spellings (`RESERVED_OUTPUT_KEYS`), developer keys rendered first so the template wins; hello-world's template, the observability guide and the configuration reference updated. P2 confirmed — the graph skills' output mapping is a synchronous `Result` with the #394 wording already (Increment 125), no change. **P10 (starter-graph dev mode) needs Eric's ruling.**

**Release preparation:** the Java `release/v4.12.14` branch (sweep re-derived 43 files / 98 occurrences, CHANGELOG from `v4.12.13..HEAD` = #436 with the lock-step paragraph). The Rust release branch follows the merges (version 4.12.12 → 4.12.14 in one step — 12 `Cargo.toml` files / 23 occurrences today; the CHANGELOG entry drafted); Eric tags and publishes both crates (`cargo publish` per Increment 96).

## Doc Gaps
- The CompileGraph task↔skill gate was enforced on both engines and stated in neither guide; the Rust skills reference now carries the sentence — the Java `skills-reference.md` graph.task section could take the same one.

## Memory References
- ot-minimalist-kafka-port (updated — K5a/K5b/K5c-docs and the lock-step round DONE; next: the release gate)
- ot-sync-over-async-port (updated — the held Q1 facade tasks closed by K5b; publication rides v4.12.14)
- schema-registry-native-codec (created, tier: working)
- headless-app-keep-running (consulted — the demo's headless backend role rests on it)
- conventions-rust-baseline (consulted)
- conv-template-version-sweep-rust (consulted)
- eric-release-rhythm-rust (consulted)
