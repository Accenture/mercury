# Session (2026-07-19T04:30:19.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-6 (L5, data-driven iteration): PASSED on the first
attempt.** First test on the post-#9–#13 grammar + the newly ported layer-1/2 docs (brief =
`docs/llms.txt` + its map). The planned tutorial-5 retest was **skipped by maintainer direction**
(proceed to tutorial-6); its tightened no-in-band-help criterion was applied observationally here.

- **Setup:** server restarted by the maintainer (guard live); primary `ws-876960-4` verified
  EMPTY; both mocks probed (`GET /api/mdm/profile/{id}`, `POST /api/account/details`); the
  orchestrator watcher subscribed **over the WS connection** (new `ws-watcher.mjs` — sends
  `session subscribe` as a WS text frame). Engine state showed `subscribed by ["ws-759054-5"]` —
  a real session route, not a capture route: the #194 read-only companion rule + WS-side
  subscribe **validated live**; the mirror held for the entire run (208 lines).
- **Test:** fresh sub-agent, problem-only brief (profile + details of EVERY account; input
  `person_id`; output `{name, address, accounts:[detail…]}`; account list is runtime data).
  Result: **18 observed POSTs, all `ok:true`, zero failed commands; dry-run honored the contract
  on the first attempt** (all 5 details for person 100). Independently re-verified by the
  orchestrator. Exported as `person-account-details`.
- **Design vs canonical `tutorial-6.json`:** semantically equivalent — chained fetchers with
  `for_each[]=profile-fetcher.result.accounts -> model.account_id`, explicit `concurrency=3`,
  POST provider with `body.*` targets; divergences all economical (one whole-profile Dictionary
  vs three field-level; `model.*` scratch + end-mapper assembly vs direct output writes; no
  island; no placeholder feature flags).
- **Grammar sufficiency:** the tut-5 fixes **held** — zero lookups for Provider/Dictionary
  authoring (incl. the POST form). Exactly **one** in-band lookup (`help graph-api-fetcher`) for
  the `for_each` idiom → **new findings #17–#21** in `docs/AI-companion-test.md`: for_each
  authoring idiom + aggregation semantics undocumented (per-iteration results collect into an
  array — confirmed by experiment; ordering unspecified), `output[]` required-vs-optional
  contradiction, POST-body Provider example missing, and the KG constants section reads as a
  closed set while `f:` plugins + `$.` JSONPath are resolvable in graph mappings (code-confirmed:
  `skills.rs` → shared Event Script mapping engine).
- **Docs updated:** `docs/AI-companion-test.md` — Tutorial-6 stage record, L5 ladder row, rollup
  #17–#21, method note (allowed set = llms.txt map from tut-6 on; in-band lookups recorded as
  gaps).

## Decisions

- Tutorial-6 defined as **L5 (data-driven iteration)** on the validation ladder.
- Findings #17–#21 recorded as **candidates awaiting maintainer direction** (the #9–#13 pattern);
  no unilateral grammar edits this time.

## Memory References

- ot-companion-validation-sweep — advanced (tutorial-6 done; next tutorial-7); substance updated
- ot-subscribe-via-sync-bug — referenced (its resolution validated live: WS-side subscribe +
  read-only companion rule worked end-to-end)
- bp-companion-sync — referenced (the whole test ran over `/sync` in-band feedback)
- port-bottom-up-faithful — referenced (judged against the canonical Java fixture)

Co-Authored-By: Claude Code <noreply@anthropic.com>
