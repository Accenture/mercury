# Session (2026-09-14T17:05:40.000Z)

**Agent:** Claude Code

## Broker-first hosting guidance (lock-step with the Java pass)

- Eric's live team demo (Java side) surfaced the friction: an agent asked to "host" a
  Playground session hand-rolls a WebSocket client, misses the session contract's
  keep-alive (ping every ~20 s), and the session dies silently at the idle timeout
  mid-collaboration. The Java repo shipped the broker-first docs pass as
  mercury-composable#383; this is the Rust twin.
- Same shape, one deliberate asymmetry: the Java starter-graph template embeds the
  playground engine, so it received a copy of the broker script; the RUST starter-graph
  does NOT serve the playground (deps: platform-core/event-script/knowledge-graph only,
  no app.env) and the Rust authoring workflow scaffolds from the minigraph-playground
  EXAMPLE (#scaffolding) — so here the broker stays in the example and the scaffolding
  boilerplate manifest gains a "scripts/ — keep" row, which carries the broker into
  derived projects. No template copy, no starter AGENTS.md change.
- Changes (branch `docs/host-sessions-with-broker`):
  - ai-agent-guide #hosting: leads with "use the shipped session broker — do not
    hand-roll a WebSocket client", names the keep-alive failure mode twice
    (recommendation + wire-contract step 4), demotes the contract to "what the broker
    implements", `--target` guidance (example default 8085).
  - ai-agent-guide #scaffolding manifest: `scripts/` keep-row.
  - playground-and-companion (sessions/multi-party viewing): hosting pointer.
  - example scripts/README.md: the why-not-hand-roll paragraph (same text as Java).
- Verified: `cargo test -p ai-contract-provider` green, including
  inventory_equals_the_documentation_closure (content edits to listed files only; no
  inventory change). The broker script itself is untouched and stays byte-identical to
  the Java repo's copy.

## Addendum — template copy (Eric's call, same session)

- Eric ruled the broker ALSO ships in `templates/starter-graph` (as in Java), so template
  consumers have the tool at hand: byte-identical `scripts/` copy with a Rust-honest
  README — the starter serves no Playground UI, so the broker targets the
  `minigraph-playground` app run alongside (default 8085); export the finished model into
  `resources/graph/` here. Starter README (layout row + next-steps line) and AGENTS.md
  (hosting item 3; old 3–4 renumbered 4–5) updated; ai-agent-guide #hosting now names
  both homes. The scaffolding-manifest keep-row stays.

## Memory References
- playground-session-broker — REACTIVATED (archived 2026-Q3 fact consulted for the
  broker contract and cross-repo copy parity; substance delta to carry on reactivation:
  the AI docs are now broker-first with the keep-alive failure mode named, and the
  scaffolding manifest carries scripts/ into derived projects)
