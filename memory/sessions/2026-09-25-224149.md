# Session (2026-09-25T22:41:49.409Z)

**Agent:** Claude Code
**Title:** Increment 142 — graph.model.automation accepts a list of manifests; the later manifest wins (Rust twin)

## Summary

The Rust twin of mercury-composable `feat/graph-manifest-list` (Eric's ruling, 2026-09-25). Born at a live demo's deploy
step: an exported graph went live in the running playground example with no rebuild through a launch-time override of the
manifest property — but one manifest REPLACED the bundled set, so a prototype delegating through `graph.extension` to a
bundled graph could not run. Same change shape as Java:
- `compiler.rs`: `compile_graphs` splits the property on `,`/space (Java `util.split(text, ", ")`), `compile_manifest` per
  entry with its own `location`, a manifest that fails to load is skipped with a warning; `compile_one_graph` drops an
  earlier copy from another location (`Graph {id} from {B} replaces the copy from {A}`) before compiling or rejecting the
  later one — a REJECTED later copy leaves the id not executable (404), never a silent fallback (Eric's refinement, agreed).
- `graphs.rs`: `add_graph(id, model, location)`, `remove_graph`, `graph_location`, `add_deployed_location`,
  `deployed_locations()` in manifest order; `deployed_location()` stays as the primary (first).
- `commands.rs`: `list graphs` enumerates every location (`deployed_dirs`); the `import graph from` fallback searches the
  compiled-from location first, then all (`find_deployed_graph`), and names the location it found.
- Tests (`tests/compiler.rs`, the `CompileGraphTest` twins): a second manifest's own location,
  `later_manifest_wins_for_a_duplicate_graph_id` (`unit-test-manifest-dup` version 1 → 2), a rejected later copy leaves
  the id out, both locations in order; fixtures `graphs-extra.yaml` + `graph-extra/` byte-identical to Java's, the test
  `application.yml` lists both manifests; manifest count 55 → 57.
- Docs: `ai-agent-guide.md` §Rapid prototyping — deploy without a rebuild (the recipe's first appearance in this repo — the
  `-D` PROGRAM ARGUMENT form `cargo run -p minigraph-playground -- -Dgraph.model.automation='classpath:/graphs.yaml,
  file:/tmp/graph/deploy/graphs.yaml'`, two manifests, the iterate-on-a-deployed-graph loop, the broker's new-session-id
  choreography), `build-your-first-graph.md`, `playground-and-companion.md`, `configuration-reference.md` (comma-sep
  manifest paths), `llms.txt`; claim `graph-manifest-list-later-wins` pinned to
  `compiler::later_manifest_wins_for_a_duplicate_graph_id`; `docs/INCREMENTS.md` Increment 142. CHANGELOG stays for the release.

Gates: `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test
--workspace` 117 test binaries, 606 passed, 0 failed; `cargo audit` no advisories reported; `check-doc-claims` 30 claims
pinned on both sides; `check-llms-links` 37 references resolve; `mkdocs build --strict` clean. Recorded as
[[graph-manifest-list-later-wins-rust]]; the decision itself is the Java repo's `graph-manifest-list-later-wins`.

## Memory References

- graph-manifest-list-later-wins-rust (created)
