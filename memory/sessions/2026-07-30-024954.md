# Session (2026-07-30T02:49:54.000Z)

**Agent:** Claude Code

## What happened

**The P5 consistency-review fix round, on branch
`feature/graph-suspend-resume` (NOT pushed — Eric gates).** The Java-side
review confirmed high fidelity with 22 findings
(/tmp/p5-consistency-review-findings.md); all 22 are addressed.

**Blockers:**
- **[#0] Restore merge is now a literal key-level putAll (Java parity).**
  The previous merge wrote each persisted key through
  `set_element("model.{name}")`, which interprets `.`/`[..]` as a composite
  path — a forged record key like `"cid.x"` or `"ttl[0]"` passed the
  literal-name reserved filter and then descended into (and replaced)
  `model.cid`/`model.ttl`. The merge now mutates the model map's entries
  literally (replace-or-append), which also preserves keys whose text fails
  path parsing (Java putAll parity). Regression: the forge test now injects
  the composite-path vectors literally and asserts the delivered cid and
  step-counter keys survive. **Java-side assessment (flagged for the
  reviewer): Java's putAll is structurally immune and I found no analogous
  exposure on its restore path (restoreMarks uses literal map puts; the
  `node` field is a name lookup) — but adding the dotted-key vectors to
  GraphSuspendResumeTest's forge loop would cheaply pin that immunity
  against a future refactor toward path-based writes (the exact trap this
  port fell into).**
- **[#6] `instantiate graph` is the dry-run's edge (Java f2107126):**
  auto-creates `model.cid` with the "No business correlation ID given -
  this dry-run created model.cid = <uuid>" reminder when the initial
  mapping omits it; a supplied cid is honored silently. Both cases pinned
  in a new companion-sync test.
- **[#10] Walker seen-marking is insert-if-absent** (putIfAbsent parity) in
  BOTH walkers — an overwriting insert transiently flipped a join's `false`
  barrier flag to `true`, which a concurrently evaluating chained
  downstream join reads as "upstream fired". The join always-evaluate
  bypass is untouched.
- **[#11] `suspend` joined RESERVED_PARAMETERS** (16 entries, with Java's
  "'ttl' is deliberately NOT reserved" comment) — the property no longer
  leaks into the state machine and a `{node}.suspend` mapping RHS is
  rejected with Java's error text.

**Should-fixes:** [#1] restore_marks truthiness Java-exact (`Boolean true`
or the exact lowercase `"true"` — the record is external input); [#7] the
rejected-graph startup log carries the full normalized path (ConfigError
passed through untouched); [#8] the playground `run` and `execute` commands
mint a fresh dry-run trace (`set_from("minigraph.playground")` +
`set_trace(cid, "/graph/playground")` — the PostOffice.trackable twin;
verified live: the playground suite now emits 22 `/graph/playground`
telemetry records where it previously emitted none); [#12] console
narration floats keep the trailing `.0` (`{spent:?}`, the repo's own
float-parity rule — both the traveler line and the execute-command line);
[#15/#16] catalog: `traversal.suspend` entry + the
Suspend/Resume/Suspensible node_types rows (Java verbatim); [#17] the
span-topology twin test — a `distributed.trace.forwarder` capture asserts
retrieve-chains-to-graph.resume, persist-chains-to-graph.suspend, and a
completed resumed run has NO graph.suspend span.

**Nits:** raw-untrimmed cid/ttl parity everywhere ([#2/#3/#9/#13] — the
cid ladder, the ttl error text, and the business-cid tag in both walkers
now use the raw value after only a blank rejection); [#4] `{node}.error`
stages the EXTRACTED error (an exact `EventEnvelope.getError()` port:
3-field composable error map → message text, bytes → `"***"`, null →
`"null"`); [#5] the consume-on-retrieve assertion after resume; [#19] the
help-file naming-convention guard; [#20] tutorial-14's dry-run rationale
reworded to THIS engine's documented repeat-run semantics (instruction
unchanged; webapp bundle rebuilt); [#21] `x-run` asserted on the HTTP
RESPONSE headers over the real stack — the engine test rest.yaml gained
the `/api/graph/{graph_id}` route (Java test-config parity).

Gate: workspace 296 tests / 0 failed, clippy 0, fmt clean.

## Memory References

- thread-graph-suspend-resume-p5 (extended: fix round complete — all 22 findings addressed)
- conventions-rust-baseline (referenced: gate conventions, the float-parity rule)
