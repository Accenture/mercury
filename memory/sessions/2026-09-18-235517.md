# Session (2026-09-18T23:55:17.664Z)

**Agent:** Claude Code

Lightweight: Mode B upgrade 4.41.2 → 4.42.0 (MINOR — an opt-in seed for the governance pair: sample
`docs/arch-decisions/ADR.md` + `RFC.md` skeletons under a new MANIFEST policy `optional` — installed only on an
explicit `--adopt`, never touched when present, listed for reference when absent and never pending; the offer
belongs to a fresh enable, never to an upgrade; from the maintainer's RFC-0005, promoted to ADR-0009). Reconcile
re-copied .agent/schema.md (the ADR section's install stance + the new RFC.md section). Protocol — unchanged (byte-identical to the template; no protocol text changed in 4.42.0).
Rung step 2 — the repo keeps its own docs/arch-decisions/ADR.md — reported ok (adopted, never touched); RFC.md listed as optional. Adapters re-synced; stamped 4.42.0; the re-copied file byte-identical to its MANIFEST
source. Ran in a throwaway worktree of origin/main (`--forge github`). memory-lint: 0 errors, 1 warning(s). Upstream release:
https://github.com/Accenture/mercury-go/releases/tag/v4.42.0 .

## Memory References

(none)
