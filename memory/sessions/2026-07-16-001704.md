# Session (2026-07-16T00:17:04.000Z)

**Agent:** Claude Code

## Summary

**Implemented increment 5 — OpenTelemetry-compatible distributed tracing, business
correlation-id propagation, and the app-log-context design** (`bp-platform-core`, Design
§5d). Maintainer directive: this is foundation code, built **before** REST automation.

**Harvested first** (Java v4.8.6 + `docs/guides/observability.md`): `Telemetry.java` (the
`distributed.tracing` sink: dataset log + `distributed.trace.forwarder`/
`transaction.journal.recorder` hooks, zero-tracing filter, `@origin` trim), `TraceInfo`
(per-execution span, `%016x` id), `LogContext`/`LogContextManager` (per-worker anchor keyed
by thread id — **deliberately not ThreadLocal/MDC**), `LogContextConfig`
(`app-log-context.yaml`: `$token` | `${ENV:default}` | literal; omit-absent), `JsonLogger`
(context block injection; JSON-message embedding), `W3cTrace` (traceparent format/parse),
`WorkerHandler`'s trace bracket + metrics emission, PostOffice propagation (cid to next touch
point; sender's span → receiver's parentSpanId).

**Built:**
- `src/trace.rs` — `TraceState` (TraceInfo+LogContext combined; W3C 32/16-hex ids; ISO-8601
  civil-from-days util, no date dep), **tokio `task_local!`** trace bracket (`run_scoped`
  returns the finished state so annotations survive), `with_current`/`with_current_mut`,
  reserved-key set. Spawn-boundary parity doc'd (same as Java's Mono/Flux boundary).
- `src/util/w3c_trace.rs` — traceparent format/parse, zero-id rejection, version-tolerant.
- `src/telemetry.rs` — the `Telemetry` service (platform handle for forwarders; permitted-
  route filter tests).
- `platform.rs` — worker trace bracket (traced event → own span parented to envelope
  `span_id`), telemetry emission (`emit_telemetry` dataset builder; f64 rounding), response
  trace-back (applyTraceContext parity), zero-traced routes (plumbing + `inbox.*` + verbatim
  `skip.rpc.tracing` default `async.http.request`).
- `post_office.rs` — `apply_current_trace` on send/request (trace/span/from/cid-if-missing;
  request applies BEFORE minting a cid so a business cid wins), `my_correlation_id`/
  `my_trace_id`/`my_trace_path`, `annotate_trace` (span sink), `update_context` (log sink;
  reserved keys → 400; untraced = silent no-op).
- `src/logging.rs` — `LogContextConfig` (ConfigReader-based; invalid token = advisory skip,
  a doc'd divergence from Java's throw) + `PlatformLogger` (`log.format=json` single-line
  JSON with context block + structured embedding of JSON messages; `text` = plain,
  context-free) + `logging::init()`.
- `envelope.rs` — `span_id` field (serde default), getters/builders.
- `app_starter.rs` — essential phase registers `Telemetry` (race-tolerant: a concurrent run
  losing the register race is benign — fixed a real TOCTOU test failure).
- **Example** — `app-log-context.yaml` (all 7 tokens + env + literal), `log.format: json`,
  traced request (`set_trace(new_trace_id(), "GET /api/greeting")` + cid `order-12345`),
  `update_context("user", …)` + `annotate_trace("greeting.for", …)` + an in-function log line.

**Two real bugs found and fixed during verification:**
1. **OnceLock re-entrancy deadlock** — the first JSON log line lazily initialized
   `LogContextConfig`, whose loader itself logs → re-entered the OnceLock initializer →
   hang (caught because the example produced zero output). Fix: `logging::init()`
   initializes the config eagerly *before* installing the logger, then reports enablement.
2. **TOCTOU race** on the essential-phase Telemetry registration across parallel lifecycle
   runs → made benign-race-tolerant.

**Verification:** **66 tests pass** (19 unit incl. trace/w3c/telemetry-filter + 18 config +
6 elastic + 11 event-bus + 7 lifecycle + **5 telemetry**: two-hop span lineage
[hop-2 parent == hop-1 span, same trace, `from`, cid at both hops, annotations],
untraced = no telemetry + no-op APIs, failure spans, reserved-key rejection, log-context
render) · clippy + fmt clean · **hello_world run live**: the function's log line and the
telemetry span carry the **same trace/span ids** — the logs↔spans join demonstrated, with
`cid=order-12345`, `user` in the context block, `greeting.for` in span annotations,
clean `exec_time: 0.114`.

**Doc:** design §5d added; §7 updated (OTLP forwarder extension + envelope annotations
listed; REST automation noted as the trace-starting HTTP edge).

> **also (same sitting, maintainer refinement after verifying the demo):** the log-format
> contract is now three-way — `text` (plain, default), `json` (**pretty-print**, Java
> `log4j2-json.xml`), `compact` (single-line jsonl, Java `log4j2-compact.xml`) — and
> **`-Dkey=value` runtime overrides** landed (the JVM `-D` analog): parsed once from the
> command line into the increment-1 override registry (`overrides::load_runtime_args` +
> testable `apply_runtime_args`), loaded by `logging::init()`/`AppStarter::run()`, so
> `hello_world -- -Dlog.format=json` switches at launch. All three formats verified live;
> 68 tests (2 new: -D parser, format resolution), clippy/fmt clean ×3 consecutive runs.
> Example `application.yml` defaults to `compact`; doc header shows the -D usage. Also
> fixed a flaky lifecycle test caught in the process: the expired-store-scan test raced the
> one-time housekeeping scan from parallel tests (two concurrent remove_dir_all calls can
> both abort mid-removal) — now asserts eventual removal with retries; production runs the
> scan once, so the concurrency was a test-only artifact.

> **also (follow-up refinement):** the example now defaults to `log.format: json` (pretty)
> and its own `[before-application]` / `[main]` lines go through the structured logger
> (`log::info!`, no raw `println!`), so every record renders as pretty JSON **with a
> `context` section**. Context rendering refined (maintainer-directed divergence from Java,
> doc'd in `render()`): when the feature is enabled the block renders on every structured
> line — trace-bound tokens omitted outside a trace, constants + `$utc` always. Verified
> live in all three formats; 68 tests, clippy/fmt clean.

## Memory References

Updated:
- `ot-design-platform-core` — increment 5 done (foundation now observable)
- Project State status (increments 1–5, 66 tests)

Referenced: `bp-platform-core`, `vision-mercury`, `platform-core-stack`,
`config-first-spring-out` (skip.rpc.tracing + config keys verbatim),
`conventions-rust-baseline` (parity-notes discipline: MDC-avoidance, advisory-vs-throw,
UTC-timestamps divergences all doc'd), `inv-never-couple-functions` (tracing rides the
envelope + task-local — functions stay decoupled)
