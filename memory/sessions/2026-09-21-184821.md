# Session (2026-09-21T18:48:22.000Z)

**Agent:** Claude Code

## What happened

**`group.protocol=auto` re-ruled and implemented as an OPTIMISTIC resolution** (Eric, 2026-09-21: "1. yes,
implement optimistic auto. 2. ship auto uncommented on both engines"). Branch
`feat/kafka-group-protocol-auto` (`0beba0f8`, pushed; PR pending — Eric opens), **stacked on
`fix/sigterm-graceful-stop` (PR #301)**: merge #301 first and this PR shows only its own commit.

1. **`client_config::resolve_group_protocol` → `bool`**: `auto` without conflicts now sets `consumer`
   and returns `true` (the binding must be ready to fall back); the conflict guard still resolves to
   `classic` naming the keys; explicit values pass through. One INFO line per process states the
   optimistic try.
2. **`consumer.rs`**: the consumer is built from its resolved `ClientConfig` inside the module
   (`build_consumer` = create + subscribe/assign, the same path for the first build and the fallback);
   the poll loop owns the config and the optimistic flag; on a `recv()` error it reads
   `consumer.client().fatal_error()` and, when `consumer_protocol_rejected` (code `UnsupportedFeature`
   or `UnsupportedVersion` AND the reason names `ConsumerGroupHeartbeat`), rebuilds once with
   `classic` and continues — a WARN states the resolution; any other fatal error (a group authorization
   failure, say) takes the existing backoff path unmasked. The binding log line names the protocol
   (`protocol consumer (auto)`). `bootstrap.rs` hands the config, not a built client, to
   `KafkaFlowConsumer::start`.
3. **The bundled `kafka-consumer.yml` ships `group.protocol: '${KAFKA_GROUP_PROTOCOL:auto}'`** with the
   Java template's explanation adapted to this mechanism; the Java twin (mercury-composable branch
   `feat/kafka-group-protocol-auto-default`, `ee37b907`) uncomments the same line — the decision is
   recorded on the Java side as `kafka-group-protocol-auto-default`.
4. **Evidence** (interop report addendum): Proof A — the default template against the KIP-848
   `kafka-standalone` (`group.version=1`): both bindings `join the consumer group using the consumer
   protocol`, server-side `uniform` assignor, a message consumed, SIGTERM → `left the consumer group`.
   Proof B — the same binary against the broker started with `group.coordinator.rebalance.protocols=
   classic` (Spring Boot `PropertiesLauncher` + `-Dloader.path` overriding `server.properties` — the
   way to run the standalone helper with a modified broker config): librdkafka's FATAL
   `ConsumerGroupHeartbeat fatal error: Broker: API version not supported`, then
   `group.protocol=auto resolved to classic for topic 'demo.inbound' … rejoining with the classic
   protocol`, classic joins on the broker, the message consumed. The e2e runs `auto` against
   `MockCluster`, which accepts the consumer protocol (every binding `protocol consumer (auto)`, no
   fallback) — the mock cannot be told to REFUSE it (rdkafka-sys 4.10's `RDKafkaApiKey` has no
   `ConsumerGroupHeartbeat` variant), so the fallback is pinned by the predicate's unit test plus the
   live Proof B.
5. **Gates:** fmt, clippy (crates), the five crate suites (35 unit), kafka-demo (7), `check-doc-claims`,
   `check-llms-links`, `mkdocs build --strict`, and the full `cargo test --workspace` (103 binaries)
   before the push.

**Spec §7 item 7 rewritten** (the K3 `auto` = `classic` delta retired; the optimistic design, both
templates on `auto`). Reversal recorded honestly: the morning's accepted delta lasted one day because the
question "does `consumer` work here, and how does the client fail without it?" had not been asked of
the running client — the K3 method note ("probe the client, don't trust the lexer") applied to the
protocol as well.

## PR opened

PR #302 (`https://github.com/Accenture/mercury/pull/302`) opened by Eric for
`feat/kafka-group-protocol-auto` (`0beba0f8`); with #301 merged it carries only its own commit. Merge is
his gate.

## Doc Gaps

(none — the Rust guide page is the K5 item; the template comment and the demo README carry the default)

## Memory References

- ot-minimalist-kafka-port (updated — the `auto` re-ruling closed as implemented; K5 unchanged)
- headless-app-keep-running (consulted — the same lifecycle path carries the fallback's rebuilt consumer through SIGTERM/Ctrl-C; no edit)
- port-bottom-up-faithful (applied — the Java resolver's semantics mapped to the client's own mechanism, not mirrored)
- eric-release-rhythm-rust (followed — branch and PR text prepared; PR open and merge are Eric's)
