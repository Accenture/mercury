# Session (2026-07-19T03:34:09.000Z)

**Agent:** Claude Code

## What happened

**Increment 34: `#[optional_service]` promoted to a first-class, order-independent attribute**
(maintainer direction: `OptionalService` is a first-class citizen annotation making a composable
function, a websocket server function, a BeforeApplication or a MainApplication optional — it must
not live only inside the `preload` macro).

- **Diagnosis confirmed the suspicion:** it was an inert marker consumed by the four registration
  macros — compiling only when written *below* them; the Java annotation order (on top) failed
  with "cannot find attribute"; `#[preload]` also carried a redundant `optional_service = "…"`
  parameter (used nowhere).
- **Fix (`crates/platform-macros`):** real `#[proc_macro_attribute] optional_service` — validates
  the condition, requires one of the four primary attributes on the item (helpful compile error
  otherwise), re-attaches the condition below where the primary consumes it → **both stacking
  orders work**. The `#[preload]` parameter form **removed** (one canonical form, the Java way).
  Re-exported from `platform_core`. Crate docs updated.
- **Tests (`annotations.rs`):** condition-above registers, condition-below still registers,
  unsatisfied condition skips, gated `#[before_application]` never runs (exact journal-sequence
  proof). Workspace: **201 tests green**, clippy 0, fmt clean.
- **Docs:** `docs/guides/event-driven/ai-agent-guide.md` — the maintainer flagged line 55
  (presenting `optional_service` as a `#[preload]` parameter): removed from the contract block +
  table; new first-class `#[optional_service]` subsection (four kinds, either order, Java
  `Feature.isRequired` condition semantics). `docs/INCREMENTS.md` — §34 added; overview rows
  31–33 backfilled (sections existed without rows).

## Decisions

- **One canonical form for conditional registration:** the stacked `#[optional_service("…")]`
  attribute (Java parity); the parameter form is gone. Breaking only in theory — no usage existed.

## Memory References

- port-bottom-up-faithful — referenced (Java annotation semantics restored faithfully)
- conventions-rust-baseline — referenced (gates part of done; ledger entry per increment
  convention)

Co-Authored-By: Claude Code <noreply@anthropic.com>
