# Session (2026-09-09T16:39:15.000Z)

**Agent:** Claude Code

## What happened

Release prep for **v4.12.5** (branch `release/v4.12.5`), lock-step with the Java
engine's v4.12.5 prepared the same hour:

- **Webapp mirror** per [[webapp-lockstep-sync-preserved-paths]]: the rsync +
  preserved-paths checkout left exactly ONE changed file — `GraphView.tsx`, the Java
  repo's community contribution (PR #339 by @skofgar: fit padding 0.1, max zoom 4,
  zoom/pinch flags pinned). 254 webapp tests green here; bundle rebuilt into
  `resources/public`.
- Version sweep 4.12.4 → 4.12.5 (workspace + the 7 inter-crate pins incl.
  minigraph-state-redis) and `cargo update --workspace`.
- INCREMENTS: 108 (route-pool lifecycle logging twin, backfilled) and 109 (field-fix
  concat-null pins + the graph-view mirror).
- CHANGELOG: Version 4.12.5, 9/9/2026 — route-pool lifecycle logging (Added), the
  graph-view tuning mirror + dev-dep refresh and the concat-null parity pins (Changed).
- Gates: `cargo fmt --all --check` clean, clippy zero findings, llms.txt 33 links OK,
  20 claims pinned, `cargo test --workspace` (result in the release PR).

PR-open, tag, and crates.io publish stay gated on Eric; continuity `status` updates
after publish, with the crates.io flag pattern from 4.12.4.

## Memory References

- webapp-lockstep-sync-preserved-paths (consulted — the mirror procedure; verified only
  GraphView.tsx drifted)
- inv-telemetry-presentation-parity (consulted — same-day lock-step release)
- conventions-rust-baseline (consulted — fmt/clippy as part of done)
