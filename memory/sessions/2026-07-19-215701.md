# Session (2026-07-19T21:57:01.000Z)

**Agent:** Claude Code

## What happened

**Backlog resumed (maintainer-prioritized). Item done: profile selection renamed to
`APP_PROFILES_ACTIVE` / `app.profiles.active` (increment 37).** Maintainer decision
2026-07-19: since Spring is irrelevant to the Rust port, rename `SPRING_PROFILES_ACTIVE`
outright — **no alias, for simplicity**. This supersedes the original 2026-07-15 plan
("keep verbatim; add a generic alias once the foundation is robust" — the gate is met).

- **Code:** `crates/platform-core/src/util/app_config_reader.rs` — constants + env/override/
  config-key lookups renamed; module doc now records the deliberate divergence. Manifest
  comment updated in `resources/app-config-reader.yml`. Mechanism and precedence unchanged
  (env → override registry → consolidated key; `application-{profile}.yml` overlays).
- **Tests:** `config.rs` profile-overlay tests updated (18 pass); workspace **202 tests /
  0 failures**, clippy 0, fmt clean.
- **Docs:** design doc §3 active-profiles bullet + §8 Q1 marked DECIDED
  (`docs/design/platform-core-port.md`); **ledger caught up** — `docs/INCREMENTS.md` gained
  increments **35** (`/sync` ok-heuristic #40, both ports — Java PR
  [#195](https://github.com/Accenture/mercury-composable/pull/195) created by the maintainer,
  merge pending CI), **36** (HTTP-boundary content-type Java parity), **37** (this rename),
  which today's earlier sessions had not yet added.
- **Flagged, out of scope:** `spring.application.name` app-name fallback in `platform.rs` —
  same rationale, separate key; maintainer call whether to rename it in a follow-up.

Also: Java branch `fix/companion-sync-ok-heuristic` **pushed to origin** at maintainer
direction (push succeeded this time); PR text supplied; maintainer created
[#195](https://github.com/Accenture/mercury-composable/pull/195) and reviewed the
src/tests changes ("All good"), merging after CI.

## Decisions

- **Profiles: rename, not alias** (maintainer, 2026-07-19) — `APP_PROFILES_ACTIVE` /
  `app.profiles.active`; the Spring names are retired in the Rust port. Config files using
  `spring.profiles.active` do NOT carry over (deliberate, documented divergence — the one
  exception to D9's verbatim-config rule).

## Memory References

- ot-profiles-alias-backlog — COMPLETED + superseded (the alias plan was reversed to a rename)
- profiles-renamed-app-active — created (born `tier: working`, supersedes ot-profiles-alias-backlog)
- Superseded: ot-profiles-alias-backlog → profiles-renamed-app-active
- ot-sync-ok-heuristic-fix — substance updated (branch pushed; PR #195 open, merge pending CI)
- port-bottom-up-faithful — referenced (first deliberate config-name divergence, documented)
- conventions-rust-baseline — applied (parity-note convention; fmt/clippy gates; ledger row)

Co-Authored-By: Claude Code <noreply@anthropic.com>
