# Session (2026-07-18T23:16:41.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-5 (L4, composition): PASSED on the first attempt.**
First test to use **this repo's own ported AI docs** (`docs/llms.txt` + the four
`docs/guides/knowledge-graph/` files) and to drive the human's live UI session entirely via
`POST /api/companion/{id}/sync`.

- **Setup:** server already running on :8100 (`app.env=dev`). Orchestrator opened a zero-dep
  Node-22 WS watcher (`ws-190414-5`) and subscribed it to the human's primary (`ws-976371-7`).
  The "fresh" primary arrived carrying a **complete leftover draft of the exercise** (`session
  reset` does not clear the draft graph); saved a copy and deleted all 7 nodes before briefing —
  contamination avoided.
- **Test:** a fresh sub-agent, briefed with problem + contracts only (parallel fetch of two
  profiles, input `person1`/`person2`, output `{profile:[…]}`, mock `GET /api/mdm/profile/{id}`),
  five allowed docs, no syntax hints, no tutorial material. Result: **18/18 commands `ok:true`,
  zero failures**; contract honored; parallelism proven from traversal timing (two ~4.5 ms
  fetches inside a ~6 ms run; join sinks then releases). Orchestrator re-ran the dry-run
  independently — same output (Peter + Mary, name/address/id). Exported as
  `parallel-profile-fetch` (`/tmp/graph/parallel-profile-fetch.json`).
- **Judgment vs canonical `tutorial-5.json`:** semantically equivalent; divergences all valid —
  one whole-profile Dictionary instead of two field-level ones; deterministic post-join indexed
  assembly (`profile[0]`/`[1]`) instead of canonical's order-undetermined per-fetcher append; no
  organizational Island. The companion independently derived the tutorial's key lesson (disjoint
  `model.*` scratch per parallel branch).
- **Key method finding:** the five AI docs alone were **not sufficient** — Provider URL `{name}`
  placeholders and the Dictionary bare `input[]` shape live only behind the **non-existent**
  `composing-the-layers.md` link; the companion bridged the gap in-band
  (`describe skill graph.api.fetcher` → `help data-dictionary`), which is why nothing failed.
- **Engine bug (orchestrator-found): `session subscribe` via `/sync` poisons the subscriber
  list** — registers the ephemeral `companion.sync.<uuid>` capture route as a durable subscriber
  (engine state showed `subscribed by ["companion.sync.3da2143d…"]`); the watcher's mirror died
  silently mid-test while its session still claimed `subscribed to ws-976371-7`. Java upstream
  (#189) mirrors the mechanism — suspected affected. New Open Thread; spawn-task chip offered.
- **Docs updated:** `docs/AI-companion-test.md` — Tutorial-5 stage record, ladder table L4 row,
  rollup findings #9–#15 (dangling doc links incl. the constant-set page, undocumented
  `help {topic}`, stale `/command` prose, `/sync` envelope null-omission vs docs (`error`/`result`
  omitted per increment-33 serializer, undocumented `id`), fan-out parallelism implied only,
  the subscribe bug, `session reset` draft-graph UX).

## Decisions

- Tutorial-5 defined as **L4 (composition)** on the validation ladder: problem-only brief,
  build-whole-then-dry-run, acceptance = behavioral contract + demonstrable parallelism.
- The subscribe-via-sync defect is **filed, not fixed** in this session (needs its own
  investigation incl. the not-yet-explained 6-minute mirror survival + delayed replay).

## Memory References

- ot-companion-validation-sweep — advanced (tutorial-5 done; next tutorial-6); substance updated
- bp-companion-sync — referenced (the `/sync` channel carried the whole test; bug found at its
  edge with `session subscribe`)
- vision-mercury — referenced (the sweep serves faithful delivery / docs-alone operability)
- port-bottom-up-faithful — referenced (judged the companion's design against the canonical Java
  fixture)
- Created: ot-subscribe-via-sync-bug (born tier: working)

Co-Authored-By: Claude Code <noreply@anthropic.com>
