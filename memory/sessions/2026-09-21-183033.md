# Session (2026-09-21T18:30:33.000Z)

**Agent:** Claude Code

## What happened

**P11 closed — the Rust entry point stops on `SIGTERM` as on Ctrl-C, and the Kafka flow adapter
drains its consumers on stop** (Eric: "please address the loose end that you reported. Fix the life
cycle management"). Branch `fix/sigterm-graceful-stop` (`5688e643`, pushed; PR pending — Eric opens).

1. **platform-core** — `shutdown_signal()` (crate-private): the `SIGTERM` listener is registered on the
   CALL (not first poll), then a `select` over Ctrl-C and `SIGTERM` resolves to the signal's name;
   `AutoStart::run` logs `Application running - stop with Ctrl-C or SIGTERM` and
   `<signal> received - stopping`, then runs the shutdown hooks as before. Non-Unix keeps Ctrl-C.
   Unit test `sigterm_ends_the_shutdown_wait` raises `SIGTERM` against the test process itself
   (`kill -TERM <pid>`) and asserts the wait ends within 5 s — safe because the listener is
   registered before the kill, so the default disposition never sees the signal.
2. **minimalist-kafka** — `KafkaFlowConsumer` gained a `stopped` flag set when its poll loop exits;
   `runtime::stop_flow_consumers() -> usize` signals every binding and waits, bounded by a 10 s
   grace, for each to report stopped (returns how many were still running — their records
   redeliver, at-least-once); the bootstrap hook calls it. The hook runs on the entry point's
   thread while the consumer tasks finish on the runtime's workers, so the blocking wait is
   sound. E2e scenario 15: the stop reports 0 still running.
3. **Docs** — the two guides and two READMEs that said "until Ctrl-C" now say Ctrl-C or `SIGTERM`;
   the interop report's Finding 2 records the closure; spec §7 item 11 likewise; **Increment 126**
   covers both lifecycle changes (this and #300's `Platform::keep_running`) — a platform-core
   behaviour every Rust application inherits, unlike the Kafka crate's K-series internals, so it
   earns an entry now rather than at K5.
4. **Live proof** on the Java `kafka-standalone` with the Rust `kafka-demo`: `kill -TERM` at
   18:22:34.154Z → `SIGTERM received - stopping` at .175 → both consumers stopping/stopped at
   .175 → `Kafka flow consumers stopped` at .200; the broker logged explicit `LeaveGroup` for both
   groups at .176 and rebalanced at once — the K4 hard-kill leg had waited 45 s.
5. **Gates:** fmt, clippy `--workspace`, platform-core (63), the five minimalist-kafka suites,
   kafka-demo (7), `check-doc-claims`, `check-llms-links`, `mkdocs build --strict`, and — the lesson
   from PR #300 applied — the full `cargo test --workspace` (103 binaries green) before pushing.

**Eric's question, answered with evidence (open for his ruling):** "KIP-848 is already supported in
the standalone Kafka server. Why does the Rust port not automatically switch to
`group.protocol=consumer`?" Findings: (a) the broker finalizes `group.version=1`; (b) the Java
template ships `group.protocol` COMMENTED OUT (`# group.protocol=${KAFKA_GROUP_PROTOCOL:auto}`), so
the Java demo also joined classic in K4 (classic generations in the broker log; the Java client's
"set group.protocol=consumer to try it out" hint) — automatic switching is opt-in on both engines;
(c) `group.protocol=consumer` WORKS on the Rust engine today — live: both bindings "join the
consumer group using the consumer protocol", server-side `uniform` assignor, a message consumed,
SIGTERM → "left the consumer group"; (d) librdkafka raises a FATAL `ConsumerGroupHeartbeat` error
(`_UNSUPPORTED_FEATURE`/`UNSUPPORTED_VERSION` → `RD_KAFKA_ERR_ACTION_FATAL`, `rdkafka_cgrp.c`)
when a broker lacks the protocol — so a real `auto` needs no probe: start each binding's consumer
as `consumer`, and on that fatal error rebuild it once as `classic`. Java-equivalent semantics,
resolved per binding at its first join, no synthetic group, no extra member. **Proposed:** replace
the accepted §7 item 7 delta with this optimistic `auto`; keep the conflict guard; template default
= Java parity (opt-in) unless Eric wants `auto` shipped on both engines (a Java change too). Awaiting
his two rulings.

## Merged

PR #301 MERGED 2026-09-21 (merge `922e13c7`); main verified moved before the branch was deleted. The stacked
`feat/kafka-group-protocol-auto` now shows only its own commit against main.

## Doc Gaps

(none)

## Memory References

- headless-app-keep-running (edited — the `SIGTERM` follow-up closed with this branch)
- ot-minimalist-kafka-port (updated — the `auto` re-ruling recorded as open; K5 list unchanged)
- eric-release-rhythm-rust (followed — branch and PR text prepared; PR open and merge are Eric's)
