# Session (2026-08-11T05:17:01.000Z)

**Agent:** Claude Code

## Summary

**Java lock-step fix, commit `8fc45b94` on `fix/dry-run-graph-scope` (Java twin `573c62aa`),
MERGED as PR #204 (merge `f5256ecc`, CI green): dry-run suspend/resume never resumed — the playground lane's
ephemeral graph id broke the v4.11.6 graph-scoped store key.**

Root cause (found on the Java engine via Eric's live tutorial-14 regression, identical here):
`commands.rs handle_instantiate` minted `playground-{uuid}` per instantiation, so the
`graph:{graph_id}:{cid}` store key never matched across instantiations — every dry-run resume
silently restarted fresh. The executor lane always passed the stable deployed id.

Fix + Eric's ruling (mirrored): dry-run identity = the root node's `name` property
(`stable_graph_identity`/`root_name`); **an unnamed root + suspend/resume model is REJECTED at
instantiation with a teaching message** (`uses_suspension` guard, placed guard-first so rejection
has no side effects); nameless non-suspending drafts keep the playground handle. Porting gotcha:
the guard first landed at the WRONG `model::remove_instance(in_route)` occurrence (session close)
— anchor scripted edits on unique context. rmpv::Value (not serde_json) for node properties.

Tests: `companion_dry_run_resumes_across_instantiations` (key-scope pin via `store_file`,
step counters for no-re-execution, consume-on-retrieve) +
`companion_unnamed_suspend_graph_rejected_at_instantiation`; the pre-run-check twin's scratch
graph gained a root name so it still reaches the check it pins.

Gates: workspace 58 suites / 305 tests exit 0, clippy 0, `cargo fmt --check` clean (fmt run
proactively before committing — the PR #200 lesson). CHANGELOG Unreleased Fixed (executor lane
explicitly never affected). The Java side also carried a live four-run tutorial-14 verification
against redis-standalone (record under `graph:tutorial-14:{cid}`, all resumes green).

Also: **v4.11.8 release PREPARED (lock-step with Java)** — `release/v4.11.8` commit
`5b659e50`: Cargo.toml 4.11.6→4.11.8 (v4.11.7 was Java-only; the CHANGELOG carries the
step note), Cargo.lock refresh, CHANGELOG cut, gate 58 suites / 305 tests exit 0 +
clippy 0 + `cargo fmt --check` clean. Java twin `7929b309`. **SHIPPED:** release PR #205 merge `d16d68f0` (tree verified),
CI green (2m34s); tag `v4.11.8` on the merge, dereference-verified; Java shipped in
lock-step (PR #279 squash `92dd64a8`). Remaining: Eric publishes both GitHub releases.

## Memory References

- referenced: thread-graph-scoped-state-and-error-context-rust (the store-key change this hides
  in), thread-dynamic-statement-targets-rust (same-day fix rhythm)
- created: thread-dry-run-graph-scope-fix-rust (tier: working), thread-release-4-11-8-rust (tier: working)
