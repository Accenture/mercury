# Session (2026-07-15T22:22:42.000Z)

**Agent:** Claude Code

## Summary

Maintainer answered the platform-core Design gate; wrote the full Design artifact. Still no
Rust code — the maintainer chose "design doc first," so implementation waits on their review of
the doc.

**Gate answers (2026-07-15):**
- Stack → **tokio + serde** (recommended). Recorded as `platform-core-stack`: tokio,
  async_trait, async-channel (per-route MPMC queue), serde + rmp-serde + rmpv + serde_json,
  uuid, thiserror; Cargo workspace, `crates/platform-core` first; explicit registration now,
  compile-time macro later.
- Wire format → **idiomatic serde** (not byte-compatible with Java) — cross-JVM interop is out
  of scope.
- Proceed → **write a design doc first** (before implementation).

**Deliverable:** `docs/design/platform-core-port.md` — a Design-altitude artifact realizing
`bp-platform-core`, serving `vision-mercury`. Covers: goal/scope, the confirmed decisions
(D1–D6) with rationale, the Java→Rust mapping table, concrete type designs (`EventEnvelope` +
body accessors + MsgPack round-trip; untyped `ComposableFunction` trait + typed adapter;
`AppError`; `Platform` registry with a per-route **MPMC (`async-channel`)** worker pool —
noting tokio mpsc is single-consumer so N workers sharing one queue need MPMC; `PostOffice`
`send` + `request`-RPC via a temporary reply inbox + `cid`; the worker-invocation bridge), crate
layout, the increment-1 test plan / acceptance criteria, an explicit **deferred** list
(broadcast, streams, lifecycle, macro registration, REST automation, event-over-HTTP, full
envelope fields), and §8 open questions (route naming verbatim, edition/MSRV, async-channel vs
tokio-only, scope tweaks).

**Next:** maintainer reviews the doc → then implement **increment 1** (smallest provable
foundation: EventEnvelope, trait, Platform, PostOffice send/request; `cargo build`/`test`/
`clippy` clean).

## Memory References

Created:
- `platform-core-stack` (Key Decision — confirmed foundation stack)

Updated:
- `ot-design-platform-core` — Design doc written (`docs/design/platform-core-port.md`), awaiting review
- Stack & Tools — confirmed Rust stack + crate list

Referenced: `bp-platform-core`, `vision-mercury`, `inv-never-couple-functions`
