# Session (2026-07-17T01:06:22.000Z)

**Agent:** Claude Code

## What happened

Maintainer directed layer 3 (with a specific hint: **reuse the Java React UI**, adjusting
the `npm run release` deploy path for the Rust folder layout). Following the design-gate
protocol: **survey + design doc, implementation gated.**

**Survey of the canonical layer-3 sources:**

- **`system/minigraph-playground-engine`** (~9.8K LOC / 51 files + a React webapp):
  the Active Knowledge Graph engine — a property graph whose nodes carry executable
  skills; traversal IS execution. Anatomy: `CompileGraph`/`CompiledGraphs`/
  `PlaygroundLoader` (graph JSON → MiniGraph), `GraphExecutor`/`GraphTraveler`/
  `GraphInstance` (state machine on MultiLevelMap; `{id}@{node}` correlation; decision
  routing next/node-name/.sink; loop detection), 8 built-in `graph.*` skills
  (data.mapper, math, js, api.fetcher, extension, task, join, island),
  `GraphCommandService` (1,494 lines — the Playground command grammar),
  `GraphUserInterface` (a `@WebSocketService` at /ws/graph — collaborative sessions),
  the AI-companion REST API, health/housekeeper/exception handler, a self-contained
  math expression engine (~700 lines lexer/parser/evaluator), 13 tutorial graph
  fixtures, help markdown powering `describe skill`, and — the layering payoff —
  **the engine exposes itself through an event-script flow** (`graph-executor.yml` →
  POST /api/graph/{graph-id}): layer 3 riding layer 2 riding layer 1.
- **`MiniGraph` (827 lines) lives in Java platform-core** — the property graph is a
  layer-1 built-in; K1 keeps that placement in Rust.
- **The React webapp** (React 19 + vite + @xyflow/react): `npm run release` = clean +
  build + deploy; `scripts/clean.js`/`deploy.js` target `../src/main/resources/public`
  → Rust adjustment is `../resources/public` (exactly the maintainer's point).
- **Two platform-core gaps layer 3 forces**: a WebSocket server (Java
  `@WebSocketService`; Rust plan: hyper upgrade + tokio-tungstenite) and the HTTP
  client (`async.http.request` — the long-deferred §7 item `graph.api.fetcher` needs;
  porting it also activates the event-script http-client fixtures deferred at E-9).
- **`graph.js` uses GraalVM JavaScript** — no free Rust analog; proposed deferral
  (boa_engine the candidate if wanted).

**Wrote `docs/design/knowledge-graph-port.md` (DRAFT v1, gated)** — decisions K1–K9
(MiniGraph in platform-core; `crates/knowledge-graph`; graph JSON + fixtures verbatim;
math engine faithful; graph.js deferred; the two platform-core extensions in lockstep;
webapp copied verbatim with only the deploy-path change; engine contributes its own
resource root via a before-application hook — the jar-resources analog; dev-gating
app.env=dev). Increments K-1…K-8: MiniGraph → math engine → compiler/registry →
runtime + core skills → HTTP client + api.fetcher → extension + REST → WebSocket +
Playground/companion → React webapp + milestone. Four open questions (crate name,
graph.js, tokio-tungstenite, examples app). INCREMENTS design-gate row added.

## Memory References

- referenced: `bp-active-knowledge-graph` (design drafted — gate pending),
  `bp-event-script` (closed; layer 3 rides it), `bp-platform-core` (two §7 gaps to
  close in lockstep), `vision-mercury`, `conventions-rust-baseline`
- created: `ot-design-knowledge-graph` (the layer-3 design/implementation thread)

## Archival (window-triggered)

`ot-greenfield-no-code` (a completed enable-day thread) crossed the 20-session window →
archived via `archive-fact`. Lint 0/0.
