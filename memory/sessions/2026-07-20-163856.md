# Session (2026-07-20T16:38:56.000Z)

**Agent:** Claude Code

## What happened

**Back-port gap survey (maintainer-requested, graduation prep): the Java engine is synced,
but the Java AI docs are frozen at the pre-tut-5 state** — a marker-based audit (one
distinctive grep marker per findings-driven change, checked in both repos) found **31 of 40
grammar-hardening markers absent from the Java AI docs**.

- **Engine: SYNCED.** Every behavior fix went upstream via PRs #187–#201 (verified
  spot-checks: `CompileGraph` enforces root purpose; dedup/`direct`; join barrier; numeric
  promotion; discovery + describe commands). No engine back-port needed.
- **KG AI docs (4 files + llms.txt): THE gap.** The AI docs were ported INTO the Rust repo
  2026-07-18 and all subsequent grammar hardening (findings #9–#63) landed on the Rust
  copies only — Java PRs carried code+tests. Missing in Java: Provider/Dictionary authoring,
  constants closed set + non-constant sources (`f:`/`$.`), `help {topic}`, fork/join
  state-safety, for_each (fetcher section; math BEGIN/END partial via #187), ordered
  aggregation guarantee, island-required, composite keys + `[]` append, delegation contract,
  skill-less End, Failure-routing section + RESET truths, discovery commands (`list
  graphs`/`describe graph {id}` — in Java's HELP pages but NOT its grammar docs),
  `sync_envelope` + #62/#63 contract notes, text() spaces, re-instantiate, bare `response`
  root, f:length types, `{node}.status` guarantee, redirect rule, f:removeKey/f:listOfMap/
  f:round/f:now examples, nested seeds. Java's llms.txt (55 lines) predates the Rust rewrite
  (72 lines, richer map + /sync guidance).
- **event-script syntax.md (Java): missing** the #196-follow-up doc relaxation (numeric
  promotion matrix, f:round), f:removeKey worked example, listOfMap order guarantee — the
  Java ENGINE has all of these (PR #196).
- **Java in-app help: partial.** `help list.md`/`help describe.md` document the new commands
  (went with #199/#200); `help graph-api-fetcher.md` lacks the failure-routing/retry pattern
  (0 RESET mentions vs Rust 5) and the HTTP-semantics block.
- **Intentional divergences — EXCLUDE from back-port:** graph.js (retired in Rust ONLY —
  Java docs must keep it), Rust-API code examples, profiles rename (Java keeps Spring
  names), Rust-specific port notes. The help-page style divergence stays (maintainer
  decision 2026-07-19); only behavior-rule gaps in Java help pages need patching.
- **Java human-doc bugs (parked during the docs-site build, never contributed):**
  flow-schema-reference.md documents `error.status` but both engines use `error.code`;
  (+ the content-length/streaming claim noted at the time).
- **No #202 doc follow-up needed** — Java docs never quoted the welcome companion line.
- Proposed plan (maintainer to approve): one focused back-port session editing ~6 Java doc
  files (command-reference, minigraph-commands.json, skills-reference, ai-agent-guide,
  llms.txt, event-script/syntax.md) + the two help-page behavior blocks, adapting
  Rust-port-specific claims back to Java truths; then validate with a fresh-agent drive
  against the JAVA Playground from the back-ported docs alone (the gold test).

## Memory References

- ot-companion-validation-sweep — referenced (the findings inventory drove the marker list)
- bp-graduate-to-accenture — referenced (survey is graduation prep)
- port-bottom-up-faithful — applied (map-don't-mirror governs the back-port direction too)
- ot-java-ai-docs-backport — created

Co-Authored-By: Claude Code <noreply@anthropic.com>
