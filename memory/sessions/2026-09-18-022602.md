# Session (2026-09-18T02:26:02.422Z)

**Agent:** Claude Code

Lightweight: Mode B upgrade 4.40.1 → 4.41.0 (MINOR — memory-lint check 16 `[undeclared-reference]`: a memory
fact whose body changes must be declared in a session log staged with the same change, else refresh-metadata
reads the edit as non-use; the pre-commit fragment runs `memory-lint --staged` (advisory, never blocks) and the
CI floor runs `--range base HEAD`; ritual text corrected — a closure is a use, only inspecting is not
(REVIEW.md steps 5/8/10, DECAY.md §2/§6, the protocol's Memory References rule); from the mercury-composable
team's 2026-09-17 proposal). Reconcile re-copied DECAY.md, REVIEW.md, .agent/schema.md, the pre-commit
secret-guard fragment, .githooks/README.md, the memory-lint built-in and the GitHub CI floor. Semantic step:
protocol — re-copied (target protocol was byte-identical to the 4.40.1 template). Rung step 3 (field shape) — absent (no [overdue] on a closed thread). Adapters re-synced; stamped 4.41.0; reconcile
converged. memory-lint: 0 errors, 0 warning(s). Upstream release:
https://github.com/Accenture/mercury-go/releases/tag/v4.41.0 .

## Memory References

(none)
