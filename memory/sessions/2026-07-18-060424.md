# Session (2026-07-18T06:04:24.000Z)

**Agent:** Claude Code

## What happened

Two things this session: (1) the AI-companion validation of **tutorial-3** passed end-to-end on
the Rust port, and (2) that exercise surfaced a real **documentation defect** in the `inspect`
command grammar, fixed in both repos (increment 32).

### Tutorial-3 AI-companion validation — PASSED (read-only; no tracked change)
A fresh general-purpose agent acting as the **AI companion**, briefed with **only** the canonical
AI-agent docs (`mercury-composable/docs/llms.txt` + `.../knowledge-graph/ai-agent-guide.md` +
`command-reference.md` + `minigraph-commands.json`) — never the tutorial walkthrough — built the
whole data-dictionary graph in the live session `ws-938776-2`, one command per step via
`POST /api/companion/{id}`, pausing for human screenshot validation at each node/connection.
- **7 nodes** (root, dictionary Island, person-name/person-address Dictionaries, mdm-profile
  Provider, fetcher Fetcher, end) + **7 connections** — an **exact structural match** to the
  canonical `tutorial-3.json` (verified by an independent nodes/edges diff).
- **Dry-run:** `instantiate graph` + `int(100) -> input.body.person_id`, `run`, inspect →
  `output.body = {name:"Peter", address:"100 World Blvd"}` (independently confirmed via
  `GET /api/inspect/ws-938776-2/output.body`). Console showed the full traversal
  (`root→fetcher→dictionary`, dictionary `graph.island` 0.017 ms, fetcher `graph.api.fetcher`
  8.489 ms, `Walk to end`, traversal 9 ms).
- This exercises the whole increment 30–31 arc end-to-end: declarative dev-gating (Playground
  services live under `app.env=dev`), the `mock.mdm.profile` dev mock over HTTP, the Dictionary
  bare-`input[]` fix, and the `{inspect, outcome}` wrapping.

### Increment 32 — `inspect` docs: `{…}` is a placeholder, not literal (both repos)
The companion sent `inspect {output.body}` (with braces) because the docs used `{…}` as a
placeholder in the *syntax* line but **repeated the braces in the examples**. Both engines then
resolve `{output.body}` as the composite key `{output`→`body}` → empty `outcome` — **correct,
identical behavior; the docs were the defect, not the code** (traced through Java
`GraphCommandService.handleInspectCommand` → `MultiLevelMap.getElement`, which splits on `.`
without stripping braces — same as Rust).
- **Fix (Java canonical first — `Accenture/mercury-composable` `c04036f8`, pushed to main):**
  examples unbraced in `command-reference.md`, `minigraph-commands.json` (+ placeholder `notes`),
  `help inspect.md` (+ `inpsect` typo fixed), `ai-agent-guide.md` (+ a pre-send-checklist
  placeholder item); braces kept only in syntax lines. The webapp autocomplete `inspect` template
  changed `inspect {variable_name}` → `inspect output.body`; bundle rebuilt (`npm run release`).
- **Rust mirror (`0252c05`):** `help inspect.md` + webapp `commandSuggestions.ts` + rebuilt
  `resources/public` bundle. Verified the rebuilt bundle: `inspect output.body` ×2 (templates),
  unbraced help examples, and the one remaining `inspect {variable_name}` is the help-doc **Syntax**
  line (a legitimate placeholder, kept). `resources/public`/`resources/help` are served from disk
  (not compile-time embedded), so no Rust rebuild needed.

**Follow-up flagged, not done:** none outstanding — the webapp template was fixed per the
maintainer's choice. Next: resume the AI-companion sweep at **tutorial-4** (fresh session id).

## Memory References

- referenced: `bp-active-knowledge-graph`, `vision-mercury`, `port-bottom-up-faithful`,
  `conventions-rust-baseline`
- created: (none)
