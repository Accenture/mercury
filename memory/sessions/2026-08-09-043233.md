# Session (2026-08-09T04:32:33.000Z)

**Agent:** Claude Code

## What happened

**graph.task `model.*` input staging + tutorial-13 as an HTTP client by configuration —
the lock-step mirror of the Java reference engine's PR #267 (squash `e16f4b40`) — plus
the default-Accept client ruling. MERGED 2026-08-08 as mercury PR #197, merge
`79212bc0` carrying `0530bd13` (incl. the Accept round), CI green — rides the next
release. Increment 83.**

Engine: `build_task_request` gains a `stage_model_variable` branch — an `input[]` RHS in
the `model.` namespace writes the state machine under `assert_mutable_model_target`
(null → remove, indexed → set Nil) instead of silently landing in the request body, so
later entries resolve it as a dynamic variable (`{model.person_id}`). `MODEL_NAMESPACE`
made pub in common.rs. Closes the same fetcher-family gap Java had: fetcher and extension
input mappings already staged model.*; graph.task was the outlier on both engines.

tutorial-13 remodeled byte-identical to Java (fixture + both help files copied verbatim):
`task=async.http.request` fetches a mock MDM profile — model staging, dynamic variable in
the url, `${rest.server.port:8080}` load-time substitution (the Rust `handle_instantiate`
has the identical ConfigReader round-trip: temp file + `load_raw_graph`; authored/export
keeps the placeholder), explicit `headers.accept`, explicit `headers.x-ttl` (5000 ms).
The `v1.hello.task` demo mock retired (mock.rs).

**Cross-engine finding → maintainer ruling (Eric): the async HTTP client now sends a
default `Accept: */*` when the caller gives none.** The tutorial initially FAILED here
with raw JSON bytes while Java passed — Java's reactor-netty sends an implicit
`Accept: */*` (negotiated to application/json by the REST edge), this port's client sent
NO default, and BOTH engines' REST servers omit the response content-type when Accept is
absent (`accept_fallback_type` → None; Java `updateContentType` accept==null → "?").
Implemented in http_client.rs (guard over the merged request+session header set; an
explicit accept is never overridden); wire-echo pinned both ways in rest_automation via
the /api/echo/chain harness (now takes an optional ?accept= for the inner hop). The
event-api transport leg already set `accept: */*` explicitly — unaffected. The tutorial
keeps the explicit `headers.accept` as taught best practice.

Tests: tutorial-13 deployed e2e (profile + X-TTL wire echo via observed_ttl + unknown
profile error); unit-test-task-6 (input mapping targeting model.ttl) rejected by the
gate → 404, in the manifest + BOTH compiler invalid-lists; staging + dotted body-path
pins in unit-test-task-1; playground dry-run twin through the sync companion (import →
instantiate → run) — the ephemeral-port harness points the `rest.server.port` override
at the ACTUAL bound port before instantiate, so the placeholder resolves through the
config chain exactly as a deployment injects the real value (the first attempt resolved
to ":0" — an honest resolution of the harness override, not an engine bug). Workspace
gate green (fmt/clippy 0/all suites). Webapp bundle regenerated (help baked at build
time).

**INCREMENTS.md ledger repair (Eric's catch):** increments 78 and 79 were never
written — reconstructed from session 2026-08-01-233448 ("Increments 76-80" = the six
v4.11.1 lock-step commits): 78 = minigraph node ttl + model-metadata immutability (R3),
79 = dry-run watcher + companion drain + fetcher x-ttl + docs mirror (R4). The section
tail re-ordered to strict ascending 76→83 (82 had been inserted before 81); the Overview
table extended with rows 76–83 plus a note that from increment 76 the gate is green
suites, not a cumulative test count.

## Memory References

- referenced: conventions-rust-baseline (fmt/clippy/INCREMENTS per increment),
  port-bottom-up-faithful (Java canonical; byte-identical fixtures/help),
  inv-telemetry-presentation-parity (the accept divergence surfaced, reported, and
  resolved by maintainer ruling rather than silently patched)
- created: (thread updated in the Java repo's continuity — the graph.task staging
  lock-step arc)
- Superseded: (none)
