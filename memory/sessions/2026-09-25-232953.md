# Session (2026-09-25T23:29:53.086Z)

**Agent:** Claude Code
**Title:** Release 4.12.19 prepared — Cargo sweep, CHANGELOG from the tag range, workspace gates (lock-step with Java)

## Summary

Lock-step release prep with mercury-composable 4.12.19 (the ports adopt the Java number). Branch `release/4.12.19` from main
`d2adb38c`. Tag range `v4.12.18..HEAD`: one content PR — #332 merge `d3d82a3f` (Increment 142: `graph.model.automation`
accepts a comma-separated list of manifests, the later manifest wins; the rapid-prototyping deploy recipe) — memory-only
commits excluded. **Sweep BUILD FILES ONLY**, re-derived: 13 Cargo.toml / 24 occurrences 4.12.18 → 4.12.19, then
`cargo update --workspace` → 24 Cargo.lock entries at 4.12.19 (0 left at 4.12.18); `cargo metadata` shows the 12 publishable
crates at 4.12.19; no prose carries the version. **CHANGELOG** "## Version 4.12.19, 9/25/2026" — the lock-step notes (Java's
single item is Increment 142; the override is a `-D` program argument here), Added (#332), Documentation (#332), the crates
publication line. **Gates:** `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets -- -D warnings`
clean; `cargo audit` no advisories; `check-doc-claims` 30 pinned; `check-llms-links` 37 resolve; `cargo test --workspace`
117 binaries, 606 passed, 0 failed — run after the Java readiness build, the suites share ports. Java twin: `release/4.12.19`
`a31a2ea9` — 43 build files / 98 occurrences, readiness 220 suites / 1520 tests / 0 failures. Not merged, not tagged, not
published — each step waits for Eric.

## Merge

Release PR #333 merged 2026-09-25T23:48:39Z as merge `ff6e269c` (gate: state MERGED, reachable from `origin/main`); local main
fast-forwarded, `release/4.12.19` deleted. This memory-only commit is the **tag target for `v4.12.19`** — one commit past the
merge, so the tag carries the merge record. Pushed after the merge commit's `rust` run completed (`cancel-in-progress`).
Next (Eric): tag `v4.12.19` on this commit, publish the GitHub release with the CHANGELOG entry as body, `cargo publish
--workspace` the twelve crates from the tag.

## Shipped

Eric tagged and published 2026-09-25: tag `v4.12.19` → `e659c683` (verified; the workspace version at the tag reads 4.12.19),
GitHub release published 23:59:10Z with the CHANGELOG entry as body (verified against the entry head), the twelve publishable
crates at 4.12.19 on crates.io (12/12 HTTP 200). The `rust` run on the tag commit completed success. Java: tag → `35000ef2`,
release 23:58:19Z. `latest_release` rewritten to v4.12.19 (4.12.18 folded into Prior).

## Incidental sweep

memory-lint flagged one `[overdue]` fact once this log entered the window (sslu 21 > `archive_window` 20):
`conv-cargo-declare-what-you-name`. Archived with `archive-fact` — the rule stands in the workspace manifests and the
archive keeps it retrievable. Not a memory reference of this session.

## Memory References

- conv-template-version-sweep-rust (consulted — the manifest sweep and the lock refresh)
- eric-release-rhythm-rust (consulted — PR-open, merge, tag and publish each wait for Eric)
- graph-manifest-list-later-wins-rust (consulted — the release content and its upgrade note)
