# Session (2026-08-24T17:06:02.000Z)

**Agent:** Claude Code

**P4 polyglot documentation — this engine's half, lock-step with the Java repo (full
initiative narrative: mercury-composable log 2026-08-24-170545).** Branch
`feature/polyglot-docs`, submitted as mercury PR #214 (Java twin: PR #294).

- **guides/polyglot-functions.md** — twin of the Java chapter in this repo's voice:
  peers-not-ports (this repository *is* a port and knows the cost), wrapper family
  table (accenture.github.io/mercury-python + /mercury-nodejs), declarative wiring,
  zero-code demo extended (wrapper demos register hello.declarative, default port 8085
  = hello-world's slot), graph.task v4.11.11 floor naming this engine's realization
  (`event_api::get_event_http_target`), contract + operations sections.
- **ADR-0014 (Proposed, dated 2026-08-22)** — peers-not-subprocesses; this ledger
  numbers independently of the Java one (its twin is ADR-0016 there); formalizes
  polyglot-event-over-http-design (design record lives in the Java repo's memory).
- **Interop report: "The polyglot wrapper round (2026-08-22)"** — honest-evidence:
  claims exactly the recorded drives; the flagship flow→python drive ran on the JAVA
  engine (no live Rust-engine→wrapper drive exists — not claimed; offered to Eric as an
  optional extra gate).
- **Eric's home-page directive:** docs/index.md rebuilt on the Java reference
  presentation (he screenshotted the "A layered ascent" table as the anchor); kept
  "Why a Rust port" + the port-divergence note; Project section links the whole family.
  **Site renamed "mercury" → "Composable for Rust"** (family naming: Composable for
  Java / Rust / Python / Node.js) — flagged to Eric as the one judgment call.
- llms.txt Layer 1 gains the polyglot line; CHANGELOG Unreleased (Added + Changed).

Gates: `mkdocs build --strict` exit 0; anchors verified in built HTML; home page served
locally and visually verified (table + chapter + wrapper links render). **CI round: the
first PR run failed at ai-contract-provider's fail-closed link validator** ("Broken
relative link in references/guides/event-over-http.md -> polyglot-functions.md") —
identically on the Java engine. Fix `d278d8ae` (Java `8c89b1ad`): one sorted line in
`resources/skill/files.list` (build.rs embeds exactly the listed docs files);
`cargo test -p ai-contract-provider` exit 0 incl. the failing endpoints_e2e, and all
the chapter's own relative-link targets verified already packaged. Lessons: new guide
pages linked from packaged references must join files.list on BOTH engines;
`gh pr checks --watch` can exit 0 before a late-registering check appears — re-list
after every watch.

**MERGED AND LIVE same day.** PR #214 merge `c5221258` carrying `d278d8ae` (tree
verified, branches deleted both ends; ADR-0014 rides the merge). **Follow-up round —
Eric's nav directive** ("add the Operate & integrate tab for consistency... easier for
the reader when you have read the Java repo", plus his catch that Polyglot sat under
Layer 1 while Java uses Operate & integrate): nav-only restructure `f26183b1` — new
"Operate & integrate" tab (Observability from Foundations; Event over HTTP + Polyglot
Functions from Layer 1) + an Orientation tab wrapping Getting Started; page URLs
unchanged; strict build exit 0; tab row + sidebar grouping verified in a local serve.
MERGED as PR #215 merge `4573bd21` carrying `f26183b1` (tree verified, branches
deleted). **Live site verified after the Pages build:** title "Composable for Rust";
tab row now IDENTICAL to the Java site — Home / Orientation / Foundations / Layer 1–3 /
Operate & integrate / Reference (extracted from the live HTML); chapter at
/guides/polyglot-functions/ → 200. Residual label difference left for Eric's call:
"Layer 2 — Event Script" here vs Java's "Layer 2 — Composable" (+ Event-Driven
capitalization). Eric confirmed the live tabs visually.

Commit `7e6797dc` — docs: polyglot functions chapter, ADR-0014, home page unified as Composable for Rust

```
 CHANGELOG.md                                 |  19 ++++
 docs/arch-decisions/ADR.md                   |  52 +++++++++
 docs/guides/event-over-http.md               |   7 ++
 docs/guides/polyglot-functions.md            | 156 +++++++++++++++++++++++++++
 docs/index.md                                |  82 +++++++-------
 docs/llms.txt                                |   5 +
 docs/test-reports/event-over-http-interop.md |  46 +++++++-
 mkdocs.yml                                   |   9 +-
 8 files changed, 330 insertions(+), 46 deletions(-)
```

## Memory References
- Referenced: thread-graph-task-event-over-http, thread-release-4-11-11-rust
