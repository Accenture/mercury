# Session (2026-08-22T18:18:39.000Z)

**Agent:** Claude Code

## Summary

Ran the **memory smoke test** at the maintainer's direction, as designed: a genuinely
fresh-context agent read ONLY the allowed memory files (protocol, instructions,
continuity, vision — this repo's project questions cite it — latest sessions, archive
index; no source, no README) and answered all 13 questions. **Score: 13/13**, matching
the 2026-08-14 run. The greenfield story held up under fresh eyes: the Vision, the
enabled-before-code rationale and its VBDI implication, the bottom-up port order and
scope fence, and the map-don't-mirror canonical-spec rule were all answered with
citations — including the reversals history (suspend=true retirement, the untitled-scope
guard reversal, the store-key break) recovered from continuity plus the archive INDEX.

Three staleness findings, all fixed (fix the memory, never soften the question):

1. **Status line said v4.11.4** while `thread-release-4-11-10` and the 2026-08-22 logs
   record v4.11.10 shipped and tagged 2026-08-21 → corrected.
2. **instructions.md "Conventions Observed" still said "None yet — no Rust code has been
   written"** — stale since increment 1; it now points at the live
   `conventions-rust-baseline` fact (map, don't duplicate) with the headline rules and the
   Eric-ratified ui-fixture carve-out inline.
3. **The `last_invariant_check` stamp said "all five confirmed" without naming its set**;
   a fresh reader counting the two facts under Architectural Invariants misreads that as
   three missing core facts. Verified against the 2026-07-26-014908 log: the five are the
   two invariants + port-bottom-up-faithful + conventions-rust-baseline + the Vision — no
   data loss, ever (git history shows no core fact removed). The stamp now names them.

Result log row appended (third row).

## Memory References

(none — staleness fixes to the status line, an instructions section, and a stamp
annotation; no facts created, reactivated, or superseded. The smoke agent's own reads are
its own; the ids it exercised are recorded in its scored output, not here, so decay clocks
stay honest.)
