# Session (2026-09-24T23:53:53.000Z)

**Agent:** Claude Code

## Summary

**v4.12.17 release prep, the lock-step twin of Java 4.12.17 (Eric's ask once mercury #327 and Java #460 were on their
mains).** Branch `release/4.12.17`, cut after the #327 memory close (`adc7dc21`) landed on main. Sweep: 13 `Cargo.toml`
manifests / 24 occurrences of 4.12.16 → 4.12.17 (re-derived from the grep, unchanged from 4.12.16) + `Cargo.lock` via
`cargo update --workspace` (24 entries at 4.12.17, 0 residual). CHANGELOG from `git log v4.12.16..HEAD` — and the tag range
is WIDER than the feature: besides Increment 139 (#327, the consumer-side Schema Registry identity, twin of Java #458/#460)
it carries two Rust-only increments merged after v4.12.16 and never released — 137 (#325, `serde_yaml` retired for
`yaml_serde`) and 138 (#326, `cargo audit` in the CI gates with the rustls / rustls-webpki / event-listener lock refresh
past RUSTSEC-2026-0285). The Java convention of deriving from the tag range, not from the session, is what surfaced them.
Lock-step notes state Java's twin-kafka item has no analogue (twin-kafka is not ported). The entry's upgrade action: none —
opt-in by presence, public API unchanged, lock refresh within declared ranges.

## Verification

All gates on the release branch, the heavy ones started only after the Java reactor readiness build had finished (shared
ports): `cargo fmt --all --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean;
`cargo test --workspace` **603 passed / 0 failed / 9 ignored** across 117 suites; `cargo audit` clean (320 crate
dependencies); `check-doc-claims` 28/28, `check-llms-links` 37/37, `mkdocs build --strict` clean. Release rhythm
unchanged: Eric opens the PR, merges (merge commit, no squash here), tags one memory commit past the merge, publishes the
GitHub release with the CHANGELOG entry as its body and the twelve crates from the tag; `latest_release` updates at the
seam close. Java side: `release/4.12.17` `fcf44c83` (43 build files / 98; readiness 1514 tests / 0 failures / 3 skipped).

## Merged

Eric opened PR #328 and merged it at 2026-09-25T00:12:35Z as merge `ad957930` (title clean). Its first `test` job failed on a
timing-sensitive test unrelated to the release content — `kafka_shutdown::close_publisher_delivers_the_lingering_records_then_forgets_the_handle`
reported the stranded record as delivered (`kafka_shutdown.rs:212`), on a branch that changes only manifests, the lock and
the CHANGELOG, while the same code had passed on main twice and locally 603 / 0 / 9; Eric re-ran it (this account lacks
the admin right `gh run rerun` needs) and it passed in 4m6s; `audit` 3m4s and memory ×2 green throughout. Gate verified
(state MERGED, `ad957930` an ancestor of `origin/main`), local branch deleted, workspace at 4.12.17 on main. Main's runs on
the merge commit (`rust`, `agent-memory`) succeeded before this push — the `rust` workflow's cancel-in-progress group is why
it waited. **This memory-only commit is the tag target** (one past the merge, as at 4.12.16). Pending for Eric: tag
`v4.12.17`, the GitHub release with the CHANGELOG entry as its body, `cargo publish --workspace` from the tag; the seam
close then updates `latest_release`. Follow-up recorded on the Java side's log: the bounce-recovery test double's
fire-and-forget `refuse()`/`bounce()` (main's flake at `adc7dc21`) and this shutdown test's unasserted precondition — a
test-only hardening PR after the release.

## Tagged and published

Eric tagged at the memory-only commit and published: `v4.12.17` → `af9d6f30` (workspace 4.12.17 at the tag), GitHub release
2026-09-25T00:20:16Z with a body identical to the CHANGELOG entry (4518 chars normalized, heading line added); `rust`, `docs` and
`agent-memory` all green on the tag commit; crates.io 12 of 12 at 4.12.17 between 00:24:48Z and 00:24:58Z (`cargo publish
--workspace`, first try — the API had confirmed 4.12.16 as the max version minutes earlier, so the lookup was sound). Java
`v4.12.17` → `8a13a02e`, release 00:19:27Z, seam closed there as `ee47d751`. The v4.12.17 cycle is CLOSED on both engines;
`latest_release` updated here.

## Memory References

- schema-registry-native-codec (consulted — the released content)
