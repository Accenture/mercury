# Session (2026-09-23T22:44:07.000Z)

**Agent:** Claude Code

## Summary

Lockstep twin of two field issues ruled by Eric in the Java repository on 2026-09-23 (mercury-composable #453,
#454; Java branch `fix/l3-null-source-mapping-parity`). **The null-source rule:** Layer 3 adopts Event Script's —
a null or unresolved mapping source CLEARS a `model.*` target (removed; set to null when the source key exists with
a null value or the target is indexed) and is IGNORED for any other target. `common::apply_null_source` replaces the
remove-on-null shape at every site the Java review found here: the mapping entry, `for_each`, the `model.*` half of
`fill_fetcher_api_parameters` (a non-model parameter from a null source stays "not supplied"), `stage_model_variable`
(graph.task parameters, now taking the source selector) and `set_fetcher_output_entry` (a null result now clears a
`model.*` target; FLAGGED in the Java PR for Eric's veto — Event Script's output mapping clears too). Until 4.12.15
this engine removed ANY target; PR #295 had documented that, so at 4.12.15 doc and code agreed on the behaviour Eric
changed. **graph.math names the variable** (the reporter's secondary suggestion, accepted by Eric, then refined
twice mid-turn): a `{selector}` is rendered as the text `null` before the math package sees the expression, so the
package can only report `Unknown identifier: null` — Eric noted that himself. `assert_variables_resolved` checks a
COMPUTE or IF expression before substitution and names every unresolved selector joined by "or";
`name_null_identifier` handles the evaluator's own `null` (a variable holding the text "null" passes the pre-check)
by re-rendering the selectors and naming the culprits, the whole list only when none can be told apart. RESET, DELAY,
jump targets and MAPPING keep the documented `null` rendering (tutorial 14's null-safe idiom lives inside a `text()`
constant).

**Verified:** `cargo fmt --all --check` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean,
`cargo test --workspace` 603 passed / 0 failed / 9 ignored across 117 runs (the run before the compiler-count fix
stopped at the manifest test: `unit-test-math-1` raised the compiled-graph count 53 → 54); `check-doc-claims` 28
claims pinned, `check-llms-links` 37 references, `mkdocs build --strict` clean. Fixtures stay byte-aligned with the
Java repo (`unit-test-lookup-1` gains the `output.body.kept` probe; `unit-test-math-1` is new, two selectors).
Docs: `command-reference.md` *Namespaces*, `minigraph-commands.json`, claim `null-source-removes-target` (text, quote,
`since 4.12.16+`), `help graph-data-mapper` (*Null source*), `help graph-math`, Increment 136. Code commit `f779e46a`
on branch `fix/l3-null-source-mapping-parity`; PR to follow ([[conventions-rust-baseline]]: parity change, same
fixtures, same messages).

## Later the same session

**Every dry-run abort carries its reason — twin of the Java change for mercury-composable #454 (Eric's ruling: the
traveler follows the executor's log record; "every abort should come with a reason").** Commit `cc18dbe8` on the same
branch (Increment 136 extended). `traveler.rs`: `emit_aborted(po, instance, status, reason)` is the single terminal on all
five failure paths — a node's thrown error (`handle_error_response` now takes the node name: `<message> (node <alias>)`),
a node's staged error map (`reason_of`: message + target), the run deadline (`timed out after N ms`, 408), a failure
before the walk starts (`e.message()`), `send_error` — and `commands.rs`'s pre-run gate emits `Graph traversal aborted:
Unable to run - <reason>` in one line. `rest.rs` drains on the prefix. **The web app matched the bare text by equality**
(`graphRunProtocol.ts`), so the classifier matches the prefix, its vitest gained the reasoned case (321 tests) and the
bundle was rebuilt and redeployed (`index-CIY9GY25.js` → `index-ae65WhKm.js`, `template/playground.html` re-pointed).
Tests (`playground.rs` ×3, `graph_runtime.rs`) and `help run.md` follow the new shape; `command-reference.md` *Run
deadline* updated; the Rust docs carry neither the graph.js deadline sentence nor the companion ADR paragraph, so those
two Java doc edits have no twin here. Gates: a first clippy run caught a needless borrow in my caller edit (`&node_name`
where `node_name: &str`) — fixed; fmt clean, clippy clean, `cargo test --workspace` 603 passed / 0 failed / 9 ignored,
check-doc-claims 28, check-llms-links 37, mkdocs strict clean. The Java side's live repro shows the shape both engines
now share: `Graph traversal aborted: Cannot convert null to a number (node use-it)`. Lesson (both repos): a console line
the web app classifies by equality is a wire contract shared by two engines and one UI — grep the webapp and both
companions before changing a terminal's text, and rebuild both bundles.

## Merged

Eric opened PR #323 and merged it at 23:22:08Z as merge commit `da133b88` (this repo merges, it does not squash); gate verified
(state MERGED, `da133b88` an ancestor of `origin/main`), GitHub deleted the remote branch, the local branch deleted after
the gate. Main CI: build and memory green on the PR; the merge's own runs are the release round's concern.

## Memory Review (2026-09-23, through 2026-09-23-224407)
- Reactivated:   0
- Superseded:    0
- Archived:      0  facts (nothing past `archive_window` — memory-lint reported no `[overdue]`, only `[review-overdue]`)
- Swept threads: 0  (the closed threads are all inside the 20-session window)
- Archive-verify: pass (memory-lint 0 errors; no id in both places)
- Tier changes:  1  (static-decision-table-is-graph-data-rust archive-candidate → active, uses 1 → 2; `refresh-metadata`)
- Lines: continuity within `continuity_max_lines` 600 (see the stamp); facts within `continuity_max_facts` 30
- Invariants: not due (cadence review, not an invariant re-verification)
- Stalled threads: none flagged

## Memory References

- static-decision-table-is-graph-data-rust (edited: its null-source statement is superseded by the shared rule)
- conventions-rust-baseline (consulted: docs, fixtures and messages stay byte-aligned with the reference)
