# Session (2026-09-09T16:57:09.000Z)

**Agent:** Claude Code

## What happened

**v4.12.5 released** (execution record; prep detail in `2026-09-09-163915.md`). Eric
merged release PR #246 (merge `358cc054`, branch commits intact) and published the
GitHub release `v4.12.5` at 2026-09-09T16:56:14Z, one minute after the Java engine's —
same-day lock-step held again. PR CI fully green (test 2m35s + build + memory gates).

Gate anomaly resolved during the round: the LOCAL workspace suite had
`https_strict_mode_rejects_a_self_signed_certificate` time out (408 after 10s) while the
Java 29-module reactor build ran concurrently on the same laptop; rerun idle it passed
4/4 in 7.9s and CI had already proven the branch. Machine contention — worth remembering
when running both engines' full gates in parallel locally.

Continuity `status` updated to v4.12.5, initially flagging the crates.io publish as
PENDING (the registry showed 4.12.4 right after the tags). Eric ran the
dependency-ordered publish shortly after; **all seven mercury-* crates API-verified at
4.12.5** and the flag is cleared — the release is fully out on both engines and the
registry.

## Memory References

- inv-telemetry-presentation-parity (consulted — same-day lock-step release)
