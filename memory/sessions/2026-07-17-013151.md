# Session (2026-07-17T01:31:51.000Z)

**Agent:** Claude Code

## What happened

**Layer-3 gate APPROVED** (maintainer): defaults accepted for crate name
(`knowledge-graph`), tokio-tungstenite, and the K-8 examples app — with one upgrade:
**`graph.js` is RETIRED, not deferred** ("running an interpreter that allows arbitrary
code is a security risk. It is there in Java because we have no other viable solution").
Design doc amended (header Approved 2026-07-17; K5 rewritten as a security retirement —
the skill name is NOT registered; a graph carrying `skill: graph.js` fails with an
explicit message pointing to `graph.math`/`graph.task`).

**Increment K-1 shipped (repo increment 21) — MiniGraph in platform-core:**

- `platform_core::graph` — faithful port of the Java `MiniGraph` (827 lines) + models
  (`SimpleNode`, `SimpleConnection`, `SimpleRelationship`, `GraphProperties`):
  reserved aliases, the `0-9 A-Z a-z _ -` charset with Java-exact error messages,
  case-insensitive lookups, idempotent `connect`, successor/predecessor adjacency,
  BFS level discovery, deterministic sorted export/import (the tutorial-graph JSON
  shape), `same_as`, `reset`, 750-node default cap.
- Rust translation: Java's shared mutable objects → `Arc`-shared with interior
  mutability; property values are `rmpv::Value` (the envelope currency); errors are
  `AppError` status 400. One deliberate divergence documented: `remove_node` uses the
  lowercased alias key (Java has a latent case-sensitivity slip). Two Java behaviors
  kept faithfully: max-nodes checks `count > max` before increment (max+1 fit), and a
  failed import resets the graph to empty.
- Parity suite `crates/platform-core/tests/graph.rs`: all 8 Java `GraphTest` methods
  + a max-nodes/import-failure test = 10 tests, green first run.
- Verification: workspace 158 tests green, clippy 0, fmt clean. INCREMENTS.md row 21 +
  section; design doc K-1 shipped note.

Next: **K-2 — the math expression engine** (lexer/parser/evaluator;
`ExpressionEngineFullTest` as the parity suite).

## Memory References

- referenced: `ot-design-knowledge-graph` (gate approved + K-1 shipped — thread
  updated), `bp-active-knowledge-graph`, `bp-platform-core` (graph module joins
  layer 1), `vision-mercury`, `conventions-rust-baseline`
- created: (none)
