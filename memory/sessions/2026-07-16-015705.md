# Session (2026-07-16T01:57:05.000Z)

**Agent:** Claude Code

## Summary

**Implemented increment 8 — the static-content protocol**: etag/HTTP-304, no-cache pages,
and the request filter (`bp-platform-core`, Design §5g). Maintainer-directed, referencing
the Java platform-core `test/resources/rest.yaml` `static-content` block; answered the
open question — no, etag had been explicitly deferred in increment 7; now it's in.

**Harvested first**: `EtagFile.java` (quoted etag; comma-aware `sameTag`),
`SimpleHttpFilter.java`, `HttpRouter` `handleFilter`/`sendStaticFile`/`getStaticFile`
(SHA-256 content hash; filter headers **always** copied onto the response; 200 = serve,
else pass the filter's response through — the SSO-redirect hook; no-cache =
Cache-Control/Pragma/Expires; extensionless filename → `.html`; `FILTER_TIMEOUT` 10 s),
`RoutingEntry` static-content parsing + pattern validation (`invalidFilterParameters`:
exact / `prefix*` / `*suffix`).

**Built:**
- `routing.rs` — `StaticContent` (no-cache pages, **default `["/", "/index.html"]`** per
  the maintainer) + `SimpleHttpFilter` + `matched_element` pattern matcher +
  `valid_patterns` validation + `static-content` block parsing (fail-on-invalid).
- `server.rs` — `serve_static` pipeline: Java path resolution (trailing `/` → index.html,
  extensionless → `.html`, traversal guard) → optional filter (AsyncHttpRequest-shaped
  event, 10 s; headers copied always; non-200 pass-through with the filter's body/status;
  unregistered service → warn + serve; divergence doc'd: filter *call failure* logs and
  serves — Java leaves the request to time out) → no-cache headers **or** the etag
  protocol (quoted SHA-256 — new dep `sha2` 0.10; comma-aware If-None-Match → 304 +
  content-length 0). Refactor: `envelope_payload`/`status_of` shared with normal dispatch.
- **Example**: `static-content` block in rest.yaml (the maintainer's reference config) +
  `http.request.filter` interceptor logging url/ip/user-agent (SSO would live here),
  `assets/app.css` for the etag demo.

**Verified live** (curl): `/` → `cache-control: no-cache, no-store` + `pragma` + epoch
`expires` + `x-filter: inspected` (the filter's header riding the response, no etag);
`/assets/app.css` → quoted SHA-256 ETag then **304 Not Modified** on If-None-Match
revalidation (css excluded from the filter per config); the interceptor's log line
showing url/ip/user-agent.

**Ops lesson recorded:** `kill $PID` on a backgrounded `cargo run` kills the wrapper and
orphans the compiled example binary — a stale pre-etag server on 8085 answered the first
demo pass and masked the new build. Demo scripts now kill the binary by name (`pkill -f
hello_world`).

**Verification:** **101 tests** (+7 static-content end-to-end: etag cycle incl. comma
list + stale tag, no-cache headers + If-None-Match ignored, filter inspect + header
stamp, filter 302 pass-through, `*.css` exclusion bypass, unregistered-service fallback,
extensionless `.html`) · clippy + fmt clean · live curl demo. Docs: design §5g, §7 trim,
`docs/INCREMENTS.md` row + section 8.

## Memory References

Updated:
- `ot-design-platform-core` — increment 8 done
- Project State status (increments 1–8, 101 tests; INCREMENTS.md ledger noted)

Referenced: `bp-platform-core`, `vision-mercury`, `platform-core-stack` (sha2 added),
`config-first-spring-out`, `conventions-rust-baseline` (divergences doc'd),
`inv-never-couple-functions` (the filter is reached only by route name)
