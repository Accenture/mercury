# Session (2026-07-16T17:30:11.000Z)

**Agent:** Claude Code

## What happened

**Increment 10 — annotation macros + `AutoStart` one-liner + `examples/` convention**
(maintainer-directed: two enhancements before event-script). Design doc §5i; ledger row +
section 10 in `docs/INCREMENTS.md`. Closes the D6 deferral.

1. **`crates/platform-macros`** (new proc-macro crate; syn 2 / quote / proc-macro2):
   `#[preload(route, instances, env_instances, typed, zero_tracing)]`,
   `#[before_application(sequence)]`, `#[main_application(sequence)]` (default 10), and a
   stacked `#[zero_tracing]` marker — the Java `@PreLoad` / `@BeforeApplication` /
   `@MainApplication` / `@ZeroTracing` analogs. Each expands to an
   `inventory::submit!` of a `platform_core::registry::*Entry` (`&'static` data + a
   `fn() -> Arc<...>` factory). **Link-time registration** (`inventory` 0.3) is the D6
   answer to Java's classpath scanning — it collects across every linked crate, so
   layer-2/3 library functions will register like app-local ones. platform-core
   re-exports the macros + `inventory`, so apps depend only on platform-core.
2. **`AutoStart`** (Java `AutoStart.main(args)` parity) in `app_starter.rs`:
   `-D` overrides → `logging::init` → collect the three inventories (`env_instances`
   resolved through the config layer, override-aware, literal `instances` as fallback) →
   `AppStarter` lifecycle → park on Ctrl-C while `rest.automation=true` →
   `shutdown_cleanup()` — which also ships the previously deferred **OS-signal shutdown
   wiring**. `auto_start_main!()` (macro_rules in lib.rs) generates the whole `fn main()`
   including prepending the *invoking crate's* `resources/` (compile-time
   `CARGO_MANIFEST_DIR`). Plumbing: `Platform::register_with_options(..., zero_traced)`,
   `AppStarter::preload_zero_traced`, worker_loop takes the per-function zero-trace flag
   (OR'd with the route-name `zero.tracing.filter` config).
3. **`examples/hello-world/`** — hello_world moved from a cargo example
   (`crates/platform-core/examples/`) to a standalone workspace app crate: annotated
   functions + the one-line `platform_core::auto_start_main!();`, `resources/` beside it
   (git renames preserved). Deps shrank to platform-core + serde/serde_json +
   async-trait + log — **no tokio, no lifecycle plumbing**. This is the convention for
   event-script / knowledge-graph example apps: one `examples/<name>/` crate each.
   Workspace members now explicit: platform-core, platform-macros, benchmark-reporter,
   hello-world.
4. **Tests** — `tests/annotations.rs`: ONE end-to-end lifecycle test *on purpose* (the
   global platform's workers live on the first test's runtime — the same
   per-`#[tokio::test]`-runtime gotcha as increment 6's server-per-test): hook ordering
   (3 → 5 → main), all `#[preload]` routes registered, `env_instances` (override 7) beats
   the literal (2), typed RPC round-trip inside a trace bracket, stacked
   `#[zero_tracing]` suppressing the bracket. **105 tests** (104 + 1), whole-workspace
   clippy 0 warnings, fmt clean — also swept five pre-existing warnings that surfaced on
   the workspace-wide clippy run (needless borrow in automation/server.rs, `Ok(...?)` +
   unused import + type-complexity aliases in tests, a write-only Java-parity field in
   benchmark workload.rs annotated rather than removed to keep the saved-record format).
5. **Live verification**: `cargo run -p hello-world` — annotated preloads register
   (greeting.demo ×10 via env_instances from application.yml), REST
   `/api/greeting/eric` with trace + cid, etag → real 304 revalidation, no-cache +
   `x-filter: inspected` on `/index.html`, `/info`, `/health` all unchanged. Server
   stopped by binary name (`pkill -f target/debug/hello-world` — the increment-8 lesson).

## Decisions

- **Registration mechanism = `inventory` (link-time), not build.rs scanning or a
  registry file** — matches the maintainer's research direction ("Rust macro feature to
  emulate Java's annotation"), stays no-code for the app author, and works across crates.
- **`typed` flag on `#[preload]`** wraps via `TypedAdapter::arc`; unit structs construct
  directly, others via `Default`.
- **`#[zero_tracing]` is a stacked marker** consumed by `#[preload]` (Java's two
  annotations stay two annotations), with a `zero_tracing` flag as the inline
  alternative.
- **Docs updated in lockstep**: design doc D6 row + mapping table marked shipped, §5c
  example paragraph redirected, new §5i, §7 deferred list trimmed (macro + OS-signal
  wiring out; shipped list extended); INCREMENTS.md header range §4–§5i, overview row 10,
  section 10, backlog refreshed "as of increment 10".

## Memory References

- referenced: `ot-design-platform-core` (increments thread — increment 10 appended),
  `conventions-rust-baseline` (examples-crate convention added), `vision-mercury`,
  `bp-event-script` (next layer, unchanged), `inv-never-couple-functions`
- created: (none — increment recorded on the existing design/impl thread)
