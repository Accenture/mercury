# Session (2026-07-19T18:16:41.000Z)

**Agent:** Claude Code

## What happened

**Help-pages rewrite — [[ot-help-pages-rewrite]] backlog CLOSED (maintainer-directed):** all 39
interactive help pages (`crates/knowledge-graph/resources/help/`: 17 command + 9 skill + 13
tutorial pages) rewritten for human operators against the engine-verified AI grammar, by four
parallel agents on disjoint file sets, then reviewed file-by-file.

- **Global fixes:** every Java port 8085 → 8100; every `accenture.github.io` link removed (replaced
  with in-console `help {topic}` pointers or in-repo doc paths); every ```java block → real Rust
  (tut-13 now shows the shipped `HelloTask` from `mock.rs`; tut-12 a faithful mock extract);
  `main/resources` → `resources/`; Java logger prefixes stripped from sample outputs (tut-2's
  telemetry sample rewritten in the real Rust logger format, verified against `logging.rs`).
- **Bug cluster fixes:** tut-12 title ("Tutorial 10"→12), deploy filename (tutorial-10.json→12),
  "error status ≥ 200" → **≥ 400**; tut-9's false "module does not need to be connected" and its
  island copy-paste error; tut-11's bogus import transcript (showed export output); tut-1's wrong
  export filename; tut-6's inconsistent island relation label; tut-7's 'update mock data' typo;
  `help run.md`'s **false run-once-per-instance claim** (engine clears the visited set per run —
  source-verified); old `Math.sin(...)` example (contradicts the no-function-call dialect).
- **Alignment with the sweep's engine-verified truths:** graph-js → retirement notice quoting the
  exact engine message; fetcher contract (dictionary[]-only required, exception= routing,
  success-only per-instance dedup, ordered for_each aggregation); graph.math statement semantics
  (IF-jump ends list, EXECUTE caller-context, RESET list/self-reset/order, NEXT/DELAY timing,
  loop guard); island = required convention; data-dictionary bare input[]/`:`=default/POST form;
  thread-safe mapping + append ordering; deprecated `:type` never shown.
- **Review + verification:** forbidden-pattern sweeps clean across all 39; no empty files; spot
  reads of the critical pages; playground help-serving test green. Net **−67 lines**
  (1763+/1830−) — tighter and truthful.
- **Upstream posture (maintainer direction):** divergence from the Java repo's help pages is now
  **intentional** for code examples and port specifics; behavior semantics stay aligned with the
  shared grammar.

## Decisions

- Help pages are now Rust-port-authoritative (like the AI docs), superseding the original
  "verbatim port" constraint — per explicit maintainer direction this session.

## Memory References

- ot-help-pages-rewrite — COMPLETED this session (marked done; substance updated)
- ot-companion-validation-sweep — referenced (the sweep's 48 findings were the fix inventory)
- port-bottom-up-faithful — referenced (claims source-verified; intentional divergence recorded)
- conventions-rust-baseline — referenced (Rust app layout in deploy sections)

Also: webapp bundle refreshed (`npm run release` in `crates/knowledge-graph/webapp` → clean,
vite build, deploy to `resources/public`) — the UI bundles help content at build time
(`helpContent.ts` + local help resolver), so the rewritten pages needed a rebuild. All 124
webapp tests pass; the new bundle verified to carry the rewritten content (graph-js retirement
text present).

Also (maintainer follow-up): **RESET placement rule refined + validated live.** Maintainer asked
to move RESET to the first entry (it clears the seen flag + node state up front). Probe found the
critical exception: an `IF` on a just-wiped variable (`{fetcher.status}` after `RESET: fetcher`)
**aborts the run** (`null == 200` fails evaluation) — so the rule is *RESET first among the action
statements, after any check that reads state it would wipe*. Applied to tutorial-12 (both nodes),
the failure-routing pattern in `command-reference.md`, the JSON catalog, and the graph-math /
graph-api-fetcher help pages; the reordered tut-12 handler validated live (3 retries → recovery →
success); webapp bundle re-released (124 tests green).

Follow-up parity check (maintainer asked whether the engine hoists RESET first): **neither engine
hoists** — Java `GraphMath.executeStatements` and the Rust port both execute statements strictly
in declaration order (faithful port, verified side-by-side). Recommendation recorded: do NOT add
hoisting — it would silently break the legitimate check-then-reset pattern (canonical tut-12's
defensive status IF must read pre-reset state); the documented placement rule is the safer design.

Co-Authored-By: Claude Code <noreply@anthropic.com>
