# Session (2026-07-21T23:32:34.000Z)

**Agent:** Claude Code

## What happened

**Event over HTTP phase 2 begins** — reviewed the Java session's handoff
(`/tmp/event-envelope-rust-handoff.md`, Java PR #212) and shipped **increment 59 =
phase-2 increment 1 (wire-format conformance)**.

**Handoff review verdict: sound.** Verified against the code: the Rust envelope's
`to_vec_named` output already IS the "standard" format (field names match 1:1 — Java met
the port at its idiomatic format, resolving D4's "revisit if interop required" clause).
Findings: (1) the 403 gate assumes a private-function concept the Rust platform lacks —
security gate REQUIRED before /api/event ships (without it, remote POSTs could reach
reserved engine routes/inboxes); (2) the golden vectors never exercise `span_id` though
cross-language trace parenting rides on it — flagged back to the Java session; (3)
`round_trip` is vector-required, not optional as stated.

**Increment 59 shipped:** `body` decodes absent-as-Nil (Java omits unset bodies —
previously such an envelope FAILED to deserialize: the handoff's callout, confirmed
real); `round_trip: Option<f32>` added (builder + getter); unset optional fields now
OMITTED on the wire (spec encoder guidance; decoders treat absent==nil so old wire stays
compatible). Java's five standard golden vectors copied VERBATIM
(`tests/resources/envelope-vectors/vectors.json`) — all decode + round-trip on the FIRST
run (unicode, beyond-f64 long surviving exactly, f32 timings); a second test locks the
encoder contract (fresh envelope = id + headers only) and the absent-body rule. Compact
format deliberately not decoded (v1 standard-only per handoff). base64 added as dev-dep.
Workspace 233 / clippy 0 / fmt. D4 row annotated in the design doc; INCREMENTS.md §59.

**Maintainer directive recorded for increment 2 (private functions): BOTH Java paths —
a `private` attribute on the `#[preload]` macro (declarative, Java @PreLoad parity) AND a
programmatic `register_private` API; engine internals then registered private.**
Increment 3 = /api/event service + client + live cross-language interop pairing.

Also: shipped as PR [#163](https://github.com/Accenture/mercury/pull/163), MERGED
2026-07-21 (true merge, CI green).

## Memory References

- created: ot-event-over-http (born tier: working)
- referenced: port-bottom-up-faithful, conventions-rust-baseline

Co-Authored-By: Claude Code <noreply@anthropic.com>
