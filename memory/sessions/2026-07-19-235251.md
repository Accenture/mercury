# Session (2026-07-19T23:52:51.000Z)

**Agent:** Claude Code

## What happened

**Chained-join observation fixed in both ports (increment 41, maintainer-directed)** — the
follow-on from increment 40: a join's own skill runs (and lands in `skill_run`) on every
arriving branch, **including evaluations that sink**, so a downstream join in a chained-join
topology counted a sunk upstream join as complete and fired prematurely, dropping the slow
branch's data.

- **Fix (both ports):** `node_completed`/`nodeCompleted` judges a **join predecessor** by the
  outcome the join records in `node_seen` (`true` = fired), not the run mark; regular skill
  nodes keep increment 40's success-only `skill_run` check; skill-less nodes keep `node_seen`
  presence.
- **Empirical proof:** probe `rust-join-chain.json` / `unit-test-join-chain.json` (three-way
  fork, `j-one` chained into `j-two`, branches paced 200/100 ms) — **red on old code in BOTH
  engines** (`expected: X, got: null`), green on new. The first probe draft passed on old code
  and taught a documentation-worthy subtlety: `DELAY:` pauses *inside* the math skill but its
  `MAPPING` writes state *before* the pause — pacing a genuinely incomplete branch needs the
  delay and the write on separate nodes (fixed with `slow-pre` → `slow-x`).
- **Gates:** Rust workspace 202 / clippy 0 / fmt clean (compile-gate 29 graphs); Java module
  suite **69 tests** green. Java branch **`fix/chained-join-outcome`** (`79a2669e`) pushed —
  **stacked on `fix/join-barrier-retry-interplay` (#197)** because both add tests to
  `GraphTests.java`; per the [[github-squash-merge-closes-stacked-pr]] lesson, the PR should
  be opened only after #197 merges (I rebase the branch onto main first, on request).
- **Docs:** "multi-stage joins compose safely" clause in `skills-reference.md`,
  `help graph-join.md`, and the catalog's join entry; webapp bundle re-released (124 tests).
- Context: PR [#197](https://github.com/Accenture/mercury-composable/pull/197) (increment 40)
  was created by the maintainer and awaits CI; Rust main already carries increments 40–41 work
  (40 committed by the maintainer earlier).

Also: **#197 MERGED upstream; `fix/chained-join-outcome` REBASED onto post-#197 main**
(`5cef43a4` = main + one commit, clean rebase, 69-test suite re-verified green,
force-pushed with lease; local Java main fast-forwarded). The branch is now safe to PR —
no stacked-base auto-close risk remains. **PR
[#198](https://github.com/Accenture/mercury-composable/pull/198) created by the maintainer**;
Rust main committed+pushed (increment 41).

## Decisions

- (semantics, completing the increment-40 arc) The join barrier's completion test is now fully
  "**valid completions only**": success-only for skill nodes, outcome-based for chained joins,
  presence-based for skill-less nodes — and `RESET` erases all of it until re-execution.

## Memory References

- join-barrier-valid-completions — substance updated (open observation → FIXED, increment 41)
- github-squash-merge-closes-stacked-pr — applied (stacked branch handling for the Java PR)
- port-bottom-up-faithful — applied (identical fix + red/green proof in both engines)
- conventions-rust-baseline — applied (gates, fixtures, ledger row)

Co-Authored-By: Claude Code <noreply@anthropic.com>
