# Session (2026-07-26T01:38:51.000Z)

**Agent:** Claude Code

## Summary

P2/D5 of the annotation→macro consistency arc: the registration-metadata
contract's Rust half, on `feature/registration-metadata-contract` on top of
D4 (commits NOT pushed — Eric gates). Java reference artifacts:
mercury-composable `73a0d1be`.

## What was built

- **Golden vectors copied VERBATIM** from the Java repo into each crate's
  `tests/resources/registration-vectors/` (core.json → platform-core,
  plugin.json → event-script, feature.json → knowledge-graph); all three
  diff-verified BYTE-IDENTICAL after copying — byte-identity is the contract.
- **Fixtures through the Rust carriers** (the same set as Java's seven):
  core kind — `VectorAliasFunction` (comma aliases + env_instances),
  `VectorMarkedFunction` with `#[event_interceptor]` deliberately ABOVE
  `#[preload]` and `#[zero_tracing]` + `#[optional_service]` below (the
  conformance fixture itself exercises P1's order-freedom),
  `VectorGatedOutFunction` (negated condition); plugin kind —
  `#[simple_plugin("vectorEcho")]` on a deliberately unrelated fn name +
  bare `#[simple_plugin] fn vector_derived` deriving "vectorDerived" (the
  cross-language name-rule proof: Java's `class VectorDerived` registers the
  same name); feature kind — `vector-feature` (gated in) +
  `vector-feature-off` (gated out).
- **Three conformance suites** (`registration_vectors` test binary per
  crate, test names mirroring Java's): core asserts declared metadata read
  STRAIGHT FROM THE INVENTORY (cleaner than Java's reflection — routes,
  declaredInstances, envInstances, isPrivate, optionalService, both marker
  flags) AND resolved registration (has_route, resolvedInstances=25 for both
  aliases via assumedConfig, is_private) plus every gatedOut absent; plugin
  asserts both golden names present + both naming-rule halves execute;
  feature asserts gated-in present with the declared condition string
  matching the vectors, gated-out absent but still declared. The
  assumedConfig comes from the vectors file itself (overrides before boot).
- **Adapted contract page** `docs/guides/registration-metadata-contract.md`
  (the macros-reference adaptation pattern): same contract content, Rust
  carrier examples canonical, capability N/As restated as the port's own
  "Rust port" note, mkdocs nav under Reference, macros-reference ↔ contract
  page cross-linked both ways, footer noting adaptation from the Java
  reference page.
- **ADR-0008** in `docs/arch-decisions/ADR.md` — the substance of Java's
  ADR-0009 in this ledger's voice, with a cross-ledger note (numbering
  differs: Java 0008 = companion-sync, no Rust counterpart; the decision is
  one). `Formalizes: registration-metadata-contract` ↔ the new Key Decision
  fact in continuity carrying the (ADR-0008) tag, per the schema's sync rule.
- **CHANGELOG** Unreleased Added #2 (contract + vectors + conformance,
  alongside D4's existing entry).
- One P1 nicety surfaced by clippy: a below-order marker needs NO macro
  import (preload consumes it before resolution) — only the above-order
  `#[event_interceptor]` import is used; noted in the test's imports comment.

## Gate

Workspace 269 passed / 0 failed (266 + 3 conformance suites), clippy 0,
fmt clean.

## Memory References

- referenced: `thread-annotation-macro-consistency` (P2/D5 recorded),
  `port-bottom-up-faithful`, `conventions-rust-baseline`
- created: `registration-metadata-contract` (Key Decision, born tier:
  working, formalized by ADR-0008)
- left untouched by design: `thread-reverify-invariants-2026q3` (pending Eric)
