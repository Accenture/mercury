# Session (2026-07-20T17:22:05.000Z)

**Agent:** Claude Code

## What happened

**AI-doc back-port to the Java repo EXECUTED and VALIDATED** (the [[ot-java-ai-docs-backport]]
plan, maintainer-approved) — branch **`docs/ai-grammar-backport`** pushed; maintainer
opened **PR [#203](https://github.com/Accenture/mercury-composable/pull/203)** (CI pending
at log time) and reviewed the fresh-agent report.

- **Back-ported surfaces:** the 4 KG AI docs copied from the hardened Rust set with
  **graph.js restored as a live skill** (statement-grammar heading back to
  `graph.math`/`graph.js`, matrix row, full #js section with the GraalVM example + 50-instance
  cap, live catalog entry — zero Rust-leakage after sweep); `llms.txt` got the additive
  "start here if you are an AI agent" MiniGraph section + the Layer-2 flow AI set (site-URL
  idiom preserved); `event-script/syntax.md` got the numeric-promotion blockquote + relaxed
  arithmetic rows + `f:round` row + `f:removeKey` worked example + `listOfMap` order
  guarantee; `help graph-api-fetcher.md` got the canonical bounded-retry pattern + an HTTP
  semantics section (redirects/status/headers/dedup); webapp bundle refreshed. Plus the
  parked human-doc bug: `flow-schema-reference.md` `error.status` → **`error.code`** (5×,
  engine-verified: `TaskExecutor` puts `CODE`). Java module suite 71 green; all relative
  links resolve; ports stayed config-substitution-neutral.
- **Gold validation PASSED: a fresh agent drove the JAVA Playground (example app :8085,
  built from the branch) from the back-ported docs alone** — columnar `f:listOfMap`
  exercise: **17/17 ok, zero failures, ZERO lookups, first attempt**, incl. an unprompted
  variable-length proof (5th row appended → count 5; export restored byte-identically).
  Judged first-hand from the WS console tee. **Streak: TWELVE consecutive zero-lookup
  first-attempt passes — the twelfth on the Java engine** (the grammar is engine-neutral).
- **Headless-client lesson:** the Java WS server idle-closes (~60 s) a connection whose only
  traffic is protocol-level pings — the keepalive contract is the UI's application-level
  `{"type":"ping","message":"keep alive"}` TEXT frame (scratchpad `ws_session_java.mjs`
  implements it); the Rust port tolerates protocol pings (leniency, not a bug).
- Drive + observations documented in `docs/AI-companion-test.md` ("Back-port validation"
  section). Agent observations (no fixes filed): listOfMap nested-wrapper example ambiguity;
  row key-order unspecified.

## Memory References

- ot-java-ai-docs-backport — executed (branch pushed + validated; PR pending)
- ot-companion-validation-sweep — updated (streak 12; first Java-engine pass)
- bp-graduate-to-accenture — referenced (this closes the biggest pre-graduation gap)
- port-bottom-up-faithful — applied (map-don't-mirror in reverse: Java truths restored)

Co-Authored-By: Claude Code <noreply@anthropic.com>
