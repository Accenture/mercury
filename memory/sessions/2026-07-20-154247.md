# Session (2026-07-20T15:42:47.000Z)

**Agent:** Claude Code

## What happened

**Redirect backlog resolved — the Rust HTTP-301 user story is WITHDRAWN (maintainer's
stated criterion met).** Empirical Java probe (maintainer-requested): a temporary JUnit
test in mercury-composable platform-core (`RedirectProbeTest`, modeled on `HttpsTest`)
drove `PostOffice/EventEmitter → async.http.request` (AsyncHttpClient, the production
Spring Reactor / Reactor-Netty path) against live `https://google.com`:

```
status   = 301
location = https://www.google.com/
body     = <HTML>…301 Moved…</HTML>   (220 chars)
Tests run: 1, Failures: 0
```

**Byte-identical to the Rust port's drive-#3 result (301 / 220-char body).** Reactor-Netty
supports redirection (`followRedirect`) but mercury-composable never enables it — the raw
3xx reaching the caller is canonical behavior in BOTH engines, and finding #61's grammar
rule ("the fetcher never follows redirects; point the Provider url at the target") is the
documented contract. Since parity already holds, there is nothing to port — story
withdrawn per the maintainer's stated decision rule ("If Java behavior is the same, we can
withdraw the Rust HTTP-301 user story").

The probe file was removed after evidence capture (it depends on a live external endpoint —
unsuitable as a permanent CI test); the Java repo is clean on main.

Also: the probe's verbatim stdout is now permanently recorded in
`docs/design/platform-core-port.md` (new §5j — outbound HTTP client: HTTPS + redirect
semantics decision record, maintainer-directed), alongside the increment-48 TLS summary;
§6's stale Kafka wording aligned with the 2026-07-20 scope refinement in passing.

Also: maintainer supplied the design rationale — redirect-following is deliberately
absent, not missing: the engines target backend applications (redirection is browser-side
automation); SSO-style needs are served programmatically (layer 1) or declaratively via
`{node}.status` routing (layers 2/3); years of Java production prove it unnecessary.
Folded into §5j and the closed continuity thread.

## Memory References

- ot-http-redirect-backlog — completed (investigation done; story withdrawn, parity confirmed)
- ot-companion-validation-sweep — referenced (drive #3's 301/220 result was the comparison baseline)
- port-bottom-up-faithful — applied (behavior compared empirically against the canonical engine)

Co-Authored-By: Claude Code <noreply@anthropic.com>
