# Session (2026-07-18T18:01:37.000Z)

**Agent:** Claude Code

## What happened

**The headline goal — real-time multi-party human+AI collaboration on one live graph — proven live;
and the knowledge-graph AI docs ported into this repo (with `graph.js` corrected for the Rust port).**

### Live multi-party demo (`bp-companion-sync`) — PASSED, maintainer verdict "flawless"
The maintainer restarted the Playground (new binary with the tee) and opened **two UI sessions** —
one as the **architect**, one as the **product owner** subscribed to the first. A fresh companion
built + ran tutorial-4 entirely via the synchronous `POST /api/companion/ws-240155-2/command`, and
both UI sessions watched it unfold **live** (console + graph populating in real time, via the tee +
the command service's subscriber fan-out — no root-edit workaround needed).
- **The self-correction was the highlight:** the companion tried integer coercion `(a+b)|0` →
  in-band `ok:false` "Unexpected character: '|'"; switched to `graph.js` → "Skill graph.js is retired
  for security reasons"; tried `parseInt` → "non-function"; then reverted to plain `COMPUTE` — and all
  three contract cases passed. It reasoned, failed, and recovered **from the in-band errors alone**,
  live in front of both watchers. Session management + tee + collaboration all validated at once.

### AI docs ported into this repo (closes `bp-companion-sync` item b; fixes the graph.js mismatch)
Surfaced by the demo: `graph.js` is **retired in this Rust port** but the Java upstream docs (which
the companions read) still list it as available — sending the agent down a dead end. Since the Rust
port *diverges*, it should own its AI docs. Ported into `docs/`:
- `docs/llms.txt` (Rust-local, knowledge-graph-scoped, points at the local files) +
  `docs/guides/knowledge-graph/{ai-agent-guide,command-reference,minigraph-commands.json,skills-reference}`.
- **`graph.js` marked retired everywhere** (skill matrix, minigraph-commands entry, skills-reference
  section) with the redirect to `graph.math`/`graph.task`; **`graph.math` dialect narrowness**
  documented (no bitwise/functions, double serialization); **the synchronous `/command` endpoint
  documented** as the preferred AI path (in-band `{ok,output,error,result}` + tee), replacing the
  fire-and-forget worked example.
- `minigraph-commands.json` `source_of_truth` repointed to `crates/knowledge-graph/resources/help/`.

Future companion tests reference **this repo's** docs (recorded in `docs/AI-companion-test.md` method
+ findings #7–#8). No engine/code change — docs only.

**Remaining on `bp-companion-sync`:** (a) PR the design + synchronous endpoint to the Java upstream
(the fire-and-forget gap exists there too).

## Memory References

- referenced: `bp-companion-sync`, `ot-companion-validation-sweep`, `vision-mercury`
- created: (none)
