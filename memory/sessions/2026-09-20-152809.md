# Session (2026-09-20T15:28:09.000Z)

**Agent:** Claude Code

## Summary

**Increment 123 — a static decision table is graph DATA; the perceived `graph.task` gap is a doc gap,
closed in lockstep with the Java repo.** Eric: "We want to configure one node to store a static decision
table in a human readable form … The feature gap that I perceive is that we cannot do input data mapping
to a composable function from a node with skill 'graph.task' and transfer all properties of the node to
the composable function." His reason: an AI agent had hard-coded the table inside a composable function
bundled with a graph — a design he wants to avoid. His hint: a map or list can be text in a node property
and `f:json` converts it before data mapping. His ruling in advance: if achievable with the current
framework, it is just a doc gap.

**Finding — no engine gap here or in Java.** `initialize_with_node_properties` (common.rs) copies every
node's properties into the state machine at instantiation — a skill node's non-reserved properties at
`{node}.{key}`, a skill-less node's whole map at `{node}` — and the shared LHS resolver reads any
selector, so `state-rules -> table` hands the whole table to the function in one `input[]` entry. The
`key[]=value` property grammar (`node_properties` in commands.rs) appends into list properties and the
`'''…'''` grammar holds JSON text for `f:json(...)` (`#[simple_plugin("json")]`); the Playground renders
a list property one row per element. Nothing validates mapping SOURCES at compile or pre-run, and node
type names are free text. The only statement of this was one row of the namespaces table
(`{node-name}` — "a node's own properties"); the graph.task reference, its help text and the AI agent
guide never showed a data node feeding a function.

**Shipped (branch `docs/static-decision-table-on-a-node`, `a0d2f4aa`, pushed; PR pending —
[[eric-release-rhythm-rust]]).** No engine change. `skills-reference.md` (graph.task) gains the block *A
static decision table is graph data, not function code* (`DecisionTable` node with `keys[]` and one list
property per rule; `input[]=state-rules -> table`; the `f:json` idiom; wire under the island; a new
table is a new graph version, e.g. `v2026-08-prime-rates`); `help graph-task.md` a *Static decision
table* section; `ai-agent-guide.md` a pre-send checklist bullet (*Static reference data is a node, not
code*) and a recipe step-4 clause; `docs/INCREMENTS.md` Increment 123. Pinned by `unit-test-task-9`
(byte-identical twin of the Java fixture: island `knowledge` → `state-rules` with list properties and
`state-rules-json` with one JSON text property; `select-rule` / `select-rule-json` call the generic
test-scope `v1.decision.table` — `DecisionTableFunction` in `graph_runtime.rs` reads rule names from
`table.keys`, so the node's `purpose` travels harmlessly): TX → `community-property`, NY →
`separate-property` through both idioms; ZZ → the function's own 404 `No rule for ZZ` as the graph
output. One compile fix on the way: `EventEnvelope::set_body` returns `Result`, so the function returns it
directly. Gates green: `cargo fmt --check`, `clippy -D warnings` (tests), the graph runtime suite,
`mkdocs build --strict`, `check-llms-links`, `check-doc-claims`. Java twin: branch of the same name,
`00283800` (`GraphTaskTest.staticDecisionTableIsGraphData`).

**Not done, deliberately.** No built-in decision-table skill or shipped lookup function — the app's own
generic function receives the table; a built-in would be Eric's call. No `files.list` change: every page
touched is already in the AI-contract inventory.

**Addendum (same session) — Eric's review of the example, `3186bde1`.** Eric confirmed the finding ("This
line of configuration solves the puzzle: input[]=state-rules -> table") and ruled the presentation: show the
table as `keys=[ "community-property", "separate-property" ]` / `community-property=[ "CA", "TX" ]` /
`separate-property=[ "NY" ]` for readability — each value a JSON array written as TEXT that the composable
function reconstructs (Java `SimpleMapper`; here `serde_json::from_str`) — and state WHY a static decision
table is useful: readability, and simplifying complex IF-THEN-ELSE, so agents do not hard-code the ruling in
`graph.math` or in a composable function. "The minigraph-playground-engine is fully capable of static decision
table handling. The new recipe would help the human and AI agent picking the optimal design path." Applied to
the skills-reference block (readability + simplicity paragraph, `graph.math` named), the help section, the
AI-agent checklist bullet and Increment 123; the fixture's `state-rules` node now carries the JSON-text values
and the test function's `as_list` accepts an array or JSON text; `key[]=` lines and the `f:json` whole-table
property are kept as variations. Gates re-run green (suite, clippy, fmt, mkdocs, links, claims). Java twin
`6ae1a628`. **PRs opened by Eric: #293 here, mercury-composable #430** — bound, CI watched by PR number.
**CI follow-up `ac882d11` (Eric: "the Rust CI failed - PR 293"):** the PR's `test` job failed in
`compiler.rs::manifest_listed_graphs_are_compiled`, which PINS the number of valid manifest graphs
(compiled-or-404) — 52 with `unit-test-task-9`, the test still said 51; the graph itself compiled. Bumped
the count and named the fixture in the test's comment. Lesson, the same shape as Increment 120's
`files.list` miss: **a new fixture in `graphs.yaml` is a compiler-test change too** — run the whole
`mercury-knowledge-graph` crate suite (not just `--test graph_runtime`) before pushing; full suite,
clippy and fmt green locally on the fix.

**PR #293 MERGED by Eric 2026-09-20 22:00Z (merge `d4b1af10`; main run green), Java #430 squash `c5adc58f`.**

**Increment 124 — the `lookup` simple plugin (branch `feat/lookup-plugin` `f942b7cb`, pushed; PR pending).**
Eric wrote the Java `f:lookup(table, value)` plugin so the common case of a decision table needs no
composable function; ported here with his two review fixes (a non-table first argument is an error, not a
silent Nil; the table itself may be JSON text): `plugin_lookup` + `decision_table` / `decision_table_entry`
in `plugins.rs`, `BUILTIN_PLUGIN_COUNT` 51, inline test `lookup_matches_java_semantics` (twin of
`SimpleLookupTest`, exact messages). Fixture `unit-test-lookup-1` (byte-identical twin; compiler manifest
count 53) with `graph_runtime.rs` assertions: TX → `community-property`, `ny` → `separate-property`, ZZ →
`unknown` via `f:defaultValue`, and a probe pinning that **a null mapping source REMOVES the target** —
so the default is a second entry, not default-then-overlay as the namespaces doc and this engine's
data-mapper help claim (reported to Eric; doc or code is his call). Docs twins: the graph.task recipe
with both paths, the `graph.data.mapper` reference, both help files, the AI-agent checklist, the Event
Script catalog with an L2 worked example; INCREMENTS 124. Gates green after two reruns: a clippy
`cloned_ref_to_slice_refs` in the new test, and a spurious port collision (the Rust flow suite's REST 8102
vs the Java Event Script suite running concurrently) — run the two repos' suites sequentially.

**Increment 124b — the optional default (`d61a1285`).** Eric: "add an optional third argument in the lookup
simple-plugin as a default value". `f:lookup(table, value, text(unknown))` returns the third argument on a
miss (2 or 3 arguments; `Expected two or three input values - actual=N`); the common case collapses to ONE
mapping entry in the fixture, the recipe, both help files, the catalog row and the L2 worked example; the
null-source probe stays. Gates green, run sequentially (Rust suites → Java suites) after the concurrent
run's port collisions. Java twin `42dc8598` (plus Eric's Sonar edit there: a raw `Map` return, S1452).
**PRs opened by Eric: #294 here, mercury-composable #431** — bound, CI watched by PR number; all checks green on both.
**MERGED by Eric 2026-09-20 23:21Z: #294 merge `c4750128` (Java #431 squash `e52aeaca`)** — Increment 124 is on
main; branch deleted; this memory push held until the merge commit's `rust` run settled (cancel-in-progress).

**Round 3 — Eric's ruling on the null-source finding (branch `docs/null-source-removes-target` `48111840`,
pushed; PR pending).** "update the doc to say a null source removes the target" — scoped to MiniGraph, with
his Event Script statement: a null LHS applies to model variables only and removes the target model variable
key; for any other RHS the entry is ignored. Changed here: `command-reference.md` (Namespaces),
`minigraph-commands.json` (`lexical.mapping_operator`), `help graph-data-mapper.md`; claim
`null-source-removes-target` registered in `claims-registry.json` against `graph_runtime::
graph_task_matches_java_semantics` (the `unit-test-lookup-1` probe), inserted as text because a JSON
re-dump reformatted the registry. Gates: `check-doc-claims.py` OK (21 claims), `check-llms-links.py` OK,
`mkdocs --strict` green. Java twin `f233067f`. **PRs opened by Eric: #295 here, mercury-composable #432** — bound,
CI watched by PR number; all checks green on both. **#295 MERGED by Eric 2026-09-20 (merge `bbb8c043`)** — the null-source
rule is on main; branch deleted; this memory push held until the merge commit's `rust` run settled.

**D1 — the catch-up release prepared (Eric: "let's do D1 - prepare the Rust catch-up release"; branch
`release/v4.12.12` `13744c77`, pushed; PR pending — [[eric-release-rhythm-rust]],
[[conv-ports-adopt-java-release-number]] on the Java side).** Version 4.12.7 → 4.12.12 across 16 files
(workspace version, inter-crate pins in event-script / knowledge-graph / platform-core / minimalist-kafka /
distributed-cache / redis-connection / sync-over-async / minigraph-state-redis, the three templates' manifests
and READMEs) plus the regenerated `Cargo.lock`; the interop report's historical "4.12.7" left as a record.
CHANGELOG derived from `git log v4.12.7..HEAD` (122 commits, 78 non-memory): Increments 118–124 plus the
sync-over-async R1–R4 and minimalist-kafka K1–K2 gates, which predate the ledger sections (they live in their
specs). **Lock-step notes stated in the entry:** five items ship on Java's main after 4.12.12 (#426–#432);
Java-only items of 4.12.8–4.12.12 have no analogue (Kafka health classloader fixes, BDB retirement, OTel
switch, benchmark log); **not yet ported** — the CompileGraph task↔skill gate (Java 4.12.12 #2; this
engine's `model_validator` has suspend/resume, ttl and metadata rules only), the case-insensitive
`input.header.*` fallback for Kafka headers (Java 4.12.12 #3; `executor.rs` still lowercases the reference
only), and dev mode in the starter-graph template (Java 4.12.9 #8; this port's starter runs the production
shape). Crates.io publication held until K5 — a GitHub release. Gates green on the swept tree: build,
`cargo test --workspace` (102 test binaries), clippy `-D warnings`, fmt. Two zsh traps on the way, both
personal-memory notes: an unquoted scalar does not word-split (`for f in $FILES` — the first sweep was a
no-op) and an unquoted `--include=*.md` glob aborts a grep. **Release PR #296 opened by Eric** — bound, CI
watched by PR number; green. **MERGED 2026-09-21 (merge `983e7550`)** — 4.12.12 is on main; the tag `v4.12.12` and the
GitHub release are Eric's next gates; branch deleted; this memory push held until the merge commit's `rust` run settled.
**TAGGED AND RELEASED by Eric 2026-09-21 00:53Z:** tag `v4.12.12` → `1ef183cb` (the memory commit one past the merge
`983e7550`; same code, `version = "4.12.12"` verified at the tag), GitHub release published with the CHANGELOG entry
as its body. D1 of the backlog is done; the Rust port is at the Java number.

**Backlog D2 — the report twin only (branch `docs/interop-finding-4-followup` `2f989150`, pushed; PR pending).**
Eric ruled interop Finding 4 on 2026-09-21: the Java `redis-connection` foundation resets its shared Lettuce
connection after a command timeout (mercury-composable `fix/redis-reset-on-timeout` `48ab9b4f`). This engine
needed no change — its client reconnected on the first command after Redis returned, as Finding 4 recorded.
The twin of `docs/test-reports/distributed-cache-interop.md` gains the Finding 4 follow-up note and the section
*Recovery after the connection-reset fix* (Java example re-driven: 40 s and 10 s outages, one in-flight 408, fast
503s, a live reply 30 ms after the restart) so the two records stay aligned (they differ only in each engine's
own-perspective sentences, as before). Gates: mkdocs strict, links, claims green. **PR #297 opened by Eric** (Java
twin #433) — bound, CI watched by PR number; green. **MERGED 2026-09-21 (merge `6f1887ac`; Java #433 merged the same
hour)** — the report twins carry the Finding 4 closure; branch deleted; this push held until the merge commit's `rust` run settled.

## Memory References

- Created: `static-decision-table-is-graph-data-rust` (tier: working)
- Referenced: `eric-release-rhythm-rust`, `conventions-rust-baseline`, `rest-skip-unregistered-and-root-fallback-rust`,
  `distributed-cache-rust` (release status updated), `ot-minimalist-kafka-port` (K5 gate on publication, consulted for the release notes)
- Superseded: (none)
