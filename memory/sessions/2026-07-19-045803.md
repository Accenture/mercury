# Session (2026-07-19T04:58:03.000Z)

**Agent:** Claude Code

## What happened

**Tutorial-5 RETEST on the updated AI grammar — PASSED on the tightened criterion: ZERO in-band
lookups.** (The originally planned retest, deferred past tut-6 by earlier direction; run now at
the maintainer's request.)

- **Setup:** primary `ws-876960-4` reset by the maintainer and **verified actually empty** (the
  tut-6 draft was gone this time); fresh WS watcher (`ws-191926-6`) subscribed over the WS
  connection.
- **Result:** fresh companion, same problem-only brief as the first tut-5 run — **20/20 commands
  `ok:true`, zero failures, first-attempt dry-run pass**; contract honored (both profiles,
  deterministic `profile[0]/[1]`); parallelism proven (overlapping fetch timings, join barrier).
  Where the first run passed only via `describe skill` + `help data-dictionary`, this run needed
  **no in-band help at all** — fixes #9–#13's self-sufficiency claim is proven end-to-end.
- **Bonus validations:** the companion wired the **Island knowledge layer unprompted**
  (`root -[contains]-> knowledge -[data]-> person-profile -[provider]-> mdm-profile`) — the #22
  mandate applied straight from the grammar; it cited the documented fork/state-safety rules
  (#13) in its rationale; the design was cleaner than run 1 (results read from each fetcher's
  own `{node}.result`, no scratch keys). Independently re-verified (island sinks in 0.014 ms;
  traversal + contract reproduced). Exported as `parallel-profile-fetch-v2`.
- **New candidates:** #23 (array-index mapping targets `-> output.body.profile[0]` never shown)
  and #24 (whole-subtree Dictionary output `response.profile -> result.profile` only implied) —
  both inference-only frictions that worked first try.
- **Docs updated:** retest addendum in the tut-5 stage record; rollup #22 marked
  retest-verified; #23/#24 added as candidates.

## Decisions

- The tut-5 retest closes the #9–#13 verification loop; the sweep resumes at **tutorial-7**.

## Memory References

- ot-companion-validation-sweep — advanced (tut-5 retest done; #22 verified live; next
  tutorial-7); substance updated
- bp-companion-sync — referenced (retest driven wholly over `/sync`)
- port-bottom-up-faithful — referenced (judged against the canonical fixture semantics)

Also (same working session, maintainer-directed): **candidates #23/#24 fixed** — "Composite keys
& arrays" block (numeric list-index targets, `-> output.body.profile[0]` post-join idiom;
interior paths map whole subtrees) added to `command-reference.md#namespaces` +
`#provider-dictionary`, indexed-assembly example in `skills-reference.md#data-mapper`,
`composite_keys` + Dictionary-output notes in `minigraph-commands.json`; rollup #23/#24 → DONE.

Also: **#25 (maintainer-supplied) fixed** — the `[]` **array-append target** documented in the KG
grammar (appends an element; creates the list with the first element when absent — engine-verified
`mlm.rs` appendIndex parity), with the maintainer's clarification that **data mapping is
thread-safe**: concurrent appends from parallel branches carry no racing risk (element order
follows completion order). The over-restrictive "write to disjoint keys" state-safety wording in
`#connect` / `graph.join` / `minigraph-commands.json` refined accordingly (same-scalar-key
overwrites are the hazard; appends are race-free; numeric indices for deterministic order).

Co-Authored-By: Claude Code <noreply@anthropic.com>
