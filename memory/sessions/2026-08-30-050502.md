# Session (2026-08-30T05:05:02.000Z)

**Agent:** Claude Code

## Summary

**Progressive rendering milestone v4.12.0 - the Rust engine's closing round.**
Merged as [PR #219](https://github.com/Accenture/mercury/pull/219) merge
`bc1a9fd2` carrying gated `8b73e01c` (tree verified == gated; branches deleted
both ends). Workspace version 4.11.11 -> **4.12.0** - Eric named this a
milestone release across ALL FOUR repos (both engines + both language packs at
the same version; the wrappers jumped 0.1.0 -> 4.12.0).

What shipped in this repo:

1. **Engine-to-wrapper streaming relay demo** (hello-world): `GET /api/hello/remote`
   (`hello.remote.relay`, stream: true) forwards the caller's reply lane into the
   event-over-http mapped `hello.tokens` (the python/node demo apps); sample
   auto-loaded `resources/event-over-http.yaml`; `hello.sse` now public for
   wrapper clients over /api/event. NO trace fix was needed here (unlike the
   Java twin's demo): the Rust PostOffice is context-aware by construction -
   `apply_current_trace` stamps trace, span AND business cid on every send.
   One cleanup: a malformed 503 message string (embedded whitespace run).
2. **Interop test report** (`docs/test-reports/progressive-rendering-interop.md`,
   in the contract-provider inventory): the live ten-combination matrix, the
   OpenTelemetry span-lineage + business-correlation-id verification (incl. the
   live Rust-edge drive `biz-rust-005` -> python echo), the ratified
   `/api/event` local-routes-only ruling (Eric: no inbound forwarding - routing
   loops; the x-event-api marker is the loop guard), and the captured
   end-to-end telemetry + app-log-context appendix.
3. v4.12.0 CHANGELOG section (Unreleased -> Version 4.12.0, 8/30/2026).

Verification: cargo build + test --workspace green (67 suites), fmt --check +
clippy clean, mkdocs strict, contract-provider link validator green.

Related context (other repos, recorded in their own memory): the wrapper twins
shipped the full Phase-3 streaming + telemetry rounds (span lineage, business
cid, app log context) same day - this engine's telemetry model was the
reference the wrappers mirrored (WorkerHandler rpc-gate, touch stamping,
EventApiService sender fill).

## Memory References

- referenced: thread-sse-client-twin (the staged Phase 2/3 items this round
  closes out), conv-lockstep-releases (v4.12.0 all-repo milestone)
- created: ot-progressive-rendering-v4120 (tier: working)
