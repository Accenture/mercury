# Session (2026-09-04T04:58:32.000Z)

**Agent:** Claude Code

## What happened

Ran the memory smoke test (`memory/smoke-test.md`) in a fresh-context agent, per its own method —
the parent session had spent the day in this code and could not be a valid runner. **10/13, a
regression from 13/13** at the two prior runs, and the misses were real rather than methodological.

### The headline: memory described a repo that no longer exists

`instructions.md` still called this a greenfield repo — "no code yet", "nothing scaffolded",
"No source directories exist yet", "None yet — no code, no test framework chosen", "No project
CI/CD yet" — five unqualified present-tense claims, while v4.12.2 ships from seven crates.io
crates and ~100 increments are closed. A newcomer asking "what's the layout / how do I run tests"
was actively misled. Rewritten to the real `crates/` workspace, the actual test harness (incl. the
one-server-per-integration-file rule), and the three live workflows.

### Fixed alongside

- The canonical Java version was pinned at **v4.8.6** in three places while this repo tracks it
  lock-step at 4.12.2 — replaced with a pointer rather than another number to re-drift.
- `last_invariant_check` had an unclosed parenthesis and said "all four" while enumerating five
  ids; normalized to "4 never-decay facts + the Vision (5 ids total)".
- **No `## Blueprint` section existed.** Threads moved one-per-file in v4.39.0 and took the
  Blueprint→Vision link out of `continuity.md` with them, which PROTOCOL requires be visible.
  Restored as a section pointing at the two `(blueprint)` threads.

### Two facts added, for answers that had no home

- `why-ai-enabled-before-code` — the smoke test could no longer answer *why* this repo was
  AI-enabled before any code existed. Both carriers had faded to the archive, and INDEX one-liners
  record the *what*, not the *why*. This is the rationale that makes VBDI real here.
- `conv-llms-txt-curated-map` — Eric's 2026-09-04 ruling (curated agent map, not a site map;
  link-integrity guard, not coverage; `docs/`-relative paths) lived only in a session log about to
  leave the read window, with a live risk that a future cross-repo parity sweep would "fix" the
  deliberate divergence back toward the Java shape. Carries his benchmark: **doc discovery and
  token efficiency**.

### Raised, not inferred

User Preferences and Team/Members both read "(none recorded yet)" while the release-gate rhythm is
visibly in force throughout the archive and every recent log, and a named maintainer is cited as
authority. The Java sibling carries this as `eric-release-rhythm`, materialized on 2026-08-22
*because its own smoke test found the same shape*. This repo's User Preferences section carries an
explicit never-infer rule, so it goes to Eric rather than into memory.

**Deferred (recorded, not fixed):** truncated `archive/INDEX.md` one-liners that drop the payload
of several entries; the toolchain line still anchored to "increments 1–2" with the ledger past 100;
the ~10-line `status:` carrying version history against its own stated rule.

## Memory References

- Created: why-ai-enabled-before-code, conv-llms-txt-curated-map
