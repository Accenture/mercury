# Session (2026-07-20T16:12:37.000Z)

**Agent:** Claude Code

## What happened

**Increment 49 — `/sync` contract gaps #62/#63 FIXED in BOTH ports** (shared design, like
#40; the [[ot-sync-companion-contract-gaps]] thread closes).

- **#62 (silent dedup):** `/sync` now marks its singleton dispatch **`direct`** — an RPC
  caller is not a flaky WS client, so the 1-second identical-command guard does not apply;
  repeats always execute with full envelopes. The WS path keeps the guard unchanged.
  Rust: `rest.rs` + `commands.rs` (flag plumbed like `forwarded`); Java:
  `PostCompanionCommandSync` + `GraphCommandService` + shared `DIRECT` constant.
- **#63 (usage hint = false success):** `Syntax: …` now classifies as the error line →
  `ok:false` with the hint in-band. Verified safe first: only ONE emission site in each
  engine and NO help page starts a line with `Syntax:` — no inverse false-positive.
- **Red/green proof (Rust):** `companion_sync_contract_gaps_closed` in `graph_runtime.rs`
  failed on stashed-old code with the exact drive-#3 symptom (second repeat → empty
  envelope), passes with the fix; also proves the WS-path guard still drops a duplicate
  (dispatched via the 1-instance singleton for a deterministic sequential pair).
- **Java learning:** the same guard case initially raced — `graph.command.service` runs
  with **50 instances**, so back-to-back identical sends passed the check concurrently;
  the deterministic test path is the 1-instance `graph.command.singleton` (mirrors the
  Rust test). Module suite **71 tests green**; branch
  **`fix/companion-sync-contract-gaps`** (`5d8ac98b`) pushed — maintainer opened **PR
  [#201](https://github.com/Accenture/mercury-composable/pull/201)** — **MERGED**
  (2026-07-20): the /sync contract fixes are live in BOTH engines. Rust increment 49 committed+pushed to main by the maintainer.
- Rust gates: workspace **206 tests** / clippy 0 / fmt clean; docs strict build green.
- **Docs:** sync-envelope contract states both rules (agent guide + catalog
  `sync_envelope` notes); rollups #62/#63 → DONE; ledger increment 49.

Also: cosmetic UI change (maintainer-directed, same working session, BOTH ports): the
"Companion endpoint: /api/companion/{id}" line removed from the Playground welcome
message — with `/sync` live it advertised the legacy fire-and-forget form, and agents
derive endpoints from the session id per the AI docs (the `session {id} started` line
stays — it is the one thing a human shares). Rust: `ws_ui.rs` +
`playground-and-companion.md` quote updated (kg tests/clippy/fmt/docs green). Java:
branch `chore/remove-companion-endpoint-line` pushed (70-test suite green) — kept off
the in-flight #201 branch so its CI is undisturbed; PR
[#202](https://github.com/Accenture/mercury-composable/pull/202) created (CI pending).

## Memory References

- ot-sync-companion-contract-gaps — completed (both fixes landed, both ports)
- ot-companion-validation-sweep — referenced (findings #62/#63 origin + rollup updates)
- port-bottom-up-faithful — applied (identical fix design in both engines)
- conventions-rust-baseline — applied (gates, red/green, ledger, doc surfaces)

Co-Authored-By: Claude Code <noreply@anthropic.com>
