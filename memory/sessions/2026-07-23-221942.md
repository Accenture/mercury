# Session (2026-07-23T22:19:42.000Z)

**Agent:** Claude Code

## What happened

**Increment 66 — temporary.inbox alignment** (Eric's design-gap ruling, second commit on
branch `feature/metadata-injection-hardening`; Java reference = `TemporaryInbox` +
`EssentialServiceLoader`). The port's per-request `inbox.{uuid}` pseudo-routes reserved
the ENTIRE `inbox.*` namespace — but "inbox" is a common workflow-application concept
(human-operator staging, e.g. `inbox.approval`). NOT pushed — Eric gates.

- **One reserved route** `temporary.inbox` (`inbox::TEMPORARY_INBOX`, pub const):
  private, zero-tracing by EXACT name (the `inbox.` prefix rule removed from
  `is_zero_traced`; `ZERO_TRACING_FILTER` 3→4), 500 instances. Registered at `Platform`
  construction on EVERY registry (isolated test platforms included) and asserted by the
  lifecycle's essential block — which now documents the Java loader consolidation
  (async.http.request 500 / temporary.inbox 500 / distributed.tracing 1, the telemetry
  singleton being deliberate for record ordering).
- **Correlation-id-keyed registry:** request sends `reply_to = temporary.inbox` + a
  unique dash-less cid; the caller's ORIGINAL cid is restored on the reply (Java
  `AsyncInbox.originalCid`); an explicit caller cid is bridged onto the `my_cid` tag so
  the callee's injected `my_correlation_id` keeps matching the port's cid-slot
  convention. Composite `{cid}-{seq}` split on the LAST `-` (Java multi-inbox parity).
- **RPC marker re-keyed to the reserved `rpc` envelope tag** (Java `EventEmitter.RPC`)
  — the one-record-per-span suppression gate no longer sniffs reply-address prefixes.
- **`@origin` addressing (Eric's refinement):** only meaningful under the legacy Kafka
  mesh — NEVER generated here; inbound `to`/`reply_to` values are parse-tolerant
  (`bare_route` at deliver, the worker reply path, and `/api/event` dispatch).
- **Port adaptations (documented in inbox.rs):** reply dispatch is DIRECT on the
  sender's runtime via the reserved engine-route path (like event.script.manager /
  task.executor) — the registered workers are the addressable identity, but a
  multi-runtime process (test binaries run parallel per-test runtimes against the ONE
  global platform) cannot rely on their liveness, and a reply must always complete;
  the listener's envelope arrives PRISTINE (Java exempts TEMPORARY_INBOX from metadata
  handling — annotations/cid intact for the caller).
- **Genuine bug found & fixed:** `AsyncHttpClientService` replied through the GLOBAL
  platform (`Platform::get_instance()`) instead of the platform it is registered on —
  now takes the platform at construction (the `EventApiService` pattern); its manual
  replies reach the invoking platform's listener.
- **Tests:** new `inbox_namespace_belongs_to_applications` (a user function on
  `inbox.approval` is reachable by send + RPC and TRACED — no zero-tracing leakage);
  routes() assertions include the permanent listener; the mixed manual+auto
  double-reply test converted to a proper interceptor (the old ordering guarantee was
  an artifact of direct-completion; Java parity: manual replies are the interceptor
  pattern). Workspace 250 / clippy 0 / fmt.
- **Acceptance:** span-signature re-run — DECLARATIVE 8/8, PROGRAMMATIC 9/9, EMPTY
  DIFF, signature UNCHANGED (the reserved route is zero-traced); live drive confirmed
  RPC + X-Correlation-Id echo + my_* injection + auth session info intact.
- **Docs:** reserved-names (exactly one reserved RPC listener entry; the `inbox.`
  prefix reservation REMOVED — "the inbox.* namespace belongs to applications"; stale
  "event-over-HTTP not ported" note fixed), api-overview request() contract, CHANGELOG
  Unreleased entry, INCREMENTS §66.

## Memory References

- referenced: thread-metadata-injection-hardening (second commit on its branch),
  inv-telemetry-presentation-parity (signature re-verified unchanged),
  inv-never-couple-functions (replies still couple by route name + envelope only),
  port-bottom-up-faithful, conventions-rust-baseline

Co-Authored-By: Claude Code <noreply@anthropic.com>
