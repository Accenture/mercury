# Session (2026-07-18T23:38:07.000Z)

**Agent:** Claude Code

## What happened

**Post-tut-5 AI-grammar hardening (maintainer-directed): findings #9–#13 fixed — the four AI docs
are now self-sufficient; agents should never need the human help pages.**

- **Maintainer clarifications folded in:** (1) the accidental **browser restart** closed/reopened
  the primary session — that (not the subscribe defect) killed the orchestrator's mirror mid-test,
  and it explains the leftover draft (the UI restores local drafts into a reconnected session);
  (2) the legacy **`:type` suffix ("simple type matching") is deprecated** — excluded from the
  grammar; a `:` in a Dictionary `input[]` is a **default value only**.
- **AI-grammar edits** (verified against engine source — `mapping.rs` constant parser,
  `commands.rs` help resolution, `help data-dictionary.md` content):
  - `command-reference.md`: new **Provider & Dictionary** authoring section (#9 — URL `{name}`
    placeholders + `${config:default}`, input LHS/RHS rules, bare `input[]` + `:default`,
    `response.* -> result.*`, dedup + name-matching rules, worked example); new **Constants**
    section (closed set: text/int/long/float/double/boolean/map(k=v)/map(config.key)/file/classpath;
    deprecation note); **`help` command** documented (#10, topics + aliases); **parallel
    fork/join/state-safety** rules in `connect` (#13); all dangling links removed.
  - `minigraph-commands.json`: `sync_envelope` truth (#12 — null fields omitted, `id` field),
    `constants` + `traversal` objects, `help` command entry, enriched `config_nodes` with examples,
    `source_of_truth` retargeted to `command-reference.md`. JSON validated.
  - `ai-agent-guide.md`: `/command` → `/sync` prose sweep (#11); envelope documented truthfully
    (absent ⇒ null); dangling `playground-and-companion.md` refs removed.
  - `skills-reference.md`: dangling refs retargeted to the new sections; fetcher gotchas +
    join fork-side note added. `llms.txt` map updated.
- **Records corrected:** `docs/AI-companion-test.md` — mirror-death attribution (browser restart),
  leftover-draft cause, rollup #9–#13 → DONE, #14 reworded (defect stands on engine-state evidence
  alone), #15 cause noted, new **#16** (help-pages rewrite backlog). Spawn-task chip replaced with
  corrected context.
- **New backlog thread `ot-help-pages-rewrite`** (maintainer-requested): polish the human-facing
  `help/*.md` in a separate session; coordinate with the Java upstream (verbatim ports).

## Decisions

- **AI grammar must be deterministic and self-sufficient for agents; help pages serve humans**
  (maintainer). The four AI docs now carry the full closed rule set; in-band `help` remains a
  documented fallback, not a requirement.
- `:type` suffix deprecated — never generated; Dictionary `:` = default value only (maintainer).
- Help-page rewrite deferred to its own session (backlog), upstream-coordinated.

## Memory References

- ot-companion-validation-sweep — substance updated (grammar fixes #9–#13 recorded; tut-6 resume
  hints hardened)
- ot-subscribe-via-sync-bug — substance corrected (browser-restart attribution; defect scope
  narrowed to wrong-registration state)
- Created: ot-help-pages-rewrite (born tier: working)
- port-bottom-up-faithful — referenced (help pages are verbatim Java ports; rewrite must be
  upstream-coordinated)

## Memory Review (2026-07-18, through 2026-07-18-233807)

Triggered by `[continuity-bloat]` (615 > 600 lines).
- Reactivated:   0
- Superseded:    0
- Archived:      1 fact → memory/archive/2026-Q3.md (bp-event-script — faded, sslu 22 > 20)
- Swept threads: 2 completed → archive (ot-design-platform-core sslu 39, ot-design-event-script
  sslu 29 — both milestones closed; condensed summaries retained in the archive)
- Condensed (lint remedy for the line backstop; full history remains in `docs/INCREMENTS.md`,
  design docs, and session logs): the Project-State `status` line (changelog accretion → short
  current-state, per AGENTS.md), and the three design threads (117/113/85 lines → ~10 each;
  ot-design-knowledge-graph stays in continuity, recently used). 615 → 324 lines.
- Archive-verify: pass (memory-lint 0 errors, 0 warnings; no id in both places)
- Tier changes:  2 (working → archive-candidate, then swept)
- Invariants:    not due (checked 2026-07-18; cadence 40)
- Promoted core: 0

Co-Authored-By: Claude Code <noreply@anthropic.com>
