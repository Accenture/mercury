# Session (2026-09-02T17:36:01.000Z)

**Agent:** Claude Code

## What happened — the v4.12.2 field release (lock-step)

Release branch `release/v4.12.2` forked from origin/main; workspace version + the six
inter-crate pins 4.12.1 → 4.12.2 with Cargo.lock regenerated; CHANGELOG flipped to
`Version 4.12.2, 9/2/2026`. Full gate green (workspace test, clippy `-D warnings`,
fmt). PR #226 merged (merge commit `85b23e92`); tag **v4.12.2** pushed on it. Java
lock-step: mercury-composable PR #313 (squash `357c38b0`), tag v4.12.2 — full round
narrative (including the orphaned-memory-commit recovery, `94581d1c` here) in the Java
repo's log 2026-09-02-172746 and this repo's 2026-09-02-163225. Release content:
async companion retirement (breaking) + the `json` simple plugin twin (Increment 98).

Commit `13e34c2f` — Release v4.12.2 (CHANGELOG, Cargo.toml ×4, Cargo.lock).

## Operational note

`gh release create` needs the token's `workflow` scope (refresh:
`gh auth refresh -h github.com -s workflow`); the tag was pushed over SSH instead and
the release-notes page handed to Eric. crates.io publication of the seven `mercury-*`
crates at 4.12.2 is Eric's step (existing crates — no new-crate burst limit; dependency
order: platform-macros → platform-core → event-script-macros → event-script →
knowledge-graph-macros → knowledge-graph → minigraph-state-redis).

## Memory References

- inv-telemetry-presentation-parity (lock-step release discipline)
- conventions-rust-baseline (release gate: test + clippy + fmt)
- ot-json-plugin-twin (shipped to the field in this release)
