# Session (2026-07-24T16:06:34.000Z)

**Agent:** Claude Code

## Summary

New field-requested feature mirrored from the Java reference in lock-step:
**configurable traceparent header name**, on branch
`feature/configurable-traceparent-header` (same name as the Java branch,
mercury-composable commit `5ee496dd`; per the cross-engine contract — identical
config keys, identical precedence, closely matching comments/doc wording).
1 commit, NOT pushed (Eric gates pushes/PRs).

## What was built

- **Config:** `http.traceparent.header` (default `traceparent`) + per-entry
  `traceparent.header` in a rest.yaml endpoint entry (precedence per-entry >
  global > default). Java's Kafka twins (`kafka.traceparent.header`,
  `secondary.kafka.traceparent.header`, adapter per-binding override) have no
  Rust surface — the Kafka service mesh is not ported — and were skipped per
  the mirror-what-exists rule; the CHANGELOG and observability "Rust port"
  note say so explicitly.
- **Inbound** (`automation/server.rs`): `RouterState.traceparent_header` +
  effective-name resolution in `process()` — parse the custom name first (a
  well-formed value under it WINS, so a sidecar-injected fresh standard
  `traceparent` cannot override the peer's context), standard header as
  fallback (Java `HttpRouter.parseInboundTraceparent` twin).
- **Outbound dual stamp:** `automation/http_client.rs` (the async HTTP
  client) and `automation/event_api.rs` (the single Event-over-HTTP encode
  path — Java had two EventEmitter sites deduplicated into `setTraceHeaders`;
  the Rust port already has one site, so map-don't-mirror) stamp the same W3C
  value under both names when the custom name differs (case-insensitive).
- **Per-entry plumbing:** `automation/routing.rs` — `RouteInfo.traceparent_header`
  parsed from `traceparent.header` (dotted-key lookup already handles it).
- **Tests (Java pattern):** `tests/resources/application.properties` sets
  `http.traceparent.header=X-Trace-Context` suite-wide, so the entire
  platform-core test suite runs with the feature ACTIVE — the untouched suite
  proves it is additive. Five regression twins in `tests/rest_automation.rs`
  (fixtures `TraceHeaderEcho` + `EchoChainCaller`, per-entry override endpoint
  `/api/renamed/traceparent/probe` with `X-Endpoint-Trace`):
  custom name carries the trace context; custom wins over an injected standard
  header; standard remains fallback; per-entry override beats the global name;
  the echo-chain proof that the client stamps identical values under
  `traceparent` AND `x-trace-context` on a real outgoing hop.
- **Gate:** workspace 257 tests (252 + 5 new), clippy 0, fmt clean.
- **Docs (Java wording, adapted to the shared surfaces):**
  configuration-reference (`http.traceparent.header` per-key section),
  observability (impedance-table row, per-entry line, the "renamed traceparent
  beats conflation" tip with the standards-deviation trade-off, extended
  Rust-port note), rest-automation grammar section, reserved-names traceparent
  row, actuators-and-http-client trace-header paragraph, CHANGELOG Unreleased,
  INCREMENTS §68.

## Memory References

- referenced: `port-bottom-up-faithful`, `conventions-rust-baseline`,
  `inv-telemetry-presentation-parity` (the Event Script / cross-engine
  contract extends to this config surface and doc wording)
- created: `thread-configurable-traceparent` (born tier: working)
