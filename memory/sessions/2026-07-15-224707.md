# Session (2026-07-15T22:47:07.000Z)

**Agent:** Claude Code

## Summary

**Implemented increment 1 — configuration management** (`bp-platform-core`, Design doc §4).
The first real code in the repo: mercury is no longer greenfield.

**Gate answer (maintainer):** keep the Spring naming (`SPRING_PROFILES_ACTIVE` /
`spring.profiles.active`) **verbatim** for side-by-side comparison during migration; a generic
`app.profiles.active` alias can come once the foundation is robust (logged as
`ot-profiles-alias-backlog`). Proceed with increment 1.

**Built** (Cargo workspace + `crates/platform-core`, Rust edition 2021, toolchain 1.95.0):
- `util/multi_level_map.rs` — `ConfigValue` enum (Null/Bool/Int/Float/Text/List/Map with
  YAML/JSON conversions, keys-as-text enforcement) + `MultiLevelMap`: dot-bracket composite
  paths (`a.b[0].c`, multi-dim indices, null-padding), `exists` vs `key_exists`,
  `flat_map()`/`from_flat_map()` (the `Utility.getFlatMap` + sorted-normalization analog),
  panic-on-invalid `set_element` (Java unchecked) + fallible `try_set_element` for loaders.
- `util/resources.rs` — the `resources/` convention: ordered resource roots (classpath
  analog), `./resources` default, prepend = shadowing (tests prepend `tests/resources`).
- `util/overrides.rs` — process override registry (the `System.getProperty` / `-D` analog),
  checked first in every lookup.
- `util/config_reader.rs` — `ConfigReader`: `.yml`↔`.yaml` interchangeable, `.json`,
  `.properties` (string values, sorted keys, composite expansion); `classpath:`/`file:`
  prefixes; `../` traversal rejected; YAML-tab tolerance (ported quirk); `${VAR:default}`
  substitution with exact Java precedence (override → env var → base-config reference with
  loop detection → `:default`), multi-segment reconstruction; `resolve_references`
  normalization; cached `get_composite_key_values`.
- `util/app_config_reader.rs` — `AppConfigReader` `OnceLock` singleton: `app-config-reader.yml`
  manifest (app copy overrides the `include_str!` embedded default), merge order
  bootstrap.properties → bootstrap.yml → application.properties → application.yml (later
  wins), active-profile overlays, sorted flat-merge normalization, resolve-once-after-merge;
  crate-internal `try_base_reader()` (no auto-init — Java parity).

**Verification:** `cargo build` clean · **30 tests pass** (12 unit + 18 integration covering
all §4.6 acceptance criteria: formats, ext fallback, file:, traversal rejection, merge order,
profile overlay, singleton, env/default/multi-segment substitution, loop detection, override
precedence, base-config refs from a standalone reader, string enforcement, list flattening,
cached composite view) · `cargo clippy --all-targets` clean (2 style warnings fixed) ·
`cargo fmt --check` clean.

**Notable porting judgments** (recorded in doc comments): properties values stay strings
(java.util.Properties parity); dotted YAML keys normalize into nested trees via the
flat-map/set_element round trip (exactly how Java behaves); `${}` left raw when no base
config initialized (Java `baseConfig == null` parity); Java's file-alt-path bug (opens
primary instead of secondary) fixed rather than ported.

**Memory updates:** status line (increment 1 implemented), Stack & Tools (real versions;
serde_yaml archived-upstream watch note), Conventions seeded (`conventions-rust-baseline`),
`ot-greenfield-no-code` resolved, `ot-design-platform-core` → increment 2 next,
`ot-profiles-alias-backlog` added.

## Memory References

Created:
- `conventions-rust-baseline` (Conventions — fmt/clippy clean, Apache header, Java-class doc
  refs, test layout, parity-notes discipline)
- `ot-profiles-alias-backlog` (deferred generic profile alias)

Updated:
- `ot-design-platform-core` — increment 1 done, increment 2 next
- `ot-greenfield-no-code` — resolved (first code landed)
- Project State status, Stack & Tools

Referenced: `bp-platform-core`, `vision-mercury`, `platform-core-stack`,
`config-first-spring-out`, `port-bottom-up-faithful`
