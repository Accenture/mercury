# Session (2026-09-13T22:50:02.000Z)

**Agent:** Claude Code

## What happened

**R1 merged, and the local-helpers ruling recorded.** PR #266 merged (merge
`8bcbbd8a`, mergedAt 2026-09-13T22:37:48Z, API-verified); main synced, branch
deleted both sides.

Eric then copied the three Java helper servers (`kafka-standalone`,
`redis-standalone`, `schema-registry-standalone`, all 4.12.8 executable jars)
into a new `helpers/` folder, for the minimalist-kafka phase that follows the
progressive-rendering work. Flagged before anything was committed: the three
jars total **158 MB**, nothing in `.gitignore` covered them, and this repo
otherwise tracks **zero** binaries — so a routine `git add -A` (my own commit
flow included) would have swept 158 MB of version-stamped build output into
public history permanently, with a refresh cost at every release and contents
no dependency scanner can see into. In the Java repo these are source modules,
built on demand, never committed as jars.

**Eric's ruling: keep the helpers folder local, commit the ignore rule.** The
`.gitignore` gained `helpers/*.jar` with a comment carrying the rationale, so
the file itself is the durable record — no continuity fact needed. The jars
remain in place for the R3/R4 dry-runs (verified runnable: all three are Spring
Boot `JarLauncher` executables). Design of the next (minimalist-kafka) phase is
deferred by Eric.

## Memory References

- ot-sync-over-async-port (updated in substance - R1 merged with its squash ref;
  the helper jars the later dry-runs need are local-only by ruling)
- eric-release-rhythm (consulted - merge API-verified before branch cleanup;
  PR creation and merge are Eric's gates)
