# Session (2026-07-18T18:11:23.000Z)

**Agent:** Claude Code
**Lightweight:** rename only — no new fact/decision beyond the naming choice.

Renamed the synchronous companion endpoint URI `POST /api/companion/{id}/command` →
`POST /api/companion/{id}/sync` (maintainer request — the URI should communicate intent;
`/sync` chosen over `/interactive` as it names the synchronous in-band contract and aligns
with the `.sync` service name). Updated across `examples/minigraph-playground/resources/rest.yaml`
and the ported AI docs (`llms.txt`, `ai-agent-guide.md`, `minigraph-commands.json`,
`design/ai-companion-sync.md`, `AI-companion-test.md`) + the `bp-companion-sync` thread. Internal
service route `post.companion.command.sync` / fn unchanged (plumbing). Live after the next server restart.

## Memory References
(none)
