# Session (2026-09-23T00:27:01.000Z)

**Agent:** Claude Code

## Summary

Two Eric rulings from the pre-v4.12.15 backlog survey, both landed on branches: **P10 closed** —
`fix/plain-home-page-outside-dev` (`037131e8`, Increment 134): the Playground entry page leaves the static folder,
`get_index_html` serves it only when `app.env=dev` (an absent `app.env` is production, it used to default to dev),
the static `public/index.html` is the plain page, and the Layer 3 starter gains dev mode (the Java starter's shape);
lock-step with mercury-composable `d54c11e3`; Eric opened Rust PR #319 and Java PR #449 this session. **Note 3 closed** — `feat/redis-lifecycle-retry` `3d4a9559` (stacked on the P10
branch; Increment 135; all gates green — fmt, clippy, `cargo test --workspace` 467 tests, claims, links, mkdocs): the restart-aware retry in the `redis-connection` foundation — `ConnectionLifecycle`, the
heartbeat monitor (`redis.heartbeat.ms`, default 1 s), one retry per lost connection for idempotent commands only,
never a replay of a non-idempotent one; the cache opts in per command, the sync-over-async store now runs on a
`RedisBackend` (`ReturnRouteStore::connect`) keeping its 500 mapping and D7; `BounceProxy` shared from the test
double; `bounce_recovery.rs` (5 tests). Eric: "fix Rust P10 before v4.12.15".

## Method notes

- The first note-3 design keyed the replay on the io kind (BrokenPipe ⇒ never delivered). Reading `redis-rs` 1.5.0
  killed it: `closed_connection_error()` is BrokenPipe on BOTH paths (a dead pipeline task the command never reached,
  and in-flight requests when the stream ends). The second idea, the library's `Disconnection` push, needs RESP3
  (`check_resp3!`). What was left — a heartbeat whose failed PING makes the manager reconnect ahead of the next
  command — is also what fixes the actual field symptom (the producer's first `RPUSH` after a restart).
- A test expectation was wrong, not the code: under a refused outage a command waits on the manager's reconnect
  attempts up to the command deadline (408), not a fast 503 — "fail fast" means one deadline-bounded attempt, never
  two. Asserted as `408 | 503`, `retries == 0`, elapsed < 2× timeout.

## Memory References

- referenced: redis-connection-foundation-rust, redis-failure-classification-rust, distributed-cache-rust,
  conv-template-version-sweep-rust, ot-minimalist-kafka-port (survey: stale — closes at the v4.12.15 seam)
- created: redis-restart-aware-retry (tier: working)

## Later the same session

- **PR #319 MERGED** (merge `49403c23`, 2026-09-23 00:30Z); the local and remote P10 branches are gone. The
  note-3 branch was rebased onto the new main — `a98d56f3`, code identical to `3d4a9559` (the rebase picked up
  only the memory commits) — and force-pushed; it now shows one commit for its PR.

- **PR #320 MERGED** (merge `34ff82d6`, 00:36Z) — note 3 is closed on main; local and remote branches gone.
- **Docs fold-in (b) of the v4.12.15 survey ready:** branch `docs/http-streaming-tracing` (`11a27336`) adds
  *Tracing a stream* to `docs/guides/http-streaming.md` (head and tail, never per token; `frames`; the relay's
  span) and the `{#edge-span}` anchor on `observability.md`; mkdocs strict, llms links, claims all green.

## Next

- Eric merges the P10 PR, then the stacked note-3 PR; the v4.12.15 round follows (fold-ins tracked in the Java
  repo's `v4-12-15-release-prep` thread). Follow-up: `minigraph-state-redis` onto the foundation's lifecycle.

Commit: `git add memory/ && git commit -m "session 2026-09-22 [Claude Code]"`
