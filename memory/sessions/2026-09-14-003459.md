# Session (2026-09-14T00:34:59.000Z)

**Agent:** Claude Code

## What happened

**Experiment R3 conducted — the cross-pod dry-run, live.** Eric's go: "conduct
R3" (the spec's gate after R2). Two Rust processes against the Java repo's
`redis-standalone` helper jar (4.12.8, from the local `helpers/` staging), a
Node SSE client with wall-clock timestamps as the witness. **All five Java E3
scenario outcomes reproduced on the first complete run; no mechanism defects.**
Permanent record: `docs/test-reports/streaming-return-route-cross-pod.md`
(same filename tradition as the Java twin), added to the mkdocs nav.

Shipped on branch `feat/sync-over-async-r3`:

1. **`examples/sync-over-async-demo`** (Q5 made real — shape decision: ONE
   crate with profile role selection, the Java demo's
   `stream-ui`/`stream-producer` pattern via `app.profiles.active`):
   `StreamNotifyFacade` (interceptor behind `GET /api/notifications`,
   `stream: true`; announces `event: cid`; per-request idle via
   `x-stream-idle-seconds`), `StreamProducer` (`POST /api/produce`, modes
   chat / notify / close / stall / lost — `lost` appends store-only through
   `ReturnRouteStore`, the suppressed-wake-up chaos), a
   `SyncOverAsyncBootstrap` before-application hook (`runtime::init` when
   `sync.over.async.enabled`, `RedisHealthCheck::register` on every role),
   profile configs, and `scripts/sse-client.mjs` (timestamped SSE witness).
   README carries the full chaos runbook.
2. **The dry-run evidence** (all in the report): exact cross-pod token order
   with terminal metadata; a suppressed wake-up healed by the next post's
   drain (billing absent mid-run, then delivered in list order); a fully-lost
   close recovered by the watchdog's single final drain at **idle + 2 ms**
   (Java: +1 ms); the watchdog's in-band 408 `Stream idle timeout` 6.007s
   after a `kill -9` of the producer — the §5 item 5 edge-grace delta proving
   itself live (the watchdog's message reached the client, not the edge
   backstop's); and the TTL-bounded orphan stop after a `kill -9` of the UI
   pod (`live: true` in the void window at kill+2s, `live: false` at 41s,
   past the 30s route TTL). Both pods' `/health` showed `soa.redis.health`
   live (placeholder during grace, then reachable).
3. **One presentation nuance recorded** (pre-existing edge behavior, not R3's):
   a metadata-less terminal renders its `done` data line as the literal `{}`
   on this engine's SSE edge (the platform-core eof arm renders Nil that way,
   deliberately). Metadata-carrying closes are byte-identical across engines.

Spec updated: §8 R3 row DONE with the outcome summary; header status now
points at R4 (the polyglot dry-run — the wire-parity acceptance gate, with the
optional real-LLM leg as the Java E4 analog).

Ripple caught by the workspace gate: the ai-contract-provider's snapshot suite
pins the documentation closure — a new file under `docs/test-reports/` fails
`inventory_equals_the_documentation_closure` until `files.list` carries it.
Added the report there, so it now ships inside the exported `mercury-platform`
skill: consumers' AI agents receive the cross-pod evidence with the contract.

## Memory References

- ot-sync-over-async-port (updated - R3 done, next gate R4)
- port-bottom-up-faithful (consulted - the Java demo and the E3 report were
  read first and reproduced outcome-for-outcome; the one nuance found was
  recorded, not silently normalized)
- conventions-rust-baseline (consulted - example-crate conventions: publish =
  false, resources/ next to Cargo.toml, one binary with profile overlays)
- inv-telemetry-presentation-parity (consulted - the report mirrors the Java
  twin's structure and evidence format so field readers see one story; the
  done-event nuance is called out precisely because presentation parity is
  the standing requirement)
- eric-release-rhythm (consulted - branch prepared and pushed; PR creation and
  merge are Eric's gates)
