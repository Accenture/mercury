# Session (2026-07-20T04:45:00.000Z)

**Agent:** Claude Code

## What happened

**Increment 48 — outbound HTTPS for the async HTTP client (Rust-only parity work).**
Maintainer requirement this session: systems of record in the field require HTTPS for
outbound calls by `async.http.request` — directly or via MiniGraph's API Fetcher (same
route; it already passed `https://` Provider URLs through). Java has supported this from
the start, so no Java changes; this closes the Rust module's documented `https` deferral.

- **TLS stack decision:** rustls via `tokio-rustls` (`ring` provider — avoids a native
  cmake/asm toolchain) + `rustls-native-certs`. Strict mode verifies against the **OS
  certificate store** (the JDK-default-truststore analog); `trust_all_cert` skips chain
  validation only (handshake signatures still verified) — mirroring Java's
  `InsecureTrustManagerFactory` escape hatch for self-signed endpoints. Client configs
  cached per mode (`OnceLock`).
- **`AsyncHttpRequest` parity:** `trust_all_cert` parsed from the map, fluent setter,
  emitted alongside `host` in `to_value` (Java `toMap` shape). `validate_url` accepts
  https (default 443); host header omits the scheme's default port. TLS failures surface
  in-band (500) like all client errors.
- **Tests** (`crates/platform-core/tests/http_client_tls.rs`, no external network): local
  rustls server + `rcgen` self-signed cert proves both modes (trust-all → 200 + decoded
  JSON; strict → in-band certificate rejection) + round-trip + protocol rejection.
  Workspace **206 tests** / clippy 0 / fmt clean; docs strict build green.
- **Docs:** `actuators-and-http-client.md` target-host section rewritten (HTTPS, OS trust
  store, trust-all caveat); module doc deferral replaced; ledger increment 48. The
  server-side HTTP(S) relay (`rest.yaml` URL services) remains deferred — unrelated.
- **Scope note:** serving HTTPS (inbound TLS on the REST automation server) stays out of
  the current scope — the maintainer's requirement was outbound only.

Also earlier in this working session: Java upstream **PR
[#200](https://github.com/Accenture/mercury-composable/pull/200)** (describe-graph,
increment 47) was created and **MERGED** — increment 47 is live in BOTH engines — and the
maintainer committed+pushed increment 47 on the Rust main. Maintainer confirmation: the
Java client's HTTPS (Spring Reactor / Reactor-Netty) is production-proven, so increment 48
mirrors field-validated behavior.

## Memory References

- port-bottom-up-faithful — applied (behavior parity with the Java client, map don't mirror)
- conventions-rust-baseline — applied (gates, ledger, parity notes, in-module Java-class doc)
- ot-companion-validation-sweep — referenced (Fetcher path context for the requirement)

(The live `## Stack & Tools` section in `continuity.md` was extended with the new deps —
tokio-rustls + rustls-native-certs + the rcgen dev-dep; no archived fact was pulled back.)

Co-Authored-By: Claude Code <noreply@anthropic.com>
