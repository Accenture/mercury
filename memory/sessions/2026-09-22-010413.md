# Session (2026-09-22T01:04:13.532Z)

**Agent:** Claude Code (Fable 5.1)
**Title:** The OpenTelemetry trace forwarder ported (`mercury-opentelemetry-forwarder`) — this engine's own OTLP encoder over the platform HTTP client, opt-in by `otel.forwarding`, certified live against Dynatrace with the A-B-A credential experiment; #306 merged and the v4.12.14 cut moved behind this PR

## Rulings (Eric)

1. "Open-Telemetry forwarder feature is missing in Rust." — the last Java extension this port had deferred
   (port-scope "Deferred", the observability guide's "write your own" note). Ported now, ahead of the shared
   v4.12.14 cut, so the Rust CHANGELOG at that number carries it (the Java side already merged its 4.12.14 PR
   #437, `dbc26f31`, 2026-09-22 00:05Z).
2. "update the examples/hello-flow (the counterpart of Java's composable-example) to import the open-telemetry
   forwarder dependency. Then run enable the open-telemetry with runtime parameter override
   'otel.forwarding=true' so that the traces can be uploaded to Dynatrace. You can use the same '/v1/traces'
   endpoint and the env vars OTLP_AUTH_HEADER, OTLP_API_ENDPOINT and OTLP_TOKEN. I will then check the traces
   in the Dynatrace non-prod dashboard to certify the open-telemetry forwarder feature." — done as stated;
   Scenario 5 of the report is his check.
3. PR #305 ("temporarily closed — when to reactivate?"): never — its head `0da85068` is on main through #303
   (`753eb9fe`) and its branch is deleted, so GitHub cannot reopen it; it stays as history.

## What happened

- **#306 merged** (`8bd5aef9`, the lock-step round P8/P9/#414 on main); the lock-step branch cleaned. The
  `release/v4.12.14` cut prepared right after it (15 Cargo.toml / 29 occurrences, CHANGELOG, lock) was
  DISCARDED uncommitted when ruling 1 arrived — re-cut from main after the forwarder PR merges, the CHANGELOG
  draft (scratchpad) already carries the forwarder entry.
- **The crate** — `extensions/opentelemetry-forwarder`, lib `opentelemetry_forwarder` (Increment 129). A
  `#[preload]` at `distributed.trace.forwarder` (2 workers, `#[zero_tracing]`) under
  `#[optional_service("otel.forwarding")]`; a `#[before_application(sequence = 6)]` hook under the same
  switch validates the endpoint (a non-http(s) URL fails the start naming the key), logs the ready line with
  header NAMES only, installs the shared `Exporter`. `src/span.rs` = `TraceMetricsSpanData` line for line
  (exact W3C ids or skipped; float `exec_time` parsed via its text; an ISO-8601 parser inverting the engine's
  formatter — no date crate). `src/otlp.rs` = the OTLP v1 trace protobuf written by hand (`ProtoWriter`,
  `oneof` members always present, proto3 defaults omitted, `flags` 0x101) + a bounds-checked `ProtoReader`
  for the response's `partial_success` and the test collectors. `src/export.rs` = the request through
  `async.http.request` (`application/x-protobuf`), the Java retry policy (5 attempts, 1 s × 1.5 on transport
  failures and 408/429/502/503/504) and diagnostics (`HTTP 401 - Token Authentication failed | the backend
  rejected the credential itself - check otel.exporter.otlp.headers (...)`; 404 signal-path and 403
  ingest-scope hints; 256-char body bound; header values never logged). **Why no SDK:** the Rust OTLP stack
  (`opentelemetry-otlp` → `opentelemetry-proto` → `prost` + `tonic`) would have been the heaviest dependency
  in the workspace for eight frozen message types with scalar fields; the platform client is the seam the
  schema registry client proved, now against a public TLS endpoint.
- **Tests:** 30 unit (wire-format golden vectors, mapping twins of `TraceMetricsSpanDataTest`, diagnostics
  twins of `ExportFailureDiagnosticsTest`, header parsing, ISO-8601 round trip with `trace::iso8601_utc`);
  `tests/otlp_export.rs` against the in-process collector double (`tests/support/mock_collector.rs`, decodes
  the protobuf with the crate's reader): round trip + credential on both vendor paths, 503→429→200 retried,
  a killed first connection retried, 404/401/403/400 final and diagnosed, the late credential, an invalid
  endpoint refused; `tests/trace_pipeline.rs`: the REAL forwarder registered by configuration alone, a
  traced `fun.1 → fun.2 → fun.3` chain, lineage/resource/scope/credential asserted at the collector.
- **`hello-flow` carries the crate with the switch off** (ruling 2; the composable-example shape):
  dependency, `use opentelemetry_forwarder as _;`, the `otel.*` block over `OTLP_API_ENDPOINT` /
  `OTLP_AUTH_HEADER` / `OTLP_TOKEN` / `OTLP_SERVICE_NAME`, `tests/otel_forwarding_switch.rs`.
- **Certified live against Dynatrace** (`docs/test-reports/otel-dynatrace-certification.md`, commit under
  test `f6e69af5`): `target/debug/hello-flow -Dotel.forwarding=true`, one `GET /api/hello/otel-cert?lang=fr`
  per leg, five spans (`http.flow.adapter` SERVER; `task.executor`, `language.router`, `greeting.composer`,
  `async.http.response` INTERNAL). **A-B-A:** real token 0/5 failures (trace
  `0ad577c2bef646cba174147ebf923c01`, 00:52:04Z); bogus token 5/5 `HTTP 401 - Token Authentication failed`
  (`eedf60cd9bbe47618859a1a7b5967837`, 00:52:15Z); real token 0/5 (`092b2a1947e54f298d5ab945aa901331`,
  00:52:26Z). Two earlier runs also reached the tenant with the real token (5 more traces; the first run's
  legs B/A′ were invalid — see lessons). No endpoint, token or tenant id in any committed file or in this log;
  the drive script redacts both through `awk ENVIRON` (secrets never in argv).
- **Docs:** observability guide §"The OpenTelemetry forwarder (ready-made)" replaces the "not ported" note
  (Rust-port box with the three declared deltas); `otel.*` keys in the configuration reference (and removed
  from its "absent keys" note); port-scope, README, `llms.txt`, mkdocs nav, `files.list` (the report packaged
  in the AI contract); claims `otel-forwarding-default-off`, `otel-forwarder-preserves-w3c-ids`,
  `otel-forwarder-credential-per-export`; the crate README with the Java-vs-Rust table.
- **Gates:** `cargo test --workspace --no-fail-fast` — 111 suites, 590 tests, 0 failures (exit 0); `check-doc-claims` 26 claims OK, `check-llms-links` 37 OK, `mkdocs build --strict` OK,
  `cargo fmt --check` OK, `clippy --workspace --all-targets -D warnings` OK.
- **Branch `feat/opentelemetry-forwarder`:** `f6e69af5` (crate + hello-flow + docs) and `27cbb0d3` (the
  report). Pushed; PR-open is Eric's gate ([[eric-release-rhythm-rust]]); handoff text in the scratchpad.
  **PR #307 opened by Eric 2026-09-22 ~01:10Z** (`27cbb0d3` → main, mergeable; build + memory checks green at
  open, the test job pending). Merge is his next gate; the `release/v4.12.14` re-cut follows the merge.

- **Re-driven under `mercury-otel-cert` (Eric: "are you using the same service name 'mercury-otel-cert'
  for the dynatrace upload test?" — no, the example's default `hello-flow` was in effect because
  `OTLP_SERVICE_NAME` is unset in his shell).** Same binary, same endpoint, `OTLP_SERVICE_NAME=mercury-otel-cert`
  exported for the run: A `f658763c71844a998d02521c033c5918` 0/5 (01:18:25Z), B `ababd406bfcf43c181ded9446d1c0198`
  5/5 `HTTP 401` (01:18:36Z), A′ `b0ee5e2087ff495a9b8f13977620c758` 0/5 (01:18:47Z). Report Scenario 3/5 updated
  (branch commit after `27cbb0d3`; PR #307 picks it up).

- **PR #307 MERGED 2026-09-22 01:29:35Z** (merge `9c56cc32`; CI on `757f6664` green — test 4m43s); branch cleaned
  after the merge-verification gate. **Scenario 5 CLOSED from Eric's Dynatrace screenshots** of both
  `mercury-otel-cert` traces (`f658763c…`, `b0ee5e20…`): nesting reconstructed as the engine recorded it
  (`http.flow.adapter` root → `task.executor`; → `language.router` → `greeting.composer` →
  `async.http.response`), kinds `Internal` under the `server` root, status OK, scope
  `mercury-opentelemetry-forwarder` **4.12.12** (the branch build), attributes as mapped incl. the flow engine's
  `annotation.execution/flow/tasks` on `task.executor`; leg B's trace absent. Field acceptance on the released
  crate (scope 4.12.14) is what the thread still holds.
- **`release/v4.12.14` re-cut from `9c56cc32`** (Eric's ruling 3, now unblocked): sweep re-derived — **13
  Cargo.toml files / 24 occurrences** `4.12.12` → `4.12.14` (the workspace version, every intra-workspace
  `version = ".."` pin incl. the new crate's, the three starter templates; the discarded 2026-09-21 cut had
  counted 15/29 with a different grep — the count is re-derived, never carried); CHANGELOG entry from
  `git log v4.12.12..HEAD` (the K3–K5 port, the forwarder #307, the facade tasks + demo, the lock-step round,
  `keep_running`/SIGTERM, `group.protocol=auto`, the port-holding test fix; lock-step notes; the three crates'
  first crates.io publication and the dependency-ordered publish sequence in the PR text); `Cargo.lock` rebuilt;
  Scenario 5 of the report closed on the same branch. Gates: `cargo build` + `clippy --workspace --all-targets -D warnings` + `cargo test --workspace --no-fail-fast` (111 suites, 590 tests, 0 failures), fmt, claims (26), llms links (37), mkdocs
  strict OK. Commit `00ee2181`, pushed; **PR #308 opened by Eric 2026-09-22 ~01:47Z** (`00ee2181` → main, mergeable; CI pending at open); merge, tag
  and `cargo publish` are his gates.

- **PR #308 MERGED 2026-09-22 02:08:02Z** (merge `b83c493f`; CI green on `00ee2181`, test 3m49s); release branch cleaned
  after the merge-verification gate; the workspace reads 4.12.14 on main. Tag, GitHub release and `cargo publish`
  remain Eric's. Registry state checked before his publish: the seven older crates sit at 4.12.7 on crates.io (the
  4.12.12 release was GitHub-only), five crates (`redis-connection`, `minimalist-kafka`, `distributed-cache`,
  `sync-over-async`, `opentelemetry-forwarder`) have never been published; `cargo package` succeeds for the leaf
  `platform-macros` and fails for every dependent until its dependency's 4.12.14 is on the index — the expected
  pre-publish state, hence the dependency-first order recorded in continuity.
- **Ruling by Q&A (Eric: "do we need to update the examples' Cargo.toml to include version = ..?"): NO — examples stay
  path-only.** All `examples/*`, `templates/*`, `benchmark/*` and `system/ai-contract-provider` are `publish = false`
  and build only inside the workspace, where cargo resolves the path; a version pin belongs where a manifest leaves
  the tree — the published crates' normal deps (all pinned, swept) and the copy-out templates (pinned, swept). Path-only
  `[dev-dependencies]` in published crates are stripped by `cargo publish`. Pinning the examples would only widen the
  sweep (six files per release) for no effect. Extends [[conv-template-version-sweep-rust]].

- **RELEASED 2026-09-22 02:12:43Z:** tag `v4.12.14` → `2c88fe2b` (workspace version verified at the tag), GitHub release
  published; the Java engine's `v4.12.14` → `e8a8d8e5` published 02:11:55Z. Verified from the repositories; main has no
  commits past either tag. crates.io publication is Eric's next step — commands handed over (`cargo publish --workspace`
  from the clean tag, or the explicit dependency-first sequence); the forwarder's field acceptance follows it.

- **crates.io publish 2026-09-22 (Eric, `cargo publish --workspace` from the tag): 11 of 12 crates published** — cargo 1.98
  packaged and verify-built all twelve, uploaded dependency-first and waited for index propagation on its own; the seven
  older crates moved 4.12.7 → 4.12.14 and `redis-connection`, `minimalist-kafka`, `distributed-cache`,
  `opentelemetry-forwarder` published for the first time. **`mercury-sync-over-async` rejected at upload:** `400 Bad
  Request: "progressive-rendering" is an invalid keyword (keywords must have less than 20 characters)`. Fixed to
  `progressive` on `fix/sync-over-async-keyword` `52cb3b70`, with the `minigraph-state-redis` readme path cargo warned about
  (`../../README.md` while the crate has its own); `cargo package -p mercury-sync-over-async` now passes against the live
  registry incl. the verify build. **PR #309 opened and MERGED by Eric 2026-09-22 02:24Z (`d8336296`)**; branch cleaned after
  the merge-verification gate; `cargo publish -p mercury-sync-over-async` from main, version unchanged, is his final publish step. Eric's question on the `dev` profile lines answered: cargo's local verify build, no effect on what is published.

- **Publication COMPLETE 2026-09-22 ~02:30Z:** the retry of `cargo publish -p mercury-sync-over-async` succeeded after the
  new-crate rate limit (429 at 02:26Z, retry-after 02:27:45Z — five first-time crates within the hour); `cargo search`
  shows all twelve mercury crates at 4.12.14. The v4.12.14 release is complete on both engines: tags, GitHub releases,
  registry. Open afterwards: the forwarder's field acceptance on the released crate (scope version 4.12.14 in the UI —
  [[otel-forwarder-certification]]), the CHANGELOG follow-ups naming all five first-time crates (both repos).

- **CHANGELOG follow-up (Eric's request):** the 4.12.14 entry now names all five first-time crates, the GitHub-only 4.12.12
  (hence 4.12.7 → 4.12.14 on the registry) and the post-tag keyword fix behind the sync-over-async publication (#309,
  `d8336296`) — branch `docs/changelog-4.12.14-crates` `aad0690b`, docs only — **PR #310 MERGED 2026-09-22 02:36Z
  (`89523616`)**; the Java twin `6fe31620` merged as PR #438 (`10eb9e0b`). Branches cleaned after the merge-verification
  gate. With that the v4.12.14 release cycle is closed on both engines.

- **Field acceptance on the PUBLISHED crate (Eric's request 1):** a standalone `otel-accept` app (scratchpad) with registry
  deps only (`cargo tree`: `mercury-opentelemetry-forwarder v4.12.14`, `mercury-platform-core v4.12.14` from crates.io) —
  two clean traces `580cbdc0…`, `5fd1cd3d…` (0/6), negative control `9b25cb38…` 3/3 `HTTP 401`; scope version reads 4.12.14.
- **Two engines, one trace (Eric's suggestion):** the K5 sync-over-async interop with the forwarder on BOTH engines (Java jar
  rebuilt with the dependency, this demo with the crate linked — neither committed), services `mercury-otel-cert-java` /
  `mercury-otel-cert-rust`, a fresh broker per pairing: A Java facade + Rust backend `f78de6d2…`, `72b2e692…` (Java 18 + Rust
  6 spans, 0 failures); B Rust facade + Java backend `ec6b3fc6…`, `3481c84b…` (Rust 16 + Java 8, 0 failures); the Kafka
  record's `traceparent` carries the context across the hop both ways (facade notification → backend `system.of.record`;
  backend notification → facade `soa.reply`). Report Scenarios 6–7 on `docs/otel-report-acceptance-4.12.14`; PR pending.
  **Finding for the JAVA engine (recorded there as an open thread):** Java consumers do not LeaveGroup on SIGTERM — the
  broker fenced every Java member by session expiry ~40 s later while every Rust member `left the consumer group`; the first
  attempt at pairing B timed out twice because the stopped Java facade still held all ten `soa.response` partitions.
  `KafkaFlowAdapter.close()` exists but nothing calls it at shutdown (`KafkaFlowAutoStart` registers no
  `Platform.onShutdown`). Also the Java demo's `SyncErrorHandler` NPEs on a null coordinator during start-up (500 for a
  request that arrives before `Return-route subscriber listening`). Infra torn down, ports asserted closed, temp edits reverted.

- **UI CONFIRMATION of Scenarios 6–7 (Eric's screenshots, 2026-09-22 ~03:00Z):** `72b2e692…` and `47100c7c…` render as ONE
  trace with both services (`mercury-otel-cert-java` root, the `mercury-otel-cert-rust` spans nested under the Java
  notification, the Java `soa.reply` under the Rust notification; 22 ms / 66 ms waterfalls); `580cbdc0…` / `5fd1cd3d…` under
  `mercury-otel-cert` with `accept.api` → `accept.work` + `async.http.response` and the `annotation.acceptance.id`
  attribute. Report confirmations committed on the docs branch — **PR #311 opened and MERGED by Eric 2026-09-22 03:08Z**
  (merge `3a3f87d7`; the Java twin #439 merged as `e51c3353` the same minute); branch cleaned after the merge-verification
  gate; **thread `otel-forwarder-certification` CLOSED.** Eric then asked for the Java fix of the LeaveGroup finding
  (`kafka-consumer-leave-group-on-shutdown`, in progress on the Java side).

## Lessons
- **A stopped member is not a gone member.** Kafka group state outlives a killed process until the session timeout; a drive
  that re-uses a group across pairings must start a fresh broker (or wait out the fence) — and readiness is per component
  (Java: `Assigned partitions` × bindings + the return-route subscriber; Rust: the adapter/coordinator lines + a few seconds).
- **Audit manifest metadata before a crate's first publish** — keywords (≤ 5, each ≤ 20 chars), categories, readme path: cargo
  does not validate them and crates.io rejects at upload, after the ordered run has published everything before the failing
  crate. A ten-line audit script (in the scratchpad) would have caught it; recorded on [[conv-template-version-sweep-rust]].
- **Name the service the reviewer will look under BEFORE the drive** — agree it (or pass it explicitly) with the
  endpoint and the token; the first report pointed the UI check at the example's default service name.

- **`${ENV}` references are resolved ONCE, when the base configuration loads** (`ConfigReader::new_base`
  → `resolve_references`; an unresolvable lone reference drops the key, so `get_property` reads `None`).
  The suite's first late-credential scenario set an env var after boot and got nothing — the live path is a
  runtime override (`overrides::set`, the `-D`/`System.setProperty` analog, consulted first on every
  lookup), which is also the true twin of Java's vault-published system property. Recorded in the guide,
  the README and the new fact.
- **Annotated functions are constructed BEFORE the `before_application` hooks run** (the lifecycle
  collects every inventory entry's factory first), so a hook cannot pre-install state for a preload's
  `Default` — the forwarder resolves its exporter on the first dataset (`OnceLock`). The tell was a doubled
  "credential header resolved" line: two suppliers, two exporters.
- **Marker attributes are matched by single-segment ident** (`is_ident("zero_tracing")`): written bare and
  below the primary attribute they are consumed before name resolution and need NO import; a path-qualified
  `#[platform_core::optional_service(...)]` is not recognized, survives, and then fails as a macro of its own.
- **A drive must assert the port hand-off.** The first drive stopped each leg with SIGTERM and waited one
  second; the graceful shutdown takes longer, so legs B/A′ hit leg A's process (real token, 0 failures) while
  their own processes died on `Unable to bind port 8100` — "datasets logged: 0" was the tell. The redriven
  script waits for the listener pid to change and hard-kills after 10 s (the Redis outage-drive lesson again).
- clippy `err_expect` wants `expect_err`, which needs `Debug` on the Ok type — a `let Err(e) = ... else`
  when the Ok type is not `Debug`; rustfmt splits `.err().expect(` across lines, so a whitespace-tolerant
  regex is needed to fix all sites.

## Memory References
- otel-forwarder-no-sdk (created, tier: working)
- otel-forwarder-certification (created — open thread: the UI confirmation and field acceptance)
- ot-minimalist-kafka-port (consulted — the release gate sequencing behind K5 and this PR)
- schema-registry-native-codec (consulted — the "own codec over async.http.request" precedent this port follows)
- conv-template-version-sweep-rust (applied — the re-cut's sweep re-derived: 13 files / 24 occurrences, not the discarded cut's 15/29; EXTENDED with the first-publish metadata audit)
- eric-release-rhythm-rust (consulted — branch + handoff text; PR-open, merge and tag are Eric's gates)
- otel-forwarder-certification (CLOSED — published-crate and two-engine drives confirmed in the Dynatrace UI; close record written)
