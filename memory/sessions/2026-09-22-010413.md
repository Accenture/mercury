# Session (2026-09-22T01:04:13.532Z)

**Agent:** Claude Code (Fable 5.1)
**Title:** The OpenTelemetry trace forwarder ported (`mercury-opentelemetry-forwarder`) — this engine's own OTLP encoder over the platform HTTP client, opt-in by `otel.forwarding`, certified live against Dynatrace with the A-B-A credential experiment; #306 merged and the v4.12.14 cut moved behind this PR

## Rulings (Eric)

1. "Open-Telemetry forwarder feature is missing in Rust." — the last Java extension this port had deferred
   (port-scope "Deferred", the observability guide's "write your own" note). Ported now, ahead of the shared
   v4.12.14 cut, so the Rust CHANGELOG at that number carries it (the Java side already merged its 4.12.14 PR
   #437, `dbc26f31`, 2026-09-22 00:05Z).
2. "update the examples/hello-flow (the counterpart of Java's composable-example) to import the open-telemetry
   forwarder dependency. Then run enable the open-telemetry with runtime parameter override
   'otel.forwarding=true' so that the traces can be uploaded to Dynatrace. You can use the same '/v1/traces'
   endpoint and the env vars OTLP_AUTH_HEADER, OTLP_API_ENDPOINT and OTLP_TOKEN. I will then check the traces
   in the Dynatrace non-prod dashboard to certify the open-telemetry forwarder feature." — done as stated;
   Scenario 5 of the report is his check.
3. PR #305 ("temporarily closed — when to reactivate?"): never — its head `0da85068` is on main through #303
   (`753eb9fe`) and its branch is deleted, so GitHub cannot reopen it; it stays as history.

## What happened

- **#306 merged** (`8bd5aef9`, the lock-step round P8/P9/#414 on main); the lock-step branch cleaned. The
  `release/v4.12.14` cut prepared right after it (15 Cargo.toml / 29 occurrences, CHANGELOG, lock) was
  DISCARDED uncommitted when ruling 1 arrived — re-cut from main after the forwarder PR merges, the CHANGELOG
  draft (scratchpad) already carries the forwarder entry.
- **The crate** — `extensions/opentelemetry-forwarder`, lib `opentelemetry_forwarder` (Increment 129). A
  `#[preload]` at `distributed.trace.forwarder` (2 workers, `#[zero_tracing]`) under
  `#[optional_service("otel.forwarding")]`; a `#[before_application(sequence = 6)]` hook under the same
  switch validates the endpoint (a non-http(s) URL fails the start naming the key), logs the ready line with
  header NAMES only, installs the shared `Exporter`. `src/span.rs` = `TraceMetricsSpanData` line for line
  (exact W3C ids or skipped; float `exec_time` parsed via its text; an ISO-8601 parser inverting the engine's
  formatter — no date crate). `src/otlp.rs` = the OTLP v1 trace protobuf written by hand (`ProtoWriter`,
  `oneof` members always present, proto3 defaults omitted, `flags` 0x101) + a bounds-checked `ProtoReader`
  for the response's `partial_success` and the test collectors. `src/export.rs` = the request through
  `async.http.request` (`application/x-protobuf`), the Java retry policy (5 attempts, 1 s × 1.5 on transport
  failures and 408/429/502/503/504) and diagnostics (`HTTP 401 - Token Authentication failed | the backend
  rejected the credential itself - check otel.exporter.otlp.headers (...)`; 404 signal-path and 403
  ingest-scope hints; 256-char body bound; header values never logged). **Why no SDK:** the Rust OTLP stack
  (`opentelemetry-otlp` → `opentelemetry-proto` → `prost` + `tonic`) would have been the heaviest dependency
  in the workspace for eight frozen message types with scalar fields; the platform client is the seam the
  schema registry client proved, now against a public TLS endpoint.
- **Tests:** 30 unit (wire-format golden vectors, mapping twins of `TraceMetricsSpanDataTest`, diagnostics
  twins of `ExportFailureDiagnosticsTest`, header parsing, ISO-8601 round trip with `trace::iso8601_utc`);
  `tests/otlp_export.rs` against the in-process collector double (`tests/support/mock_collector.rs`, decodes
  the protobuf with the crate's reader): round trip + credential on both vendor paths, 503→429→200 retried,
  a killed first connection retried, 404/401/403/400 final and diagnosed, the late credential, an invalid
  endpoint refused; `tests/trace_pipeline.rs`: the REAL forwarder registered by configuration alone, a
  traced `fun.1 → fun.2 → fun.3` chain, lineage/resource/scope/credential asserted at the collector.
- **`hello-flow` carries the crate with the switch off** (ruling 2; the composable-example shape):
  dependency, `use opentelemetry_forwarder as _;`, the `otel.*` block over `OTLP_API_ENDPOINT` /
  `OTLP_AUTH_HEADER` / `OTLP_TOKEN` / `OTLP_SERVICE_NAME`, `tests/otel_forwarding_switch.rs`.
- **Certified live against Dynatrace** (`docs/test-reports/otel-dynatrace-certification.md`, commit under
  test `f6e69af5`): `target/debug/hello-flow -Dotel.forwarding=true`, one `GET /api/hello/otel-cert?lang=fr`
  per leg, five spans (`http.flow.adapter` SERVER; `task.executor`, `language.router`, `greeting.composer`,
  `async.http.response` INTERNAL). **A-B-A:** real token 0/5 failures (trace
  `0ad577c2bef646cba174147ebf923c01`, 00:52:04Z); bogus token 5/5 `HTTP 401 - Token Authentication failed`
  (`eedf60cd9bbe47618859a1a7b5967837`, 00:52:15Z); real token 0/5 (`092b2a1947e54f298d5ab945aa901331`,
  00:52:26Z). Two earlier runs also reached the tenant with the real token (5 more traces; the first run's
  legs B/A′ were invalid — see lessons). No endpoint, token or tenant id in any committed file or in this log;
  the drive script redacts both through `awk ENVIRON` (secrets never in argv).
- **Docs:** observability guide §"The OpenTelemetry forwarder (ready-made)" replaces the "not ported" note
  (Rust-port box with the three declared deltas); `otel.*` keys in the configuration reference (and removed
  from its "absent keys" note); port-scope, README, `llms.txt`, mkdocs nav, `files.list` (the report packaged
  in the AI contract); claims `otel-forwarding-default-off`, `otel-forwarder-preserves-w3c-ids`,
  `otel-forwarder-credential-per-export`; the crate README with the Java-vs-Rust table.
- **Gates:** `cargo test --workspace --no-fail-fast` — 111 suites, 590 tests, 0 failures (exit 0); `check-doc-claims` 26 claims OK, `check-llms-links` 37 OK, `mkdocs build --strict` OK,
  `cargo fmt --check` OK, `clippy --workspace --all-targets -D warnings` OK.
- **Branch `feat/opentelemetry-forwarder`:** `f6e69af5` (crate + hello-flow + docs) and `27cbb0d3` (the
  report). Pushed; PR-open is Eric's gate ([[eric-release-rhythm-rust]]); handoff text in the scratchpad.

## Lessons

- **`${ENV}` references are resolved ONCE, when the base configuration loads** (`ConfigReader::new_base`
  → `resolve_references`; an unresolvable lone reference drops the key, so `get_property` reads `None`).
  The suite's first late-credential scenario set an env var after boot and got nothing — the live path is a
  runtime override (`overrides::set`, the `-D`/`System.setProperty` analog, consulted first on every
  lookup), which is also the true twin of Java's vault-published system property. Recorded in the guide,
  the README and the new fact.
- **Annotated functions are constructed BEFORE the `before_application` hooks run** (the lifecycle
  collects every inventory entry's factory first), so a hook cannot pre-install state for a preload's
  `Default` — the forwarder resolves its exporter on the first dataset (`OnceLock`). The tell was a doubled
  "credential header resolved" line: two suppliers, two exporters.
- **Marker attributes are matched by single-segment ident** (`is_ident("zero_tracing")`): written bare and
  below the primary attribute they are consumed before name resolution and need NO import; a path-qualified
  `#[platform_core::optional_service(...)]` is not recognized, survives, and then fails as a macro of its own.
- **A drive must assert the port hand-off.** The first drive stopped each leg with SIGTERM and waited one
  second; the graceful shutdown takes longer, so legs B/A′ hit leg A's process (real token, 0 failures) while
  their own processes died on `Unable to bind port 8100` — "datasets logged: 0" was the tell. The redriven
  script waits for the listener pid to change and hard-kills after 10 s (the Redis outage-drive lesson again).
- clippy `err_expect` wants `expect_err`, which needs `Debug` on the Ok type — a `let Err(e) = ... else`
  when the Ok type is not `Debug`; rustfmt splits `.err().expect(` across lines, so a whitespace-tolerant
  regex is needed to fix all sites.

## Memory References
- otel-forwarder-no-sdk (created, tier: working)
- otel-forwarder-certification (created — open thread: the UI confirmation and field acceptance)
- ot-minimalist-kafka-port (consulted — the release gate sequencing behind K5 and this PR)
- schema-registry-native-codec (consulted — the "own codec over async.http.request" precedent this port follows)
- conv-template-version-sweep-rust (consulted — the discarded release cut; re-derive at the re-cut)
- eric-release-rhythm-rust (consulted — branch + handoff text; PR-open, merge and tag are Eric's gates)
