# Session (2026-09-25T19:54:22.000Z)

**Agent:** Claude Code

## Summary

**v4.12.18 release prep — the lock-step twin of Java 4.12.18 (Eric's ask before his break).** Branch `release/4.12.18` off
main `0ae374a7`: 13 `Cargo.toml` manifests / 24 occurrences of `4.12.17` → `4.12.18` (re-derived by grep —
[[conv-template-version-sweep-rust]]) plus `Cargo.lock` via `cargo update --workspace` (24 entries, all workspace crates, no
third-party version moved). CHANGELOG entry from `git log v4.12.17..HEAD` — the WHOLE tag range: Increment 141 (#330, the Java
#462 twin: graph.math typed and finite, `CONDITION`, the READ note from [[graph-math-typed-arithmetic-rust]]) and Increment 140
(#329, the two timing-sensitive tests hardened, test-only); lock-step notes: Java's #463 Snyk dependency bumps have no analogue
here (`cargo audit` green). Twelve publishable crates (`cargo metadata`). Gates on the branch: fmt-check, clippy, `cargo audit` (320 crates, no advisory), claims 29/29, links 37/37, mkdocs strict, `cargo publish --workspace --dry-run` (twelve crates packaged) all green; `cargo test --workspace` 603 passed / 0 failed / 9 ignored (117 suites); `cargo test
--workspace` ran after the Java readiness build finished (shared ports). Release rhythm unchanged ([[eric-release-rhythm-rust]]):
Eric opens the PR, merges, tags one memory commit past the merge, publishes the GitHub release with the CHANGELOG entry as its
body and the crates from the tag.

## Advisory sweep at the seam

`memory-lint` raised `[overdue]` (sslu 21 > `archive_window` 20) once this log entered the window; archived with `archive-fact` (retrievable via `memory/archive/INDEX.md`): rest-skip-unregistered-and-root-fallback-rust. Not a cadence review — no other fact moved.

## Memory References

- conv-template-version-sweep-rust (consulted — the manifest sweep and the lock refresh)
- graph-math-typed-arithmetic-rust (consulted — the READ note for the Changed item)
- eric-release-rhythm-rust (consulted)
