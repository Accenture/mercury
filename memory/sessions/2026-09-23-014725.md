# Session (2026-09-23T01:47:25.000Z)

**Agent:** Claude Code

## Summary

**Release v4.12.15 shipped** — PR #322 (Eric) merged as `87ee371f` at 01:21Z, tag `v4.12.15` → `28cd3328`, GitHub release
01:36:57Z (body = the CHANGELOG entry); the workspace reads 4.12.15 at the tag; the `rust` workflow green on main; the
release branch gone locally and remotely after the merge gate. Lock-step the same hour: Java #451 (`aafeff04`, tag
`b705e9ff`), mercury-python #37 (`95101575`), mercury-nodejs #105 (`13426732`). The crates.io publication had not started at persist time — Eric's step; the command was
handed over (`cargo publish --workspace`; the crate sources on main are identical to the tag, memory commits aside).

## Seam close (the v4.12.15 survey's fold-ins d and e on this repo)

- `latest_release` → v4.12.15; the v4.12.14/4.12.12 narratives condensed to Prior pointers.
- [[ot-minimalist-kafka-port]] CLOSED — K1–K5 shipped in v4.12.14 (its tail still said crates.io waits for K5).
- [[otel-forwarder-no-sdk]]: "Eric's Dynatrace lookup is the remaining gate" → confirmed the same day, and it found the
  broken trees that [[connected-edge-spans]] fixed.
- `refresh-metadata` run (the lint's skipped re-tier) — 19 footers refreshed (8 active→archive-candidate — playground-session-broker, for-each-suspend-index-key-rust,
  fork-join-awaits-on-calling-task, l1-caller-checks-reply-status-rust, rest-error-body-standard-shape,
  typed-function-envelope-reply, rest-skip-unregistered-and-root-fallback-rust, static-decision-table-is-graph-data-rust;
  4 working→active — schema-registry-native-codec, otel-forwarder-no-sdk, connected-edge-spans, redis-restart-aware-retry;
  ot-sync-over-async-port archive-candidate→active, ot-minimalist-kafka-port working→active); lint 0 errors, 0 warnings.
  Not a review: 9 sessions since 2026-09-21 (cadence 10) and 34 live facts (cap 30 counts decaying facts — lint raised no
  `[continuity-bloat]`).

## Method note

- A count that comes out of a `tail` is not a count: the release queue's inline `grep | tail -40` cut the `test result:`
  lines, and "467 tests" went into a merged PR description; a full `cargo test --workspace` captured whole is 603 passed,
  0 failed, 9 ignored (117 test runs). Capture whole, filter afterwards.

## Memory References

- referenced: redis-restart-aware-retry (shipped), connected-edge-spans (shipped), otel-forwarder-no-sdk (wording
  corrected), conv-template-version-sweep-rust, schema-registry-native-codec
- closed: ot-minimalist-kafka-port

## Next

- Eric: `cargo publish --workspace` from the tag (12 crates) — recorded here when visible on crates.io. Then the AI
  SDLC/MCP backlog resumes (Java-side `bp-agent-orchestration`). Follow-up carried in the Java repo's
  `post-4-12-15-carry-overs`: `minigraph-state-redis` onto the foundation's lifecycle; the `async.http.response` graph-run
  record's `from`.

Commit: `git add memory/ && git commit -m "session 2026-09-23 [Claude Code]"`
