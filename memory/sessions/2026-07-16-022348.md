# Session (2026-07-16T02:23:48.000Z)

**Agent:** Claude Code

## Summary

**Implemented increment 9 — the lightweight RPC inbox + benchmark-reporter — and CLOSED
the platform-core milestone** (`bp-platform-core` → `[x]`; Design §5h). Maintainer's
framing: Rust is compiled with ownership-based memory management (no GC pause); benchmark
platform-core because event-script and knowledge-graph ride on it.

**Part 1 — lightweight RPC inbox (Java `AsyncInbox` parity), pulled forward from §7:**
identified before benchmarking that `PostOffice::request` registered a full route per RPC
(manager task + worker + elastic queue) — the benchmark would have measured that throwaway
machinery, not dispatch. New `src/inbox.rs`: a one-shot correlation-map entry
(`inbox.<uuid>` → oneshot sender); the route worker's reply delivery recognizes the
`inbox.` prefix and completes the oneshot directly — the reply bypasses the ServiceQueue
machinery exactly like Java; late replies after timeout drop silently. All 101 prior tests
passed unchanged (same API, same semantics).

**Part 2 — `benchmark/benchmark-reporter`** (new workspace member; the Java module ported
faithfully): the same six-scenario suite — RPC 1→C / C→C / paced callback (normal),
RPC 2C→C / callback flood (overload), latency probe under background flood (mixed
isolation) — same `Stats` (nearest-rank percentiles, log-spaced histogram bins, population
stddev), same self-contained HTML report (inline SVG histogram + percentile plot, same CSS
so records sit side-by-side with the Java `analysis/` snapshots). Parameters are
`-Dbench.*` runtime args with Java-parity defaults; `build.rs` embeds the rustc version
for the Environment block, which also records "ownership / no GC, no GC pause". Doc'd
divergence: paced scenarios ride tokio's ~1 ms timer vs Java's `parkNanos` — the paced row
is rate-limited by design; compare latency there, not throughput.

**The record** (`analysis/rust-tokio.html`, defaults: 200k ops/scenario, C=50, 256-byte
payload; Apple Silicon 12-core, release build) vs the Java **file-vthread** record (same
machine class, JDK 21):

| scenario | Java | Rust |
|---|--|--|
| RPC 1→50 | 18.5K ops/s, 54µs mean | **155K ops/s, 6µs mean (8.4×)** |
| RPC 50→50 | 179K ops/s | **411K ops/s (2.3×)** |
| RPC 100→50 (overload) | 69.6K | **101K (1.4×)** |
| Callback flood | 73.8K | **99.9K (1.4×)** |
| Probe under flood | 157µs mean / 1.62ms max | **17µs / 210µs (~9×)** |

1,003,000 timed operations per run, **0 failures both runtimes** — the FIFO elastic
back-pressure behaves identically (spill → drain → recover), just faster; and with no GC
the p99.9+ tails stay near the median (every scenario's max 2–47× lower). Analysis
narrative: `benchmark/benchmark-reporter/analysis/README.md`.

**Verification:** **103 tests** (2 new Stats unit tests; all prior suites unchanged
through the inbox rewrite) · clippy + fmt clean · smoke run + full default run executed.
Docs: design §5h (+§7 trim), `INCREMENTS.md` row + section 9 + milestone banner,
module README + analysis README.

**Milestone state:** platform-core is configured, evented, back-pressured, lifecycled,
observable, HTTP-serving, operable — and measured. Remaining §7 items are enhancements to
fold in as event-script needs them, not blockers.

> **also (maintainer question: "did you implement the TemporaryInbox pattern?"):** precise
> answer given after reading Java `TemporaryInbox.java`/`InboxCorrelation.java`/
> `EventEmitter` wiring — the Rust port implements the **correlation-map half** (unique
> `inbox.<uuid>` → oneshot, the `InboxBase.getHolder` analog) but not the shared
> `temporary.inbox@origin` route: replies complete at the delivery boundary, and the
> business cid rides through untouched (Java swaps in a sequenced cid and restores it via
> InboxCorrelation — only needed for fork-n-join multi-inboxes, deferred §7). **One real
> gap found and fixed in the process:** manual replies via `po.send(reply_to)` (the
> `@EventInterceptor` pattern) would have 404'd — `Platform::deliver` now resolves `inbox.`
> ids, making inboxes uniformly addressable (what event-over-HTTP will need). New
> `manual_reply_reaches_the_rpc_inbox` test; divergences doc'd in `inbox.rs` + design §5h.
> **104 tests, clippy/fmt clean.**

## Memory References

Updated:
- `bp-platform-core` — **marked done** (milestone closed; §7 leftovers noted as
  fold-in-as-needed)
- `ot-design-platform-core` — increment 9 recorded
- Project State status (milestone banner + headline numbers)

Referenced: `vision-mercury`, `platform-core-stack`, `elastic-fifo-no-bdb` (the benchmark
exercises the spill), `port-bottom-up-faithful`, `conventions-rust-baseline`,
`inv-never-couple-functions`
