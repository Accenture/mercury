# Session (2026-07-17T00:12:41.000Z)

**Agent:** Claude Code

## What happened

Maintainer committed increment 18, directed proceed → **Increment E-8 (repo
increment 19) — the complete plugin catalog + `#[simple_plugin]`, SHIPPED.**

1. **23 remaining built-in bodies** ported faithfully (all 42 now execute):
   arithmetic on whole numbers (`promoteNumber` promotion incl. numeric strings;
   divide-by-zero guards on divisors only — a zero dividend is valid, Java comment
   preserved), `now` (iso/local/ms), `dateTime` (Java-pattern formatting via a small
   yyyy/MM/dd/HH/mm/ss → chrono converter — the tokens flow files use; zone arg not
   ported, no fixture uses it), `gt`/`lt`/`ternary`, case-insensitive string
   operators with list-membership `includes`, `parseDate`/`parseDateTime`
   (NaiveDate/NaiveDateTime + local-midnight → ms/local/iso outputs), list-of-map
   normalization + column merge (`listOfMap`/`updateListOfMap` with the
   equal-length-list validation), `removeKey` (map + list-of-map forms),
   `uniqueSet` (order-preserving), `defaultValue`, and the full `validate` rule
   engine (String/Integer/Long/Float/Double/Boolean/Map/List type checks — Integer
   and Long both map to rmpv Integer, doc'd; required/evaluate modes; range checks
   with Java's exact messages). Bodies live in `plugins_e8.rs` (include! keeps
   plugins.rs navigable). New dep: chrono.
2. **`#[simple_plugin]`** — new `crates/event-script-macros` proc-macro crate (the
   Java `@SimplePlugin` analog): annotate `fn(&[Value]) -> Result<Value, String>`;
   name defaults to camelCase of the fn name (Java's class-name convention),
   `name = "..."` overrides. Emits an inventory submit of
   `plugins::SimplePluginEntry`; the **`SimplePluginLoader`**
   `#[before_application(sequence = 3)]` collects entries — the exact Java loader
   slot, before CompileFlows at 5, so user plugin names pass compile-time `f:`
   validation.
3. **Two parity fixes found by fixtures**: (a) plugin errors now propagate
   UNWRAPPED (Java only wraps the plugin-not-found case) — input-validation-2
   asserts the exact "user (ABC) < CCC" message reaching the exception handler;
   (b) the type-conversion fixture's response body carries REAL bytes
   (`f:binary` → Value::Binary — the E-2/E-3 rmpv byte-fidelity decision paying
   off), so the test asserts on the rmpv tree, not a JSON view.
4. **Fixtures activated E2E**: arithmetic (sum 11 / difference 1 / product 12 /
   quotient 3 / inc 7 / dec 5), type-conversion (the whole catalog incl. b64
   round-trip + binary bytes), string-util, parse-date, input-validation-1 (pass) +
   -2 (400 range violation); plus the user-defined `shout` plugin registered via
   the macro, confirmed in the registry, and resolved through `f:shout(model.word)`.

**148 workspace tests, 0 failures; clippy 0; fmt clean.** Docs: ES design §5h;
INCREMENTS row + section 19.

## Memory References

- referenced: `ot-design-event-script` (E-8 shipped), `bp-event-script`,
  `vision-mercury`, `conventions-rust-baseline`
- created: (none)
