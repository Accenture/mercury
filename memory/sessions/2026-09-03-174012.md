# Session (2026-09-03T17:41:30.000Z)

**Agent:** Claude Code

## What happened

Follow-up to the session-broker contribution (previous log `2026-09-03-172834.md`), at Eric's
direction: implemented the AI-docs improvements proposed in `ai-enabled-repo-demo`'s feedback
items 9–10, in the guide sources under `docs/guides/` (mapped into the ai-contract-provider's
`references/` by `build.rs`).

1. **`knowledge-graph/ai-agent-guide.md`** — three additions, mirroring the Java repo's edits
   with Rust-specific grounding:
   - Rules of engagement reworded: "single operator" → turn-taking; explicit statement that
     **session sync is symmetric** (every command except `session` topology commands propagates
     to primary and all subscribers — equal co-authorship).
   - New **"Hosting the session yourself" {#hosting}** section: agent-as-host topology, the WS
     handshake (`/ws/graph/playground`, welcome frame, `session ws-NNNNNN-N started`, 20s
     ping/pong), restart caveat, and the `scripts/playground-session-broker.mjs` reference.
   - New **"Scaffolding a project from the example" {#scaffolding}** section: boilerplate
     manifest for a `minigraph-playground`-derived project (`Cargo.toml`,
     `resources/application.yml`, `rest.yaml`, `graphs.yaml` + models, `src/main.rs`; noted the
     `graph-executor` flow is engine-provided — no `flows.yaml` here, unlike Java) and the two
     named `rest.yaml` profiles — `headless-minimal` (`http.flow.adapter`,
     `post.companion.command.sync`, `get.live.graph`) and `playground-enabled` (adds
     `get.index.html`, `get.ws.html`, `show.graph.model`, `upload.json.content`,
     `upload.mock.content`, `inspect.state.machine`); mock routes marked example-specific.
     At Eric's follow-up request the two profiles are also rendered as a **copy-paste
     `rest.yaml` boilerplate block** (full entries with per-route comments, Rust flavor — no
     cors/headers refs, 40s companion timeout), with the operator-facing upload dialogs called
     out: `upload.json.content` = the console `upload` command's paste-a-JSON dialog,
     `upload.mock.content` = the `upload mock data` dialog.
2. **`event-driven/ai-agent-guide.md`** and **`event-script/ai-agent-guide.md`** — new
   "Scaffolding a project from the example" sections grounded in `examples/hello-world` and
   `examples/hello-flow` (file manifests; minimal `rest.yaml` = your own routes only / one
   `http.flow.adapter` entry per endpoint).
3. `playground-and-companion.md` needed **no** change — this repo's copy already documents the
   two-way mirror explicitly (the Java repo's copy did not and was fixed there).

## Memory References

- Referenced: playground-session-broker
