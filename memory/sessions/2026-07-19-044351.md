# Session (2026-07-19T04:43:51.000Z)

**Agent:** Claude Code

## What happened

**Post-tut-6 AI-grammar hardening (maintainer-directed): findings #17–#21 fixed + the new #22
island mandate** — the same rhythm as the post-tut-5 #9–#13 sweep, engine-verified before
documenting.

- **Engine verification first (`fetcher.rs`):** (a) `for_each` aggregation is **deterministic** —
  requests build in input order, batches (`chunks(concurrency)`) execute in order, responses join
  in request order, `result.{key}` values `[]`-append per response ⇒ the aggregated array
  preserves the source list's order; documented as a guarantee. (b) `output[]` is **optional**
  for `graph.api.fetcher` — only `dictionary[]` is hard-required (`input[]` conditional on the
  dictionaries' parameters); the AI matrix was wrong (and the engine help contradicts itself —
  noted for the help-rewrite backlog #16).
- **Grammar fixes:** `command-reference.md` — new **Iterative fetching (`for_each`)** section
  (cross-node array source, `model.*` wiring, `concurrency`, aggregation guarantee, worked
  example), a second **POST-body Provider** worked example, corrected skill-matrix row, and a
  **non-constant source forms** table (`f:plugin(...)` + `$.` JSONPath — code-confirmed valid in
  graph mappings via the shared Event Script engine). `minigraph-commands.json` — `for_each`
  object, corrected required/conditional/optional, `_non_constant_sources`. `skills-reference.md`
  — fetcher for_each/aggregation notes. `llms.txt` map updated. JSON + links validated.
- **NEW #22 — `graph.island` is REQUIRED (maintainer decision):** islands are isolated from
  traversal but link data entities and dictionaries into the graph's **entity-relationship
  diagram** — the graph is *living documentation of enterprise knowledge* (a new joiner discovers
  domain knowledge from the connected dictionaries/entities). The AI grammar now mandates **no
  node left unconnected**: new `command-reference.md#island` section (rationale + wiring
  `root -[contains]-> island -[data]-> dictionary -[provider]-> provider`), invariants/node-types/
  provider-dictionary reworded, `graph.island` rewritten in `skills-reference.md`, pre-send
  checklist item + recipe step in `ai-agent-guide.md`, `minigraph-commands.json` role/invariants/
  island notes. (Trigger: the tut-6 companion left its four config nodes floating — the old docs
  said "optionally group".)
- **Rollup updated:** #17–#21 → DONE (with engine-verified notes); #22 added as DONE.

## Decisions

- **The island convention is a documentation-level requirement** (the engine still tolerates
  orphan config nodes at export; the grammar mandates the wiring) — knowledge discovery is a
  first-class purpose of the graph, per the maintainer's rationale.
- `for_each` ordering documented as a **guarantee** (not an observation) after code verification.

## Memory References

- ot-companion-validation-sweep — substance updated (findings #17–#22 resolved; tutorial-7 next)
- ot-help-pages-rewrite — referenced (engine help's self-contradiction on `output[]` + the
  island-policy update belong to the help rewrite)
- port-bottom-up-faithful — referenced (engine-truth verification before documenting)

Co-Authored-By: Claude Code <noreply@anthropic.com>
