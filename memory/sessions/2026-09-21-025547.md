# Session (2026-09-21T02:55:47.000Z)

**Agent:** Claude Code
**Title:** Guide parity round from the Java W2 probe (R1–R5) — branch `docs/w2-parity-r1-r5`, PR pending

## Summary

Eric asked whether the Java engine's W2 guide-sufficiency fixes (mercury-composable #434, merged
2026-09-21 as `940994d5`) apply to this port, then approved the branch for the five items that do.
Each Java change was checked against this port's guides and, where a claim depends on engine
behaviour, against the source (`automation/server.rs`, `post_office.rs`, `actuator.rs`,
`knowledge-graph/src/common.rs`). Branch `docs/w2-parity-r1-r5`: `8d69b61f` (engine message wording +
`greeting.api` guard), `1b11d45b` (docs + Increment 125). PR [#298](https://github.com/Accenture/mercury/pull/298) opened by Eric 2026-09-21; merge is his gate.

- **R1** `event-driven/ai-agent-guide.md` — the REST-wired worked example read `body["name"]` from the
  raw envelope; `build_event` delivers the whole AsyncHttpRequest map as the body and copies no HTTP
  header into the event headers, so the documented `Hello, Mercury!` would have run as `Hello, world!`.
  Now a `TypedFunction<AsyncHttpRequest, serde_json::Value>` reading the query parameter or the body
  (`query_parameter` returns `Option<String>`, `path_parameter` `Option<&str>` — checked before
  writing doc code), the `headers` bullet corrected (HTTP headers live in the request or a flow's
  `input.header.*`), the free-form pattern's caveat, a checklist item, the GET curl.
- **R2** `rest-automation.md` — "Replying with a status and headers", from `server.rs` 804–900: status
  → HTTP status; headers minus `PROTECTED_METADATA` (`my_route`, `my_trace_id`, `my_trace_path`, the
  correlation id…), `x-stream-id`/`x-ttl` withheld, `content-type` overrides Accept negotiation,
  `set-cookie` split on `|`, then `headers.response`, the cid echo, CORS; a failing function's text body
  renders as the standard `{status, message, type: error}` unless it already looks like JSON/XML.
- **R3** the `graph.task` output-source rule on `skills-reference.md`, `help graph-task.md`
  (byte-identical with Java — diffed empty) and the commands JSON note. **The engine enforced the rule
  all along** (`set_fetcher_output_entry`: constant, `f:` plugin, `result*`, `model.`, the node's own
  namespace); its two errors now carry the Java #394 wording — quote `'lhs -> rhs'`, name the node, give
  the `input.*` clue — replacing the stale "API fetcher"/"data dictionary" labels. No test pinned the
  old text.
- **R4** `event-script/syntax.md` — a JSON array body becomes a list (`ParsedBody::Json` → `to_value`),
  pointing at "Special consideration for Lists as input".
- **R5** `examples/hello-world` `greeting.api` checks `has_error()` before `body_as()` (`request()`
  returns an error reply as `Ok`; only a timeout is `Err` 408) — the twin of the Java `hello.rpc` demo;
  the tutorial's two snippets follow the source and section 4 says why; `api-overview.md` points at it.
- Not applicable here, verified: `/health` shape and the info/health contract (`actuator.rs` accepts a
  string or object message), `Vec<T>` input, the `#[zero_tracing]` example, `location.graph.temp`, the
  cache guide's Layer 3 snippet, the skills count, the envelope reference's header wording.

**Method note (the durable half).** My first pass concluded "the Rust engine has no such gate" because
I grepped for Java's REWORDED message; the twin carried the older wording. Grep for the mechanism
(the function, the namespace constants, a fragment that predates any rewording), and prove an absence
by reading the twin's path before writing "no X" anywhere durable. A stale message on the twin is a
finding, not evidence of absence.

## Verification

`cargo fmt --check`; `cargo clippy -p hello-world -p mercury-knowledge-graph --all-targets -- -D
warnings`; `cargo test -p mercury-knowledge-graph -p hello-world` (19 unit + 33 integration tests in
knowledge-graph, hello-world's `event_api_auth`); `mkdocs build --strict`; `check-llms-links` (35
references); `check-doc-claims` (21 claims).

## Doc Gaps

(none — the round IS the guide fix; source was read to verify guide claims, and each verified claim
went into the guide)

## Memory References

- conv-declare-consulted-references-rust (edited — second instance recorded at this review's archival check)

## Memory Review (2026-09-21, through 2026-09-21-025547)

- **Trigger:** cadence — 10 sessions since `2026-09-17-004239.md` (`review_every` 10). Window: the ten logs after it.
- **Events applied:** none new — the window's closures (the 2026-09-17 closure gate) were recorded in their own logs.
- **Re-tier (refresh-metadata, 14 footers):** → `active`: for-each-suspend-index-key-rust,
  redis-connection-foundation-rust, distributed-cache-rust, rest-error-body-standard-shape,
  rest-skip-unregistered-and-root-fallback-rust, conv-template-version-sweep-rust (from archive-candidate, see below);
  → `archive-candidate`: the six closed threads bp-foundation-to-ui, bp-kafka-connectors-backlog,
  ot-close-stalled-threads-20260917, ot-cratesio-followups, ot-sync-over-async-port,
  thread-reverify-invariants-20260917 (sslu 9–15); uses only: playground-session-broker, ot-minimalist-kafka-port.
- **Archived: 0.** Near-miss: `conv-template-version-sweep-rust` read sslu 25 (> archive_window 20) and the first
  pass moved it to `2026-Q3.md`. Step 6's verification found the convention APPLIED on 2026-09-20 — the v4.12.12
  catch-up release swept the templates' Cargo manifests and READMEs — with no declaration in that log: an
  undeclared use, the failure `conv-declare-consulted-references-rust` describes. The archive move was reverted in
  the working tree before any commit, the declaration was added to my own `2026-09-20-152809.md` (same working
  session), and the refresh re-run: `last_used` 2026-09-20, tier `active`, uses 4.
- **Swept: 0** — no closed thread is older than the 20-session window.
- **Archive verify:** nothing archived; the reverted move left `2026-Q3.md` and `INDEX.md` untouched (git-clean).
- **Invariants:** not due — 10 of 40 sessions since `2026-09-17-004239.md`.
- **Stalled threads:** none — `ot-minimalist-kafka-port` is the only unchecked thread and was referenced this window.
- **Stamp:** `last_review` 2026-09-21 | through 2026-09-21-025547.md.
