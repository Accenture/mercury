# Session (2026-09-25T00:44:18.000Z)

**Agent:** Claude Code

## Summary

**Two timing-sensitive tests hardened after the v4.12.17 cycle's CI flakes (Eric's ask; Increment 140); branch
`fix/flaky-test-hardening`.** Test-only — `mercury-redis-test-double` is `publish = false` and the other changes are test
files. (1) `BounceProxy::bounce()` / `refuse()` are now `async` and return once the severing has happened: `bounce()`
aborts every relay task and awaits each handle (an aborted task resolves once dropped, its sockets with it); `refuse()`
signals the acceptor, awaits its exit — the listener is gone and no link can be added behind the severing — then bounces.
They were fire-and-forget (`abort()` and a watch `send()` schedule the work), which is why main's `rust` run on the
memory-only push `adc7dc21` failed `store_bounce_recovery::retry_is_single_and_bounded_under_a_full_outage` with
`expect_err` meeting `Ok(())` — the save had travelled the not-yet-severed link — while the identical tree had passed at
`7d5fbc17`; 60 local runs of the old double never failed (the race needs a starved runner). Eleven call sites in
`redis-connection` and `sync-over-async` gain `.await`. (2) `kafka_shutdown`'s bounded-close half established its
precondition — one stranded record queued against `127.0.0.1:1` — with a spawned `publish()` polled by `wait_for` and
asserted nothing about it; the v4.12.17 release PR #328's first `test` job reached the close with nothing queued and it
truthfully reported 0 undelivered (expected 1). Not reproduced locally (0 failures in 30 runs alone + 10 with its two
siblings), so the one timing-dependent step is removed rather than tuned: the record is enqueued with
`FutureProducer::send_result` on a clone of the publisher's producer and the precondition is asserted before the close.
**Lesson (INCREMENTS 140, and my personal memory):** a double that schedules a severing and returns is a flake waiting for
a slow machine — await the effect the test's next step asserts about. Continuity is at its 600-line cap and gains no fact
here; the lesson lives in the ledger for the next review to absorb if it wants a fact.

## Verification

Rebuilt shutdown test green once (2.24 s), 20 / 20 alone and 5 / 5 with its siblings; both bounce binaries green once and
20 / 20 each; `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` clean;
`cargo test --workspace` **603 passed / 0 failed / 9 ignored** (117 suites); `cargo audit` clean; `check-doc-claims`
28/28, `check-llms-links` 37/37.

## Merged

Eric opened PR #329 and merged it at 00:50:51Z as merge `66283468` (title clean); checks green on head `27532195` — `test`
3m51s, `audit` 2m38s, `build` 18s, memory (docs deploy skipped on a PR). Gate verified (state MERGED, `66283468` an ancestor of
`origin/main`), local branch deleted, tree clean. Main's runs on the merge commit — `rust`, `docs`, `agent-memory` — all
succeeded before this memory push. The two flakes that surfaced during the v4.12.17 cycle are fixed on main; the night's
sequence is closed on both engines.

## Memory References

(none — no fact consulted or changed; the durable lesson is recorded in docs/INCREMENTS.md Increment 140)
