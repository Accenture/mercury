# Session (2026-09-09T23:41:02.000Z)

**Agent:** Claude Code

## What happened

**Webapp lock-step for the Java PR #341 arc** (branch `feature/webapp-lockstep-341`;
Java side merged as `3aaa24b0` + housekeeping branch `chore/webapp-housekeeping-post-341`).
Wholesale rsync of the Playground webapp from the Java repo, carrying: the Help
profiles + minimap + graph-run controls forward-port (contributor PR by
JiahaoAccenture, agent: Codex), the post-review UX round (in-place "Mock Graph
Input" panel replacing the modal — zero modals again; minimap as a draggable
floating island, promo hint removed; JSON-Path payload-only; left-panel width
defaults console 40% / node editor 30% / mock input 30%), the fix round
(versioned `-panel-split-v2` layout key; session live-graph restore via
`GET /api/graph/session/{id}` when the pinned temp-model path is a dead end),
the nanoid 3.3.18 audit fix, and the restored `vendor-router` chunk matcher.

**Sync convention amended** (fact updated): `helpContent.ts` and
`localHelpCommand.test.ts` now carry real feature content (help profiles), so
they are PORTED — take the Java file, re-apply the Rust path lines — instead of
preserved wholesale; `vite.config.ts` same treatment (fs-allow comment). The
engine `resources/help/help.md` prose is deliberately divergent (Rust-adapted,
graph.js retired): the Java Keyboard-shortcuts section (5 lines, Ctrl+M /
Ctrl+backtick) was ported INTO it, not copied over it. Preserve-wholesale
survivors: deploy.js/clean.js, graphTransformer.test fixture imports,
COMPANION_ENDPOINT_README's graph.js line.

Gates: `npm audit` 0 vulnerabilities, typecheck clean, **324/324 vitest**
(matching the Java suite exactly), `npm run release` bundle committed
(`index-CKkgr39d.js` — hash differs from Java's legitimately: the help
markdown is glob-inlined and the prose differs; `vendor-router-DrFk6dZO.js`
byte-identical). Live smoke: the prebuilt 4.12.5 `minigraph-playground`
release binary served the fresh bundle from `resources/public` (new index
hash + vendor-router HTTP 200) — filesystem serving needs no engine rebuild.

Engine follow-up queued from the Java side (lock-step item when picked up):
`GET /api/graph/model/{session}/{id}` answers 200 with the current draft for a
bogus id — Eric ruled it must 404; Java + Rust twins together.

## Memory References

- webapp-lockstep-sync-preserved-paths (consulted + updated — the sync ritual
  followed and amended with the porting convention)
- conv-declare-consulted-references-rust (consulted — this declaration habit)
