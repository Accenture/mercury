# Session (2026-07-16T23:02:51.000Z)

**Agent:** Claude Code

## What happened

Maintainer committed increment 15, directed proceed → **Increment E-5 (repo
increment 16) — parallel + fork/join, SHIPPED.**

1. **`parallel`**: fan out to every next task — no barrier (Java parity: the canonical
   parallel-test fixture converges through model state + a shared-counter decision;
   ported `ParallelTask` with its 200 ms straggler sleep).
2. **`fork`/`join`**: `FlowInstance` gained `pipe_counter` + `pipe_map`
   (`PipeInfo::Join(JoinTaskInfo{forks, join_task, result_count})` — the E-6 pipeline
   variant will extend the enum). Branch callbacks carry `uuid#seq`; the barrier
   increments under the pipe-map mutex and the LAST branch fires the join task.
   Fall-through preserved (seq with no pipe entry → normal dispatch, Java parity).
3. **Dynamic `source`**: a single branch replicates per model-list element;
   `<source>.ITEM`/`<source>.INDEX` pseudo-keys resolve in the input mapping when the
   normal lookup misses (Java `getInputDataMappingLhsValue`); a non-list source is a
   400 abort with the Java message shape.
4. **Exception cleanup**: Java-exact — a task with its own exception handler clears
   only its pipe entry; otherwise ALL pipe queues clear before the generic handler.
5. **Fixture strategy**: parallel-test + fork-n-join-test run verbatim (new ported
   task functions: `parallel.task`, `begin.parallel.test`, `exception.simulator`).
   The canonical dynamic-fork fixture needs `flow://` + `ext:` (E-7), so a
   clearly-marked **Rust-side supplement** (`tests/resources/flows-rust/`
   + `rust-flows.yaml`, documented in the fixtures README) covers `.ITEM`/`.INDEX`
   and concurrent `[]` appends until E-7; the compiler parity test's expected
   loaded-set gained `dynamic-fork-test` (now 66 flows).
6. **Concurrency note**: increment 15's direct execution makes forked callbacks
   genuinely concurrent — the sorted-set assertions on `model.serialized[]` /
   `model.index[]` prove the dataset-mutex append path under real parallelism.

**Scenarios added to flow_runtime**: fork/join happy path (`*` pojo + both branch
keys), failing branch → 400 abort, parallel convergence, dynamic fork (3 items, each
ITEM/INDEX exactly once). **148 workspace tests, 0 failures; clippy 0; fmt clean.**
Docs: ES design §5e; INCREMENTS row + section 16.

## Memory References

- referenced: `ot-design-event-script` (E-5 shipped), `bp-event-script`,
  `vision-mercury`, `conventions-rust-baseline`
- created: (none)
