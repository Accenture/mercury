# Session (2026-09-03T19:03:00.000Z)

**Agent:** Claude Code

## What happened

Guide refinements to `docs/guides/knowledge-graph/ai-agent-guide.md`, mirroring the Java repo's
edits, distilled from two fresh-agent live builds run today in `ai-enabled-repo-demo` (the Rust
round: branch `layer-3-live-rust` there — 42/42 companion commands first send, 8/8 conformance,
scaffolded from this repo's `examples/minigraph-playground` per the new scaffolding section, with
a human subscribed to the broker-hosted session throughout):

1. **Export step (recipe step 7) rewritten:** export is file-based to `/tmp/graph/{name}.json`;
   delete any stale file first, verify fresh content after. This engine's overwrite guard fails
   loud with an actionable message — the correct behavior (the Java 4.12.x engine noops silently;
   divergence filed as feedback item 11 in the demo repo's feedback doc for upstreaming).
2. **New best practice (recipe step 2 + pre-send checklist), from Eric:** stamp the root node
   with `name={graph-id}` and `created={current timestamp}` at creation — export authorization
   plus content-uniqueness; also neutralizes the mirrored-export subscriber symptom (feedback
   item 12: symmetric sync executes `export` in subscriber sessions too, where the missing
   `name=` stamp shows the watching human a refusal while the primary succeeded).

Rust-round findings already noted for this repo's guides from the build itself: the example's
`Cargo.toml` does not transplant verbatim outside the mercury workspace (workspace-inherited keys
+ path deps — candidate addition to the scaffolding section), and `graph.math` string/boolean
comparison semantics are undocumented (the fresh agent resolved them by reading the evaluator
source). Both are recorded in the demo repo's `docs/layer-3-live-rust/creation-steps.md`.

## Memory References

- Referenced: playground-session-broker
