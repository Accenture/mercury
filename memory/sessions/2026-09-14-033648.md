# Session (2026-09-14T03:36:48.000Z)

**Agent:** Claude Code

## What happened

**Experiment K2 conducted — the inbound Kafka flow adapter core** (Eric's go:
"conduct K2"). Branch `feat/minimalist-kafka-k2`. Records now flow from Kafka
topics into REAL Event Script flows, configuration only:

1. **`adapter`** — `kafka-flow-adapter.yaml` parsing with the Java module's
   fail-fast validation table: missing/unknown topic-flow shapes named by
   entry, unknown flows cross-checked against the live registry, dlq-equals-
   source rejected, the `kafka-flow-adapter.<topic>` group default, and the
   dead-letter-needs-producer deployment guard. **Later-increment fields
   (topic-pattern, flows, partition, serializer, schema.enabled, ttl,
   auto-commit, max-poll-records, header overrides) are rejected BY NAME** —
   never silently ignored.
2. **`consumer`** — one tokio task per binding: async `recv()` → dataset
   (`header` + `metadata` with the record's ACTUAL topic/partition/offset/
   timestamp/key + raw byte `body`, tombstone = null) → flow launch via
   `event.script.manager` (flow_id + business correlation_id headers, the
   flow's own ttl as the deadline, `KAFKA /<topic>` trace path, chaining onto
   the record's W3C traceparent span) → **sync offset commit under
   `block_in_place` only after the flow finishes** (at-least-once). Bounded
   retry + backoff; retry exhaustion parks the record on the binding's
   `dlq-topic` with a CONFIRMED write (`dlq.origin.topic` + `dlq.error`,
   original headers/body preserved); no DLQ (or a failed write) = the loud
   DATA-LOSS drop that protects partition liveness; consumer errors pause
   with escalating backoff instead of killing the binding.
3. **`bootstrap` K2 half** — parses/validates at startup (after
   CompileFlows), enforces the producer guard, applies the pinned overlay
   (group.id + enable.auto.commit=false), strips `group.protocol=auto` with
   a note (the resolver is K3), and holds the running consumers in the
   runtime for shutdown.

**The e2e** (single-lifecycle test, Java @BeforeAll shape): MockCluster +
`KAFKA_BOOTSTRAP_SERVERS` env → the bundled templates → the library's own
auto-start — dataset/trace/cid contracts pinned end to end, exactly-one-retry
success, DLQ parking with origin facts, and the no-DLQ drop with the binding
proven live after. Whole crate: 22 tests.

**Findings → spec §7 items 5–7:** `max.poll.records` needs no librdkafka
analog (per-record async recv IS the poll-batch-of-one); **pure-config
activation needs one `use mercury_minimalist_kafka as _;` line** (the Rust
linker drops an unreferenced rlib, inventory entries included — found when
the e2e's lifecycle reported "Missing main application"); `group.protocol=
auto` deferred to K3. Fixture lesson (not a delta): a bare `header` TARGET
in a task input mapping addresses the event-header namespace, not a body key
— map a record-header payload to a plain key like `record_header`.

**Post-suite catch:** the unit table caught a validation-ORDER divergence from
canon — the unknown-flow cross-reference ran before the dlq-equals-source
shape check, where Java deliberately runs self-contained shape checks first
and cross-references last. Reordered to parity; whole workspace re-run green.

## Memory References

- ot-minimalist-kafka-port (updated - K2 done, next gate K3)
- port-bottom-up-faithful (consulted - KafkaFlowAdapter/KafkaFlowConsumer/
  RetryPolicy read line by line; the commit/retry/DLQ/liveness decisions
  ported with their rationale comments, and every divergence recorded)
- inv-telemetry-presentation-parity (consulted - dataset keys, DLQ header
  names, trace-path shape and log wording kept Java-identical; the traceparent
  chain is what makes a Java producer -> Rust flow trace continuous)
- conventions-rust-baseline (consulted - suite layout, single-lifecycle e2e
  idiom, license headers)
- eric-release-rhythm (consulted - branch prepared and pushed; PR creation
  and merge are Eric's gates)
