# Session (2026-09-11T00:12:11.000Z)

**Agent:** Claude Code

## What happened

**The Rust repo gains its own AI developer guide** (Eric's follow-up ruling
after confirming the asymmetry: the Java repo had the cross-layer
"read this first if you are an AI agent" umbrella page; this repo had only
per-layer guides):

- New `docs/guides/ai-developer-guide.md` — an adapted port, not a copy, of
  the Java guide: Rust mental model (`#[preload]` + ComposableFunction /
  TypedFunction, link-time registration), the full entry-point playbook
  (greenfield conversation; AI-enabling an existing repo), brownfield
  orientation for `resources/` layout (`rest.yaml`, `flows.yaml`,
  `application.yml`, `graph.model.automation`, `grep "#[preload"`), the
  layer-choice tree, adding-a-feature with Rust snippets (verified against
  the Layer-1 agent guide and `examples/hello-flow` — the flow binding needs
  both `service: "http.flow.adapter"` and `flow:`), cargo test idioms, and
  port-honest invariants (serde rules replace the Java Gson/MsgPack gotchas;
  no service mesh, no graph.js — Port Scope pointer). No `--8<--` snippet
  includes (the snapshot fails closed on them; the Java guide's rest-bindings
  include was replaced with an inline example from hello-flow).
- Wiring: Foundations nav gains "AI Developer Guide"; `llms.txt` gains a
  "Cross-layer orientation — read this first if you are an AI agent" section
  (the Playground section's "start here" mantle moved to it — verified
  nothing pins the old heading); `files.list` packages
  `references/guides/ai-developer-guide.md` into the AI contract (count
  assertions are all relative — safe).
- `guides/methodology.md`'s interim "Two typical entry points" subsection
  slimmed to the same pointer the Java methodology carries — the two repos'
  agent surfaces are now symmetric.

Verified: `mkdocs build --strict`, claims + llms-links checks, and the FULL
ai-contract-provider test suite (snapshot, exporter, endpoints e2e, catalog —
all green with the new file in the closure); rendered page checked.

## Memory References

- eric-release-rhythm-rust (consulted — branch/commit/push and PR text
  prepared; PR-open and merge stay Eric's steps)
