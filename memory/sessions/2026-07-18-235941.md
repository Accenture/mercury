# Session (2026-07-18T23:59:41.000Z)

**Agent:** Claude Code

## What happened

**Fixed the tut-5 companion-endpoint defect (maintainer-directed): both companion endpoints now
limit the `session` command to the read-only status query.**

- **Maintainer decision:** a companion is an *assistant to* the session named in the URL, not a
  WebSocket session of its own — so `session subscribe`/`unsubscribe`/`reset` are rejected on
  **both** `POST /api/companion/{id}` (fire-and-forget) and `POST /api/companion/{id}/sync`,
  before dispatch. Only the read-only `session` status query is allowed. (Root defect: via
  `/sync`, subscribe registered the ephemeral `companion.sync.<uuid>` capture route as a durable
  subscriber.)
- **Rust:** `session_topology_subcommand` + `refuse_session_topology` helpers in
  `crates/knowledge-graph/src/rest.rs`; sync path returns `ok:false` + error in-band, legacy path
  returns 400 — both echo the refusal to the session's live console (the human sees what the AI
  sees). Test `companion_sync_rejects_session_topology_commands` (both endpoints, subscriber-state
  assertions via the in-band `session` status, console-tee assertion) in
  `tests/graph_runtime.rs`. Workspace green: all tests, clippy 0, fmt clean.
- **Java (upstream parity):** same guard in `PostCompanionCommand`/`PostCompanionCommandSync` +
  shared statics in `GraphCommandService`; refusal text **byte-identical** to the Rust port (the
  companion REST contract stays language-neutral). Test
  `companionEndpointsLimitSessionCommandToReadOnly` over the real wire; module suite **65 tests
  green**. Committed on local branch `fix/companion-session-readonly` (`9aa34ea9`); **push + PR
  left to the maintainer** (agent push to the public repo blocked by permissions, correctly). PR
  What/Why body was drafted and handed over in-chat.
- **Docs:** AI grammar updated for the new contract (`ai-agent-guide.md` rules-of-engagement,
  `command-reference.md#session`, `minigraph-commands.json` session notes + sync_envelope note);
  `docs/AI-companion-test.md` rollup #14 → DONE.
- **Ops note:** the running dev server (:8100) predates the guard — restart before tutorial-6
  (also clears the in-memory poisoned subscriber entry).
- Also in this working session, before the fix arc: a **memory review** ran (triggered by
  `[continuity-bloat]`) — see the `## Memory Review` block in `2026-07-18-233807.md`.

## Decisions

- **Companion endpoints are read-only with respect to session topology** (maintainer,
  2026-07-18). Fixed by rejection-before-dispatch on both endpoints, not by re-resolving the
  subscriber route — topology stays a WebSocket-session privilege.
- The `/sync` refusal keeps the standard envelope (`ok:false` + `error`), so an AI caller
  self-corrects from the in-band reason like any other command error.

## Memory References

- ot-subscribe-via-sync-bug — resolved this session (marked `- [x]`, substance updated)
- ot-companion-validation-sweep — substance updated (tut-6 resume: WS-side subscribe by design,
  server restart first)
- bp-companion-sync — referenced (the guard extends the companion contract; byte-identical
  cross-engine refusal per its language-neutral principle)
- port-bottom-up-faithful — referenced (Java parity fix prepared upstream)

Co-Authored-By: Claude Code <noreply@anthropic.com>
