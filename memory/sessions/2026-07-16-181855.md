# Session (2026-07-16T18:18:55.000Z)

**Agent:** Claude Code

## What happened

Maintainer **approved the event-script design** ("assumptions are fine", defaults
accepted on all four open questions; committed + pushed) and directed increment E-1.

**Increment E-1 (repo increment 11) — flow model + compiler, SHIPPED.**

1. **`crates/event-script`** (new workspace member; deps: platform-core + async-trait +
   log only): `model.rs` (compiled `Flow`/`Task` — Java field names kept for
   side-by-side review; monitor fields deferred to the E8 mock port, model stays
   immutable), `flows.rs` (template registry; instance registry comes with E-4),
   `compiler.rs` (the full 922-line `CompileFlows` port), `converter.rs`
   (`SimpleTypeMatchingConverter`: legacy `:type` → `f:plugin(...)`), `validator.rs`
   (compile-time `DataMappingHelper` half: validInput/validOutput/validModel +
   `reserved_model_key_violation` exported for the E-4 runtime re-check), `plugins.rs`
   (plugin **name** registry, 42 built-in names — **pulled forward from E-8** because
   `validInput` checks `f:` names at compile time; execution bodies remain E-8),
   `util.rs` (Java `Utility.split`/`isNumeric`/`getDurationInSeconds` ports, verified
   against the Java source). Engine self-registers via
   `#[before_application(sequence = 5)]` — the increment-10 annotation layer doing its
   job one increment later.
2. **Java failure semantics ported exactly**: unreadable list file = WARN + skip
   location; invalid flow = ERROR + skip flow; **invalid data mapping = ERROR + drop
   the TASK while the flow still loads** (and `end_task_found` is computed from
   metadata before mapping validation, so a flow can load even with tasks dropped);
   3-part mappings decompose to two 2-part rules; `!model.x` normalizes to
   `model.x:!`; text( constants are exempt from 3-part splitting (may contain `->`);
   bad shapes become poison strings that fail validation downstream.
3. **Fixture parity (E2)**: all 90 Java fixtures copied verbatim
   (`tests/resources/README.md` attributes the source and forbids local edits);
   test `application.yml` mirrors the Java module's
   `yaml.flow.automation: not-found.yaml, flows.yaml, more-flows.yaml`. 8 integration
   tests pin: the **exact loaded-flow set** (65 ids), every whole-flow rejection,
   task-drop semantics (23/25/26/27/28–31 + the FlowTests reserved-key quartet),
   normalized greetings mappings (f:int/f:not/f:isNull/f:uuid/f:concat forms),
   loop/fork/dynamic-source/sub-flow metadata, duplicate-flow-id resolution.
4. **Findings — code over comment** (each verified against the Java source before
   accepting): parser-test-7 (`invalid-condition-mode`) is grammar-valid legacy naming
   and LOADS in Java; parser-test-19's `ext.user` (dot form) is a plain body key —
   only `ext:` triggers the external-state-machine requirement — it loads too. My two
   initial test expectations were wrong, the Rust compiler was right; also
   parser-test-26's task IS dropped (`output.something` as output LHS — its flow title
   says exactly that; I'd misread the truncated fixture).

**Verification**: 15 event-script tests; **120 workspace tests, 0 failures**;
workspace clippy 0 warnings; fmt clean. Docs in lockstep: design doc §5a (E-1 note +
Approved header), INCREMENTS.md row + section 11.

## Decisions

- Plugin name registry forward-pull from E-8 (names only, execution later) — same
  pattern as increment 9's RPC-inbox pull-forward; recorded in design §5a.
- Fixture comments are NOT spec — the Java code is; divergent legacy-named fixtures
  documented in the design doc rather than "fixed" locally.

## Memory References

- referenced: `ot-design-event-script` (gate approved → E-1 shipped),
  `bp-event-script`, `vision-mercury`, `conventions-rust-baseline`,
  `inv-never-couple-functions`
- created: (none — progress recorded on the layer-2 thread)
