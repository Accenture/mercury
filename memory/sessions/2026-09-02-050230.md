# Session (2026-09-02T06:05:00.000Z)

**Agent:** Claude Code
**Round:** Retire the async companion endpoint — lock-step with the Java engine's
ADR-0021. **MERGED as [PR #224](https://github.com/Accenture/mercury/pull/224) merge
`206ce31a` carrying gated `32fda198`; tree verified; branches deleted both ends.
Increment 97.**

## What happened

Driven by a field AI agent's build feedback against the Java engine (the async form hid
errors and forced sleep-padded drivers; measured 12 s vs 0.1 s for a 25-command build).
This port's changes: `post_companion_command` handler + rest.yaml mappings removed (bare
URL answers 404 via REST automation); /sync — which originated here — unchanged, now the
only companion endpoint; the playground test's async hop became the 404 retirement pin;
companion guides rewritten around the single endpoint with the "restart ends the session
— export first" and "404 means the session is gone" warnings; webapp
COMPANION_ENDPOINT_README rewritten. graph.js needed no action (never carried here; the
Java engine's ADR-0022 deprecation converges with this port's stance). The array-mapping
doc gap was Java-only (syntax.md already documents `[0]`/`[]`).

## Lesson (durable, re-learned)

**CI caught what a piped local gate masked:** `graph_runtime.rs` drove the removed
function DIRECTLY (`rest::post_companion_command`) — a call-site a URL grep cannot find —
and my `cargo test | grep | tail` chain exited 0 on the failing compile (the
ot-route-pool-twin unpiped-exit-code lesson, from the other side). Sweep the SYMBOL when
retiring an API, and verify gates with bare exit codes. Fix: the direct-call legacy
section retired (`32fda198`); the REST-layer pin in playground.rs is the right home.

## Memory References

- Referenced: ot-route-pool-twin (its unpiped-exit lesson re-confirmed),
  inv-telemetry-presentation-parity
- Created: (none)
- Reactivated: (none)
- Superseded: (none)
