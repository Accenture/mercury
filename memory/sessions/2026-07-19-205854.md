# Session (2026-07-19T20:58:54.000Z)

**Agent:** Claude Code

## What happened

**`/sync` ok-heuristic false-negative FIXED (rollup #40) — both ports.** The companion `/sync`
endpoint classified `import graph from {deployed}` as `ok:false` on a command that succeeded:
the per-line `is_error_line` heuristic tripped on import's benign "Graph model not found in
/tmp/…" line, which the engine legitimately prints before falling back to the deployed
classpath copy. A false negative misleads an AI caller into "fixing" a working command.

- **Fix (design, identical in both engines):** classification now runs over the **whole
  captured output**, not line-by-line in isolation. The not-found line is forgiven **only**
  when the same output also carries the fallback's success marker ("Found deployed graph
  model"); a genuine miss prints the not-found line alone and stays `ok:false`. Verified
  against both import handlers first (Rust `commands.rs`, Java `GraphCommandService`
  `handleImportGraphCommand`): a genuine miss emits nothing after the not-found line, so the
  forgiveness rule cannot mask a real failure.
- **Rust:** `first_error_line()` in `crates/knowledge-graph/src/rest.rs` (outcome builder now
  collects lines, then classifies); new both-direction test
  `companion_sync_import_fallback_reports_ok` in `tests/graph_runtime.rs` (deployed-only
  import → ok:true with both fallback lines + no error; `no-such-graph-xyz` → ok:false with
  the not-found error in-band). Workspace green: all tests pass, clippy 0 warnings, fmt clean.
- **Java (prepared for upstream):** mirrored `firstErrorLine` in
  `PostCompanionCommandSync.java` + test `companionSyncImportFallbackReportsOk` in
  `CompanionSyncTest.java` (uses `tutorial-113` — deployed-only, no test exports it; deletes
  any stale `/tmp/graph` copy first so the fallback path is deterministic). Full
  minigraph-playground-engine suite green (66 tests, 0 failures). Committed on local branch
  **`fix/companion-sync-ok-heuristic`** (`c149bf93`) — maintainer pushes/PRs (agent push to
  the Java repo is permission-blocked, per the #194 precedent). The `/sync` REST contract
  stays byte-identical across engines.
- **Docs:** `ai-agent-guide.md` #40 caveat replaced with the fixed semantics ("ok is derived
  from the console lines with whole-output context"); matching note added to
  `minigraph-commands.json` sync_envelope; rollup #40 → DONE in `docs/AI-companion-test.md`
  (+ scorecard follow-up line updated).

Also: **live-verified by the maintainer** on the running dev server (`:8100`, session
ws-304419-2): `import graph from tutorial-1` via `/sync` → `ok:true` with both fallback lines
in `output` and no `error` key.

Also: the maintainer's Java live test (`:8085`) still showed `ok:false` — the running app was
built from `main`, which doesn't carry the fix. Staged for their retest: Java repo checked out
on `fix/companion-sync-ok-heuristic`, engine installed to `~/.m2`, example fat jar rebuilt
(`examples/minigraph-playground/target/minigraph-playground-4.8.6.jar`) and byte-verified to
contain the fixed classifier. Maintainer restarts the app, retests, then pushes the branch /
opens the PR; after merge, back to `main` + pull. **Java live-validated by the maintainer**
(`:8085`, session ws-735231-4): same import → `ok:true`, both fallback lines, no `error` key —
the fix is now verified live in BOTH engines.

## Decisions

- The ok-classification is whole-output-aware with a **narrow, marker-gated forgiveness**
  (only the import-fallback pair) — no broad whitelisting of "not found" lines, so every other
  error path keeps its current classification.

## Memory References

- ot-companion-validation-sweep — referenced + substance updated (#40 follow-up closed)
- bp-companion-sync — referenced (the `/sync` contract this fix hardens; still byte-identical)
- ot-sync-ok-heuristic-fix — created (born `tier: working`; records the fix + pending Java push)
- conventions-rust-baseline — referenced (fmt/clippy-clean gates applied)
- port-bottom-up-faithful — referenced (fix designed once, mirrored faithfully in both engines)

Co-Authored-By: Claude Code <noreply@anthropic.com>
