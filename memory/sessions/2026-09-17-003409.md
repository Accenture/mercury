# Session (2026-09-17T00:34:09.809Z)

**Agent:** Claude Code

Lightweight: Mode B upgrade 4.39.2 → 4.40.0 (MINOR — stalled open threads → human closure gate; from the
mercury-composable field report of 2026-09-16). Reconcile re-copied `DECAY.md`, `REVIEW.md`,
`.agent/schema.md` and the memory-lint built-in (check 15 `[thread-stale]`, 73 mirrored tests per runtime);
`thread_stale_window: 40` added to `memory/decay-policy.md` (the optional semantic step); adapters
re-synced; stamped 4.40.0; reconcile converged; Python/Node lint output identical. **Pre-existing, not from
this upgrade:** memory-lint's one ERROR — `[over-archived] playground-session-broker` — the 2026-09-14 log
marked it REACTIVATED under Memory References, but the move back from `memory/archive/2026-Q3.md` into
`continuity.md` was never performed; the next review (overdue: 27 ≥ 10) does it per REVIEW.md
"Reactivation". That review also meets the first closure gate (REVIEW.md step 8): the lint names three
stalled unchecked threads — `bp-kafka-connectors-backlog` (91 sessions) and `bp-foundation-to-ui` (67), both
`(blueprint)` gaps, so closing is an altitude decision, and `ot-cratesio-followups` (65); the owner closes
each (undelivered items recorded as deliberately dropped) or re-affirms it under Memory References;
inspecting is not a use. Three completed threads (sslu 26–40) await the sweep. Upstream release:
https://github.com/Accenture/mercury-go/releases/tag/v4.40.0 .

## Memory References

(none)
