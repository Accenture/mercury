# Session (2026-07-18T18:39:53.000Z)

**Agent:** Claude Code

## What happened

**Implemented the synchronous AI-companion endpoint in the Java upstream (mercury-composable) and
opened PR #189** — the full Java port of the Rust sync feature, after the maintainer merged the
design ADR-0008 (#188).

- **`PostCompanionCommandSync`** (`system/minigraph-playground-engine/.../rest/`,
  `@OptionalService("app.env=dev") @PreLoad(route="post.companion.command.sync")`,
  `POST /api/companion/{id}/sync`) — mirrors the Rust design: dispatch the command to the singleton
  handler via **request/response RPC** (`po.request`) with a **private per-call capture route**
  (`platform.registerPrivate`) supplied as the command's `out`; each captured line is buffered (for the
  in-band `{ok,output,error,result}` envelope) and **teed** to the session's real WebSocket `.out`
  (best-effort, via `EventEmitter`); a **FIFO sentinel** (+ `CountDownLatch`) marks the buffer drained
  deterministically. The `say()`-based command functions are untouched; the fire-and-forget endpoint +
  WS console unchanged.
- **rest.yaml** route added in **both** `src/main/resources` and `src/test/resources` (the test uses
  its own copy — a 404 on the first run caught that).
- **`CompanionSyncTest`** (drives the endpoint over real HTTP): invalid command → `ok:false` + error
  in-band; valid command → `ok:true` + output; the tee asserted via a stand-in at the session `.out`.
  **Full minigraph-playground-engine suite green: 64 tests, 0 failures** (no regressions), mvn 3.9.16 /
  Java 21.
- **ADR-0008 → Accepted** (+ a "now implemented in Java" note); **CHANGELOG** Unreleased/Added entry
  (PR #189).
- PR: **Accenture/mercury-composable#189** (branch `feat/companion-sync-endpoint`). `gh` switched to
  `acn-ericlaw` for the PR op, then back; Java checkout tidied to main.

**`bp-companion-sync` is now delivered in Rust and PR-complete in Java** — only the merge of #189
remains (owned upstream). The feature exists in both ports of the platform.

## Memory References

- referenced: `bp-companion-sync`, `vision-mercury`, `port-bottom-up-faithful`
- created: (none)
