# Session (2026-09-21T17:54:30.000Z)

**Agent:** Claude Code

## What happened

**Gate K4 conducted — the `kafka-demo` port and the live Java ⇄ Rust interop drive** (Eric's rulings at
the gate: `group.protocol=auto` = classic ACCEPTED; copy the Node helper programs; the chained-hop
interop shape "because it proves the whole interop end-to-end"). Branch `feat/minimalist-kafka-k4`
(`e9f07188`, pushed; PR pending — Eric opens).

1. **`examples/kafka-demo`** — the Java worked example mapped one to one: `demo.processor` (untyped,
   bytes in, JSON bytes + cid header out), `demo.order.processor` and `demo.refund.processor` (typed
   `HashMap<String, Value>` → JSON — a non-object body fails deserialization, which IS the demo's
   failure path), `demo.catch.all` (untyped, handles bytes/map/list); each derives its context from
   the injected `my_*` headers, so the four Java tests port directly (7 tests). The three flows and
   the two-binding adapter YAML are the Java files with comment tweaks; the five Node programs are
   copied (kafkajs), with an optional topic argument on the listener and publisher and the two
   interop topics in the admin script. New: an `interop` profile whose adapter YAML relays
   `demo.interop.in → demo.inbound` (Rust producer → Java flow adapter) and
   `demo.outbound → demo.interop.out` (Java producer → Rust flow adapter), both reusing
   `demo.processor` — whose log line now names the injected trace path (`KAFKA /<topic>`) instead
   of a fixed topic, so the hops are legible.
2. **The live drive** on the Java `kafka-standalone` 4.12.13 (Apache Kafka 4.3.1, KRaft) with the Java
   `kafka-demo` 4.12.13 jar unchanged — recorded in `docs/test-reports/minimalist-kafka-interop.md`
   (in the mkdocs nav): direct routing (one trace id at publisher/app/listener, 11 ms), the four
   routing rules, the poison-order DLQ path (4 attempts, 1.5 s, origin + error + body preserved),
   interop leg 1 (Rust → Java → Rust, three hops, span chain exact across the boundary: the Java
   `demo.processor`'s parent span IS the Rust notification's span and the Rust relay's incoming span
   IS the Java notification's), leg 2 (Java → Rust), a **mixed Java + Rust consumer group**
   (generation 4, 2 members; 20 records split 10/10, exactly once), a **`SIGKILL`ed Rust member**
   (expired at the 45 s session timeout; 20 records published while dead: 10 at once on Java's
   partitions, 10 after the takeover 45.5 s later, 20/20 once; 4 poison orders with one in flight at
   the kill all dead-lettered by Java — redelivered, not lost, not duplicated), and the `SIGINT`
   graceful stop (consumers drain, explicit `LeaveGroup`, immediate rebalance).
3. **Finding 1, FIXED in platform-core — a headless Rust app exited right after boot.** The first live
   run processed nothing: `AutoStart::run` parked only for HTTP/websocket serving; the demo (no REST)
   booted, started its consumers and exited 200 ms later while the broker held the six published
   records. The mock e2e could never see it (a test function keeps the runtime alive) and the JVM
   never has it (non-daemon consumer threads). Fix: `Platform::keep_running(reason)` +
   `Platform::is_kept_running()`, honoured by `AutoStart::run` exactly like serving; the flow
   adapter declares it when its consumers start and registers `stop_flow_consumers` as a shutdown
   hook (which produced the graceful-stop evidence). Embedders awaiting `AutoStart::main` are
   unaffected. Unit test on the flag; spec §7 item 11; new fact [[headless-app-keep-running]].
4. **Finding 2, RECORDED as a platform-wide follow-up — `SIGTERM` is not handled.** `AutoStart::run`
   waits on `tokio::signal::ctrl_c()` only; a Kubernetes pod stop (`SIGTERM`) therefore looks like
   the hard-kill leg (partitions held for the 45 s session timeout, shutdown hooks skipped) rather
   than the graceful one. Not Kafka-specific — every Rust application. Backlog P11; not patched
   here to keep K4 focused.
5. **Presentation differences, not defects** (report Finding 3): `dlq.error` reads
   `flow 'demo-order-flow' returned status 400` here (a typed function's undeserializable input is
   a 400) vs Java's `java.lang.IllegalStateException: … status 500`; Java's outbound JSON is
   pretty-printed, Rust's compact. Finding 4: a new consumer group starts from the template's
   `auto.offset.reset=earliest` — the relay's new group consumed the four earlier outbound records.

**Gates:** `cargo fmt --check`, `clippy --workspace --all-targets -D warnings`, platform-core lib
(62), the five minimalist-kafka suites, kafka-demo (7), `check-doc-claims`, `check-llms-links`,
`mkdocs build --strict` (uv scratch venv).

**Method notes:** (a) wait for the broker's `Stabilized group <id>` line before publishing — a
consumer's join takes seconds and kafkajs listeners see nothing published before their assignment;
(b) the first chaos attempt killed the Rust member after it had already processed its half (1 ms per
record) — to have work genuinely in flight, kill mid-retry on poison orders (1.5 s window) and
publish the burst while the member is dead; (c) a format-agnostic `grep -o 'burst-N-[0-9]*'` count —
the first duplicate tally missed Java's pretty-printed outputs and read 10 where there were 20;
(d) the kafka-standalone JVM outlives its closed port by a few seconds — verify the process, not the
port; (e) a `#[test]` calling `Platform::get_instance()` needs a Tokio runtime (`#[tokio::test]`, or
`Platform::new()` as the neighbouring tests do).

**Not done, deliberately:** no INCREMENTS entry (K-series precedent; one entry at K5 covers the port),
the root README's non-goals paragraph and the `docs/guides/minimalist-kafka.md` twin wait for K5.

## PR opened

PR #300 (`https://github.com/Accenture/mercury/pull/300`) opened by Eric for
`feat/minimalist-kafka-k4` (`e9f07188`); merge is his gate.

## Doc Gaps

(none — the Java demo, guide and source were the canon for a port, not a consumer question)

## Memory References

- ot-minimalist-kafka-port (updated — K4 DONE 2026-09-21, next gate K5; the follow-ups listed)
- headless-app-keep-running (created — the `Platform::keep_running` decision and the `SIGTERM` follow-up)
- port-bottom-up-faithful (applied — the demo is mapped one to one; the keep-alive is the explicit analog of the JVM's implicit non-daemon hold)
- conv-cargo-declare-what-you-name (applied — the demo declares platform-core, event-script and minimalist-kafka because its code names items from each)
- eric-release-rhythm-rust (followed — branch and PR text prepared; PR open and merge are Eric's)
