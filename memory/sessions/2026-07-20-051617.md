# Session (2026-07-20T05:16:17.000Z)

**Agent:** Claude Code

## What happened

**Post-sweep drive #3: live HTTPS fetch — PASSED (16/16, zero failures, ZERO lookups,
first attempt). Increment 48's TLS transport is field-validated end-to-end**, and the
zero-lookup first-attempt streak extends to **ELEVEN**.

- **Purpose (maintainer-proposed):** the strict OS-trust-store TLS path can't be proven with
  a self-signed unit fixture — a fresh agent drove Fetcher → `async.http.request` → rustls
  against the real `https://google.com` CA chain.
- **Pre-flight (orchestrator, scratch session ws-655120-2):** transport probed before the
  brief — `https://www.google.com/` → 200/82,777-byte page through the graph;
  naked domain → 301 with body captured, traversal proceeds. Scratch closed pre-brief.
- **The run (session ws-192994-1, problem-only brief):** textbook Provider + Dictionary +
  fetcher + unprompted island; `response -> result.page` (whole raw body),
  `page-fetcher.status -> output.body.status`, `f:length -> output.body.page_size`. The
  agent reported the redirect **truthfully**: `{status: 301, page_size: 220}` — reasoned
  301 < 400 = non-failure — and re-counted the 220 chars outside the engine. Judged
  first-hand from the WS console tee + my own pre-flight.
- **Findings #58–#61 (agent ambiguities) — all engine-verified and doc-FIXED same day:**
  bare `response` root = whole raw body (`fetcher.rs` allows it explicitly); `f:length`
  type table (string → char count; `conversions.rs`); `{node}.status` set unconditionally
  on every fetch (success included); redirects never followed + `response.*` body-only +
  headers via `feature[]=log-response-headers` → `{node}.header.response.*`. Fixed across
  `command-reference.md`, `minigraph-commands.json` (surgical), `skills-reference.md`,
  `help graph-api-fetcher.md`; webapp bundle re-released (124 tests green). Gates:
  clippy 0 / fmt clean / kg + workspace tests green.
- **Findings #62–#63 (orchestrator pre-flight) — OPEN, engine decisions for the maintainer
  (BOTH engines share the behavior):**
  - **#62:** silent 1-second identical-command dedup (`is_duplicate` in `commands.rs`;
    Java `GraphCommandService:178`) — a repeated identical command within 1s is dropped
    with a debug log only; `/sync` returns `ok:true` with EMPTY output, violating the
    documented envelope. Recommend: `/sync` bypasses the dedup + explicit
    "duplicate ignored" line.
  - **#63:** malformed commands answered with a `Syntax: …` usage line classify `ok:true`
    (false-positive; mirror of #40's false-negative). Recommend: classify the usage
    response as the error line (`ok:false`).
- Drive #3 section + findings table appended to `docs/AI-companion-test.md`. The playground
  app is still running on :8100 with the agent's graph live in session ws-192994-1.

Also: maintainer follow-up (same working session) — new backlog thread
[[ot-http-redirect-backlog]]: investigate HTTP redirection for the outbound client in both
ports. Preliminary grep: `followRedirect` absent from the whole Java repo (Reactor-Netty
defaults it off), so no-redirect is currently SHARED behavior; Rust would implement an
explicit bounded 3xx loop. Next session: #62/#63 + redirect backlog, then graduation prep
for github.com/Accenture/mercury.

## Memory References

- ot-companion-validation-sweep — updated (drive #3, streak 11, findings #58–#63)
- ot-sync-companion-contract-gaps — created (the #62/#63 engine decisions)
- ot-http-redirect-backlog — created (redirection investigation, both ports)
- port-bottom-up-faithful — applied (Java parity checked for the dedup guard before judging)
- conventions-rust-baseline — applied (gates, doc surfaces, bundle re-release)

Co-Authored-By: Claude Code <noreply@anthropic.com>
