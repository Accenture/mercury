# Session (2026-07-17T02:42:13.000Z)

**Agent:** Claude Code

## What happened

**Increment K-4 shipped (repo increment 24) — the graph runtime, the biggest layer-3
increment.** K-3 was committed and pushed at the maintainer's direction at session
start (a0f01cb).

- **`graph.executor`** (zero-tracing interceptor): traversal start on a plain cid,
  skill callbacks on composite `{flowInstanceId}@{nodeName}` cids, decision routing
  (next / jump / .sink), loop detection (graph.max.loop.interval /
  graph.node.high.frequency), compiled-model reuse + lazy load + tutorial dev-gating.
  Exposed through the verbatim `graph-executor.yml` flow shipped with the crate.
- **Core skills**: graph.data.mapper, graph.math (COMPUTE/IF/MAPPING/RESET/DELAY/NEXT
  + EXECUTE merge + BEGIN/END for-each, on the K-2 engine), graph.task (whole-body
  `*`, headers both ways, for_each fork-join with clamped concurrency, per-node
  exception routing), graph.join, graph.island. Support: graph.housekeeper (wired via
  event-script's END-FLOW LISTENER — the v-layer-2 hook fit perfectly),
  graph.exception.handler, graph.health.
- **`GraphLambdaFunction` (756 Java lines) → `common.rs` free functions**: {var}
  substitution with logical-quoting, RHS validation + reserved properties, node-property
  seeding, for_each resolution, fetcher-style output mapping, statement blocks.
  `GraphInstance` state = Mutex<MultiLevelMap> with guards never crossing awaits.
- **graph.js retirement ENFORCED**: route never registered; executor fails a
  `skill: graph.js` node with "Skill graph.js is retired for security reasons - use
  graph.math or graph.task instead" (deliberate divergence, maintainer decision).
- **Scope adjustments recorded in the design doc**: GraphTraveler = dev-only Playground
  walker → K-7; fetcher/extension tutorials → K-5/K-6; OptionalService dev-gating → K-9.
- E2E suite (one flow-engine boot per process — AutoStart can't run twice): tutorials
  1/2/4/7/8/9/13 + GraphTaskTest 1..5 (Java task functions ported into the test binary)
  + Rust-supplement lazy-loaded graphs (rust-join, rust-loop, rust-js-retired — also
  exercising the lazy path) + graph.health. **Tutorial suite passed on the first run.**
- Verification: workspace 177 tests green, clippy 0, fmt clean. INCREMENTS.md row 24 +
  section; design doc K-4 shipped note.

Next: **K-5 — platform-core HTTP client** (async.http.request, lockstep) →
graph.api.fetcher with dictionary/provider nodes; activates the deferred E-9 fixtures
and the fetcher tutorials (3/5/6/12/113/114, unit-test-1, GraphExecutionTest).

## Memory References

- referenced: `port-bottom-up-faithful` (bug-for-bug fidelity calls in K-1/K-4),
  `ot-design-knowledge-graph` (K-4 shipped — thread updated),
  `bp-active-knowledge-graph`, `bp-platform-core` (K-5 will close the §7 http-client
  deferral), `vision-mercury`, `conventions-rust-baseline`
- created: (none)

## Review (cadence-triggered, 10 sessions)

- `refresh-metadata` recomputed 5 footers (conventions-rust-baseline 21→26,
  bp-platform-core 16→19, bp-event-script 14→15, bp-active-knowledge-graph 3→8,
  ot-design-knowledge-graph 1→5).
- Attempted to archive 3 archive-candidates (config-first-spring-out,
  elastic-fifo-no-bdb, platform-core-stack); **memory-lint's over-archival guard
  correctly rejected all three** (last referenced 15–18 sessions ago, within the
  20-session window) — moves reversed, facts stay in continuity. No archival this
  review. Note: `port-bottom-up-faithful` is referenced below (genuinely applied
  throughout K-1…K-4 — the faithful-port + map-don't-mirror calls), reactivating it.
- Completed blueprint threads (bp-platform-core, bp-event-script) recently referenced —
  not swept. `last_review` stamped through this session. Lint 0 errors.

**Also referenced:** `port-bottom-up-faithful` (bug-for-bug fidelity decisions in
K-1 MiniGraph and K-4 runtime: maxNodes off-by-one kept, combine() lowercasing kept,
remove_node case fix documented as deliberate divergence)

**Also:** maintainer review caught a Java-parity gap — `AutoStart.main` is guarded by
an `AtomicBoolean` in Java (repeated execution = no-op) but the Rust port had no guard
(second call failed with "Route ... already exists"; the K-4 test merge had only
worked around the symptom). Fixed in `platform-core/src/app_starter.rs` (static
`AtomicBool` + swap) and asserted in the E2E test (a second `AutoStart::main` call
must succeed as a no-op). Note the Rust-specific caveat: separate `#[tokio::test]`
functions still can't share one boot, because each test owns its runtime and the
platform's actors die with the first runtime — one boot per test process remains the
pattern; the guard restores graceful Java semantics within a process.
