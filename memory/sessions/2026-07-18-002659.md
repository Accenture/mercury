# Session (2026-07-18T00:26:59.000Z)

**Agent:** Claude Code

## What happened

**Increment K-8 shipped (repo increment 29) — the React webapp + a runnable Playground app.
LAYER 3 (active knowledge graph) MILESTONE CLOSED.** Three layers ported bottom-up:
platform-core → event-script → active knowledge graph.

- **Webapp copied verbatim** into `crates/knowledge-graph/webapp/` (React 19 + Vite +
  `@xyflow/react`, 573 modules). Per the K7 decision only the deploy path changes:
  `scripts/clean.js` + `scripts/deploy.js` retarget `../src/main/resources/public` →
  `../resources/public`.
- **Third path retarget (maintainer-approved via AskUserQuestion).** The design doc said
  "only clean.js/deploy.js change," but `src/data/helpContent.ts` bundles the in-app Help
  panel at build time via `import.meta.glob('../../../src/main/resources/help/*.md')`. In our
  layout that resolves to a nonexistent `crates/knowledge-graph/src/main/resources/help/` →
  empty Help panel. Retargeted the glob (+ the `vite.config.ts` dev comment) to
  `../../../resources/help/*.md` (the engine crate's help dir). Same class of change as the
  scripts; surfaced because it contradicted the written scope. Maintainer chose "retarget."
- **`npm run release`** (clean → vite build → deploy) → the bundle lands in the engine
  crate's `resources/public/`, served at `/` by REST automation (static content; the Rust
  analog of the Java jar's resources). The served bundle (js/css/html, ~1 MB) is **committed**
  so a fresh clone serves the Playground with no npm step (Java parity); the 3+ MB of Vite
  **source maps are gitignored** (`resources/public/**/*.map`) as regenerable debug artifacts
  (map, don't mirror — a deliberate hygiene deviation from Java, which commits maps).
- **`examples/minigraph-playground`** (open question 4, default yes → resolved yes): a
  one-line `auto_start_main!` app linking the engine, with `resources/application.yml`
  (`app.env=dev`, `rest.automation`, port 8100) and `resources/rest.yaml` (Playground/
  companion endpoints ported from the Java engine `rest.yaml`; the two demo-mock routes
  omitted — those services are test fixtures, not engine built-ins). Mirrors the `hello-flow`
  convention: the app owns its deployment config, the engine stays a clean library. Added to
  the workspace members.
- **Live-verified against the running app** (`cargo run -p minigraph-playground`). The Chrome
  extension was not connected, so I verified the exact protocol/paths the browser's React app
  uses (Node 22's built-in WebSocket + fetch): (1) static — `GET /` → index.html (title
  "Minigraph Playground") + `/assets/*.js` 200; (2) websocket — connect
  `ws://127.0.0.1:8100/ws/graph/playground` → session greeting → `create node root` →
  `node root created` → `help connect` streamed the ported help content; (3) AI-companion —
  `POST /api/companion/{id}` → 200 accepted → the command output streamed to the session's WS
  console. All PASS.
- **Verification:** `cargo test --workspace` 181 green (K-8 adds no Rust tests — verified
  live), `cargo clippy --workspace --all-targets` 0 warnings, `cargo fmt --all --check` clean.
  INCREMENTS row 29 + section (milestone banner); design-doc K-8 marked shipped + open
  question 4 resolved. `bp-active-knowledge-graph` blueprint marked delivered.

Honest residual: no rendered-browser screenshot (extension down); verification was the
underlying HTTP + WebSocket protocol the browser drives. A human should still eyeball the
canvas UI once.

**Also (same session, release prep):** added the governance boilerplate from the origin
`mercury-composable` — `LICENSE` (Apache-2.0), `INCLUSIVITY.md`, `CODE_OF_CONDUCT.md` (all
verbatim), and `CONTRIBUTING.md` (near-verbatim). Cut the first `CHANGELOG.md` entry —
**v0.1.0 (7/18/2026)**, Keep a Changelog + SemVer, matching the origin's format — recording the
three-layer port as the first release ready for manual end-to-end testing.

**Then (maintainer follow-up, same session):**
- **Maintainer correction.** The origin's "Kevin Bader" reference was a historical copy-paste
  mistake; the maintainer of both the Java `mercury-composable` and this Rust port is **Eric
  Law**. Corrected in CoC + CONTRIBUTING **in both repos** — the owner explicitly authorized
  editing the normally-read-only Java origin for this one fix (scoped to the two governance
  files, one occurrence each).
- **README populated** (brief, per "proper docs later"): what it is, the three layers, quick
  start (the three example apps), repo layout, non-goals, and links to CHANGELOG/CONTRIBUTING/
  CoC/LICENSE/docs.
- **ADR ledger created** — `docs/arch-decisions/ADR.md`, adapted from the Java repo's ADR.md
  (ADR-0001…0007; faithful port so most carry over). Realization diffs called out:
  **ADR-0002** (tokio async/await, not Java 21 virtual threads over Vert.x — same
  sequential-reading-RPC goal; port benchmark outperformed the Java baseline) and **ADR-0006**
  (Kafka mesh out of scope for this port; principle retained). Tagged the existing invariant
  `inv-never-couple-functions` with `(ADR-0001)`; CONTRIBUTING's Design Decisions now points to
  the ADR ledger + design docs + increments. Remaining invariants (ADR-0002…0007) are distilled
  incrementally per the protocol — `formalizes:` names each intended id.

**Manual E2E test (maintainer, same session) — tutorial-1 side-by-side Java vs Rust.** The
Playground rendered and ran identically in the browser on both (canvas, tutorials, help panel,
`import → start → run` → `{output:{body:"hello world"}}`). One parity gap found: the
performance metric printed full float precision (`… in 1.2663339 ms`) vs Java's 3 dp
(`0.725 ms`). **Fix:** standardize `exec_time` to **3 decimal points at the source**
(`platform-core/src/platform.rs`, the single place it's set on the reply envelope), porting
Java `WorkerHandler.getExecTime` (clamp ≥ 0, round to 3 dp) — so every consumer (reply
envelope, telemetry dataset, the traveler's "Executed … in T ms" narration) reports the same
rounded value; no per-display formatting needed (Java relies on the same source rounding).
Re-verified live: `Executed end with skill graph.data.mapper in 0.104 ms`. Workspace 181 green,
clippy/fmt clean.

## Memory References

- referenced: `bp-active-knowledge-graph` (blueprint DELIVERED — milestone closed),
  `ot-design-knowledge-graph` (K-8 shipped — thread complete), `bp-platform-core`,
  `vision-mercury`, `conventions-rust-baseline`, `port-bottom-up-faithful`
- created: (none)
- Completed: `bp-active-knowledge-graph`, `ot-design-knowledge-graph` (both marked `- [x]`)
