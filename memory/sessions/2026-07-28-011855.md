# Session (2026-07-28T01:18:55.000Z)

**Agent:** Claude Code
**Lightweight:** cross-repo handoff closure; no code changed here.

The managed-cache-eviction-determinism handoff is RESOLVED on the Java side:
Eric ruled option 1 (accept + document), shipped as Java PR #237 (squash
`8a81950c`, CI green) — javadoc on both `ManagedCache.createCache` overloads
(size eviction approximate + non-deterministic under maxItems pressure;
expiry exact; never rely on which entry survives; explicit Rust-differs
note) + CHANGELOG. Investigation detail: a "frequency aging" remediation
suggestion was ruled a category error (caffeine-3.2.4's FrequencySketch has
reset()/RESET_MASK/sampleSize — aging is built in; it fixes stale
popularity, not reproducibility; the admit() jitter is orthogonal). Parity
boundary confirmed: eviction is internal state, not a presentation surface —
the cross-engine asymmetry (Rust strictly more predictable via
EvictionPolicy::lru) stays documented, not closed. Revisit trigger: the
first consumer that truly runs at capacity. Recorded on
[[ot-managedcache-port]] and in docs/design/managed-cache-port.md (the
divergence row now carries the resolution).

## Memory References

- referenced: `ot-managedcache-port` (Java-side resolution recorded)
