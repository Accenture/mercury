# Session (2026-09-23T23:50:47.000Z)

**Agent:** Claude Code

## Summary

**v4.12.16 release prep — the lock-step twin of Java 4.12.16 (Eric's ask after mercury #323 / Java #456 merged; Java PR
#457 opened by Eric; "for Rust, commit and push first").** Branch `release/4.12.16`, commit `748f3094`: version sweep 13
manifests / 24 occurrences (re-derived from `--include=Cargo.toml`, unchanged from 4.12.15) + `Cargo.lock` via
`cargo update --workspace` (24 entries at 4.12.16, 0 left); CHANGELOG entry from `git log v4.12.15..HEAD` (one PR, #323,
Increment 136) with the lock-step note that Java's OpenSSL doc and JaCoCo items have no analogue here. Gates: `cargo fmt
--all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `check-doc-claims` 28, `check-llms-links` 37,
`mkdocs build --strict` all clean. **`cargo test --workspace` locally: NOT green** — the run stopped at platform-core's
`event_over_http_stream` binary, 12 passed / 3 failed (`server_pool_exhaustion_answers_503` got 200 where 503 was expected,
`single_shot_target_over_capable_path_is_classic`, `idle_stall_fails_in_band_408`); it had passed 603/0/9 three times
earlier today on the same code, and the run was started only after the Java reactor build reported success — a stale
listener on the shared test ports is the suspect ([[dual-repo-gates-share-ports]] in the personal notes). Investigation and
a re-run follow in this session; CI on the branch is the arbiter. The Java twin: `release/4.12.16` `57c595b6`, PR #457.
Seam note: the `rust` workflow's run on merge `da133b88` was cancelled by the cancel-in-progress concurrency group when
memory `504821c0` pushed two minutes later — that commit's run passed; after the release merge, let the tag's run count.

## Merged

Eric opened PR #324 and merged it at 23:58:25Z as merge commit `743d4ea2`; PR checks green — `test` 4m11s (the first run
on `c6a9e240` was cancelled by the cancel-in-progress group when the footer refresh `caa66cc8` pushed nine seconds after
the PR opened; lesson: refresh footers BEFORE the handoff), memory ×2. The local `cargo test --workspace` re-run on the idle
machine passed 603 / 0 / 9, confirming the three earlier `event_over_http_stream` failures as environmental (no stale
listener; CPU contention right after the Java reactor build). Gate verified (state MERGED, `743d4ea2` an ancestor of
`origin/main`), GitHub deleted the remote branch, the local branch deleted after the gate, workspace at 4.12.16 on main.
This memory-only commit is the tag target (one past the merge). Pending for Eric: tag `v4.12.16`, the GitHub release with
the CHANGELOG entry as its body, `cargo publish --workspace` from the tag (clean tree); then the seam close updates
`latest_release` and sweeps the two items past `archive_window` (`ot-cratesio-followups`,
`thread-reverify-invariants-20260917`). Java twin: PR #457 merged 23:58:16Z as squash `df605533`, tag target `dc0ee6fa`.

## Tagged and published

Eric tagged `v4.12.16` at `cc138af3` (workspace 4.12.16 at the tag) and published the GitHub release at 2026-09-24T00:09:26Z with
a body identical to the CHANGELOG entry (3053 chars normalized); `rust` and `agent-memory` green on the tag commit. crates.io:
12 of 12 crates at 4.12.16 between 00:12:36Z and 00:12:52Z (`cargo publish --workspace`, first try; the macros first, then
platform-core, then the rest in dependency order). Java twin: `v4.12.16` → `dc0ee6fa`, release 00:08:35Z. The v4.12.16 cycle is
CLOSED on both engines.

**Follow-up surfaced by Eric during the publish (not done):** `serde_yaml v0.9.34+deprecated` compiles into every crate — the
author's own deprecation marker (archived 2024), a direct dependency of `platform-core` (`config_reader`, `multi_level_map`,
`app_config_reader`, `automation/routing`) and `ai-contract-provider`. No known vulnerability; `cargo-audit` is not installed
here to check RustSec. Candidate: evaluate the maintained fork `serde_yaml_ng` as a drop-in; a Java-side lock-step is not needed
(a Rust-only dependency). **Next review:** two items sit past `archive_window` — `ot-cratesio-followups`,
`thread-reverify-invariants-20260917` — for the cadence sweep.

## Memory References

- conventions-rust-baseline (consulted: same number, same content as the reference)
