# Session (2026-07-15T23:09:05.000Z)

**Agent:** Claude Code

## Summary

**Implemented increment 2 — the event-bus foundation** (`bp-platform-core`, Design doc §5),
plus made `.gitignore` **stack-aware** (maintainer request: Rust section with `target/`,
`**/*.rs.bk`, `*.pdb`, labeled OS/editor section; Cargo.lock stays tracked). The tool-side
enhancement (stack-aware gitignore seed at enable) remains with the spawned background task
for the agent-memory repo.

**Built** (new modules in `crates/platform-core`, deps added: tokio, async-trait,
async-channel, rmp-serde, rmpv, uuid):
- `envelope.rs` — `EventEnvelope`: metadata (id/to/from/reply_to/cid/trace/status/exec_time),
  headers, dynamic `rmpv::Value` body with `set_body<T: Serialize>`/`body_as<T>`; fluent
  builders; `to_bytes`/`from_bytes` via rmp-serde named encoding (**idiomatic serde wire
  format — D4**, not Java-byte-compatible); `status()` defaults 200, `has_error()` ≥400;
  crate-internal mutators for worker bookkeeping.
- `function.rs` — `AppError` (AppException analog), `ComposableFunction` (untyped registry
  currency; instance is 1-based, Java parity), `TypedFunction<I,O>` + `TypedAdapter`
  (deserialize body → typed handler → wrap output; `TypedAdapter::arc()` convenience).
- `platform.rs` — `Platform` registry: route validation ported from
  `Utility.validServiceName` (lowercase alnum + `.`/`-`/`_`, ≥1 dot, no lead/trail/double
  dots); per-route **`async-channel` MPMC** + N worker tasks sharing one receiver →
  point-to-point to exactly one free instance (tokio mpsc is single-consumer — the design's
  reason for async-channel); duplicate-route refusal; `release` = drop sender → workers exit
  on channel close; worker loop stamps `from`/`exec_time`, propagates the request `cid` to the
  reply, converts `Err(AppError)` → error envelope, drops replies to released inboxes
  silently, logs unhandled fire-and-forget errors.
- `post_office.rs` — `PostOffice`: `send` (400 missing-to / 404 unknown route);
  `request` RPC exactly like Java's `AsyncInbox` — temporary `inbox.<uuid>` route + oneshot,
  `reply_to` + kept-or-minted `cid`, `tokio::time::timeout` → **408**, inbox always released
  (success, error, and timeout paths).

**Verification:** `cargo build` clean · **41 tests pass** (13 unit + 18 config + 10
event-bus: envelope MsgPack round-trip, route validation + duplicates, send-reaches-exactly-
one-worker, 404/400 sends, RPC round-trip with cid correlation + inbox cleanup, typed
adapter end-to-end, timeout→408 with cleanup, error→status propagation,
**concurrency bounded by instances=4 under 8 parallel requests**, and worker-count-from-
config — dogfooding "everything relies on configuration management") · clippy clean ·
fmt applied + clean. All first-run green.

**Deliberate divergences/notes** (in doc comments): no global Platform singleton yet
(`Platform::new()` per test is cleaner; the lifecycle increment decides singleton-ness);
broadcast/streams/tracing deferred per §7.

## Memory References

Updated:
- `ot-design-platform-core` — increments 1+2 done; §7 remainder listed
- Project State status (increments 1+2 implemented; repo pushed to acn-ericlaw/mercury)
- Stack & Tools (increment-2 deps now in use; stack-aware .gitignore note)

Referenced: `bp-platform-core`, `vision-mercury`, `platform-core-stack`,
`inv-never-couple-functions` (enforced in the trait/worker design),
`conventions-rust-baseline` (fmt/clippy/parity-notes discipline followed)
