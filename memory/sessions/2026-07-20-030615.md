# Session (2026-07-20T03:06:15.000Z)

**Agent:** Claude Code

## What happened

**Maintainer scope refinement recorded (Vision-level, human-directed) + docs-site review
follow-ups** while the maintainer browses the completed site.

- **Scope refinement:** the Kafka *service mesh* (service discovery + sync-over-Kafka — the
  `connectors/` tree) stays permanently out of scope, but **`minimalist-kafka` and
  `twin-kafka` are lightweight, cloud-native connectors — reclassified to a future-port
  backlog**, together with **`sync-over-async`**. This is also the stated reason HTTP config
  keys keep their `http.` prefix (connector counterparts arrive later). Updated:
  `memory/vision.md` non-goals (refinement dated + attributed), `memory/instructions.md`
  out-of-scope list, and the public `docs/background/port-scope.md` (connectors moved from
  "deliberately out" to "Deferred — not yet, not never"); new backlog thread
  [[bp-kafka-connectors-backlog]]. Strict build stays green.
- **Header-keys check resolved by the maintainer's own correction:** `trace.id.header` /
  `correlation.id.header` in the REST reference are the per-entry rest.yaml overrides;
  the global keys `http.trace.id.header` / `http.correlation.id.header` are separately
  documented in the configuration reference. No doc change needed.
- **`.claude/launch.json`** (created to demo the in-app Browser pane serving the docs site)
  confirmed gitignored — the whole `.claude/` dir was already ignored; a redundant explicit
  line was added then reverted.
- Context: the in-app Browser pane verified the site visually (deep-purple palette, mermaid
  rendering, D-H2 layout at mobile width); the Claude-in-Chrome extension is not connected
  in this environment (noted to the maintainer with install steps).

## Decisions

- **Vision non-goal refined (maintainer, 2026-07-20):** mesh ≠ connectors. The non-goal now
  excludes only the discovery/sync-over-Kafka mesh; the lightweight connectors + sync-over-
  async are future ports. Recorded as a refinement (substance update), not a supersession —
  the underlying "no mesh" intent is unchanged.

## Memory References

- vision-mercury — referenced + substance refined (maintainer-directed non-goal wording)
- bp-kafka-connectors-backlog — created (born `tier: working`)
- ot-human-guides-backlog — referenced (the site under review is its deliverable)

Co-Authored-By: Claude Code <noreply@anthropic.com>
