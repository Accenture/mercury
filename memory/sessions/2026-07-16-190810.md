# Session (2026-07-16T19:08:10.000Z)

**Agent:** Claude Code

## What happened

Maintainer code-reviewed and committed E-2, directed **proceed** → **Increment E-3
(repo increment 13) — the four platform-core extensions (design E5), SHIPPED.**

1. **Event-interceptor registration mode** (Java `@EventInterceptor`):
   `FunctionOptions { zero_traced, interceptor }` struct replaces the increment-10
   bool on `Platform::register_with_options` and `AppStarter::preload_with_options`
   (old `preload_zero_traced` removed — internal API, one release old). Worker
   behavior verified against the Java `WorkerHandler` before implementing: **the
   success reply is interceptor-guarded (`if (!interceptor …) po.send`), the error
   path is NOT** — so a failing interceptor still answers `reply_to` with the error.
   The raw envelope keeps `reply_to`/`cid` for manual replies (our worker already
   passed it through untouched). `#[preload]` gained the `interceptor` flag + stacked
   `#[event_interceptor]` marker (macro + `PreloadEntry.interceptor` + AutoStart
   plumbing).
2. **Scheduled events**: `PostOffice::send_later(event, delay) -> timer_id` +
   `cancel_future_event(id) -> bool` — abortable tokio timer task behind the Java
   `sendLater`/`cancelFutureEvent` API (decision E7), global timer registry,
   self-removing on fire; misses log a warning. The flow TTL watcher's substrate.
3. **rest.yaml `flow:` binding**: `RouteInfo.flow` parsed; the automation server
   injects `x-flow-id` into request headers after header transforms — exactly what
   `http.flow.adapter` reads. Closes the increment-6 "flow binding" deferral
   (routing.rs header + platform-core design §5e/§7 notes updated).
4. **Deep-copy**: satisfied by design — `rmpv::Value::clone()` IS a deep copy (owned
   tree); documented in ES design §5c rather than adding a redundant API.

**Tests** (new `tests/interceptor.rs` ×5 + rest_automation flow-binding + annotations
marker): manual-reply round-trip; **no-auto-reply proof** (silent interceptor → RPC
408 while the call counter shows it ran); failure-still-replies (409 envelope);
send_later timing (not-yet/delivered split); cancel semantics (true→false, no
delivery, unknown-id no-op); `flow:` endpoint echoes `x-flow-id: demo-flow` while an
unbound endpoint has none; `#[event_interceptor]` end-to-end in the annotations
lifecycle. **145 workspace tests, 0 failures; clippy 0; fmt clean**; hello-world
live-checked after the registration-signature change (serves unchanged).

## Decisions

- `FunctionOptions` struct over a bool parade — the registration surface will grow
  (Java has more annotations: KernelThread, CoroutineRunner n/a in Rust); one options
  struct keeps the API stable from here.
- Error-path parity chosen over "interceptors never reply": matched to the Java
  worker's actual guard placement, not the annotation's folk description.

## Memory References

- referenced: `ot-design-event-script` (E-3 shipped), `bp-event-script`,
  `bp-platform-core` (its §5e flow-binding deferral closed), `vision-mercury`,
  `conventions-rust-baseline`
- created: (none)
