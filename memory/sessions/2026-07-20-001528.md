# Session (2026-07-20T00:15:28.000Z)

**Agent:** Claude Code

## What happened

**Backlog item done: the `f:listOfMap` forced test-drive (the last "optional" item from the
maintainer's backlog table) — PASSED: 17/17 commands ok, ZERO in-band lookups, first-attempt
dry-run.** The one reshaping plugin the sweep never exercised (tut-8 took the `f:removeKey`
route) got a dedicated fresh-agent validation.

- **New capability exercised: fully headless companion testing** — no human session needed.
  Orchestrator started the dev server, opened a Playground session with a zero-dep raw-WS
  client (`/ws/graph/playground`, session `ws-301207-1`, heartbeat pings, console tee logged),
  staged a column-oriented export at `/tmp/columnar-catalog.json`, verified the draft empty.
- **Brief (L3, problem-only):** ingest the columnar export; answer row-oriented
  (`books` = rows with EXACTLY title/price/sku in row order), `internal_cost` confidential,
  `count`, and **export length varies run to run** (kills hard-coded-index bypasses).
- **The fresh agent's design beat the intended solution:** one mapper chains
  `file(json:…)` → `f:removeKey(…, text(internal_cost))` → `f:listOfMap(…)` — the
  confidential column is dropped **before rows exist** — plus `f:length` for count and an
  unprompted island with data-entity nodes (the #22/#30 conventions shaping design again).
- **Judged first-hand:** orchestrator re-ran the dry-run (rows/fields/absence/count all
  asserted), then **swapped the export to 2 books and re-ran** — length-agnosticism AND
  `file(json:…)`-at-execution-time both empirically proven.
- **Findings #49–#52, all engine-verified and FIXED same day:** mapping[] in-order rule
  stated (mapper loop verified); `f:listOfMap` order preservation guaranteed in text
  (`normalize_map_of_lists` row *i* = index *i*); `file(…)` mapping-evaluation-time read
  stated (empirically shown); `f:length` added to the KG grammar's examples cell. Surfaces:
  `skills-reference.md`, `syntax.md`, `command-reference.md`, JSON catalog,
  `help graph-data-mapper.md` (webapp bundle re-released, 124 tests green; playground
  help-serving test green).
- **Self-sufficiency streak: nine consecutive zero-lookup first-attempt passes.** Log:
  `docs/AI-companion-test.md` ("Post-sweep forced test-drive").
- Infra cleaned up (server + WS client stopped). Exercise artifacts: `/tmp/columnar-catalog.json`,
  exported `/tmp/graph/columnar-books.json` (throwaway).

Also: **PR [#198](https://github.com/Accenture/mercury-composable/pull/198) MERGED** — the
join-barrier arc (increments 40+41) is now live in BOTH engines; local Java main synced
(`e74a0ed7`).

## Decisions

- (methodology) Headless orchestrator-owned sessions (raw-WS client + `/sync`) are now a
  proven alternative to human-owned UI sessions for companion tests — useful when the
  maintainer is not at the keyboard; the human-watching mode remains the default for
  tutorial-grade validation.

## Memory References

- ot-companion-validation-sweep — referenced + substance extended (post-sweep drive, streak 9)
- conventions-rust-baseline — applied (doc-fix gates, bundle re-release)
- port-bottom-up-faithful — referenced (docs pinned to engine-verified behavior)

Co-Authored-By: Claude Code <noreply@anthropic.com>
