# Session (2026-09-03T21:33:15.000Z)

**Agent:** Claude Code

## What happened

Two recipe additions to `docs/guides/knowledge-graph/ai-agent-guide.md`, mirroring the Java
repo's edits, distilled from the real live demo (`ai-enabled-repo-demo` branch
`layer-3-live-demo`, "flawless") cross-read against the two fresh-agent validation rounds:

1. **`output.status` surfaced in the agent-facing recipe** (step 2): a mapper can set the HTTP
   response status (`int(400) -> output.status`; this engine's event-script executor validates
   it as an HTTP code — `executor.rs`); non-2xx routes through the exception path (pointer to
   `workflow-suspension.md`). Previously documented only outside the agent read-first set, which
   led two independent fresh agents to conclude it was impossible and ship a divergent
   `200 + rejected:true` rejection shape. Placement gap; feedback item 13 in the demo repo.
2. **New recipe step 8:** a fresh Playground session after the deploy-restart starts empty (no
   root node) even though the graph is deployed — `import graph from {name}` is required before
   `instantiate`/`run`.

## Memory References

- Referenced: playground-session-broker
