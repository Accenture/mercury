# Session (2026-09-13T16:44:39.000Z)

**Agent:** Claude Code

## What happened

**The port spec is APPROVED — Eric ruled Q1–Q5 ("proceed with the Q1-Q5 rulings"),
stamped into `draft-design-specs/sync-over-async-port.md` §9:**

- **Q1** one-shot facade tasks: DEFERRED (they follow with the Kafka-connectors
  backlog, or earlier over Event-over-HTTP on demand; the coordinator still ships
  both rendezvous patterns — D8 makes that free).
- **Q2** RESP double's home: the shared dev-only `publish = false` crate ACCEPTED
  (lift, never copy).
- **Q3** health check: YES — `soa.redis.health` ships in the increment.
- **Q4** publication: ADD `mercury-sync-over-async` as the eighth crates.io crate.
- **Q5** demo: ADD a sync-over-async (progressive rendering) demo under
  `examples/`, driving the R3/R4 dry-runs; exact shape decided when built.

Scope text, §6 component map, and §7.2 updated from "pending ruling" to ruled;
thread `ot-sync-over-async-port` now points at R1 (the crate + the ported E1 suite
against the extended double) as the next gate.

## Memory References

- ot-sync-over-async-port (updated - spec approved, rulings recorded, next = R1)
- conventions-rust-baseline (consulted - Q5's ruling text keeps the
  standalone-examples convention as the demo's frame)
