# Session (2026-07-19T16:56:09.000Z)

**Agent:** Claude Code

## What happened

**AI-companion validation sweep — tutorial-10 (L9, composition by delegation: `graph.extension`
sub-graph): PASSED — 16/16 commands, zero failures, ZERO in-band lookups, first attempt**
(session `ws-783755-2`, verified empty).

- **Task (problem-only; deployed model id + contract as environment facts):** delegate a person
  lookup to the deployed, governed `tutorial-3` model — rebuilding/copying its internals
  forbidden. Input `{person_id}` → `{name, address}`.
- **Result:** the companion chose **delegation over import** unprompted (`graph.extension` +
  `extension=tutorial-3`, field-level `result.*` mapping, zero own providers/dictionaries/
  fetchers); output `{"address":"100 World Blvd","name":"Peter"}`; independently re-run with
  person 200 → Mary. Exported `delegate-profile`. The encouraged-form island documents the
  **delegation itself** (a `person` entity + a `person-lookup-model` node with
  `graph_id`/contract properties) — external dependencies as enterprise knowledge.
- **Findings #34–#36, all FIXED same day (engine-verified first):** #34 `graph.extension`
  delegation contract rule-stated (bare `input[]` targets = sub-graph `input.body` keys;
  `result.*` IS the sub-graph's `output.body`; `extension={graph-id}` resolves among DEPLOYED
  models — drafts not addressable) in `skills-reference.md#extension` + JSON catalog; #35 the
  node-name lexical rule wrongly excluded **digits** (canonical fixtures use `fetcher-1`;
  engine has no charset validation) — "lowercase letters, digits and hyphen" aligned across all
  five places; #36 skill-less `End` explicitly blessed in the node-types table.
- **Docs updated:** tutorial-10 stage record + L9 ladder row + rollup #34–#36 in
  `docs/AI-companion-test.md`.

## Decisions

- Tutorial-10 defined as **L9 (composition by delegation)**; briefs may state a deployed model's
  id + contract as environment facts (like mock URLs) without leaking the mechanism.

## Memory References

- ot-companion-validation-sweep — advanced (tutorial-10 done; next tutorial-11); substance updated
- bp-companion-sync — referenced (driven wholly over `/sync`)
- port-bottom-up-faithful — referenced (extension resolution + name validation verified against
  the engine before documenting)

Also (same working session, maintainer refinements on #35): node names **can include digits
(0-9)**. A briefly-added hyphen-avoidance style note was **withdrawn by the maintainer** —
hyphenated names are MORE communicative and should not be discouraged; the `{node-name.key}`
substitution syntax in `graph.math` expressions is robust to hyphens (never parsed as a minus).
Final doc state: charset rule (letters/digits/hyphen) + a positive robustness note in
`#math-statements` and `minigraph-commands.json`.

## Memory Review (2026-07-19, through 2026-07-19-165609)

Triggered by `[review-overdue]` (10 sessions since last review ≥ review_every 10).
- Reactivated:   0
- Superseded:    0
- Archived:      0 facts (none faded)
- Swept threads: 4 completed → memory/archive/2026-Q3.md (ot-reverify-invariants-2026-07 sslu 23,
  bp-platform-core sslu 25, bp-active-knowledge-graph sslu 21, ot-design-knowledge-graph sslu 28 —
  all milestones closed/confirmed)
- Archive-verify: pass (memory-lint 0 errors, 0 warnings; no id in both places)
- Tier changes:  via refresh-metadata during the session (reference-log recompute)
- Invariants:    not due (checked 2026-07-18; cadence 40)
- Promoted core: 0

Co-Authored-By: Claude Code <noreply@anthropic.com>
