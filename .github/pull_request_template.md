## What

<!--
  1–2 short paragraphs: what this PR changes. A bullet or two is fine.
  Draw it from the session log(s) included in this PR.
-->

## Why

<!--
  1–2 short paragraphs: the intent this change serves — the Blueprint gap,
  decision, or problem behind it. Substantive intent, NOT a restatement of What.
-->

---
Co-Authored-By: <!-- the AI collaborator's stable agent name (e.g. "Claude Code", "Gemini CLI") — not a model version; same identity as your session logs. At most ONE per collaborator, keyed on email: if the forge/harness already adds one at your email (even under a model-version name like "Claude Opus 4.8"), don't add a duplicate — reconcile to a single line. -->

<sub>agent-memory convention: PRs lead with **What** and **Why** (the *why* is a first-class artifact here — see the session log), and self-identify the authoring agent (above), same as commit trailers & session logs. Advisory, not a gate.</sub>
